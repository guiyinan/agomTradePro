# Copy to deploy/prometheus-query.env on the target host (mode 600).
# Generate the bcrypt value with:
# docker run --rm caddy:2-alpine caddy hash-password --plaintext 'random-password'
PROMETHEUS_QUERY_USER=monitoring
PROMETHEUS_QUERY_PASSWORD_HASH=
