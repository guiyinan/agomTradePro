# LSTM model configuration for CSI 300
# Qlib 训练配置文件 - LSTMModel
#
# 说明:
#   - 本文件定义了 LSTM 神经网络模型的超参数配置
#   - 适用于沪深300股票池
#   - 用于捕捉时间序列的长期依赖关系
#
# 使用方式:
#   python manage.py train_qlib_model --name lstm_csi300 --type LSTMModel \
#       --config config/qlib/lstm_csi300.yaml --activate

model:
  class: LSTMModel
  loss: mse
  d_model: 64
  # LSTM 网络结构
  hidden_size: 64
  num_layers: 2
  dropout: 0.2
  # 训练参数
  batch_size: 2048
  n_epochs: 100
  early_stopping_rounds: 20
  lr: 0.002
  # 优化器配置
  optimizer: adam
  weight_decay: 0.0001
  # GPU 配置
  GPU: 0
  n_jobs: 4

# 数据集配置
dataset:
  class: DatasetH
  handler:
    class: Alpha360
    # 时间范围
    start_time: 2020-01-01
    end_time: 2026-03-03
    fit_start_time: 2020-01-01
    fit_end_time: 2024-12-31
    # 股票池
    instruments: csi300

  # 数据分段
  segments:
    train: [2020-01-01, 2023-12-31]
    valid: [2024-01-01, 2024-12-31]
    test: [2025-01-01, 2026-03-03]

# 训练配置
training:
  # 批次大小
  batch_size: 2048
  # 训练轮数
  n_epochs: 100
  # 早停配置
  early_stopping_rounds: 20
  # 评估指标
  metric: ic
  # 学习率调度
  lr_scheduler: reduce_on_plateau
  lr_patience: 5
  lr_factor: 0.5

# 回测配置（用于模型评估）
backtest:
  # 策略类型
  strategy: TopkDropoutStrategy
  # 策略参数
  topk: 30
  n_drop: 5
  # 交易频率
  exchange_freq: 1d
  # 基准
  benchmark: SH000300
  # 交易成本
  trade_cost: 0.0015
