# Default install requirements for local runtime / first-time setup.
# - Use `requirements-prod.txt` for explicit runtime-only installs.
# - Use `requirements-dev.txt` when contributor tooling is needed.
-r requirements-prod.txt
