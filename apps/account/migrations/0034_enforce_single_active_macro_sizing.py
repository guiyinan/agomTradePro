# Generated by Django 5.2.12 on 2026-07-24 22:41

from django.db import migrations, models


def keep_latest_active_macro_sizing(apps, schema_editor):
    """Keep the newest active version and preserve older rows as inactive."""

    model = apps.get_model("account", "MacroSizingConfigModel")
    active_ids = list(
        model.objects.filter(is_active=True)
        .order_by("-version", "-updated_at", "-pk")
        .values_list("pk", flat=True)
    )
    if len(active_ids) > 1:
        model.objects.filter(pk__in=active_ids[1:]).update(is_active=False)


class Migration(migrations.Migration):

    dependencies = [
        ("account", "0033_require_explicit_minimum_commission"),
    ]

    operations = [
        migrations.RunPython(
            keep_latest_active_macro_sizing,
            migrations.RunPython.noop,
        ),
        migrations.AddConstraint(
            model_name="macrosizingconfigmodel",
            constraint=models.UniqueConstraint(
                condition=models.Q(("is_active", True)),
                fields=("is_active",),
                name="account_one_active_macro_sizing",
            ),
        ),
    ]
