"""Macro application DTO anchors.

The legacy macro app now delegates primary sync/query contracts to
``apps.data_center`` and keeps a thin compatibility surface. This module
remains intentionally light so the app preserves the expected layer shape.
"""

__all__: list[str] = []
