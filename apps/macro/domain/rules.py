"""Macro domain rule anchors.

Canonical unit governance and fact storage now live in ``apps.data_center``.
The macro app still keeps this module so its four-layer shape remains stable
for governance checks and future domain-level rules.
"""

__all__: list[str] = []
