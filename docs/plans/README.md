# 活跃计划索引

> 更新日期：2026-09-03
> 本目录只保留仍需开发、真实数据、生产验收或外部依赖闭环的计划。已完成的实施计划、阶段记录、复盘和历史证据统一放在 [`../archive/plans/`](../archive/plans/)；归档记录见 [`../archive/ARCHIVE_INDEX.md`](../archive/ARCHIVE_INDEX.md)。

## 维护规则

- 机器唯一真源是 [`governance/active_plan_registry.json`](../../governance/active_plan_registry.json)；本页是面向人的执行投影，不单独维护另一套状态。
- 注册表中的 `closure_backlog` 是 review queue 与 9 条工作流的 canonical 分阶段收口单元；本页只解释其执行顺序，不复制机器状态。
- `docs/plans/`：只允许登记在机器注册表中的主计划、支撑文档、证据或限期审查项。新增未登记文件、重复归属、陈旧路径和无截止日审查项由 CI 直接拒绝。
- `docs/archive/plans/`：仓库范围已实现，或阶段已经验收/被新计划取代；归档文档仅作历史证据，不代表当前运行状态。
- “代码已完成但生产未验收”仍属于活跃计划，不能仅因本地测试通过而归档。
- `active` 表示仍有仓库实施；`production_validation` 表示主缺口已转为真实数据/迁移/观察；`external_validation` 与 `blocked_external` 不得伪装为开发完成。
- `review_required` 不是长期状态，只允许出现在限期审查队列；到期必须合并、归档或转入明确工作流。
- 唯一可执行待办是注册表内的 `closure_backlog.units`。主计划里的历史 `- [ ]` 仅作需求与验收来源，不再直接计数、排期或领取；新增细项必须归入既有 closure unit，或先通过注册表评审新增唯一 unit。
- 注册表中的 `execution_focus` 是唯一 repository 执行锁；只有该 unit 可以处于 `active`。其他工作流只能按允许的 execution mode 收集只读生产或外部证据，不得并行扩展仓库代码边界。
- 同一 closure unit 的测试、文档、生产证据和回滚要求是一个验收包，不能拆成多份 plan 重复计算；依赖未完成的 unit 不得抢跑。
- `production` / `awaiting_production` 描述证据来源和当前阶段，**不是代理停止状态**。在当前任务允许推进该 unit 时，代理必须先完成全部安全的只读采集、候选绑定、报告派生和 collector 缺口实现；只有剩余动作会改变生产状态、产生真实交易/外部费用，或必须由人/外部机构作出决定时，才请求授权或等待。
- 归档时必须同步修正注册表、`docs/INDEX.md` 和活跃计划中的引用，不复制第二份文档。
- 提交前运行 `python scripts/check_active_plan_registry.py`。

## 自主 Goal 调度入口

- 仓库级自主调度合同见 [`AUTONOMOUS_GOAL.md`](../../AUTONOMOUS_GOAL.md)。它负责约束 Sol/Luna
  分工、单 repository 执行锁、权限边界、验证、状态晋级和停止条件，不维护任务状态副本。
- 每个 material checkpoint 回写对应 primary plan；每个 closure unit 完成时，必须把证据 artifact、
  primary plan 实施记录、机器注册表状态/下一焦点和本页人工投影作为同一个验收包同步。勾稽与
  `python scripts/check_active_plan_registry.py` 未通过时，不得晋级下一 unit。
- 后续 Goal 只从注册表、对应 primary plan、规范化证据、本页投影和 Git 工作树恢复上下文；禁止
  依赖聊天记忆或另建第二份 Goal 进度表。

## 生产证据自动采集纪律

生产证据要求“来自真实生产源且可复核”，不要求人工抄录。机器真源中所有 `production` / `external` closure unit 都必须登记 `evidence_collection`，并分成三类：

- `auto_collect`：默认主动执行。包括只读 API/数据库/指标/CI 查询、候选身份与 manifest 对账、下载已有备份、哈希校验、观察天数计算、缺陷/telemetry 快照派生、隔离恢复或回滚演练、浏览器确定性断言。原始响应、来源时间、候选身份和 hash 必须保留；collector 不接受调用方自报 `passed`。
- `authorization_required`：代理先完成 preflight、dry-run、影响范围和回滚点，只对具体的生产写入、部署/切换、故障注入、备份创建/清理、付费调用或真实交易请求授权。授权动作完成后，后续只读取证继续自动执行。
- `human_or_external_required`：真实审批、业务 UAT 判断、owner/reviewer 双签、券商权限和策略参数由相应主体完成；代理自动准备证据包、校验身份与完整性，但不得代签、补造或把“未回应”当通过。

若某个退出门可机械验证但尚无 collector，缺口归入当前 closure unit 的 repository 工作：先实现幂等、fail-closed、候选绑定的采集器及测试，不能把“缺少采集器”误报成外部阻塞。只读监控任务仍遵守其自身的禁止写入范围，只报告采集结果。

| Units | 默认自动采集 | 只在这里等待授权/人工 |
|---|---|---|
| `EVID-02` | PostgreSQL 并发 harness、current-head/rollback 查询与报告 hash | 生产审批记录写入；真实审批人决定 |
| `AUD-03` | migration 状态、outbox backlog/age、metrics/alert、恢复与 archive 完整性 | 生产 migration/rollback/fault/archive/restore；生产签字 |
| `DATA-01/02/03` | 已有备份下载与 SHA、隔离 restore/RTO、覆盖/freshness/reconciliation、双 readiness、观察计时 | 新建/清理备份、回填/删改、维护态、切换和 live rollback；数据 owner 例外 |
| `STRAT-01/02/03` | owner/policy 缺口盘点、PIT/OOS 时长、receipt/lineage/reconciliation、权限与 consumer 测试 | 生产登记/Promotion/回填；策略 owner 参数与业务 UAT |
| `TUI-01/02` | commit/version/OCI/matrix/graph/runtime 对账、health/ready、角色只读旅程、108 UAT 派生、14 日计时、缺陷、101 telemetry、隔离 rollback | 候选部署、生产写回执、registry backup 创建、cleanup/live rollback；双签 |
| `TAR-06` / `TAR-05` / `AI-01` | 默认无网络校验、已批准 staging 的 load/soak/browser、metrics、event timeline、候选绑定与报告 | 任何生产 load/chaos/canary/cutover、付费模型调用；容量与业务验收 |
| `QMT-01/02` | 获权后的 Phase 0 只读探针、仿真 timeline、callback/reconciliation 报告 | 券商权限、任何真实订单/撤单/live enable；券商与业务 owner 签字 |

当前已有的直接 collector 包括：`check_web_to_tui_cutover_readiness.py --json`、`record_web_to_tui_candidate_evidence.py`、`reset_web_to_tui_observation.py`、`build_web_to_tui_defect_evidence.py`、`build_web_to_tui_production_telemetry.py`、`drill_web_to_tui_rollback.py`、`backup-vps-postgres.py --download-latest`、`verify_postgres_backup_restore.py`、`record_data01_restore_evidence.py`、`record_data02_reconciliation_evidence.py`、`record_data02_successor_checkpoint.py`、`record_data03_readiness_evidence.py`、`record_aud03_operational_observation_evidence.py` 和 `run_terminal_runtime_staging_baseline.py`（默认 validation-only）。执行具体命令时仍以 `--help`、专项计划和当前任务授权范围为准。

## 当前治理快照

| 口径 | 数量 |
|------|-----:|
| 独立工作流 | 9 |
| 主计划 | 18 |
| 支撑文档、证据与矩阵 | 23 |
| 限期审查项 | 0 |
| 注册表覆盖的活跃文件 | 41 |
| 历史未勾选细项 | 136（非执行口径） |
| 去重后 canonical closure units | 40 |

“主计划”是需求和证据入口，不等于独立工程量；同一工作流下的路线图、readiness 和生产跟踪不会再重复计算成多条主线。完整文件归属、owner、状态、依赖和唯一退出门见机器注册表的 `closure_backlog`。

## 当前工作流

| ID | 优先级 | 状态 | Owner | 主计划 | 下一退出门 |
|----|--------|------|-------|--------|------------|
| `evidence-hard-gate` | P0 | production_validation | Personal Project Owner / Research / Risk / Portfolio / Broker / Account | [Evidence hard gate](evidence-governance-and-decision-hard-gate-remediation-plan-2026-08-12.md) | 单一所有者授权已替代多人签字，但 13 张 authority/evidence 表及 operator/approval/activation 仍 zero-seed/empty-head；有效 DEFER 已记录。下一门是按真实 owner receipt 建立 canonical authority heads，再执行 PG race/revocation/rollback 与 same-alias 回执，执行 deny 保持不变 |
| `strategy-research-production` | P0 | production_validation | Research / Data Center / Signal / Portfolio / Broker | [Completion audit](strategy-research-capability-completion-audit-2026-08-05.md)、[Roadmap](strategy-research-capability-roadmap-execution-2026-08-05.md)、[生产数据跟踪](strategy-research-production-data-closure-tracking-memo-2026-08-12.md)、[R1-R2](strategy-research-r1-r2-readiness-plan-2026-08-05.md)、[R3-R4](macro-factor-r3-r4-readiness-and-staged-delivery-2026-08-05.md)、[R5-R8](strategy-research-r5-r8-readiness-and-staged-delivery-2026-08-05.md) | 最终候选的 65/7/16/35 表 owner-ledger inventory 全零；R1–R8 逐项 owner/定义审核入口已就绪，审核后仍须 canonical dry-run 与独立 registration 授权，PIT/OOS、Promotion、consumer UAT 不得预签 |
| `data-production-reliability` | P0 | production_validation | Data Center / Operational Readiness / Task Monitor | [综合清零方案](release-blocker-closure-execution-plan-2026-08-29.md)、[Canonical architecture](data-center-canonical-architecture-refactor-2026-08-02.md)、[生产可靠性](production-data-reliability-full-remediation-2026-08-01.md)、[关键测试](critical-reliability-test-closure-2026-07-22.md)、[UAT 整改](uat-remediation-2026-07-20.md) | `DATA-06` repository exit 已关闭；当前候选 `aa7127ff4…` / `20260901232812` 在 `2026-09-01T16:08–16:10Z` 的 DATA-04 只读重验中 client backend 恒为 2、health/ready/Audit health 均为 200，但 `2026-09-02T09:24Z` 后续 Web liveness refresh 观察到 web unhealthy、公网 502，整体生产可用性仍为 `DENY`。DATA-02 仍因 completed-session price 全部不合格、financial fact 仅 `1,923/5,533` 而保持 `DENY`；下一门是 provider refresh/backfill、写后 reconciliation 与授权切换 |
| `system-audit-consolidation` | P0/P1 | production_validation | Personal Project Owner / Audit / Data Center / Task Monitor | [统一审计日志](system-audit-log-consolidation-plan-2026-08-13.md) | `AUD-04` repository exit 已补齐 candidate/authority 双重绑定、半开归档窗口、manifest/content/predecessor hash、append-only artifact 与 memory-only exact replay；successor 生产证据仍为 `missing_section_count=2`，下一门是 authority/profile、writer smoke 后真实 rollback/recovery 与获批 archive/restore |
| `web-to-tui-m5` | P0 | production_validation | Personal Project Owner / Terminal / Operational Readiness | [迁移总计划](web-to-tui-migration-plan-2026-07-25.md)、[M5 readiness](web-to-tui-m5-readiness-2026-07-27.md) | `TUI-01/TUI-03` completed，`TUI-02=active`：候选 `aa7127ff4…` / `20260901232812` 仍运行，UAT、cleanup、rollback 与 Day 0 backup 有效；2026-09-02 受控 web-only restart 后旧 retained source 作废，等待重启后首个真实样本及精确 14 日窗口，readiness=`5/10 DENY` |
| `terminal-agent-multi-user-runtime` | P0 | production_validation | Agent Runtime / Terminal / Task Monitor / Operational Readiness / SDK / MCP | [多用户队列与服务端 CLI 运行](terminal-agent-multi-user-runtime-plan-2026-08-18.md) | TAR-01 至 TAR-04 与 `TAR-06` repository 合同已完成；TAR-06 corrective 已把任意摘要文件冒充批准和假 Worker-ready 两条旁路封闭为 24h semantic approval、全 envelope/action binding、non-billable runtime profile 与真实 heartbeat preflight。collector 尚无真实 run，TAR-05 capacity/chaos/provider/canary/观察/退役仍须真实证明 |
| `ai-native-release` | P1 | external_validation | Agent Runtime / Terminal | [AI-Native delivery pack](ai-native/README.md) | `TUI-01` 已完成；等待 TAR-05 后绑定同候选 staging/production 真实模型 UAT 与单一所有者验收 |
| `qmt-live-bridge` | P2 | blocked_external | Broker Execution / 外部券商 Owner | [QMT 实盘桥](qmt-live-trading-bridge-plan.md) | Windows XtQuant Phase 0、连续仿真和受控小额实盘 |
| `tui-usability-governance` | P1 | production_validation | Terminal | [TUI 可用性与 metadata 治理](tui-usability-and-metadata-governance-plan-2026-08-18.md) | TUX-01～05 repository gate 全部完成，当前候选 `aa7127ff4…` / `20260901232812` 已部署；production-safe UAT、cleanup matrix 与 rollback drill 已通过，剩余为 TUI-02 自然观察、结构化快照和单一 owner 最终确认 |

## 当前执行焦点

- `DATA-08/DATA-09` 均已 repository-complete，当前没有 active repository unit。DATA-08 在 2026-09-01 关闭时的 App-root composition inventory 为 1151 项、`candidate-review=0`、legacy direct/wrapper=0；当前因新增受治理的 DATA-02 successor recorder，deterministic inventory 为 1152 项、`candidate-review=0`。历史 [closure evidence](../testing/data08-data-center-entrypoint-inventory-closure-evidence-2026-09-01.json) SHA=`7bc2d406…07db0` 保留其当时的 1151 项口径。DATA-09 仅登记 decision-runtime activation 的三个精确 probe-failure reason，7 项聚焦回归与 53 个 current-data contracts 全绿；[closure evidence](../testing/data09-decision-runtime-reliability-boundary-closure-evidence-2026-09-01.json) SHA=`acc07216…01663b`。完整 corrective stack 已以 `e38ebdcf8…` 推送，并经 PR #15 合并为 `main@1546ca0ec…`；合并后的 Architecture、Security、Consistency、Fast Feedback 四组 CI 全部成功。[GitHub checkpoint](../testing/pr15-closure-corrective-merge-main-ci-evidence-2026-09-01.json) SHA=`2febac54…f48e6a`。该 Git checkpoint 不替代 DATA-02/03 的生产门禁，也不授权部署或生产写入。
- `DATA-06=completed`，隔离历史 simulation harness 已以已有 SHA-verified dump 完成真实演练：7,229 restore entries、72 个 Data Center migrations、5,533 active A-share、repeatable-read read-only、provider/network-free、数据库与容器零残留。四类历史 gate 均为 `DENY`：全部 stale，financial 事实覆盖 `1,923/5,533`，四类 current publication reconciliation 均失败。规范化 [closure evidence](../testing/data06-isolated-historical-simulation-repository-closure-evidence-2026-09-01.json) SHA=`e4883f46…579a`；该结果不会冒充生产 exit gate。
- `DATA-05` 已作为唯一 repository focus 完成并回到 `execution_focus.unit_id=null`。既有 `financial_fact_repository.py` 243/200 行失败在不抬预算的前提下降为 189/200，availability owner 独立为 65/100；公开 `FinancialFactRepository` identity/行为不变，新 source 已纳入 current-data 清单。结构化 [closure evidence](../testing/data05-financial-repository-owner-closure-evidence-2026-08-31.json) SHA=`7c535f2a…8a56a`；聚焦 `12 passed`、扩大回归 `70 passed`，未触碰生产。
- `DATA-04` 已作为唯一 repository focus 完成并回到 `execution_focus.unit_id=null`。生产 Daphne/ASGI `CONN_MAX_AGE` 固定为 0；coverage-universe 读取删除 `get_or_create`，缺配置稳定 `MISSING_CONFIG`，只有显式 PUT/save 可初始化。结构化 [closure evidence](../testing/data04-asgi-db-select-only-preview-repository-closure-evidence-2026-08-31.json) SHA=`aaaa675e…ea8d`。当前候选 `aa7127ff4…` / `20260901232812` 在 `2026-09-01T16:08–16:10Z` 的只读重验中 3 个跨 scrape 样本 client backend 恒为 2、health/ready/Audit health 均为 200；随后 `2026-09-02T09:24Z` 的 Web liveness refresh 为 unhealthy/502，故不能把 DB 样本稳定误报为整体生产恢复。DATA-02 仍须在此候选上完成 provider refresh/backfill 与写后 reconciliation。
- `AUD-04` 已作为唯一 repository focus 完成并回到 `execution_focus.unit_id=null`。新增 scoped/candidate-revalidated 半开 ledger window、deterministic manifest/source/stream-anchor hashes、strict canonical codec、append-only content-addressed store 与 `memory_only` exact replay；对 candidate drift、scope/window substitution、truncation、duplicate/loss/fork、byte/sidecar tamper 和 production-claim 改写均 fail closed。结构化 [closure evidence](../testing/aud04-audit-archive-rehearsal-repository-closure-evidence-2026-08-31.json) SHA=`1c64e66a…4e317`；本次没有生产读写、archive/restore、删除、fault、runtime 变更或部署，不能替代 `AUD-03` 生产验收。
- `TAR-06` repository corrective 保持 completed；其结构化证据为 [`tar06-staging-preflight-corrective-closure-evidence-2026-08-31.json`](../testing/tar06-staging-preflight-corrective-closure-evidence-2026-08-31.json)，SHA=`926f1105…36746`。runner 仍要求最长 24 小时 semantic APPROVE、全 envelope/action binding、non-billable stub/MCP-disabled runtime、逐阶梯真实 Worker heartbeat 与一次性授权；仍未执行 staging/生产负载或授予 `TAR-05` acceptance。
- 生效的单一所有者授权记录为 [`personal-project-single-owner-authorization-2026-08-30-80ea002b.json`](../deployment/personal-project-single-owner-authorization-2026-08-30-80ea002b.json)，SHA=`f675c446…300e1`。同一所有者可承担 owner/root/reviewer/role-owner，不再要求第二个自然人；这不填充 authority 行、业务定义、环境、观察历史或执行结果。
- `DATA-01/DATA-04/DATA-05/DATA-06/DATA-07/DATA-08/DATA-09`、`TAR-07/GOV-02` 与 `AUD-01/AUD-02/AUD-04=completed`；当前无 active repository unit；`DATA-02/AUD-03=awaiting_production`，`EVID-03` 仍仅在 EVID-01/EVID-02 均完成后启动。
- `AUD-03` 已随当前候选 `aa7127ff4…` 做一次非重复 SELECT-only 重绑定；[`候选 checkpoint`](../deployment/aud03-operational-observation-select-only-2026-09-02-aa7127ff.json) 证明 496 migration 全 applied、outbox/失败为 0、563 条 operation logs、12 条 Prometheus alert rules 健康且无 active alert；同候选 108/108 UAT 证明 10 个 Audit route 全通过。recovery/archive 仍 unavailable，read-only artifact 保持 `production_ready=false`。
- `EVID-01/02` 与 `STRAT-01` 不再等待多名人员；同一项目所有者可以填写各治理角色，但仍须提供真实 authority head、业务 definition/policy/calendar/scope 与 typed production value。稳定入口仍为 [`evidence-strategy/README.md`](../reviews/release-36b72d2f/evidence-strategy/README.md)，零播种表不得由 fixture、User/Profile/session 或默认值补齐。
- `TUI-01` 与 `TUI-03` 已完成。`TUI-02=active`：当前候选部署、production-safe UAT、cleanup、isolated rollback 与 Day 0 registry backup 仍有效；2026-09-02 受控 web-only restart 后，旧 checkpoint [`tui02-production-observation-checkpoint-2026-09-02-aa7127ff.json`](../deployment/tui02-production-observation-checkpoint-2026-09-02-aa7127ff.json) 保留为历史但不再绑定。新的 reset artifact [`tui02-production-observation-reset-2026-09-02-aa7127ff.json`](../deployment/tui02-production-observation-reset-2026-09-02-aa7127ff.json) 记录同一候选的 web/prometheus 健康、public health/ready=200、decision-ready=503 fail-closed；等待重启后首个真实 retained sample 及精确 14 日 eligible instant，再做 v2 defect/101-task telemetry、post-window backup/review snapshot 和同一 owner 的两个 role-bound attestations；`TAR-05` 保持 awaiting production。
- `TAR-06` 的代码入口为 `scripts/run_terminal_runtime_staging_baseline.py`，默认 validation-only；显式执行还必须提供同一 manifest 全字段绑定、最长 24 小时且 action scope 精确收窄的 APPROVE preflight、20 个 stdin actor credential、Prometheus credential 与本地 output root。联网前还要求 non-billable stub/MCP-disabled profile 和 live Worker heartbeat。`TAR-05` 的稳定审核入口仍为 [`terminal-runtime/README.md`](../reviews/release-36b72d2f/terminal-runtime/README.md)，下一真实门是 P1 staging/candidate envelope；P2 capacity、P3 chaos、P4 provider/MCP、P5 production staff canary、P6 observation/cutover 和 P7 inline retirement 逐阶段保留，旧候选证据不能复用。
- 用户于 2026-08-30 以唯一真人项目所有者身份授权仓库整改和技术前置满足后的精确生产阶段；同一 action envelope 内不再重复索要多人签字。授权不替代券商 XtQuant、业务定义、外部模型结果或不可压缩观察时间，所有失败线继续 fail-closed。
- 历史 AUD-03/DATA-02/TUI 审核包仍保留为证据入口，但 single-owner 模式已取消职责分离和第二名真人要求；同一项目所有者的 repository identity、真实 receipt、候选/依赖校验和 SHA sidecar 足够。TUI 已由 successor Day 0 checkpoint 接管动态状态；旧模板不得覆盖新候选，也不能预签 14 日终审。

## 滚动执行排期

下表是基于 `closure_backlog` 依赖关系的目标时间箱，不复制 unit 状态；状态、焦点和依赖仍只以机器注册表为准。前置门未通过时后续日期顺延，不允许通过伪造审批、回填观察时间或放松 fail-closed 门禁赶工。

| 时间箱 | 唯一 repository 主线 | 并行生产/外部工作 | 完成或切换条件 |
|---|---|---|---|
| 2026-08-25～08-27 | `AUD-01`：canonical publisher、runtime、scoped authority composition 与无 fallback 合同（已完成） | `EVID-01/02` 准备 root approval/PG 验收包；`DATA-01` 完成已有备份与隔离恢复证据；准备 TUI-01 正式 preflight | AUD-01 代码、focused tests、架构/mypy/治理门禁全绿，2026-08-27 已切换焦点至 AUD-02 |
| 2026-08-28～08-30 | `AUD-02`、`TUX-04`、`TUX-03`、`TUX-05` 顺序完成原登记 gate；current-data 全登记回归推翻 AUD-02 的完整回归结论后完成 corrective exit | EVID/DATA/STRAT/TUI/TAR 只执行各自已登记且无需新授权的 auto_collect；AUD-03 恢复 awaiting production | AUD-02 的 eager composition 与测试 critical-profile 根因均已最小修复，完整 current-data 和原 AUD-02 gate 全绿 |
| 2026-08-31～09-03 | `DATA-07 -> TAR-07 -> GOV-02 -> DATA-08 -> DATA-09` 已推送并经 PR #15 合并为 `main@1546ca0ec…`，当前无 active repository unit | 当前候选 `aa7127ff4…` / `20260901232812` 已完成 DATA-04/06 只读重验与 DATA-02 dry-run；DB 连接样本稳定，但后续 Web liveness refresh 为 unhealthy/502，且 completed-session price、financial coverage 与 publication freshness 仍不足。继续等待 TAR-05 staging envelope 与 AUD authority/profile | main 四组 CI 已全绿；下一状态变化只能来自另行授权的生产回填/验收或真实外部依赖，Git/CI 不能替代生产 gate |
| 2026-09-04～09-07 | 仅在 EVID-01/02 均真实完成后激活 EVID-03，否则继续无 eligible repository unit | 继续已登记且有新外部状态的生产只读取证；任何写入、部署、故障注入、备份创建或人工判断仍需授权 | EVID-03 依赖真实解除，或满足 Goal 的无安全 eligible work 停止条件 |
| 2026-09-08～09-11 | 以机器注册表为准；当前无其他依赖已满足的 repository unit | TUI-02 仅在 TUI-01 完成后观察；AI-01 仅在 TUI-01/TAR-05 后启动；QMT 不占内部排期 | 不通过刷新日期、重复探针或放宽依赖制造仓库工作 |
| 2026-09-15 起 | 当时最高优先级且依赖已满足的唯一 repository unit；若不存在则停止 | 为 TUI-02 收集完整 defect/101-task telemetry，导出 post-window registry backup/attestation，生成 review snapshot 并由同一 owner 完成两个 role-bound attestations | readiness 必须从真实 artifact 重算为 ALLOW；日期到达本身不等于通过 |

生产轨当前目标：successor M5 候选 `aa7127ff4…` / release `20260901232812` 仍运行；2026-09-02 受控 web-only restart 后，旧 retained sample 已作废，新的 exact eligible 时间尚未产生。只有重启后首个真实样本、其 14 日窗口自然结束、候选无漂移、结构化 defect/101-task telemetry 全绿、formal registry attestation、review snapshot 和同一 owner 的两个 role-bound attestations 齐备后才能进入最终 readiness。候选漂移或 P0/P1 修复必须重置，日历到期本身不等于通过。

提交节奏：每个 repository 时间箱以“代码 + 测试 + 必要文档”形成一个或少数 coherent commit；同一轮生产观察的 JSON、SHA、INDEX 与 Plan 状态合并为一个 `evidence:` commit。纯日期刷新、重复 HEAD 绑定和没有新证据/状态变化的 docs-only commit 不进入主线。

## 限期审查队列

**2026-08-15 已清空。** 剩余 7 项均已逐项对账、在原文写入收口说明并移入 [`../archive/plans/`](../archive/plans/)，未新开实现线：

- Account performance → `evidence-hard-gate`（`EVID-01/EVID-03`）的 owner scope、source freshness/hash、真实估值回填与 PostgreSQL 验收。
- Account refactor → `evidence-hard-gate`（`EVID-01`）；破坏性账本迁移另受 `DATA-01/02/03` 约束。
- Admin settings → `web-to-tui-m5`（`TUI-01/TUI-02`）的角色 UAT、保留页清理和观察窗口；Config Center 生产 profile 仍归 Data Center。
- Alpha exit loop → `strategy-research-production`（`STRAT-02/03`）与 Evidence `EVID-03`。
- Alpha homepage → `web-to-tui-m5` 的 Alpha/TUI 迁移与角色 UAT，PIT/OOS 与 owner/receipt 仍归 Strategy/Evidence。
- Streamlit dashboard → 归档为兼容 sidecar 历史方案，当前用户任务由 TUI 承接，不再保留独立 cutover 线。
- Workflow upgrade → 当前 Decision Workspace/Transition Plan 文档为真源；legacy writer、receipt、exact-current 和执行门禁归 Evidence `EVID-03`，Promotion/UAT 归 `STRAT-03`。

审查队列已清空；后续不得重新创建同类游离计划，残余工作必须进入上述 canonical closure unit。

本轮已归档两项旧计划：

- [Macro sizing multiplier（归档）](../archive/plans/macro-sizing-multiplier-outsourcing-2026-03-31.md)：实现资产已存在，残余 source freshness、config/version/hash、owner scope 与 exact-current 验收转入 Evidence `EVID-03`。
- [0.8.0 后稳定化（归档）](../archive/plans/post-0.8.0-stabilization-priority-2026-07-08.md)：过期两周排期保留为历史来源，未完成生产门禁按 S1-S10 映射至当前 Data / Strategy / Evidence / TUI 工作流。
- [2026-08-15 review queue 七项（归档）](../archive/ARCHIVE_INDEX.md#2026-08-15-review-queue-七项收口)：实施资产、取代关系和未完成生产证据已逐项登记；`GOV-01` 已完成。

## 去重后的收口 Backlog

136 个历史未勾选细项按共同产物、共同依赖和共同验收证据折叠为以下 **40 个唯一工作包**。状态值与机器注册表保持一致；本表不形成第二套待办。

| 波次 | Canonical unit | 类型 | 状态 | 依赖 | 唯一交付 |
|------|----------------|------|------|------|----------|
| W0 | `GOV-01` | governance | completed | — | 7 份剩余限期审查 plan 已全部归档或转入 canonical workstream；队列清空 |
| W1 | `EVID-01` | production | awaiting_production | — | 当前候选 `aa7127ff4…` / release `20260901232812` 的只读快照确认 0050–0055 已应用、13 张 authority/evidence 表仍 zero-seed；待真实 root approval、PG first-winner/revocation/rollback 和双签 |
| W1 | `EVID-02` | production | awaiting_production | — | 同一只读快照确认 operator/approval/activation 三表及 approval/current-head 为空；待真实 PostgreSQL first-winner、并发、回滚与 reviewer approval |
| W1 | `EVID-03` | repository | waiting_dependency | EVID-01/02 | Research/Portfolio/Broker adapters 与执行前 exact-current 重验 |
| W1 | `EVID-04` | repository | completed | — | legacy authority-inventory fixture 与 canonical serializer bytes 对齐，平台换行不再造成 hash 漂移 |
| W1 | `AUD-01` | repository | completed | — | canonical publisher/runtime/authority composition |
| W1 | `AUD-02` | repository | completed | AUD-01 | 同 UOW/回放 gate 与 corrective cross-App current-data 回归均完成 |
| W1 | `AUD-04` | repository | completed | AUD-02 | candidate/authority 双重绑定的半开 Audit archive window、hash manifest、append-only artifact 与 memory-only exact replay |
| W1 | `TAR-01` | repository | completed | — | 多用户 runtime、队列、服务端 Agent 与薄客户端、安全、SLO 与 deterministic test matrix 合同已冻结；不代表生产容量验收 |
| W1 | `TAR-02` | repository | completed | TAR-01 | PostgreSQL 持久接单、幂等、有界准入和 commit 后派发 |
| W1 | `TAR-03` | repository | completed | TAR-02 | 专用 Agent Worker、租约/取消/崩溃恢复和可续传事件流；SDK queued facade 已补齐 |
| W2 | `AUD-03` | production | awaiting_production | AUD-02/AUD-04 | successor 已完成 migration/outbox/metrics/alerts/admin TUI 只读重绑；仍待真实 authority/profile、writer smoke、rollback/recovery、获批 archive/restore 与 single-owner 最终确认 |
| W2 | `DATA-01` | production | completed | — | 校验备份、维护态与回滚预演 |
| W2 | `DATA-02` | production | awaiting_production | DATA-01/DATA-04 | 受控回填与 canonical reconciliation；dry-run 已完成，execute 等待真实 typed Audit authority/profile |
| W2 | `DATA-03` | production | waiting_dependency | DATA-02 | M9/M10 切换、readiness/smoke 与观察窗口 |
| W2 | `DATA-04` | repository | completed | DATA-01 | ASGI PostgreSQL 连接生命周期与 SELECT-only DATA-02 preview 收口 |
| W2 | `DATA-05` | repository | completed | DATA-04 | financial repository owner contract 与预算收口 |
| W2 | `DATA-06` | repository | completed | DATA-01/DATA-04/DATA-05 | 隔离历史 DATA-02 simulation harness 与 fail-closed gate |
| W2 | `DATA-07` | repository | completed | DATA-06 | AKShare quote fixture 恢复 authoritative QuoteSnapshot contract |
| W2 | `STRAT-01` | production | awaiting_production | — | 当前候选 `aa7127ff4…` / release `20260901232812` 的 65/7/16/35 表四组 owner-ledger inventory 仍全零；等待真实业务 owner 提供并批准 definition/policy/calendar/scope/qualification |
| W2 | `STRAT-02` | production | waiting_dependency | STRAT-01/DATA-02 | PIT/OOS 历史、canonical receipts 与对账证据 |
| W2 | `STRAT-03` | production | waiting_dependency | STRAT-02/EVID-03 | Promotion、权限、consumer 与回滚 UAT |
| W2 | `TAR-04` | repository | completed | TAR-03 | 服务端 Agent Runtime 的 CLI/API、SDK/MCP薄客户端和TUI queued结果路径；用户侧不安装 provider-backed Agent |
| W3 | `TUI-01` | production | completed | TAR-03 | manifest 候选、机器 UAT、写回执、cleanup/rollback 与单一所有者业务确认均完成 |
| W3 | `TUI-03` | repository | completed | TUI-01 | digest-pinned Prometheus、21 日/4GB 持久留存、可达 scrape target、健康检查、M5 rules、打包与受控 HTTPS query 合同均通过 |
| W3 | `TUI-02` | production | active | TUI-01/TUI-03 | successor 与 Day 0 gate 已完成；重启后等待新首样本及其精确 14 日 eligible instant，再收集 defect/101-task telemetry、post-window registry attestation、review snapshot 与单一 owner role-bound attestations |
| W3 | `TAR-06` | repository | completed | TAR-04 | 默认无网络、拒绝生产 target 的 20-actor 1/5/10/20 HTTP/Prometheus collector；semantic approval 最长 24h 且绑定全 envelope/action，non-billable runtime 与真实 Worker heartbeat 前置，缺失指标 unavailable，凭据不落盘 |
| W3 | `TAR-07` | repository | completed | DATA-07 | TAR staging harness HTTP boundary ownership 与 deterministic inventory 收口 |
| W3 | `TAR-05` | production | awaiting_production | TAR-03/TAR-06 | 真实候选的 1/5/10/20 用户容量、故障恢复、回滚、观察与生产切换验收 |
| W3 | `AI-01` | external | waiting_dependency | TUI-01/TAR-05 | 同候选 staging/production 真实 UAT 与独立双签 |
| W3 | `GOV-02` | repository | completed | TAR-07 | 文档 route consistency 检查过滤 filesystem path，同时保留真实 API route |
| W3 | `DATA-08` | repository | completed | GOV-02 | Data Center entrypoint inventory 通过 App-root composition deterministic 收口 |
| W3 | `DATA-09` | repository | completed | DATA-08 | decision-runtime dynamic reliability reason boundary 登记与守卫 |
| W4 | `QMT-01` | external | blocked_external | — | 券商 XtQuant 权限和目标机 Phase 0 |
| W4 | `QMT-02` | external | blocked_external | QMT-01 | 连续仿真和受控小额实盘验收 |
| W5 | `TUX-01` | repository | completed | — | 非法 published payload 降级渲染 + 存量记录批量重校验 |
| W5 | `TUX-02` | repository | completed | — | 三真源合一、死 patch 删除、8 处漂移双写对账 |
| W5 | `TUX-03` | repository | completed | TUX-02 | 430-action 基线全部对账（413 保留、17 冗余注册退役），文案/引用机检与全屏密度预算通过，主任务浏览器可达 |
| W5 | `TUX-04` | repository | completed | — | 分组重排、入口消歧、12 个 runtime screen 补齐契约与普通角色浏览器走查 |
| W5 | `TUX-05` | repository | completed | TUX-03/TUX-04 | corrective 修复深链 action 可达性、confirmed write loading 收敛与 production-safe TUI-01 recorder；不降低 108-route/角色/receipt/cleanup 门禁 |

执行纪律：机器注册表 `execution_focus.unit_id=null`，当前没有 active repository unit；`TUI-02` 作为 active production observation line 自然运行。AUD-03、EVID-01/02、DATA-02、STRAT-01 与 TAR-05 仅在技术前置和真实新状态到位后推进。single-owner 模式取消多人签字，不取消 external AI、queued runtime、authority/approval、load/fault、maintenance/live rollback 的技术停止线。

## 分阶段执行记录

| 日期 | 期次 | 阶段 | 完成情况 | 后续 |
|------|------|------|----------|------|
| 2026-08-24 | 第一期 P0 | EVID-01 Evidence read composition 机器守卫 | 新增 `scripts/check_evidence_scope_composition.py` 并接入 Consistency Check；扫描 `apps/core/shared` 共 `2921` 个生产 Python 文件，禁止绕过 composition root 直接导入/实例化 Evidence repository/facade，并要求 staff-scoped Evidence API 调用 fail-closed factory；guard 通过，composition/API focused `23 passed`，Ruff/Black/isort/增量 mypy 通过 | 仅补强生产 read boundary 的静态证明，不创建 owner/tenant authority、不接 production writer/route、不解除 zero-seed、Evidence hard gate 或 global execution deny |
| 2026-08-24 | 第一期 P0 | EVID-01 composition guard module-import hardening | 扩展 AST guard，除显式 repository/facade 符号外，亦拒绝 module-only alias/import 绕过 composition root；保留 Research infrastructure 内部实现所需的私有 helper import；新增两类回归，guard 扫描通过（`2921` production files） | 只扩大静态 bypass 覆盖，不创建 owner/tenant authority、不接 production writer/route、不解除 zero-seed、Evidence hard gate 或 global execution deny |
| 2026-08-24 | 第一期 P0 | EVID-02 recorder 服务器端直接执行入口修复 | `scripts/record_evid_02_postgres_evidence.py` 增加仓库根目录导入引导，直接从仓库根运行 `--help` 不再依赖本地 `PYTHONPATH`；新增直接 CLI 回归，EVID-02 recorder focused `16 passed` | 仅修复服务器端离线 auto_collect 工具的可执行性，默认仍 dry-run、无 DB/VPS/生产写入；不创建 approval/activation、不解除 EVID-02 的生产 PostgreSQL 并发、人工审批或 owner/reviewer 门禁 |
| 2026-08-24 | 第二期 P0 | DATA-03 双 readiness 只读观察 recorder 合同 | 新增纯 Application `data03_readiness_evidence.py` 与服务器侧 `record_data03_readiness_evidence.py`；严格校验 `http_get_read_only`、每个样本的 candidate commit/version/OCI/matrix、`/api/ready/` 与 `/api/decision-ready/` 状态/HTTP 码/`must_not_use_for_decision` 一致性、源时间、canonical smoke checks、候选漂移与 observation duration；输出 service/decision/smoke blocker 计数，固定 `production_claim=false`/`production_ready=false`/`runtime_enablement=not_authorized`；回归 `11 passed`，entrypoint inventory `1138` 项、`candidate-review=0` | 只补齐外部只读响应到 fail-closed 观察报告的服务器端自动采集合同，默认 dry-run、显式写入仅本地 content-addressed append-only；不连接 VPS/HTTP/DB、不执行 M9/M10 切换、不改变维护态、不宣称 freshness/coverage/观察窗口或生产 readiness 通过，`DATA-03` 继续 `waiting_dependency` |
| 2026-08-24 | 第二期 P0 | AUD-03 只读运营观察 recorder 合同 | 新增纯 Application `aud03_operational_observation_evidence.py` 与服务器侧 `record_aud03_operational_observation_evidence.py`；严格校验 `select_only`、每个样本的 candidate commit/version/OCI/matrix、migration/outbox/metrics/alerts/admin-TUI/recovery/archive 白名单字段、UTC 顺序与未来时间、敏感/raw-log 字段、候选漂移；不可用 section 保持 `unavailable`，从原始 hashes/counters 派生 backlog、recovery duration、duplicate/loss、archive integrity，不把缺失指标转成零；回归 `12 passed`，与既有 audit observability focused `27 passed` | 只补齐 AUD-03 外部快照到 fail-closed 运营观察报告的自动采集合同，默认 dry-run、显式写入仅本地 content-addressed append-only；不连接 publisher/authority/Celery/VPS/DB，不运行 migration/rollback/fault/archive/restore，不宣称 AUD-03 生产验收，`AUD-03` 继续 `waiting_dependency` |
| 2026-08-24 | 第一期 P0 | TAR-01 当前公网只读健康复核 | 仅对 `demo.agomtrade.pro` 执行 HTTPS `GET`：`/api/health/`=`200`、`/api/ready/`=`200`，database/redis/celery/critical_data 为 `ok`，decision-data 为 `warning`；`/api/decision-ready/`=`503` 且 `must_not_use_for_decision=true`；原始响应 hash 与状态见 [`tar01-public-health-readonly-recheck-2026-08-24.json`](../deployment/tar01-public-health-readonly-recheck-2026-08-24.json) | 这是短窗口公共健康与 fail-closed 证据，不识别或重绑候选，不证明容量、队列/Worker、角色 UAT、写后 receipt/refresh、14 日 telemetry、恢复/回滚或 owner/reviewer 签署；未部署、未迁移、未写生产 |
| 2026-08-24 | 第二期 P0 | TUI-01/TAR-05 candidate boundary 只读复核 | `python scripts/check_web_to_tui_cutover_readiness.py --json` 仍为 `DENY`：source consistency、execution dependency 通过；正式 M5 stable candidate binding、108 路由/任务 UAT、101 项 telemetry、rollback、registry backup 与 owner/reviewer 均未通过。当前 `94abd76e` 仅绑定 TAR/EVID 受控部署，未作为 M5 候选重绑 | 只完成候选边界与 readiness 自动核对，不部署、不写生产、不执行角色 UAT/容量/混沌/cleanup/rollback；避免把同一 VPS 候选重复部署或误标 TUI-01/TAR-05 生产门禁，正式 M5 binding 与生产授权仍待 |
| 2026-08-24 | 第二期 P0 | STRAT-01 owner-ledger auto-collect recorder | 新增纯 Application 快照解析与 `scripts/record_strat_01_owner_inventory.py`；严格校验 v1/v2 candidate、default alias、SELECT-only、inventory/query scope、未知/敏感字段、未来时间、负数/重复项；现有 v1/v2 真实快照均 dry-run 为 `zero_seed`，即使非零也只会 `nonzero_unverified`；回归 `14 passed`，Ruff/Black/isort/增量 mypy/debt ceiling 通过 | 只补齐离线自动取证合同，不连 DB/VPS、不写生产、不推导 authority/readiness；`STRAT-01` 仍 `awaiting_production`，不解锁 STRAT-02/03 或任何 promotion/UAT 门禁 |
| 2026-08-24 | 第二期 P0 | TAR-05 offline chaos evidence recorder | 新增 `scripts/record_terminal_runtime_chaos_evidence.py`；严格复用 candidate/OCI/test-matrix、UTC timeline、worker/run/stream/recovery、duplicate/isolation counter 与 `runtime_enablement=not_authorized` 合同；支持独立 candidate JSON 绑定校验、默认 dry-run 和本地 content-addressed append-only 写入；回归 `19 passed`，不访问网络/VPS、不启动 Worker/Redis/Celery、不注入故障 | 仅补齐外部受控快照的离线收集边界，不生成负载/混沌结论，不启用 queued/Worker、不解除 `TAR-01/TAR-05` 的 capacity/chaos/telemetry/restore/rollback 或 owner/reviewer 门禁 |
| 2026-08-24 | 第二期 P0 | DATA-01 isolated restore evidence recorder | 新增纯 Application `data01_restore_evidence.py` 与 `scripts/record_data01_restore_evidence.py`；严格校验 committed restore snapshot 的 dump SHA 前后不变、source/restore 数据库隔离身份、539 张表/460 个序列/72 项迁移逐项一致、canonical schema/difference 为空、UTC 计时与未来时间；canonical report 固定 `production_claim=false`/`production_ready=false`/`runtime_enablement=not_authorized`；回归 `7 passed`，直接 dry-run 产出 `dc705a7878…` | 只固化本机 disposable PostgreSQL 隔离恢复的离线证据，不执行 pg_restore/DB/VPS 操作，不把 `590.844s` restore 或 `1626.207s` 总耗时冒充生产 RTO/RPO；`DATA-01` 仍 `awaiting_production`，维护态 rollback、生产 restore、回填/reconciliation 与 owner/reviewer 签署不解锁 |
| 2026-08-24 | 第二期 P0 | DATA-01 entrypoint inventory registration | 为使上述离线 recorder 通过 Data Center 入口生成物门禁，登记 recorder 与测试证据的生命周期并刷新 `governance/data_center_entrypoints.json`；确定性 inventory 为 `1136` 项、`candidate-review=0`，专门 inventory 回归 `18 passed`，未触碰 VPS/生产 | 仅是本地治理生成物与入口归属修复，不改变 recorder 的 `isolated_restore_only`/`not_authorized` 语义，不解锁 DATA-01 生产 restore/RTO/RPO/rollback 或 owner/reviewer 签署 |
| 2026-08-24 | 第二期 P0 | DATA-01 latest-download read-only guard | 修正 `scripts/backup-vps-postgres.py --download-latest` 的远端脚本组合：默认 latest 只执行已存在归档的选择、`pg_restore --list`、hash/size/manifest 读取，不再生成 `mkdir/chmod/pg_dump/mv/delete`；显式 prune 仍单独保留删除语义；backup 回归 `11 passed`，Ruff/Black/isort/增量 mypy 通过 | 只收口 auto_collect 的远端只读契约，不执行 VPS 命令、不创建备份、不改变生产数据；只有用户显式请求 prune 才允许远端删除，DATA-01 生产 restore/RTO/RPO/rollback 与 owner/reviewer 签署仍不解锁 |
| 2026-08-24 | 第二期 P0 | DATA-02 只读 reconciliation recorder 合同 | 新增纯 Application `data02_reconciliation_evidence.py` 与服务器侧 `record_data02_reconciliation_evidence.py`；严格校验 `select_only`、candidate commit/version/OCI/matrix、legacy/canonical source、UTC observed_at、JSON 快照与分类自然键；输出两侧 hash/分类/时间和固定 `not_authorized` 状态，默认 dry-run、显式写入仅本地 content-addressed append-only；定向回归 `19 passed`，Ruff/Black/isort/增量 mypy 通过 | 只补齐 DATA-02 回填前/后外部快照对账的自动采集入口，不连接 DB/VPS、不写 Django/生产、不执行 backfill；不覆盖全量 coverage/freshness，不解除 DATA-01 前置、DATA-02/03 生产回填、维护态切换、容差例外或 owner/reviewer 门禁 |
| 2026-08-24 | 第一期 P0 | EVID-01 authority 回滚历史与恢复授权 | `bc6760465` 曾被 `a11ee1944` 回滚；用户随后明确授权恢复独立 owner/tenant authority 实现，同时保留回滚后新增的只读观察与机器守卫 | 恢复仅落代码与 schema，不部署 VPS、不执行 migration、不写生产、不创建审批；EVID-01 仍需真实 root approval、PG race/rollback、端到端验收与 owner/reviewer 签署 |
| 2026-08-24 | 第一期 P0 | EVID-01 独立 owner/tenant authority 与自动 scope 采集 | 新增 Account append-only root/successor/revocation/expiry lifecycle、schema-only `0055`、独立 authenticated staff approval、principal+authority+artifact exact revalidation、Research same-alias ScopeSource writer/selector；authority/core/composition/guard 与账本 component 合计 `24 passed`，mypy/架构/治理门禁通过 | 实现阶段未部署/未写生产/未造审批；随后受控候选已应用 `0055`/`0029`，13 张账本仍 zero-seed。当前仍需真实 owner/admin approval、PG first-winner/successor/revocation/rollback、端到端验收和双签；批准后的 scope 证据由服务器自动采集 |
| 2026-08-24 | 第一期 P0 | EVID-01 composition root ownership fix | 将 private ScopeSource writer 工厂保留在 `apps/research/evidence_composition.py`；Account composition 不再反向导入 Research implementation，而是通过 `core/integration/owner_tenant_evidence_scope_v1.py` 注入 typed owner/tenant reader；Evidence scope guard 扫描 `2931` 个 production files、`0` violations，新增 factory 回归并纳入上述 `24 passed` | 仅修复本地 composition boundary，不部署/迁移/写生产，不改变 zero-seed、global execution deny 或 EVID-01 production gate |
| 2026-08-24 | 第一期 P0 | EVID-01 composition cycle guard 与 CI 验收 | `Account → Research` 反向 import 已移除；module-cycle guard=`206 edges / 0 cycles / 0 budget overflow`，Evidence composition guard=`2931 files / 0 violations`，authority/core/composition/guard focused=`24 passed`；[Architecture Layer Guard](https://github.com/guiyinan/agomTradePro/actions/runs/32683263266)、[Security Scan](https://github.com/guiyinan/agomTradePro/actions/runs/32683263238)、[Consistency Check](https://github.com/guiyinan/agomTradePro/actions/runs/32683263269)、[CI Fast Feedback](https://github.com/guiyinan/agomTradePro/actions/runs/32683263257) 均成功 | 仅仓库与 CI 验收；未部署 VPS、未执行 `0055` 生产迁移、未写生产或造审批。EVID-01 仍需真实 root approval、PostgreSQL first-winner/revocation/rollback、owner/reviewer sign-off；`/api/decision-ready/` 继续 fail-closed |
| 2026-08-24 | 第一期 P0 | EVID-01 owner/tenant authority PostgreSQL 并发 harness | 新增专用 opt-in component harness，覆盖 empty-root/same-predecessor first-winner、revoked terminal head no-fallback、outer-transaction rollback、`0055` forward/reverse/re-forward zero-seed；默认无 URL 明确 `5 skipped`，使用本机 disposable PostgreSQL 16 空库实际 `5 passed in 25.82s`，测试库已清理；Ruff/Black/isort、增量 mypy/debt ceiling 通过 | 只完成本机 PostgreSQL 软件证据与采集合同，不连接 VPS、不执行生产迁移/写入、不把 SQLite/skip 当 PG 证据；EVID-01 仍需真实生产 race/revocation/rollback、独立审批与 owner/reviewer sign-off |
| 2026-08-24 | 第一期 P0 | EVID-01 authority inventory v2 parser/report | 保留 v1 历史快照与 content hash；新增 v2 snapshot/report 分派，严格纳入 Account `0050–0055` 与 `account_owner_tenant_authority_v1`，非零新 ledger 仍派生 `blocked_unverified_authority`，所有 report 固定 `production_claim=false`、`production_ready=false`、`authority_ready=false`、`runtime_enablement=not_authorized`；focused inventory 回归 `18 passed`，增量 mypy/debt ceiling、Ruff/Black/isort 通过 | 只修复下一 schema 版本的只读 inventory 兼容性；post-0055 受控候选已单独验收，仍需独立 root approval、同 alias authority/provider、生产 PG revocation/rollback 与 owner/reviewer sign-off |
| 2026-08-24 | 第一期 P0 | EVID-01 当前 VPS pre-0055 只读验收与 inventory contract | 对运行候选 `4cef9040cccc2127c3f8128c8d858bc7958df2a4` / release `20260822134658` / image `sha256:cfaf17560df2…` 通过 SSH 只读查询确认 Account `0050–0054` 已应用、`0055` 尚未部署，12 张既有 authority/evidence 表均为 `0` 行；新增严格 `v2-pre-0055` snapshot/report，派生 `blocked_missing_owner_tenant_migration`（report `0ad0a48982…`）。同一低频窗口公网 `/api/health/`、`/api/ready/`、`/api/audit/health/` 为 `200`，`/api/decision-ready/` 为 `503` 且 `must_not_use_for_decision=true`，匿名 `/api/tui/` 为 `403`；完整工件见 [`evid-01-current-vps-readonly-2026-08-24.json`](../deployment/evid-01-current-vps-readonly-2026-08-24.json) 与 [`evid-01-authority-inventory-snapshot-2026-08-24-pre-0055.json`](../deployment/evid-01-authority-inventory-snapshot-2026-08-24-pre-0055.json) | 仅做低频 SELECT/HTTPS GET 自动取证与 fail-closed 中间态建模；未部署、未执行 `0055`、未写生产、未创建审批，代码验收不等于生产验收。EVID-01 仍 `active`，下一步必须受控迁移、独立 root approval、生产 PG first-winner/revocation/rollback、同 alias 端到端验证与 owner/reviewer 双签，global execution deny 保持关闭 |
| 2026-08-23 | 第一期 P0 | EVID-02 当前候选 approval/current-head 新鲜只读复核 | 在 `4cef9040c` / release `20260822134658` 上以同一 `default` alias 执行一个 `REPEATABLE READ READ ONLY` 事务；`research_evidence_operator_spec`、`research_evidence_operator_spec_approval`、`research_activated_evidence_operator_spec` 均为 `0` 行，head=`empty`；工件 [`evid-02-select-only-vps-snapshot-2026-08-23-1336.json`](../deployment/evid-02-select-only-vps-snapshot-2026-08-23-1336.json)，SHA-256=`3fea758393a9bf2ca2f449d3dabe1c0726388ee964a76a97e068139be8e028a1` | 仅刷新真实账本现状，未创建 approval/activation、未回滚、未写生产；`EVID-02` 继续 `awaiting_production`，人工审批、PG 并发/rollback 和 owner/reviewer 签署不解锁 |
| 2026-08-23 | 第二期 P0 | STRAT-01 当前候选 owner-ledger 新鲜只读复核 | 在仍运行的 `4cef9040c` / release `20260822134658` 上，通过同一 PostgreSQL `default` alias 做 SELECT-only 盘点：Research `65` 张、Portfolio `7` 张、Account authority/assignment 广义匹配 `15` 张、owner/policy/operator/assignment 广义匹配 `34` 张均为 `0` 行；新工件 [`strat-01-owner-ledger-readonly-recheck-2026-08-23-1326.json`](../deployment/strat-01-owner-ledger-readonly-recheck-2026-08-23-1326.json)，SHA-256=`3ea5b041e6f59a2936d0b28aa89ea262eaf70c4f16769094d4d409a16c159849` | 只读复核确认 owner/definition/policy/calendar/scope/qualification 仍 zero-seed；广义 selector 仅作新鲜 recheck，不替换既有 canonical 9/9 口径；`STRAT-01` 继续 `awaiting_production`，不创建、回填、promotion 或审批 |
| 2026-08-23 | 第二期 P0 | 当前 HEAD 验收复核与 CI 收口 | `dev/next-development` HEAD=`07fd7c676946d1c7e5150ad57a5eee8f77cfcc8d`，与 `origin/dev/next-development` 同步；注册表与治理一致性均 `0` 违规，Evidence/MCP 与 decision write surface freeze 通过，TUI source guard 为 `12/24 screens`、`430/890 actions`、`0 violations`，EVID 定向回归 `59 passed`、TUI/冻结回归 `65 passed`、完整 `tests/unit/test_tui_workbench.py` `257 passed`；当前 HEAD 的四条 push CI 均成功 | 本轮只做本地/CI 验收回写，未部署 VPS、未写生产、未创建 approval/activation；EVID-01 仍因 zero-seed owner/tenant authority `active`/fail-closed，TAR-01 `capacity_ready=false`、M5 `DENY`，继续等待真实 authority、候选角色 UAT、容量/恢复/观察与 owner/reviewer 签署 |
| 2026-08-23 | 第二期 P0 | 当前分支计划验收与门禁复核 | `dev/next-development` HEAD=`c03d3c037` 且与 origin 同步；注册表、治理一致性、证据输出面冻结、决策写面冻结、TUI source guard 均通过，TAR-01=`CONTRACT_COMPLETE`/`safety_ready=true`/`capacity_ready=false`，M5 readiness=`DENY`；本轮未部署 VPS、未写生产、未改变 runtime gate | 代码/文档与自动化门禁已验收；EVID-01 仍因 zero-seed owner/tenant authority 保持 active/fail-closed，TAR-05/TUI-01 等仍等待候选、账号、容量/恢复/观察与 owner/reviewer；不把本地合同或只读健康误报为生产完成 |
| 2026-08-18 | P0 优先线 | Terminal Agent 多用户 runtime 立项 | 已完成现状根因、目标架构、服务端队列、专用 Worker、可恢复事件流、服务器端 CLI（用户不安装 provider-backed Agent）、角色分工、16–24 人日工期、SLO/测试/灰度/回滚的专项计划，并登记 TAR-01 至 TAR-05 | 当前原子工作包落盘后优先执行 TAR-01；未完成 TAR-05 前保持 inline 并发 1，不以直接扩容替代隔离整改 |
| 2026-08-18 | P0 优先线 | TAR-01 queued intake boundary | 新增 dormant `SubmitTerminalQueuedRunUseCase` 与 `TerminalQueuedSubmissionPort`；只接受 `web_queued`，校验 admission adapter 保留 run identity；纯边界回归 `31 passed`、增量 mypy/architecture 通过 | 未接入 Web/TUI 路由、durable queue、Celery/Worker 或生产运行；继续完成 TAR-01 ADR、负载基线和 failing-first/API boundary 证据 |
| 2026-08-18 | P0 优先线 | TAR-01 API/SSE wire contract | 新增纯 Application wire contract，冻结 `/api/terminal/runs/` create/status/events/cancel/queue 路径、`202` 接单响应、owner-scoped status/cancel envelope、SSE replay envelope、aware timestamps 与敏感字段拒绝；API/ports/run contract 合计 `41 passed`，增量 mypy/architecture 通过 | 仅完成命名与响应合同；未接入 Web/TUI route、durable PostgreSQL queue、broker/Worker、真实 SSE、容量基线或生产观察，TAR-01 仍 active |
| 2026-08-18 | P0 优先线 | TAR-01 baseline evidence contract | 新增纯 Application baseline 合同，要求单一环境/候选下 1/5/10/20 四档样本、web p50/p95/p99、CPU/RSS、Daphne、Redis、DB、MCP/模型指标，unavailable 不得伪装成零值；纯回归 `10 passed`、增量 mypy/architecture 通过 | 仅冻结未来采集器的证据形状，尚未进行受控 staging/production 压测、chaos 或容量验收，TAR-01/TAR-05 生产门禁不变 |
| 2026-08-12 | 基线 | 归档与排期 | 已在 `dev/plan-closure-by-priority` 创建基线提交 `919a9cea7` | 按本表期次继续独立提交 |
| 2026-08-12 | 第一期 P0 | Evidence M1 Domain 首批 | 统一分类、ArtifactRef、Track Record、Envelope、权限交集和 fail-closed 传播已实现；canonical hash、非有限 Decimal 和有效期防线已加固；纯 Domain `19 passed`，standalone strict mypy `0 errors` | 做 M1 persistence、API、审批激活和 adapters |
| 2026-08-12 | 第一期 P0 | Evidence M0 owner/freeze | ADR-0007 owner/接口矩阵已接受；54 个 HTTP、15 个 SDK、25 个发布态 TUI 决策 action、23 个 TUI mutation/AI/admin action 与 32 个 MCP 仓位相关写能力被机器门禁精确冻结，发布图 SHA 漂移也会阻断；聚合验证 `19 passed` | 补输出、raw/governed MCP 与旧 Transition Plan 语义分类，再推进 M1 持久化 |
| 2026-08-12 | 第一期 P0 | Evidence M0 输出分类首批 | 41 个高风险输出（其中 11 个直接影响仓位）被精确冻结；全部如实标记为 legacy boolean/ungated 或 research-only，机器门禁已建立 | 扩展 R1–R8、动态 payload、raw/governed MCP，并逐项接入 Evidence |
| 2026-08-12 | 第一期 P0 | Evidence M1 persistence 首批 | schema-only/zero-seed 的 Operator Spec、Track Record、Envelope append-only ledger，strict codec、公共 exact/PIT reader 和私有幂等 append store 已实现；内存数据库 `8 passed` | 补标准 pytest-django/PostgreSQL 并发验证、审批激活和各 App adapters |
| 2026-08-12 | 第一期 P0 | Evidence M1 exact read API 首批 | Application port/facade 与三个 staff-only detail endpoint 已实现；强制 identity/version/hash/PIT，未来 cutoff 双层拒绝，写方法 405；隔离 Django/DRF `18 passed` | 定义真实用户/租户 scope 后再做 owner-scoped 授权；继续审批激活和 adapters |
| 2026-08-12 | 第一期 P0 | Evidence M1 Operator Spec lifecycle 首批 | ID-only activation、可信 definition provider/Risk Center approval port、双读防漂移、原子 receipt+activation ledger 与完整 supersession replay 已实现；DB 约束阻断双 root/双 successor；纯测试 `11 passed`、隔离仓储 `3 passed` | 实现真实 Risk Center provider/composition，补 PostgreSQL 并发验证和各 App adapters |
| 2026-08-12 | 第一期 P0 | Evidence M0 CI wiring | 决策写面冻结与 Evidence 输出清单两条机器守卫已接入 consistency-check workflow，并纳入 governance wiring 自检 | 继续扩展输出分类与统一 Evidence adapters |
| 2026-08-12 | 第一期 P0 | Evidence M1 summary contract | 紧凑 `EvidenceSummaryDTO` 已固定 hashes/分类/权限/blockers/有效期，并区分 Track Record `not_required/unavailable/empty/available`；纯测试 `5 passed` | 接入各 App 输出 DTO 与 TUI Evidence Strip |
| 2026-08-13 | 第一期 P0 | Evidence M1 Risk approval provider 首批 | Risk Center 专用 immutable subject/approval ledger、human staff 非自审批、server clock、exact/PIT selector、tamper/ORM mutation 防线已实现；纯测试 `10 passed`、隔离 component `6 passed` | 补 Research↔Risk composition、生产人工审核入口和 PostgreSQL 并发验证 |
| 2026-08-13 | 第一期 P0 | Evidence M1 Research↔Risk composition | Research 经 Risk Center Application facade 精确读取审批，二次校验 owner/capability/definition/supersession，并以 approval record hash 封存外部真源；纯单元 `3 passed` | 补 Risk 人工 subject/审批入口、PostgreSQL 并发验证和各 App adapters |
| 2026-08-13 | 第一期 P0 | Evidence M1 Data Center legacy adapter 首批 | `QuoteResponse` 已转成 content-bound legacy Envelope 与统一 summary；精确重建阻断伪造 spec/权限/lineage，始终 `legacy_unverified + DISPLAY_ONLY`；聚合纯测试 `19 passed` | 补真实 Operator Spec/持久化/consumer 接线，并逐项迁移其余 40 个输出 |
| 2026-08-13 | 第一期 P0 | Evidence M1 人工 subject/审批写入面 | ID-only 注册/审批、Research 可信 definition、server-auth staff actor、CSRF/session/staff/POST-only 与两人审批已实现；纯 Application `13 passed`，架构扫描 0 违规 | 完整项目 DRF/component 未在当前依赖环境验证；补 PostgreSQL 并发、owner scope 与生产人工记录 |
| 2026-08-13 | 第一期 P0 | Evidence M1 Broker approval legacy adapter | `OrderApprovalSnapshot` 全字段复用 approval digest 做 content-bound legacy Envelope；严格 expiry/Decimal/金额/JSON/source IDs，恒为 display-only 且不接入 Broker 授权；聚合 `22 passed` | 这不是正式 Evidence 或执行许可；补真实 Operator Spec/持久化/consumer，并继续直接仓位输出 adapters |
| 2026-08-13 | 第一期 P0 | Evidence M0 Transition Plan internal writer freeze | 修正为 6 个 legacy（含 plan-linked approval status 旁路）与 4 个 canonical writer；canonical 模式统一阻断 legacy plan mutation，approve/reject 稳定返回 409，freeze `8 passed` | 默认开关仍关闭；先补同表 schema family 隔离和 cross-family 阻断，再迁移完整 generate/update/approval 工作流 |
| 2026-08-13 | 第一期 P0 | Evidence M0 Transition Plan 同表 family 隔离 | nullable schema-only family 字段；新 legacy/canonical 写入显式分类，双方跨 family decode/write/approve 均失败关闭；聚合纯测试 `16 passed`，架构 `207 edges / 0 cycles` | 补完整 Django migration/repository component、存量分类审计与 PostgreSQL 并发；默认开关仍关闭 |
| 2026-08-13 | 第一期 P0 | Evidence M0 Broker 动态输出冻结 | AST 精确冻结 8 个 `BrokerExecutionQueryService` dict 查询方法及其 8 个 GET 发布入口；新增、删除或 symbol 漂移均阻断，专属 `8 passed` | 这只是迁移分母；补 typed contract、Envelope/summary adapter、consumer 与执行硬闸重验，并盘点其余动态输出 |
| 2026-08-13 | 第一期 P0 | Evidence M0 R1–R6 输出分母扩展 | 补冻结 13 个 R1–R6 research-only 结果与 2 个 internal presenter；显式/直接仓位/marker/dynamic 分母为 `54/11/45/18`，专属 `9 passed` | 只完成分类冻结；补 R7/R8、mixed/variant schema、MCP 语义与正式 Evidence adapters/consumer binding |
| 2026-08-13 | 第一期 P0 | Evidence M0 MCP P0 输出语义冻结 | 精确冻结 11 个 tagged read、6 个 Broker native 与 Terminal read bridge；published 闭包 `430/408`，`raw_debug=430`、`evidence_binding=0`，integrated=`0`；专属 `6 passed` | Terminal result bridge 已另批暂停；继续迁移 Broker/tagged reads，并登记其余 28 个 marker 高风险面 |
| 2026-08-13 | 第一期 P0 | Evidence M0 Terminal MCP read kill switch | result bridge manifest 禁用且 handler 在 POST 前稳定阻断，coverage 不再把 disabled bridge 误报可达；408 个未绑定 read action 全部 blocked，普通 TUI/两张 graph 不变；聚合 `12 passed, 3 skipped` | 完成 metadata binding、runtime summary exact 校验与 MCP 白名单投影后才能恢复；当前 integrated 仍为 0 |
| 2026-08-13 | 第一期 P0 | Evidence M0 Broker MCP read kill switch | 禁用全部 6 个 Broker native read：4 个决策/执行发布面、存在 heartbeat freshness 假阳性的 connection，以及可泄漏任意 before/after 的 audit；P0 MCP disabled=`7`、integrated=`0` | 补 typed contract/summary/runtime binding；connection 增 freshness/blocker，audit 动态字段白名单后逐项恢复 |
| 2026-08-13 | 第一期 P0 | Evidence M1 Strategy DecisionResult legacy adapter | Strategy 决策 action/reason/有效期/confidence 已绑定 content hash，恒为 `legacy_unverified + display_only + must_not_execute`；聚合 `28 passed`、架构 0 违规 | 未接 production consumer；补 Operator Spec/持久化/硬闸。11 个 tagged MCP read 的暂停需用户明确授权 |
| 2026-08-13 | 第一期 P0 | Evidence M0 R7–R8 输出分母 | 新增 R7 结果/监控/lifecycle 与 R8 report/assembly/run/canonical snapshot/execution feedback 8 面，精确扩展既有合同；分母为 `62/11/53/18`，聚合 `39 passed` | 分母冻结不等于 Evidence；补 mixed/variant、其余动态面及 R7/R8 Operator Spec/Envelope/consumer |
| 2026-08-13 | 第一期 P0 | Evidence M1 Strategy OrderIntent legacy adapter | 完整 OrderIntent 顶层、Decision/Sizing/Risk 字段以 Decimal/aware time canonical hash 绑定，状态变化即使时间未刷新也产生新版本；恒为 display-only/must_not_execute；聚合 `46 passed` | 未接执行 consumer/正式 Operator Spec/持久化硬闸；有损 `OrderIntentResponseDTO` 仍未集成，旧直提交流程未改变 |
| 2026-08-13 | 第一期 P0 | Equity research snapshot 四入口收口 | 将 identity/quote/history/valuation/financial/news/flow 与严格 readiness 的归并迁入 Equity Application；新增 authenticated REST 与 SDK，MCP 改为单次 SDK 薄代理，Agent 保持能力路由；纯 Application+SDK/MCP `39 passed`，current-data 46 面与架构门禁通过 | API runtime 当前无 Django 5.2/DRF/Celery 完整环境，专属 API tests 未执行；Evidence 仍为 semantic overclaim，未声明 integrated |
| 2026-08-14 | 第一期 P0 | Equity research snapshot Django runtime contract 复跑 | Django 5.2.12 下 API `15 passed`、SDK/MCP/routing/evidence `36 passed`、use case/gateway `26 passed`，合计 `77 passed`；证明四入口软件契约与 fail-closed 行为一致 | 使用 mock/fake 与 `--no-migrations`，不代表生产数据覆盖、PostgreSQL 规模/故障注入、备份恢复或 readiness 解锁；生产证据仍缺 |
| 2026-08-13 | 第一期 P0 | Strategy execution preview 真实发布面收口 | 移除 Interface 的价格100/Unknown Regime/0.8置信度伪事实；行情/信号/Regime/账户及四类观测时间必填且 freshness 失败关闭；Domain allow 也恒为 display-only/can_execute=false；分母校正为 `63/12/53/18`，纯测试 `8 passed` | 尚非 Evidence wrapper；补正式 Operator Spec/Envelope。API/serializer 测试因当前环境缺 Django 未运行；下步先处理 Advisor live-draft consumer 与真实 Portfolio 分母 |
| 2026-08-13 | 第一期 P0 | Portfolio canonical transition 真实分母 | 补冻结 production `TransitionPlan/OrderDraft` 全字段与 composite，均标未接 Evidence/legacy ungated；确认 create/get/approve/submit 可达但尚无 Broker 真接线；分母校正为 `65/14/53/18`，专属 `12 passed` | 补 approve/submit Evidence exact 重验或独立授权的 display-only 硬阻断；默认 snapshot 校验仍关闭，旧 Domain 读链仍未退役 |
| 2026-08-13 | 第一期 P0 | Portfolio canonical account/submit 硬闸 | create/detail/approve/submit 全部绑定当前用户实际账户，跨账户403；APPROVED 计划在无正式 Evidence 时稳定阻断 execution_handoff，原链无 Broker 真调用；纯测试 `4 passed`、写面/架构门禁通过 | 创建/审批仍非 Evidence，默认 snapshot 校验仍关闭；补 exact Evidence graph 后方可恢复 submit；Django API tests 已写但当前环境未运行 |
| 2026-08-13 | 第一期 P0 | Advisor→Broker live draft consumer 硬闸 | 保留 advisor sheet 只读 preview，但发布 commit_allowed=false/display-only/must-not-execute；commit 与底层 converter 双层阻断，Classic 工作台不再展示提交按钮；inventory 如实改为 hard-blocked，分母仍 `65/14/53/18` | 补完整 Advisor Evidence graph 与 consumer exact 重验后才能恢复；Django component/API 待合格 runtime 复跑 |
| 2026-08-13 | 第一期 P0 | Broker connection 双时钟 current-data 硬闸 | Agent 上送 source observed_at，服务端保留 source/receipt 双时钟；connections/onboarding/lease 统一90秒规则，旧Agent或缺失/未来/过期源时间均降级且不能领单；current-data=`48 surfaces`，纯+SDK/MCP验证 `35 passed` | 生产 Windows Agent 须升级并重新观察 fresh heartbeat；MCP保持disabled，其他 Broker 输出仍待 typed Evidence 收口；Django component/API 待合格runtime复跑 |
| 2026-08-13 | 第一期 P0 | Broker overview 完整 current-evidence READY 门禁 | READY 改由 Application 从逐账户 binding、双时钟连接、SLA 内 snapshot、同 snapshot completed reconciliation、开放订单、kill/alert/difference 完整推导；查询时钟不再洗白源时间，SDK 原样保留门禁字段，MCP 保持 disabled；current-data=`49 surfaces`，纯测试 `29 passed` | 尚非正式 Evidence/Risk Authorization；生产 Agent/snapshot/reconciliation 与 Django API/component 待合格 runtime 和真实运行复验 |
| 2026-08-13 | 第一期 P0 | Broker live-order 四节点 Evidence 硬暂停 | create/approve/lease/submitting 在 Application+repository 双层 fail-closed；approve/advisor commit 的 MCP/TUI 发布面关闭，preview 与 reject/cancel/kill/对账保持；CI guard 冻结4节点，纯+MCP回归 `44 passed` | 明确暂停实盘新提交，尚非 Evidence 集成；须完成 owner-bound Evidence/Risk Authorization/plan receipt 四节点 exact 重验并补 Django/Agent runtime 后才能恢复 |
| 2026-08-13 | 第一期 P0 | Broker order detail legacy Evidence 只读收口 | exact重验 approval digest；raw risk/event payload 改 hash/白名单；lifecycle 与 actor授权分离；只恢复 closed-schema MCP详情读，聚合 `51 passed`，P0 MCP=`18 / disabled 6 / integrated 0` | 永久 display-only/must_not_execute，不解除四节点硬闸；Django/API/core-only MCP runtime待补，audit/reconciliation/catalog继续disabled并分批收口 |
| 2026-08-13 | 第一期 P0 | Broker audit 动态字段白名单与脱敏 | HTTP/SDK/Classic audit 不再透传 actor/resource/request身份、IP/UA、credential、risk、approval、recommendation、任意 Agent result；未知writer/command仅元数据+blocker，纯+SDK `12 passed` | MCP audit保持disabled且integrated=0；14-writer AST机器guard、Django component与Classic回归待补，随后收口reconciliation/catalog |
| 2026-08-13 | 第一期 P0 | Broker reconciliation typed current-evidence 只读收口 | 四维expected/actual白名单、源时点顺序、run/resolution状态、计数守恒、P0 auto-stop与唯一identity全部fail-closed；canonical hash+display-only markers，纯+SDK `13 passed` | 不是正式Evidence，MCP reconciliation保持disabled；total_asset差异、snapshot created_at绑定、Django/PostgreSQL持久化与并发验证待补 |
| 2026-08-13 | 第一期 P0 | Broker order catalog actor-aware display-only 收口 | 动态risk改hash、目录删敏感详情；16态/Decimal/时点不变量fail-closed；lifecycle×role/account×Evidence四层合成，approve固定false，权限缓存3A，纯+SDK `48 passed` | MCP catalog保持disabled且integrated=0；reject grant策略、Classic effective_actions、Django/query-count待补，正式receipts未完成 |
| 2026-08-13 | 第一期 P0 | Broker execution authorization 本地合同冻结 | Broker自有exact scope绑定Plan/审批、订单快照、Evidence envelope/spec/track record、Risk authorization、benchmark及有效期交集；canonical hash与supersession已冻结，聚合 `22 passed` | 合同固定inactive/must-not-execute；先补Portfolio与Risk owner receipts/ledger/provider，再做Broker签发、四节点重验，现有总闸不得解除 |
| 2026-08-13 | 第一期 P0 | Portfolio canonical plan integrity 与 inactive approval 合同 | canonical-v1哈希提升为Domain真源且字节兼容；repository读/批/重放重算拒绝篡改；approval receipt绑定plan/hash/actor/expiry但固定inactive，聚合 `15 passed` | 补append-only ledger、可信actor/clock、exact/PIT provider和canonical-v2完整输入hash；Risk/Broker未完成前submit与实盘总闸保持关闭 |
| 2026-08-13 | 第一期 P0 | Risk Center Broker order authorization Domain 合同 | Risk scope绑定account、execution scope、plan/approval、order、policy及有效期；双human-staff非自批、PIT与supersession已冻结，Risk+Broker gate聚合 `33 passed` | 尚无policy/provider/Application/ledger或first-winner并发证明；Broker issuer/四节点重验未接，总闸继续false |
| 2026-08-13 | 第一期 P0 | Risk Center Broker order authorization Application 合同 | register/approve写命令仅ID/version；server clock、scope+policy双读、账户闭合、current-head predecessor、first-winner/CAS与exact/PIT读协议已完成，纯fake+gate `39 passed` | 尚无生产policy/scope composition与Django append-only ledger/人工入口/PG并发；不可签发生产授权，Broker总闸保持false |
| 2026-08-13 | 第一期 P0 | Risk authorization first-winner/current-head 修正 | 修复register/approve跨时钟重试冲突；approve核对persisted subject winner；新增closed scope selector并拒绝已supersede旧授权，聚合 `41 passed` | 当前仍是pure fake协议；repository必须原子实现logical-head CAS，生产provider须保证exact版本也是current head |
| 2026-08-13 | 第一期 P0 | Risk Broker authorization append-only persistence | 独立private-UOW、两张append-only ledger、strict codec、identity/content/header seals、root/successor约束与current-head predecessor CAS已落盘；0008为schema-only/zero-seed | 当前Python缺Django，component/migration断言未运行；补项目runtime+PG并发、production provider/人工入口，未接Broker总闸 |
| 2026-08-13 | 第一期 P0 | Risk ledger chain/current-head 修正 | current-head改为全ledger restore后按Domain identity验证单根/同链前驱/时钟/可达；header篡改不能复活旧head，IntegrityError只返回exact candidate | component仍因缺Django未运行；补PG双事务root/successor race与完整tamper runtime，Broker总闸不变 |
| 2026-08-13 | 第一期 P0 | Portfolio inactive plan approval Application | ID-only register/approve/exact-PIT、trusted plan双读、server clock、persisted first-winner、跨时钟幂等与非自批已完成；聚合 `22 passed` | 仅pure fake且固定inactive；补Django exact provider/append-only ledger/人工入口/PG first-winner，不接旧approve/submit/Broker |
| 2026-08-13 | 第一期 P0 | Portfolio inactive receipt subject seal 修正 | receipt hash补绑定subject identity/hash与requester；actor_id/user_id双重非自批，winner replay限原requester/approver，exact read同时闭合subject+plan；专属 `19 passed` | plan_status仅inactive签发快照、未进入canonical-v1 hash；仍缺owner lifecycle event/ledger/runtime，不能用于submit/Broker |
| 2026-08-13 | 第一期 P0 | Portfolio inactive approval append-only persistence | private-UOW两表账本、strict codec、exact first-winner/PIT与approved_at provider已落盘；0017 schema-only/zero-seed；SQLite最小往返1+1条通过 | 完整Django component/migration与PG并发未验证；无人工入口，不接旧approve/submit/Broker，status仍非canonical-v1内容 |
| 2026-08-13 | 第一期 P0 | Risk-owned Broker execution policy Domain | 7项Decimal风险参数、source snapshot seal、账户/时钟/有效期/前驱与execution-eligible authority固定为content-addressed合同；聚合 `48 passed` | 仅Domain；现有mutable policy不得直接adapter，补source snapshot/activation/ledger/current-head provider前继续Unavailable且总闸false |
| 2026-08-13 | 第一期 P0 | Broker order approval owner artifact | 将canonical订单UUID/version、完整approval snapshot与既有digest、账户、批准人双身份/角色、批准时点和snapshot expiry封存为Broker自有content-addressed工件；金额闭合与推荐lineage失败关闭，专属 `21 passed` | 仅Domain seal，固定activation不可用/must-not-execute；尚无append-only ledger/provider，也不等于Plan、Research或Risk授权，总闸继续false |
| 2026-08-13 | 第一期 P0 | Risk execution policy Application workflow | ID-only激活绑定完整floor/template/account override/global+account exception source bundle；trusted source双读、server human-staff actor seal、first-winner/CAS predecessor与exact current PIT provider协议完成，Risk/Broker聚合 `66 passed` | 仅Application+pure fake；尚无source snapshot生产器、Django ledger/codec/provider或PG并发，zero-seed下authorization仍Unavailable且Broker总闸false |
| 2026-08-13 | 第一期 P0 | Broker order approval artifact append-only persistence | private-UOW单表账本、strict codec、identity/order/content first-winner、全header/ledger seal与历史exact/PIT reader已落盘；0008 schema-only/zero-seed，Django5.2 SQLite最小往返为 `1 / True / must_not_execute=True` | 只提供历史批准工件，不提供current execution permission；完整component/迁移往返与PG并发未验证，尚无pre-risk scope/provider，总闸继续false |
| 2026-08-13 | 第一期 P0 | Risk execution policy append-only persistence | 五类source bundle与actor-bound activation两表账本、strict codec、first-winner、account root/successor CAS和full-chain current head已落盘；0009 schema-only/zero-seed，Django5.2 SQLite最小往返 `1 source / 1 activation / current=True` | 尚无mutable policy→exact source生产composition或PG并发；zero-seed下不会自动激活policy，Risk authorization与Broker总闸继续关闭 |
| 2026-08-13 | 第一期 P0 | Broker/Risk ledger contract audit修正 | 0008/0009 model↔migration constraint deconstruct已对齐，Broker clock约束补persisted=recorded；Risk policy DTO/authorization scope拆分policy content hash与actor-bound activation hash，聚合 `53 passed`、架构2688/0 | 完整项目makemigrations仍被当前Django5.2环境缺cryptography阻断；Risk source exact selector闭集扫描与PG并发仍待补，执行总闸不变 |
| 2026-08-13 | 第一期 P0 | Risk policy source closed-world restore | source first-winner与activation binding改为先restore完整source ledger再按Domain identity选winner；双改identity tuple+seal也不能隐藏坏行或重开原identity | component回归已补但完整Django runtime尚待执行；PG race仍未验证，zero-seed与Broker总闸不变 |
| 2026-08-13 | 第一期 P0 | Broker pre-Risk inactive scope | ID-only注册在单一server cutoff双读Portfolio plan/inactive receipt与Broker order artifact，封存三源exact identity/hash/有效期并形成本地first-winner/current-head链；纯测试 `36 passed`、strict mypy与架构门禁通过 | scope固定inactive并保留5个blocker；尚无ORM ledger、跨账户namespace owner binding或Risk adapter，最终issuer/四节点重验与总闸均未启用 |
| 2026-08-13 | 第一期 P0 | Broker pre-Risk append-only persistence | private-UOW账本、strict codec、identity/content/root/successor first-winner与closed-world current-head restore已落盘；0009 schema-only/zero-seed，Django5.2最小往返 `1 / inactive / must_not_execute=True` | 完整pytest-django与PG race未验；固定inactive且Risk active port selector/依赖方向不闭合，未加伪adapter，最终issuer与四节点总闸继续关闭 |
| 2026-08-13 | 第一期 P0 | Broker/Portfolio 账户 namespace binding Domain | Broker int 与 Portfolio str 账户身份原样分域；账户身份真源归Account而非Portfolio，binding封存两侧owner/type/id/version/hash、human-staff双身份、identity/content hash、时钟与supersession，纯测试 `47 passed` | 仅inactive Domain；尚无Account/Broker可信source provider、ID-only workflow、ledger/人工入口/PG并发，不能解除pre-Risk namespace blocker或执行总闸 |
| 2026-08-13 | 第一期 P0 | Broker Plan→Order inactive binding Domain | 精确封存canonical-v1 plan/receipt/subject/order ordinal row bytes与Broker order artifact identity/digest；固定三方owner/type和最早有效期、successor logical subject，纯测试 `59 passed` | 仅inactive Domain；尚无owner providers、ID-only workflow/ledger/真实签发，不能解除pre-Risk blocker或执行总闸 |
| 2026-08-13 | 第一期 P0 | Broker/Account namespace binding Application | ID-only注册在同一server cutoff双读Broker与Account exact-current source；要求相同owner user、real+active，并以human-staff actor、first-winner/CAS封存inactive binding，聚合 `74 passed` | 仅协议+pure fake；两侧immutable source、binding ledger/composition/人工入口/PG并发均缺，不能解除pre-Risk blocker或执行总闸 |
| 2026-08-13 | 第一期 P0 | Broker/Account namespace binding ledger | strict codec/private UOW、single-root/predecessor CAS、closed-world exact/PIT/current与全seals完成；Django5.2 minimal zero-seed/append/exact通过 | 完整component/migration drift与PG并发未验；真实两侧source facade/composition/actor仍缺，固定inactive且不解除pre-Risk/执行闸 |
| 2026-08-13 | 第一期 P0 | Portfolio policy benchmark snapshot Domain | 封存Account identity、planning-policy activation、benchmark definition exact refs；Decimal组件唯一/有序/权重守恒，三源最早有效期与live inception时钟闭合，纯测试 `27 passed` | 固定inactive；三类owner source/ledger/provider、daily valuation、审批与Broker issuer均未实现，现有Float配置不得冒充正式benchmark |
| 2026-08-13 | 第一期 P0 | Portfolio policy benchmark definition Domain | 完整冻结成分/price ID/币种/Decimal权重、日历/价格/FX/公司行动/费用税费五类owner ref、估值cutoff/窗口/陈旧度与missing fail-closed；definition+snapshot `34 passed` | 五类methodology owner ledger/provider、definition ledger/activation、daily valuation与approval未完成；固定definition-only/must-not-execute |
| 2026-08-13 | 第一期 P0 | Portfolio policy benchmark definition ledger | strict codec、private UOW/claim、first-winner、全写绕过阻断及closed-world exact/PIT完成；Django5.2隔离组件 `11 passed`、Domain `7 passed`，0020 zero-seed | 不提供current/activation；PG并发、完整project drift和全目标mypy/ruff未验；五类methodology provider、activation/daily valuation/approval仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark trading-calendar methodology Domain | IANA timezone、完整逐日membership、valuation session/cutoff与DST fail-closed进入hash；纯测试 `8 passed`，与R8 monitoring calendar/Data Center事实分离 | 固定definition-only；ledger/两人activation/current provider及price/FX/corporate-action/cost-tax其余四类methodology未完成 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark trading-calendar methodology ledger | private UOW/claim、first-winner、逐日/DST/header seals与closed-world exact/PIT完成；Django5.2组件 `7 passed`，0021 zero-seed | 不提供current/activation；PG并发、完整mypy plugin及其余四类methodology/activation仍缺，benchmark不得提前active |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark price-fixing methodology Domain | close/nav/settlement与unadjusted显式口径、有序exact source refs、IANA cutoff、stale/missing/source failure全fail-closed；纯测试 `10 passed` | 固定definition-only；ledger/两人activation/current provider及FX/corporate-action/cost-tax三类methodology未完成 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark price-fixing methodology ledger | private UOW/claim、first-winner、source/DST/header seals与closed-world exact/PIT完成；Django5.2组件 `8 passed`，0022 zero-seed | 不提供current/activation；PG并发、两人activation及FX/corporate-action/cost-tax三类methodology仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark FX-fixing methodology Domain | currency pair/方向/inverse显式闭合，IANA cutoff、exact sources、stale/missing/source failure fail-closed，v1禁三角换汇；纯测试 `8 passed` | 固定definition-only；ledger/两人activation/current provider及corporate-action/cost-tax两类methodology未完成 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark FX-fixing methodology ledger | private UOW/claim、first-winner、source/DST/header seals与closed-world exact/PIT完成；Django5.2组件`7 passed`、Domain`8 passed`，0023 zero-seed | 不提供current/activation；首次发现的既有transition drift已在下一阶段修复，PG并发/mypy plugin未验；corporate-action/cost-tax仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark corporate-action methodology Domain | 五类事件矩阵闭合；分红/送股除权与支付日防重复、拆并股零收益、配股缺exact条款与选择证据即block，IANA业务日/source有效期与防双计策略入hash；纯测试`9 passed` | 固定definition-only/must-not-execute；ledger、两人activation/current provider、单次行动owner facts及cost-tax仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark corporate-action methodology ledger | private UOW/claim、first-winner、五类event/source/DST/header seals与closed-world exact/PIT完成；Django5.2组件`9 passed`、0025 zero-seed、portfolio no-drift | 无current/activation；PG并发/mypy plugin、单次行动owner facts与两人activation仍缺，definition不得冒充事实或执行权限 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark cost/tax methodology Domain | fee/tax exact sources逐条绑定scope/jurisdiction/方向/基数/时点/币种/Decimal与取整；显式零可封存但missing/unknown不得转零，分红entitlement一次计费并防重复；纯测试`46 passed` | 固定definition-only；不背书fixture税率，ledger、两人activation/current与真实fee/tax owner producers仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark cost/tax methodology ledger | private UOW/claim、first-winner、完整source/rule/Decimal/header seals与closed-world exact/PIT完成，DB守恒计数/authoritative clock；Django5.2组件`12 passed`、0026 zero-seed、portfolio no-drift | 无current/activation；PG并发/mypy plugin、真实fee/tax producers与统一五源两人activation仍缺，不背书fixture费率 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark methodology bundle activation Domain | benchmark definition与固定顺序五源refs一次性封存为bundle hash；server requester+第二staff双重非自批、single-root/predecessor successor与时钟/有效期闭合；纯测试`11 passed` | 只激活configuration bundle，daily valuation/Broker authority均false；Application/ledger/current provider/staff composition未完成，总闸不变 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark methodology bundle activation Application | ID-only注册/审批以单一cutoff双读definition+五源graph，actor-bound first-winner、同definition logical head与predecessor CAS；exact/current读区分unavailable与source substitution corruption；Domain/Application `20 passed` | 仅Protocol+pure fake且固定configuration-only/must-not-execute；ledger、真实owner readers/staff composition、daily valuation与PG并发仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio benchmark methodology bundle activation ledger | subject+activation双账本、private UOW/claim、per-definition root/predecessor CAS、五源/FK/actor/header seals与closed-world exact/PIT/current完成；Django5.2组件`13 passed`、0027 zero-seed、portfolio no-drift | 固定configuration-only/must-not-execute；完整全迁移启动超时，PG并发、owner readers/staff composition与daily valuation仍缺 |
| 2026-08-13 | 第一期 P0 | Portfolio inactive approval persistence clock修复 | subject/receipt显式写`persisted_at=authoritative recorded_at`并由DB约束；0024仅AlterField+2 checks，Django5.2隔离`5 passed`，portfolio migration no-drift | PostgreSQL migration/race和全项目回归未验；inactive语义、旧审批入口与Broker总闸不变 |
| 2026-08-13 | 第一期 P0 | Broker Plan→Order binding Application | ID-only注册在单一Broker cutoff双读exact plan ordinal row、inactive receipt与order artifact；三源owner/type/hash/account/有效期闭合、first-winner/CAS与closed-current完成，聚合 `90 passed` | 仅协议+pure fake；三个owner public readers、binding ledger/composition/真实签发与PG并发均缺，账户namespace/pre-Risk blocker和总闸不变 |
| 2026-08-13 | 第一期 P0 | Broker Plan→Order binding ledger | strict codec/private UOW、四元logical subject root/predecessor CAS、canonical-v1 raw row bytes/hash与closed-world exact/current完成；Django5.2 minimal round-trip通过 | 完整component/migration drift与PG并发未验；owner readers/composition/真实签发仍缺，固定inactive且不解除pre-Risk/执行闸 |
| 2026-08-13 | 第一期 P0 | Portfolio exact-active plan order owner reader | ID-only plan/version/ordinal/PIT查询由Portfolio发布canonical-v1 row bytes/hash与recorded/valid clock；相邻纯测试 `29 passed` | 仅Application Protocol；factory/composition未接，receipt与Broker artifact ID-only reader仍缺，Plan→Order真实签发不可用 |
| 2026-08-13 | 第一期 P0 | Portfolio inactive receipt ID-only owner reader | receipt ID/version/PIT查询重验receipt/subject/plan/account seals、issued=recorded clock及inactive markers；相邻测试 `29 passed` | owner factory/composition未接；只表达exact inactive receipt，不冒充logical current或执行许可 |
| 2026-08-13 | 第一期 P0 | Broker order artifact ID-only owner reader | artifact ID/version/PIT查询保留approved/recorded双时钟，封存digest/risk policy/order identity与inactive markers；纯测试 `20 passed` | identity-winner infra/factory/composition未接；三源真实同cutoff双读与Plan→Order签发尚未完成 |
| 2026-08-13 | 第一期 P0 | Plan→Order Portfolio owner adapters | exact plan adapter复用owner provider，receipt仅暴露identity winner；read-only runtime/factory缺失fail-closed，纯/factory `25 passed` | Django5.2 ORM component未执行；Broker runtime与跨App registry/真实三源签发未接，总闸不变 |
| 2026-08-13 | 第一期 P0 | Plan→Order Broker artifact owner adapter | closed-world restore后ID/version匹配，权威row recorded clock不被approval/request洗白；Django5.2组件 `4 passed`、纯 `22 passed` | 全仓component未完成；跨App fail-closed registry/三源composition未接，仍read-only/inactive |
| 2026-08-13 | 第一期 P0 | Portfolio planning policy definition Domain | 从mutable status中拆出policy ID/version、lot与5项exact Decimal定义；canonical hash/有效期闭合且无status/current/activation语义，纯测试 `75 passed` | 仅definition；先补append-only ledger/exact provider，再做两人activation；legacy status=active不得冒充正式activation |
| 2026-08-13 | 第一期 P0 | Portfolio planning policy definition ledger | strict codec+private UOW/claim、全写绕过阻断、identity/content/header/clock seal与closed-world exact PIT完成；Django5.2 zero-seed往返及隔离组件 `13 passed` | 不提供current/activation；完整manage.py drift因缺Celery、PG并发未验；activation ledger/composition与legacy迁移未完成 |
| 2026-08-13 | 第一期 P0 | Portfolio planning policy activation Domain | exact definition subject绑定server requester、definition seals/clock与predecessor；第二名human staff按actor/user双重非自批后签activation，definition+activation组合 `83 passed` | 仅configuration activation Domain，固定must-not-execute；ID-only workflow、两张ledger/current provider、legacy迁移和benchmark composition均未完成 |
| 2026-08-13 | 第一期 P0 | Portfolio planning policy activation Application | ID-only注册/审批，server actor/clock、definition与subject双读、first-winner/predecessor CAS及closed-current selector闭合；组合 `92 passed` | 仅Protocol+pure fake；definition/activation持久化、真实composition/actor interface、PG并发和legacy迁移未完成，执行总闸不变 |
| 2026-08-13 | 第一期 P0 | Portfolio planning policy activation ledger | subject+activation双账本、private UOW/claim、single-root/predecessor CAS、closed-world restore、FK/header/hash/clock seal与current head完成；Django5.2组件 `15 passed` | PG并发、完整manage.py drift与ruff未验；真实composition/actor、legacy迁移及benchmark消费未完成，仍must-not-execute |
| 2026-08-13 | 第一期 P0 | Account-owned account identity snapshot Domain | 字符串Account identity与底层整数unified provenance分离；封存underlying source seal、owner/real/active、TTL最早有效期；legacy默认user强制Account reclaim receipt，纯测试 `61 passed` | 仅inactive Domain；trusted provider、ID-only发行/reclaim、append-only ledger/current facade未实现，不能供namespace binding真实签发 |
| 2026-08-13 | 第一期 P0 | Account raw identity source Domain | 字符串Account identity与整数underlying provenance不cast；row source与assignment evidence入seal，authoritative/legacy-default/unknown三态fail-closed，纯测试 `27 passed` | 固定inactive；capture workflow/ledger/simulated adapter/assignment provider未实现，0013默认user不得洗白，reclaim receipt须以后置raw artifact为锚 |
| 2026-08-13 | 第一期 P0 | Account raw identity source Application | ID-only capture双读row observation与assignment evidence；server actor/clock、first-winner/CAS、authoritative/legacy exact矩阵与closed-current完成，组合 `40 passed` | 仅Protocol+pure fake；raw ledger/simulated adapter/assignment provider/composition未实现，unknown/legacy无marker保持零写 |
| 2026-08-13 | 第一期 P0 | Account raw identity source ledger | private UOW/claim、assignment/actor/header seals、single-root/predecessor CAS与closed-world exact/current完成；Django5.2组件 `11 passed`，0038 no-drift/zero-seed | 全仓pytest/真实migrate/PG并发未验；simulated adapter/assignment provider/composition仍缺，mutable row不获背书 |
| 2026-08-13 | 第一期 P0 | Account identity snapshot Application | 普通Issue与legacy Reclaim均ID-only；raw/receipt双读、server actor/clock、first-winner/CAS和exact/current reader闭合，legacy无exact reclaim receipt零写入；组合 `95 passed` | 仅Protocol+pure fake；raw adapter、reclaim receipt owner ledger、snapshot ledger/composition、真实actor与PG并发未完成 |
| 2026-08-13 | 第一期 P0 | Account identity snapshot ledger | actor/provenance/reclaim refs/identity-content-header-clock seals、private UOW/claim、single-root/predecessor CAS与closed-world exact/current完成；Django5.2 minimal往返通过 | 完整组件pytest无最终结果，PG并发/完整migrate未验；raw adapter、reclaim receipt owner ledger/provider、composition与actor仍缺 |
| 2026-08-13 | 第一期 P0 | Broker-owned broker account identity snapshot Domain | 封存Broker账户、Account exact source、binding/Agent owner seals和keyed QMT ref digest；owner/real/active一致、TTL最早有效期与successor闭合，纯测试 `34 passed` | 仅inactive Domain；Account facade、Broker raw provider/digest service、ID-only发行、ledger/current reader均缺，不能接namespace binding真实composition |
| 2026-08-13 | 第一期 P0 | Broker account identity snapshot Application | ID-only发行双读Account exact source与Broker binding/Agent raw facts；QMT opaque bytes仅进keyed digest service，server actor、first-winner/CAS和closed-current闭合；组合 `54 passed` | 仅Protocol+pure fake；真实Account facade/raw provider、key service、snapshot ledger/composition、actor与PG并发未完成，ruff未验证 |
| 2026-08-13 | 第一期 P0 | Broker account identity snapshot ledger | actor/Account ref/binding/Agent/keyed digest与全套seals、private UOW/claim、single-root/predecessor CAS、closed-world exact/current完成；Django5.2 minimal往返通过 | 完整组件/migration state/mypy/ruff与PG并发未验；真实facade/raw provider/key service/composition/actor仍缺，不能接namespace binding |
| 2026-08-13 | 第一期 P0 | Account owner assignment evidence Domain | canonical Account字符串身份与SimulatedTrading整数row provenance分离；authoritative/legacy-default按精确row与provenance receipt封存，claimant+独立staff approver两人制，纯测试`25 passed` | 固定inactive/evidence-only；Application/ledger/provider/simulated adapter/人工接口未实现，0013默认user仍不得洗白 |
| 2026-08-13 | 第一期 P0 | Account raw-source assignment evidence 类型收口 | authoritative/legacy-default统一消费正式`account_owner_assignment_evidence`，仅以state区分语义；Domain/Application/ORM/0038同步收紧，纯测试`40 passed` | 0038 zero-seed且无composition；Django migration drift/component未复跑，正式两人workflow/provenance provider仍缺，不升级历史默认user |
| 2026-08-13 | 第一期 P0 | Account owner assignment Application | ID-only subject注册与staff审批；claimant只取exact provenance receipt，单事务单时钟双读，subject seal进入最终Evidence，first-winner/CAS与exact/current闭合；`37 passed` | subject/evidence ledger、owner receipt/provider、Simulated row adapter、composition/interface未完成；0013无receipt故legacy零写，总闸不变 |
| 2026-08-13 | 第一期 P0 | Account owner assignment ledger | subject/evidence双账本封存row/provenance/claimant与subject hash，private UOW/claim、single-root/predecessor CAS、closed-world exact/PIT/current及authoritative persisted clock完成；Django5.2组件`12 passed`、0039 zero-seed、account no-drift | PG并发/full regression/mypy plugin未验；owner receipt/provider、Simulated adapter、composition/interface仍缺，固定inactive且不解除总闸 |
| 2026-08-13 | 第一期 P0 | Account physical account-row observation Domain | Account字符串identity与Simulated整数row provenance原样封存，nullable/present row user均固定unknown owner；raw-source/TTL min validity、successor与PIT final不回退闭合；纯测试`25 passed` | 固定inactive/evidence-only；capture workflow/ledger/Simulated provider及creation/manual-reclaim/migration receipts未完成，不背书0013默认user |
| 2026-08-13 | 第一期 P0 | Account physical account-row observation Application | 8字段ID-only capture、raw row同cutoff双读、server actor-bound first-winner、logical-head/predecessor CAS及exact/current读闭合；nullable user/type/active/row clock原样保留；Domain/Application`40 passed` | 仅Protocol+pure fake且owner固定unknown；ledger、Simulated adapter/composition与独立provenance receipts未完成，总闸不变 |
| 2026-08-13 | 第一期 P0 | Account physical account-row observation ledger | private UOW/claim、raw-source/logical-row first-winner、root/predecessor CAS、closed-world exact/PIT/current与actor/header/clock seals完成；Django5.2组件`21 passed`、0040 zero-seed、account no-drift | 真实migration/PG并发未验；Simulated adapter/composition、生产actor与provenance签发未完成，owner固定unknown |
| 2026-08-13 | 第一期 P0 | Account owner-assignment provenance receipt Domain | creation/manual-reclaim/migration单一closed discriminator；exact绑定physical-row owner/type/id/version/identity/content与validity，authoritative owner=claimant，migration staff reviewer永不声称owner；相邻Domain`57 passed` | 固定inactive/evidence-only；Application/ledger/真实签发provider与assignment adapter未完成，0013/current row不得冒充receipt |
| 2026-08-13 | 第一期 P0 | Account owner-assignment provenance receipt Application | 5字段ID-only签发，同cutoff双读physical row；creation/manual当前claimant、migration当前staff reviewer，issuer-bound first-winner、logical-head/predecessor CAS与exact/current闭合；组合`75 passed` | 仅Protocol+pure fake；ledger、真实owner provider/composition、签发入口与assignment adapter未完成，0013 legacy继续零写 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment provenance receipt ledger | strict单表账本封存row/issuer/authority/header/clock seals，private UOW/claim、receipt first-winner、single-root/predecessor CAS与closed-world exact/PIT/current闭合；Django5.2组件`10 passed`、0041 zero-seed | PG并发/真实migrate未验；physical provider/composition、签发入口与assignment adapter仍缺，固定inactive且0013不得回填 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading account-row source Domain | owner-issued source ID/version/hash封存Account字符串identity、underlying整数row、nullable user/raw type/active/row clocks；tombstone与source/TTL validity、successor/PIT final不回退闭合；纯测试`29 passed` | 固定unknown-owner/inactive；Application/ledger、全create/update/delete事务writer、Account adapter/composition未完成，禁止pk/updated_at/now临时洗白 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading account-row source Application | 6字段ID-only capture，同cutoff双读owner-issued typed observation；actor-bound first-winner、logical-head/predecessor CAS、exact/current及tombstone/expired不回退闭合；Domain/Application`47 passed` | 仅Protocol+pure fake；ledger、全writer、production adapter/composition与Account映射未完成，禁止updated_at/now临时发明source |
| 2026-08-13 | 第一期 P0 | SimulatedTrading account-row source ledger | strict owner账本封存row/presence/tombstone/actor/header/clock seals，private UOW/claim、source first-winner、logical-row single-root/predecessor CAS与closed-world exact/PIT/current闭合；Django5.2组件`3 passed`、0021 zero-seed | PG并发/真实migrate未验；全create/update/delete writer与raw provider仍缺，ledger当前zero-seed；下一步owner-side Account physical provider只能消费existing row |
| 2026-08-13 | 第一期 P0 | SimulatedTrading owner-side Account physical-row provider | exact identity winner与final logical head双闭合，present/active/fresh才逐字段映射Account DTO；superseded/tombstone/expired/missing均None且不回退；pure`3 passed` | ledger zero-seed；raw observation/outbox与全writer仍缺，真实composition启动因缺Celery未验，Account capture继续零写 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw account-row observation Domain | owner raw observation封存row/user/type/active/presence/tombstone与owner clocks/hash；root/successor/PIT final no-fallback闭合；pure`23 passed` | Application/ledger/outbox/全writer未完成；source v1未封raw hash，后续必须v2，禁止现表回填或clock/hash洗白 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw account-row observation Application | exact Domain-only record；version=owner transaction/outbox event identity；authoritative recorded clock、first-winner/predecessor CAS与exact/current闭合；组合`31 passed` | 仅Protocol+pure fake；raw ledger、owner adapter/source v2与全writer同事务outbox未完成，production zero-seed |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw account-row observation ledger | strict无FK账本、private UOW/claim、identity first-winner、logical-row root/predecessor CAS及closed-world PIT/head闭合；Django5.2组件`5 passed`、0022 zero-seed | PG并发/真实migrate未验；owner provider/source v2与全writer同事务outbox仍缺，既有行不回填 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw-bound account-row source v2 Domain | 独立v2 type/schema封存raw owner/type/schema、ID/version、identity/content/predecessor hash与exact clocks；source/raw identity不可alias，双链successor与PIT no-fallback闭合；pure`41 passed` | 仅Domain；Application、0023 zero-seed ledger、owner/Account v2 adapters与全writer同事务outbox仍缺；禁止fallback v1，总闸不变 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw-bound account-row source v2 Application | ID/hash-only capture同cutoff双读exact-current raw owner source；source/raw双链first-winner/predecessor CAS，current重验raw final head防止projection lag；组合`56 passed` | 仅Protocol+pure fake；0023 ledger、raw owner/Account v2 adapters和全writer同事务outbox仍缺；v1不得current fallback，总闸不变 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw-bound account-row source v2 ledger | 独立strict/closed-world账本封存raw/row/logical/clock/header seals；private UOW/claim、identity/raw binding/root/predecessor CAS闭合；Django5.2组件`5 passed`、0023 zero-seed | PG并发/真实migrate、raw owner/Account v2 adapters与全writer outbox仍缺；不回填/不fallback v1，总闸不变 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading raw observation → source v2 owner provider | raw identity winner与logical final head双闭合，ID/version/hash/row/clocks原样映射；tombstone可读，superseded/expired/future不回退；pure`8 passed` | 只读composition无UOW/writer；两账本zero-seed、Account v2 consumer和全writer raw outbox仍缺，总闸不变 |
| 2026-08-13 | 第一期 P0 | Account physical-row observation v2 Domain | 独立v2完整封存并重算source/raw双层canonical hashes，三重predecessor与三层时钟/有效期闭合；联跑`101 passed`、architecture`2799/0` | Application、0042 ledger、owner provider与全writer raw outbox仍缺；v1不fallback，provenance/总闸不变 |
| 2026-08-13 | 第一期 P0 | Account physical-row observation v2 Application | ID/hash-only capture；source exact-final/current双读面、同cutoff双读、actor first-winner、三重predecessor CAS、exact PIT/full-selector current闭合；pure`43 passed` | 0042 ledger、owner provider/composition与全writer outbox仍缺；v1/provenance/总闸不变 |
| 2026-08-13 | 第一期 P0 | Account physical-row observation v2 ledger | strict/closed-world账本封存Account/source/raw/actor/header seals；private UOW/claim、identity/source/root/predecessor CAS；Django5.2组件`2 passed`、0042 zero-seed | PG并发/真实migrate、owner composition与全writer outbox仍缺；v1/provenance/总闸不变 |
| 2026-08-13 | 第一期 P0 | SimulatedTrading source v2 → Account v2 owner provider | source winner/final head双闭合；exact-final传播terminal、exact-current仅live；完整source/raw/header/hash/clock原样映射；pure`9 passed` | read-only composition；三账本zero-seed、canonical selector/Account wiring与全writer outbox仍缺，总闸不变 |
| 2026-08-13 | 第一期 P0 | SimulatedAccount raw mutation writer 安全前置 | typed mutation + physical-row head；显式同alias外层事务、opaque stream/event、create/update/delete/tombstone CAS；unit`11 passed`、component`5 passed` | 尚未接任何生产writer；必须一次性覆盖全部ORM/Admin/cascade路径并封旁路后才能seed，总闸不变 |
| 2026-08-13 | 第一期 P0 | Account v2 自动证据 recorder 语义修正 | human-staff capture改为fixed service/evidence_projector；codec/0042/header/recorder+record+ledger seals同步；pure`43 passed`、component`2 passed` | 仅技术provenance修正，不提升owner/permission；pipeline/canonical identity/全writer cutover仍缺，总闸不变 |
| 2026-08-13 | 第一期 P0 | SimulatedAccount 三账本 Evidence Pipeline 安全前置 | 外层同alias事务内固定raw→source-v2→Account-v2；hash/terminal原样传播、exact type重验、无v1 fallback；unit`8 passed`、architecture 2807/0 | 未composition/未接生产writer；canonical Account identity仍须owner exact provider，三账本zero-seed、总闸不变 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment claimant provenance receipt v2 Domain | 独立v2精确绑定physical/source/raw三层hash/clock/presence；claimed_owner/legacy_default_claim仅human声明、live row签发、双predecessor no-fallback；pure`22 passed`、architecture 2808/0 | 仅Domain；Application/0043/staff approval evidence v2与authoritative provider仍缺，不得接production identity，总闸不变 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment claimant provenance receipt v2 Application | 6字段ID/hash-only签发；physical-v2同cutoff双读、actor-bound first-winner/head CAS，exact/current重验upstream且无v1 fallback；pure`9 passed`、architecture 2809/0 | 仅Protocol+fakes；0043/provider/staff approval evidence v2与authoritative identity仍缺，pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment claimant provenance receipt v2 ledger | strict/closed-world 0043单表账本；private UOW、identity/root/predecessor CAS与physical/source/raw/actor/header seals；Django5.2组件`5 passed`、migration state无漂移 | zero-seed；PG race/真实migrate、provider/staff approval evidence v2与authoritative identity仍缺，pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment staff approval evidence v2 Domain | subject重验完整physical-v2与claimant receipt-v2；独立human staff双维防自批，claim→authoritative/legacy→ownerless精确映射，双root、approval TTL与相邻successor闭合；pure`28 passed`、architecture 2813/0 | 仅Domain；两阶段Application、0044账本、authoritative current provider与PG并发仍缺，pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment staff approval evidence v2 Application | ID/hash-only subject注册与staff审批；两upstream同cutoff双读、双mapping head一致、actor-bound first-winner/predecessor CAS及exact/current重验闭合；组合`36 passed`、architecture 2814/0 | 仅Protocol+fakes；0044账本、双root DB约束、authoritative provider/composition与PG并发仍缺，pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment staff approval evidence v2 strict codec | 完整嵌套subject/physical-v2/claimant-receipt-v2/Evidence，exact shape/type、UTC-Z与canonical roundtrip；codec`9 passed` | 无ORM/repository/migration；0044双表/双root/closed-world component与authoritative provider仍缺，不能视为可用账本 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment staff approval evidence v2 0044 schema | subject/Evidence双表、OneToOne PROTECT、private UOW/claim、全mutation guard；Account/underlying双root分别partial unique，successor predecessor unique；Django5.2 model component`4 passed`、architecture 2816/0 | 尚无repository；closed-world restore/双head/IntegrityError、真实migrate/rollback与PG race仍缺，不可作为可用账本 |
| 2026-08-13 | 第一期 P0 | Account owner-assignment staff approval evidence v2 repository | full-table restore后canonical selector；双mapping完整链/同head、predecessor CAS、exact/PIT/first-winner与IntegrityError exact幂等闭合；component`9 passed`、pure`45 passed`、architecture 2817/0 | PG四类race/真实0044 migrate、authoritative provider/composition仍缺；zero-seed且pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account authoritative mapping v2 read-only facade | 以underlying namespace/id + PIT选择最终Evidence head，委托双mapping head与physical/claimant receipt current重验；authoritative只发布identity_mapping_only，legacy/missing/stale返回None；pure`11 passed`、architecture 2818/0 | 无production composition；首次create仍存在canonical identity→physical-v2→staff Evidence→canonical identity的bootstrap循环，pipeline/writer禁用，execution恒false |
| 2026-08-13 | 第一期 P0 | Account canonical creation allocation/binding Domain | Account owner在row产生前分配opaque canonical ID；一次性binding闭合allocation、live Physical-v2 root、user/type及Account/underlying双claim，固定pending-owner-approval/inactive/must-not-execute；pure`5 passed`、architecture 2819/0 | 仅Domain；Application/ledger、Physical-v2封存allocation、creation receipt绑定binding和全writer原子cutover仍缺；现pipeline仍禁用unverified caller reference |
| 2026-08-13 | 第一期 P0 | Account canonical creation allocation/binding Application | allocation命令不含Account ID/user/clock，由server requester/generator/clock签发并按request first-winner幂等；binding仅ID/hash selector，同cutoff双读exact-unconsumed allocation与Physical-v2 root，四唯一锚append；Domain+Application`9 passed`、architecture 2820/0 | 仅Protocol+fakes；未提供ledger/composition，现Physical schema仍未封存allocation，pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account canonical creation strict codec | Allocation完整requester/service/fixed/hash/UTC-Z；Binding嵌套完整Allocation及Physical-v2 canonical payload，exact keys/types与canonical roundtrip/tamper fail-closed；unit`12 passed` | 无ORM/repository/migration；0045 schema、closed-world ledger、Physical allocation seal与pipeline仍缺 |
| 2026-08-13 | 第一期 P0 | Account canonical creation 0045 schema | Allocation/Binding双append-only表，private UOW/exact claim及全mutation guard；Account ID/request幂等、allocation OneToOne消费、Account/underlying/Physical四锚唯一与fixed/persisted clock DB约束；Django5.2 isolated component`3 passed`、architecture 2822/0 | 尚无repository；0045仅CreateModel/zero-seed，真实migrate/rollback、PG竞争、Physical allocation seal及pipeline仍缺 |
| 2026-08-13 | 第一期 P0 | Account canonical creation repository | 全表strict restore后做allocation identity/request/exact/current-unconsumed与binding four-anchor/exact/append；逐列seals/OneToOne/时钟重验，消费/过期不回退，IntegrityError仅exact candidate幂等；Django5.2 models+repo component`9 passed`、architecture 2823/0 | PG同allocation/request/Account/underlying/Physical竞争与真实migrate未验；Physical/receipt新schema及pipeline/writer仍禁用 |
| 2026-08-13 | 第一期 P0 | Account allocated Physical-v3 creation-root Domain | 独立v3 wrapper完整封存exact allocation + Physical-v2 root，强physical/source/raw三predecessor为空、live/present、label/user/type一致、三路min-validity与canonical nested hash；pure`28 passed`、architecture 2824/0 | 仅creation root，无binding/successor/Application/ledger；待durable Binding-v2后再定update/delete，无v2 fallback，pipeline/writer禁用 |
| 2026-08-13 | 第一期 P0 | Account durable canonical creation Binding-v2 Domain | exact allocation + allocated Physical-v3 root、Account/underlying双claim及v3/v2/source/raw hashes完整封存；仅在recorded_at要求上游有效，之后映射耐久不受TTL影响，固定inactive/unknown/must-not-execute；pure`10 passed`、architecture 2825/0 | 仅Domain；Application/codec/ledger/provider及Physical-v3 successor仍缺，旧Binding-v1不做update/delete anchor |
| 2026-08-13 | 第一期 P0 | Account durable canonical creation Binding-v2 Application | ID/hash-only命令同cutoff双读exact-unconsumed allocation与Physical-v3 root；server binder/clock、identity+四锚first-winner/append与永久exact knowability闭合；Domain+Application`29 passed`、architecture 2826/0 | 仅Protocol+fakes；独立ledger/provider、跨v1/v2 allocation消费、Physical-v3 Application/账本及receipt/Evidence-v3仍缺，pipeline/writer禁用 |
| 2026-08-14 | 第一期 P0 | Account allocated Physical-v3 creation-root Application | ID/hash-only命令双读allocation+Physical-v2，独立service projector、winner/head/root-CAS、exact PIT/closed-current与消费后winner replay闭合；四批组合`61 passed`、architecture 2827/0 | 仅Protocol+fakes；0046 ledger、0047统一consumption claim/Binding-v2 ledger、successor及receipt/Evidence-v3仍缺，pipeline/writer禁用 |
| 2026-08-14 | 第一期 P0 | Account durable canonical creation Binding-v2 strict codec | 完整嵌套allocation、Physical-v3/v2、source/raw与binder canonical payload；exact keys/types、UTC-Z、Domain重建与全层tamper fail-closed；codec`48 passed` | 无model/repository/migration；0047统一consumption claim/Binding-v2 ledger、跨v1/v2并发与真实migrate仍缺 |
| 2026-08-14 | 第一期 P0 | Account allocated Physical-v3 creation-root 0046 ledger | 单表完整nested allocation+Physical-v2/source/raw与projector seals；五锚unique、private UOW、root-only CAS、closed-world exact/PIT/head/tamper闭合；Django5.2 isolated component`4 passed`、architecture 2831/0 | 0046仅CreateModel/zero-seed；PG五锚竞争/真实migrate未验，0047统一consumption claim/Binding-v2 ledger仍缺，pipeline/writer禁用 |
| 2026-08-14 | 第一期 P0 | Account canonical creation unified Consumption Claim Domain | exact allocation + exact Binding-v1/v2 consumer分支；非递归consumer ref防hash循环，同recorded_at、Account/underlying raw claims及Physical-v2/v3矩阵闭合；pure`30 passed`、architecture 2832/0 | 仅Domain；codec、0047 expand、v1 dual-write/backfill、Binding-v2 repo与0048 contract仍缺，生产zero-seed未逐alias证明 |
| 2026-08-14 | 第一期 P0 | Account canonical creation unified Consumption Claim strict codec | nested allocation完整恢复，consumer由repository按非递归ref加载并注入核对；exact keys/types、UTC-Z、canonical roundtrip与全selector/hash/tamper fail-closed；codec`32 passed` | 无model/repository/migration；0047 expand、closed-world consumer恢复、v1 dual-write/backfill与0048 contract仍缺 |
| 2026-08-14 | 第一期 P0 | Account canonical creation consumption 0047 expand schema | 新Claim+Binding-v2 append-only表及旧Binding-v1 nullable claim FK；raw Account/underlying/Physical跨generation unique、branch/fixed/clock checks与private guards闭合；Django5.2 isolated`6 passed`、architecture 2834/0 | 仅expand且zero-seed/no-backfill；v1 dual-write、逐alias盘点/backfill、Binding-v2 repo、0048 NOT NULL contract与PG交叉竞争仍缺，writers禁用 |
| 2026-08-14 | 第一期 P0 | Account canonical creation unified Claim Application workflows | Binding-v1/v2均由allocation identity+generation确定性派生claim identity；两者先replay winner，再按authoritative recorded clock双读并原子append Binding+Claim pair；聚合pure`85 passed`、architecture 2834/0 | 仅Protocol+fakes；v1/v2 production repositories、逐aliasinventory/backfill、0048 contract与PG交叉竞争仍缺，writers/pipeline禁用 |
| 2026-08-14 | 第一期 P0 | Account Binding-v2 unified Claim repository | closed-world恢复allocation/root/v1/v2/claim并验证全headers/seals；allocation parent锁、Claim→Binding-v2同事务双写、legacy null-claim占用与exact pair replay闭合；Django5.2 isolated repo`8 passed`、组合`17 passed` | v1 repo dual-write、逐aliasinventory/backfill、0048 contract与PG交叉竞争仍缺；repository保持dormant，writer/pipeline禁用 |
| 2026-08-14 | 第一期 P0 | Account Binding-v1 unified Claim dual-write repository | 0045/0047同alias双UOW，full-world恢复v1/v2/root/claim；current-unconsumed跨generation闭合，新写强制Claim→Binding-v1 pair且legacy null-FK仅历史fail-closed兼容；Django5.2 v1`6 passed`、组合`17 passed`、architecture 2835/0 | 逐aliasinventory/backfill、0048 contract与PG v1/v2竞争仍缺；composition/writer/pipeline禁用 |
| 2026-08-14 | 第一期 P0 | Account canonical creation consumption 逐alias盘点与回填预览 | 精确0045–0047 migration、列/nullability/constraint/FK及全账本closed-world后发布稳定inventory SHA；deterministic v1 backfill只预览候选与冲突，Django5.2 isolated`10 passed` | 未访问生产alias；缺backfilled-at与writer-freeze，写模式稳定阻断；0048/PG交叉竞争/production zero-seed均未证明 |
| 2026-08-14 | 第一期 P0 | Account canonical creation consumption 运维命令 | inventory/backfill命令发布稳定单行JSON；dry-run默认，batch明确reserved；`--write`即使PG+fresh SHA也因writer-freeze缺失稳定阻断；pure`14 passed`、architecture 2839/0 | 不接Task Monitor/Celery；补advisory writer-freeze、真实backfilled-at、逐alias签字与PG竞争后方可写入/0048 |
| 2026-08-14 | 第一期 P0 | Account canonical creation consumption writer-freeze 前置 | v1/v2 writer UOW在PG事务内取得同key shared advisory lock；maintenance exclusive helper默认PG-only，SQLite须显式test degradation；组合`18 passed` | backfill仍在写事务前阻断；缺真实PG双连接竞争与knowledge/backfilled clock，不能启用exclusive maintenance或0048 |
| 2026-08-14 | 第一期 P0 | Account canonical creation consumption knowledge clock expand | 0048 nullable/indexed knowledge_at不改Domain/hash/seals；live同钟，winner/anchor/unconsumed PIT按真实knowledge clock，NULL/倒退fail closed；component`29 passed`、command`14 passed`、architecture 2840/0 | 既有Claim仍NULL；补exclusive knowledge backfill、NOT NULL/check contract、真实PG迁移与竞争，完整no-drift因环境缺Celery未运行 |
| 2026-08-14 | 第一期 P0 | Account canonical creation consumption knowledge backfill engine | transitional closed-world区分已有NULL Claim/legacy null-FK/缺Claim；exclusive lock后取DB clock并以完整anchors CAS，错误整批rollback；component`34 passed`、architecture 2840/0 | 默认无write authorization且command继续阻断；补production inventory SHA授权、PG双连接证明与逐alias签字后再回填/contract |
| 2026-08-14 | 第一期 P0 | Account owner-assignment creation claimant receipt v3 Domain | 仅接受exact durable Binding-v2，并重验allocation、allocated Physical-v3、Physical-v2/source/raw及Account/underlying全seal；creation claimant必须同时等于allocation requester与live physical row user，固定claim-only/inactive/must-not-execute；pure`16 passed`、strict mypy、architecture 2841/0 | 仅Domain；manual reclaim/migration明确不在本合同，Application/ledger、独立staff Evidence-v3、Physical-v3 successor/current provider及生产writer均未完成 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment creation claimant receipt v3 Application | 6字段ID/hash-only签发先重放immutable winner，再在单事务cutoff双读durable Binding-v2、exact-current Physical-v3与当前human claimant；历史exact在recorded后永久可读，closed-current才要求receipt TTL、最终head与Physical-v3 current；Domain+Application`24 passed`、strict mypy、architecture 2842/0 | 仅Protocol+pure fake；append-only ledger/provider、独立staff Evidence-v3与真实actor/composition未完成，manual/migration与生产writer继续阻断 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment creation claimant receipt v3 strict codec | exact Receipt-v3 record完整嵌套并重建Binding-v2→allocation→Physical-v3→Physical-v2→source/raw；exact keys/types、UTC-Z、claimant/issuer、fixed booleans/hash与canonical roundtrip篡改失败关闭；codec`17 passed`、strict mypy | 无model/repository/migration；0049独立zero-seed账本、provider/composition、staff Evidence-v3与PG并发仍缺 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment creation claimant receipt v3 0049 ledger | append-only账本完整封存Binding-v2/Claim knowledge、allocation/Physical-v3/v2/source/raw、actor及chain seals；全Receipt与Consumption closed-world后再PIT，late knowledge不可洗白；Django5.2 isolated`7 passed`、architecture 2846/0 | zero-seed；PG竞争/真实migrate、owner provider/composition与production actor入口仍缺 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment staff approval Evidence v3 Domain | creation-only subject显式绑定11项Receipt/Binding/root/Account/underlying/Physical/source/raw seals；独立human-staff双维防自批，authoritative owner=claimant，双mapping root分域且candidate-independent；组合pure`33 passed`、architecture 2846/0 | 仅Domain；Application/codec/0050账本/current facade、staff composition与production入口仍缺，Receipt claimant不得直接提升为authority |
| 2026-08-14 | 第一期 P0 | Account owner-assignment staff approval Evidence v3 Application | ID/hash-only Subject注册与staff审批；winner-first历史重放不依赖当前approver，new write在recorded clock重验Receipt-v3/Physical-v3/approver/双heads；exact永久而current按ID/hash重验双root/upstream；组合pure`48 passed`、architecture 2848/0 | 仅Protocol+fakes；0050双表账本、production providers/composition、staff入口与PG竞争仍缺 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment staff approval Evidence v3 strict codec | 公开Subject/Evidence codecs完整重建Receipt-v3→Binding-v2→Physical-v3/v2→source/raw；exact keys/types、UTC-Z、fixed booleans、hash/seal与canonical roundtrip；codec`24 passed`、组合`30 passed` | 无ORM；0050需逐行闭合Receipt/Binding/Physical FK与Consumption Claim knowledge，provider/composition仍缺 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment staff approval Evidence v3 0050 ledger | Subject/Evidence append-only双表以PROTECT FK封存Receipt-v3/Binding-v2/Physical-v3和独立approver；full restore后逐行闭合FK、0049 Receipt与Claim knowledge，双mapping各自unique root；Django5.2 isolated本批`4 passed`、0049组合`11 passed`、architecture 2850/0 | zero-seed；PG双连接竞争/真实migrate、owner providers/staff composition/current mapping facade仍缺，执行总闸不变 |
| 2026-08-14 | 第一期 P0 | Account authoritative mapping v3 read-only facade | underlying selector先取0050 Evidence-v3 head，再以ID/version/hash重验current exact equality；只投影identity_mapping_only/inactive/execution=false，legacy/missing/stale均None且无v2 fallback；pure`6 passed`、architecture 2851/0 | 无production composition；需以Account 0046/0047/0049/0050 exact-first组装providers，staff入口/PG证明与全writer cutover仍缺 |
| 2026-08-14 | 第一期 P0 | Account Evidence-v3 Account-only read composition | 同alias组装0046/0047/0049/0050 exact/current readers；Physical/Receipt均exact-first再current，读异常fail-closed，无Simulated反向依赖且不暴露writer/approval；pure组合`18 passed`、Django5.2空账本组件`1 passed`、architecture 2852/0 | 仅只读图且全账本zero-seed；staff写入口、knowledge contract、PG竞争与SimulatedAccount全writer cutover仍缺，execution不开放 |
| 2026-08-14 | 第一期 P0 | Account Physical/Receipt-v3 current ID/hash-only boundary | 两个current command只接ID/version/hash/PIT，服务端exact恢复后再派生head/TTL/upstream selectors；composition直接委托scalar current reader，terminal/expired无回退；pure`41 passed`、组件链`12 passed`、architecture 2852/0 | 无schema/write变化；zero-seed、staff入口、knowledge contract、PG竞争与全writer cutover仍缺，execution不开放 |
| 2026-08-14 | 第一期 P0 | Account owner-assignment v3 actor authority Application | request principal仅作selector，每次get_current重新读exact-current authority；claimant限active nonstaff/nonsuperuser，approver限active staff+Account admin，撤权/过期None而替换corruption；组合pure`32 passed`、architecture 2853/0 | 仅Protocol/DTO+fakes；无Django authority source/request adapter/write composition/路由，mutable User/Profile不得现场hash冒充authority，execution不开放 |
| 2026-08-14 | 第一期 P0 | Account actor authority source v3 Domain | 封存opaque principal/auth-context与User/RBAC exact refs、actor/facts、三上界和domain-separated seals；同session successor锁actor与source clock，terminal撤权不恢复，new session新root；组合pure`65 passed`、architecture 2854/0 | 仅Domain；无capture/codec/ledger/Django raw providers/request adapter，mutable User/Profile/session不得现场hash冒充source，write composition与execution仍关闭 |
| 2026-08-14 | 第一期 P0 | Account actor authority source v3 Application | 单一atomic bundle provider封存auth-context/User/RBAC exact sources；winner-first，cutoff双读+recorded-at第三读，same-session predecessor CAS；exact永久、current重验final head且terminal/expired不回退；pure`40 passed`、architecture 2855/0 | 仅Protocol/DTO+fakes；无codec/ledger/migration/真实bundle provider/request adapter/staff write composition，mutable auth rows不得现场hash，execution继续关闭 |
| 2026-08-14 | 第一期 P0 | Account actor authority source v3 strict codec | 完整principal/context/User/RBAC refs、facts、clocks、root/predecessor、fixed semantics与全部domain-separated seals canonical roundtrip；exact keys/types、UTC-Z及篡改fail closed；codec`28 passed`、组合`68 passed` | 无model/repository/migration；codec不证明mutable auth来源真实性，zero-seed、bundle provider、staff入口与execution仍关闭 |
| 2026-08-14 | 第一期 P0 | Account actor authority source v3 0051 schema/guards | schema-only双CreateModel：candidate-independent source/root anchor + 完整ledger；PROTECT predecessor、service recorder/ledger seals、private UOW/exact claim与全mutation guards；Django5.2`3 passed`、mypy 0 regressions、architecture 2857/0 | 无repository/closed-world/CAS/PG race；zero-seed且上游raw authority/bundle provider、staff入口与execution仍关闭 |
| 2026-08-14 | 第一期 P0 | Account actor authority source v3 0051 repository | anchor create+row lock、whole-append savepoint、first-winner/predecessor CAS、strict codec/recorder/ledger seals与full-table closed-world；terminal/expired final head不回退；Django5.2 model+repo`7 passed`、architecture 2858/0 | SQLite不证明PG race；raw auth/User/RBAC ledgers与atomic bundle provider缺失，0051仍zero-seed且staff/execution关闭 |
| 2026-08-14 | 第一期 P0 | Account actor authority raw-source v3 primitives | Account Domain内复用exact identity、aware observed/recorded/valid clock、root/predecessor XOR、UTC-Z/domain hash与fixed inactive header；pure`10 passed`、architecture 2859/0 | 仅primitives；三种concrete raw artifacts/ledgers/atomic provider/request adapter均缺，禁止mutable auth rows现场hash |
| 2026-08-14 | 第一期 P0 | Account auth-context/User/RBAC raw authority source v3 Domains | 三种独立artifact封存secret-free session认证、User active/staff/superuser、7-role canonical RBAC；各自domain hashes/root/successor/terminal/PIT，identity/root显式含owner/type/schema；组合pure`85 passed`、architecture 2862/0 | 仅Domain；无codec/ledger/immutable Django writer/atomic bundle/request adapter，禁止live rows现场hash且staff/execution关闭 |
| 2026-08-14 | 第一期 P0 | Account auth-context/User/RBAC raw authority v3 strict codecs | 三个codec各自完整nested identity/clock/chain/facts/fixed/seals exact roundtrip；bool-int、UTC-Z、role/state与hash篡改fail closed且无跨artifact fallback；codec`100 passed`、组合`175 passed`、architecture 2865/0 | 无raw model/repo/migration/writer；codec不把live auth rows升级为owner evidence，atomic bundle/staff/execution仍关闭 |
| 2026-08-14 | 第一期 P0 | Account actor authority raw-source v3 Application primitives | 统一scalar exact/current selector、固定自动service recorder与Unavailable/Conflict/Corruption taxonomy；pure`22 passed`、Ruff/strict mypy/AST边界通过 | 仅公共Application语言；无concrete reader/repository/capture/ledger/bundle/request adapter，禁止mutable auth rows现场洗白 |
| 2026-08-14 | 第一期 P0 | Account auth-context/User/RBAC raw authority v3 Application readers | 三套Persisted+typed repo Protocol+scalar exact/current；future/substitution Corruption，terminal/expired/superseded None且no fallback；定向`29 passed`、组合`236 passed`、architecture 2869/0 | 无Django repo/capture/version allocator/atomic bundle；append仅Dormant合同，staff/request/execution关闭 |
| 2026-08-14 | 第一期 P0 | Account auth-context/User/RBAC raw authority v3 0052 schema/guards | 三套独立anchor+concrete ledger共6表，完整facts/seals/recorder、PROTECT链、fixed/state/clock/root/role约束及private nonnested UOW guards；Django5.2与0051合计`19 passed`、architecture 2870/0 | schema-only zero-seed；无repo/closed-world/PG migrate-race/lifecycle writer/atomic bundle，staff/request/execution关闭 |
| 2026-08-14 | 第一期 P0 | Account auth-context/User/RBAC raw authority v3 0052 repositories | 三repo按alias锁anchor、whole-append savepoint、strict codec/recorder/ledger full-world restore与predecessor CAS；terminal/expired final head不回退；repo`14 passed`、与model合计`30 passed`、architecture 2873/0 | PG双连接race/production migrate/lifecycle writer/version allocator/atomic bundle仍缺；zero-seed且staff/request/execution关闭 |
| 2026-08-14 | 第一期 P0 | Account RBAC authority mutation v3 dormant fact-outbox合同 | 单一UOW Protocol约束stable identity、Profile lock/CAS、0052 winner/head/append和server clock同事务；winner-first历史重放零Profile读取，expired final仍可作唯一predecessor而revoked阻断同epoch；pure`13 passed`、architecture 2874/0 | 仅休眠Application合同；无concrete UOW/生产接线，且未封存issuer、mutation kind、exact old/new Profile hashes或持久mutation→source绑定，不是mutation receipt，0052仍zero-seed |
| 2026-08-14 | 第一期 P0 | Account RBAC mutation binding v3 Domain合同 | exact Profile old/new id/version/content refs、human staff+canonical admin operator authority、service recorder、bootstrap/role_change/revoke/reactivate状态机、initial/reactivation epoch与binding/raw-source双链分域；pure`16 passed`、architecture 2875/0 | 仅Domain；无codec/0053 schema/repository/持久mutation issuer/Profile version ledger/concrete same-alias UOW，现存Profile不得用updated_at回填，旧写入口与execution保持关闭 |
| 2026-08-14 | 第一期 P0 | Account RBAC mutation binding v3 strict codec | 完整嵌套epoch、old/new Profile、human operator、service issuer及binding/raw-source双链与全套seals；exact keys/types、UTC-Z、Domain重验和canonical roundtrip篡改fail closed；codec`18 passed`、architecture 2876/0 | 仅codec；不证明mutable Profile/operator来源真实性，无0053 schema/repository/UOW/lifecycle writer，zero-seed与execution关闭 |
| 2026-08-14 | 第一期 P0 | Account RBAC mutation binding v3 Application读合同 | ID/version/hash/PIT selector、Persisted wrapper与typed repository Protocol；Exact历史与Current最终head/TTL双重闭合，terminal/expired/superseded不回退，future/substitution统一Corruption；pure`4 passed`、architecture 2877/0 | 仅dormant读/持久化合同；无Django repository/0053 schema/capture/issuer/Profile UOW/生产入口，zero-seed与execution关闭 |
| 2026-08-14 | 第一期 P0 | Account RBAC mutation binding v3 0053 schema-only基座 | 独立epoch anchor、Profile authority anchor/version ledger与mutation binding ledger；四个CreateModel、PROTECT FK/self predecessor、old/new Profile exact refs、human operator/service issuer、binding/raw-source双链、fixed/state/clock/unique约束及全append-only guards；isolated Django5.2 `9 passed`，0052组合`25 passed`、architecture 2878/0 | 仅zero-seed schema；无closed-world binding repository、持久mutation issuer/UOW、mutable Profile/User/operator lifecycle接线、生产入口或execution，真实PostgreSQL migrate/race未验证 |
| 2026-08-15 | 第一期 P0 | Account migration-state drift correction | VPS deploy warning 复现为 Physical v2 `acct_phys_v2_fixed_ck` 的 model/migration serialization drift；新增 schema-only `0054_normalize_physical_v2_fixed_constraint`，`makemigrations --check --dry-run` 为 `No changes detected`，Django check 通过，isolated SQLite forward/reverse/re-forward 通过 | 仅修复 migration state warning；下一候选仍需真实 PostgreSQL migration/rollback 观察，Evidence owner authority、生产 lifecycle 与 execution gate 不变 |
| 2026-08-14 | 第一期 P0 | Account RBAC mutation binding v3 dormant closed-world repository | 同alias/private UOW；winner/exact/head/append前恢复全部epoch、Profile、binding与0052 raw source，strict codec/逐列/recorder/双链图校验；inner savepoint、epoch锁、predecessor CAS与exact replay，terminal/expired head不回退；repository `4 passed`，与Domain/codec/model组合`47 passed`、architecture 2879/0 | 仅休眠持久化合同；空epoch/Profile version需未来owner UOW提供，mutation-id issuer、Profile/User/operator lifecycle、生产入口与真实PG空链/竞争证据仍缺，zero-seed与execution关闭 |
| 2026-08-15 | 第一期 P0 | Account authority component evidence runner correction | 为八组 schema-editor 组件补 `django_db_blocker.unblock()` 的显式隔离边界；使用 `--ds=tests.settings_account_actor_authority_source_v3 --confcutdir=tests/component/account` 重跑 RBAC mutation binding、RBAC/User raw、actor raw、owner-assignment actor authority 与 authentication-context repository/model，合计 `50 passed`（其中 mutation-binding repository `4 passed`）；配套 dormant writer unit `20 passed` | 这是 SQLite/no-migrations 组件证据可复现性修复，不是 PostgreSQL 空链/同 predecessor 并发证明；production lifecycle writer、mutation issuer、atomic bundle 与 execution 仍关闭 |
| 2026-08-14 | 第一期 P0 | Account RBAC mutation binding v3 dormant Application writer contract | ID/hash-only command、server-issued mutation/epoch identity resolver与单一typed UOW；winner-first重放不读Profile/operator/raw source，完整old/new Profile、human staff+canonical-admin operator、0052 raw source exact/current与source-role校验，final predecessor（过期可续、revoked阻断）及append后winner/head复核；writer `7 passed`，组合`54 passed`、architecture 2879/0 | 仅休眠编排合同；无concrete Profile mutation receipt/version issuer、mutable lifecycle同alias实现、0053跨epoch reactivation PG闭环或production route/composition；生产写入口/staff approval/bundle/provider/execution继续关闭 |
| 2026-08-14 | 第一期 P0 | Evidence composition boundary / governance consistency | Operator Spec approval/lifecycle concrete Django 组装移入 Risk Center/Research owner composition；`core/integration` 降为无 infrastructure import 的兼容导出；`check_governance_consistency.py`、architecture audit 均 `0 violations`，Data Center/Celery/current guards 通过；五组隔离 component `22 passed`，并修正 subject tamper 先于时序检查的断言 | 仅 SQLite/`--no-migrations` 软件契约证据；PostgreSQL并发、真实人工审核与生产数据仍缺，Evidence hard gate/execution继续关闭 |
| 2026-08-13 | 第一期 P0 | 跨 App 决策读边界与模块循环收口 | Portfolio账户访问与legacy Broker Evidence改走app-neutral registry；Account冷启动用Django app registry解析Strategy model；module guard收口为`206 edges / 0 pairs / 0 cycles`且全预算绿色 | Broker Evidence局部`33 passed`；默认环境缺Django/Celery/Playwright，Portfolio API与Account command完整Django回归未执行；执行总闸不变 |
| 2026-08-13 | 第二期 P0 | Web→TUI M5 candidate/observation gate 加固 | UAT/cleanup/rollback 改为绑定 commit+matrix+graph+schema+runtime/build/manifest；旧 108/108 与旧 rollback 已改判 FAIL；observation 只接受新鲜、已提交的 production deployment attestation，禁止 caller 回填日期；observation `15 passed` | 补真实候选部署、14 日窗口、生产样本与双签 |
| 2026-08-13 | 第二期 P0 | Web→TUI M5 cleanup wave guard | 修复 post-delete/pre-cleanup SHA 不可达循环；强制双签重放、≤10 route/波、rollback manifest、串行 commit、≥48h+定时周期观察、缺陷与错误率门禁；`15 passed` | 补 cleanup wave/rollback/observation ledger recorder；无真实证据前保持 DENY |
| 2026-08-13 | 第二期 P0 | Web→TUI rollback drill v2 | 从 migration anchor 自动推导 baseline，全部 patch/manifest/graph/schema/runtime 绑定 immutable candidate；真实隔离 reverse/forward 通过 31 artifacts，actions `402→430` | 补 Django registry 往返 runtime、生产备份/恢复和最终候选重跑 |
| 2026-08-13 | 第二期 P0 | Web→TUI candidate evidence recorders | UAT/cleanup 固定执行套件并重解析 JUnit，rollback 只接受 drill v2，报告绑定 exact candidate 且不接受自报 passed；`5 passed` | 建立真实 candidate 后重跑；另补 M5-B wave/48h observation ledger recorder |
| 2026-08-13 | 第二期 P0 | Web→TUI M5-C final inventory gate | 新增独立 `--require-finalized`：只允许 41 个 C 档物理模板，A/B/D 全 deleted，并检查孤儿 view/route/static 与 legacy alias；普通 196 行冻结检查保持通过，最终模式因 148 个 A/B 未清理保持 DENY；检查器已纳入 IA runtime canonical screens，消除 `capability-router.gateway` 的 dangling 误报 | 仅在逐波生产观察、rollback 和审批齐全后执行删除；清理仍无活生产消费者的 11 个 dead alias |
| 2026-08-13 | 第二期 P0 | Web→TUI current production preflight | 公开 health/ready 为 200/ok；当前 release=`source-20260813002655`，但 OCI revision=`unknown` 且 release 无 Git/source manifest，不能绑定 candidate，观察仍未开始 | 修复 release provenance，重新部署干净最终候选，并在部署后生成结构化 attestation |
| 2026-08-13 | 第二期 P0 | Web→TUI M5-B cleanup wave recorder | 从 candidate Git snapshot 重算单波删除/≤10 route/task/rollback；强制已提交且 commit+OCI 精确绑定的 deployment preflight，以及部署后 48h telemetry/defect/scheduled-cycle 原始证据；`14 passed` | 当前无删除候选和生产证据，CLI 保持 FAIL；待候选部署后逐波实际执行 |
| 2026-08-13 | 第二期 P0 | Web→TUI release provenance fail-closed | 发布工具拒绝 dirty/unknown source，clone 锁定 exact commit；构建强制 OCI revision 并生成只读 release manifest，deploy 在启动服务/切换 current 前复核 manifest+image；相关 `44 passed` | 尚未部署；当前生产仍为 revision=`unknown`/无 manifest，须经授权部署干净候选并重新 preflight |
| 2026-08-14 | 第二期 P0 | Web→TUI release provenance verifier 对齐 source-upload | 本地 `deploy_vps_verify` 身份检查强制读取只读 `.agom-release-manifest.json`，校验字段白名单、源码 commit、OCI revision 与镜像 ID，并移除 Git fallback；构建/部署脚本继续核验 release/image 标识、构建时间和 source mode；`50 passed`、增量 mypy 通过 | 未部署；当前生产仍为 revision=`unknown`/无 manifest，未生成 attestation，M5 观察窗口继续 DENY |
| 2026-08-14 | 第二期 P0 | Web→TUI backend contract provenance | runtime manifest 逐文件覆盖 IA、Application metadata、IA loader、repository/signals 与全部 `tui_metadata_runtime_*.py`；manifest digest contract `1 passed`，与 observation/candidate recorder 定向回归合计 `21 passed`，`npm run build:tui` 已重生成 | 只完成本地候选 provenance；生产仍 revision=`unknown`/无 manifest，未部署、未生成 attestation，14日窗口/telemetry/backup/双签继续 DENY |
| 2026-08-15 | 第二期 P0 | Web→TUI M5 readiness recheck | `check_web_to_tui_cutover_readiness.py --json` 明确返回 `DENY`；matrix/catalog/evidence SHA 一致，108/108 route/task 覆盖与 scope/rollback 静态证据存在 | 候选版本与 commit 绑定缺失；14 日 observation、生产 telemetry（`0/101`）、生产 rollback/registry backup、owner/reviewer 双签均缺失；不得执行 cleanup 或 cutover |
| 2026-08-14 | 第二期 P0 | Web→TUI R0 actionability audit | 归一化 runtime graph 的 277 个 write/admin action 全部要求真实写方法与 mutation effect；创建/编辑/删除/审批等动作必须有可见字段，8 个无输入整批命令和 4 个 POST/read 预览/测试命令分别显式登记；`test_tui_actionability_contract.py` `5 passed`、TUI JS `33 passed` | 仅完成本地“可填写/可提交”机器门禁；最终候选的普通用户/owner/staff 浏览器 UAT、写后刷新/回执、人工签字和生产审计仍待 M5 证据 |
| 2026-08-15 | 第二期 P1/R1 | Web→TUI row-level edit form closure | Workbench 对带可见字段的非 GET 行操作先打开/聚焦 action form，按 row identity 与可匹配值预填，提交前不发 PATCH/POST；无额外字段的 approve/delete/toggle/批量命令保持直执行；已覆盖 IA 编辑/更新入口及用户治理 `identity-access.reject-user`、`identity-access.set-user-role` 行表单；浏览器契约 `34 passed`、Python actionability `9 passed`、`npm run check:tui` 通过 | 仅本地 Workbench 交互闭环；最终候选角色化浏览器 UAT、写后 receipt/refresh、人工审批、生产审计与 M5 candidate evidence 仍未满足 |
| 2026-08-15 | 第二期 P1/R1 | Web→TUI direct row-action semantics | 行操作只有可见 `body` 字段时才打开表单；path/query identity-only 的批准、删除、切换和批量动作保持直接执行，浏览器契约 `22 passed`，`npm run build:tui` 与 `npm run check:tui` 通过 | 仅修正本地交互判定；角色化浏览器 UAT、写后 receipt/refresh、人工审批、生产审计与 M5 candidate evidence 仍待完成 |
| 2026-08-15 | 第二期 P1/R1 | Web→TUI role-filtered row affordance closure | Dashboard `row_actions` 重新按当前用户已过滤的 `visible_action_keys` 投影，普通用户不再看到无法执行的 Signal/Beta Gate/Rotation 管理员写按钮，管理员行操作保持；角色双向定向回归 `2 passed` | 仅收紧前端可见 affordance；后端权限、最终候选角色化浏览器 UAT、写后 receipt/refresh、人工审批与 M5 生产证据仍待补 |
| 2026-08-15 | 第二期 P1 | Web→TUI runtime graph compatibility guard | actionability contract 新增 static-vs-runtime guard，要求 compact published graph 经过 runtime injection 后才可作为宿主图，并保留 mutation fields/row_actions；定向回归 `10 passed` | 仅完成 AgomTradePro 本地 source boundary；AgomTUI 外部 schema/validator、双仓库同步、候选部署、角色 UAT 与 14 日观察仍未完成 |
| 2026-08-15 | 第二期 P0 | Web→TUI 本地角色边界回归 | disposable SQLite + `core.settings.playwright` 下 Playwright UAT `test_operator_group_can_open_queue_but_regular_user_cannot` `1 passed`；operator 可进入 AI 任务队列，普通用户不可见 | 仅本地角色/权限边界证据；不替代 VPS 真实角色账号、写后 receipt/refresh、人工审批、生产审计、14 日观察与 M5 双签，TUI-01 继续 awaiting |
| 2026-08-15 | 第二期 P0 | STRAT-01/02 readiness runtime preflight guard | `test_capability_readiness_runtime.py` 新增 R1/R2 防伪断言，确认 R1 六项生产要求仍为 `UNVERIFIED`、Forecast specification 为 `MISSING`，R2 五项要求仍为 `UNVERIFIED`；定向组件回归 `8 passed` | 仅锁定“机制 manifest 不得冒充 production readiness”；真实 R1–R8 owner/definition/policy/calendar/scope、PIT/OOS 历史、canonical receipts、对账与 Promotion 仍缺，`STRAT-01`/`STRAT-02` 状态不变 |
| 2026-08-15 | 第一期 P0 | EVID-02 Evidence scope PostgreSQL concurrency harness | 新增显式 opt-in、专用数据库 identity guard 的 Evidence scope source v1 harness，覆盖空 root first-winner、同 predecessor 单 successor 与 rollback/no-orphan；默认环境 `3 skipped`，不会把 SQLite 视作 PG 证据 | 本轮 Docker daemon 未响应，尚未取得 disposable PostgreSQL 实际通过结果；EVID-02 仍 planned，owner/tenant lifecycle、生产 composition、PG race 与执行总闸继续阻断 |
| 2026-08-15 | 第一期 P0 | EVID-02 本机 PostgreSQL 连接重试 | 尝试创建受约束的临时数据库 `evidence_scope_test_20260815`；本机 `127.0.0.1:5432` 在 3 秒连接窗口内超时，数据库未创建，harness 未运行 | 仅记录工具链阻断；没有 SQLite/VPS/生产替代证据，EVID-02 继续 planned，owner/tenant lifecycle、生产 composition、PG race 与执行总闸继续阻断 |
| 2026-08-15 | 第一期 P0 | EVID-02 本机 PostgreSQL harness 实际通过 | 修正 `django_db(transaction=True)` 隔离声明，并在本机 `postgres:18.4` 专用空库 `evidence_scope_test_20260815` 实跑 `test_evidence_scope_source_v1_postgres_concurrency.py`，空 root、同 predecessor、rollback/no-orphan `3 passed in 207.58s`；测试后专用库已删除 | 仅本机 disposable PostgreSQL 软件证据；不替代生产 alias/owner-tenant lifecycle/人工授权/publisher/runtime/execution，EVID-02 仍 planned，不能把本地结果升级为生产门禁 |
| 2026-08-15 | 第二期 P0 | DATA-01 production PostgreSQL backup evidence | 运行 `scripts/backup-vps-postgres.ps1` 成功创建并下载 custom-format 归档；远端 `pg_restore --list`、尺寸和本地 SHA-256 均通过；归档 `postgres-20260815T030811Z.dump`，`139057048` bytes，SHA-256 `a8f005eb3a461f28d21689ecef6d5aee89b59a353d06944b79e08c82662839cc` | 仅完成备份子步骤；维护态、restore/rebuild、回滚演练、受控回填、reconciliation 与观察证据仍缺，`DATA-01` 继续 awaiting，不能解锁 DATA-02/03 |
| 2026-08-15 | 第二期 P0 | DATA-01 latest PostgreSQL backup refresh | 候选 `a76db97d` 部署前以 `-DownloadLatest` 下载远端 custom-format 归档 `postgres-20260815-093506.dump`，`140095243` bytes；远端 `pg_restore --list`、完整下载、尺寸和本地 SHA-256 `a1e7092aacc1241525ba52a083395f3d38bb0b88c7b8f6436b3ad508f4520bc0` 均通过，prune 未启用 | 仍只是恢复点；没有 restore/rebuild、维护态、回滚演练、回填或 reconciliation，`DATA-01` 继续 awaiting，不能解锁 DATA-02/03 |
| 2026-08-15 | 第二期 P0 | DATA-01 post-deploy backup refresh | 最新候选 `ae1e5e532` / release `20260815162419` 部署后重新下载并验证 `postgres-20260815-103019.dump`，`140112628` bytes，SHA-256 `46dd5003de2943ac23d8ab599c24454e3e770b7828b088857be355fa4f5a364d`；远端 `pg_restore --list`、SFTP 完整下载、尺寸与本地校验通过 | 仍只是恢复点；未执行 restore/rebuild、维护态、回滚演练、回填或 reconciliation，`DATA-01` 继续 awaiting，不能解锁 DATA-02/03 |
| 2026-08-15 | 第二期 P0 | DATA-01 local restore/rebuild verification attempt | 复用 `scripts/verify_postgres_backup_restore.py` 的严格受控恢复合同；脚本 unit `10 passed`。新的 PostgreSQL 临时容器在 `initdb` bootstrap 超时，现有本地容器中的专用数据库归档传输也因 Docker Desktop API 超时而未进入 `pg_restore`/快照对比；专用数据库与临时文件已清理，未写 VPS | 未取得 restore/rebuild、RTO 或回滚通过证据；仅确认本机 Docker 阻断，`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-15 | 第二期 P0 | DATA-01 当前候选部署后 backup refresh | 当前候选 `cf68dc1e9` 部署后以 `-DownloadLatest` 下载并校验远端 custom-format 归档 `postgres-20260815-123539.dump`，`140176474` bytes，SHA-256 `e1c0821543a36f19d2ea292d9c4fdc544003010579ccef5df9175d083a2e2e2f`；远端 `pg_restore --list`、SFTP 完整下载、尺寸和本地校验均通过，prune 未启用 | 仍只是恢复点；本机缺 `pg_restore`/`psql` 且此前 Docker restore 链路超时，没有 restore/rebuild、维护态、回滚演练、回填或 reconciliation，`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-15 | 第二期 P0 | DATA-01 只读源快照与恢复工具链修复 | VPS 只读 `REPEATABLE READ` 快照取得 `536` 张 public 表、`71` 项 Data Center migrations、schema SHA `9306657014b2861f1095e2f4132f37074ebf7c0debb79d95d6d98d0d1c4291ab`；恢复脚本新增显式 `--pg-restore-container` 客户端与 `.pgpass` 隔离，契约回归 `12 passed` | Docker restore 仍在 10 分钟内未完成且 stdin 并行参数被 PostgreSQL 拒绝；未取得 restore/snapshot match、RTO/RPO 或 rollback 证据，`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-15 | 第二期 P0 | DATA-02 backfill control-plane PostgreSQL preflight contract | 为真实回填任务补 PostgreSQL-only fake-provider 预演：同参数重试保持唯一 `run_id/batch_id/checkpoint`，单一 provider 失败持久化 `partial` 与计数/错误/checkpoint；SQLite 结构回归 `1 passed, 2 skipped`，任务单元 `8 passed` | PostgreSQL-only 用例尚未在本机/生产执行，SQLite skip 不构成锁/事务/并发证据；`DATA-01/02` 状态不变，生产回填、coverage/reconciliation、M9/M10 继续锁定 |
| 2026-08-15 | 第二期 P0 | DATA-02 control-plane atomic snapshot rollback | 将 run、batch、checkpoint 三个持久化调用收口到 Data Center composition root 的同一事务；checkpoint 写入失败时三张控制面表均回滚，组件 `2 passed, 2 skipped`、任务单元 `8 passed`、architecture/mypy/Celery/format 门禁通过 | 仅本地事务边界与回滚证据；PostgreSQL-only 并发/锁预算、生产回填、coverage/reconciliation 与 DATA-01 前置条件仍未完成，`DATA-02/03` 继续 waiting |
| 2026-08-15 | 第二期 P0 | DATA-01 当前候选部署后 backup refresh 20:07 | 当前候选 `11594964f589c5f0ec3bf6a541d61d471b79b67f` 部署后以 `scripts/backup-vps-postgres.ps1 -DownloadLatest` 下载并校验 `postgres-20260815-141446.dump`；`140206603` bytes，SHA-256 `d3134e5c5551ec92c77724b6567d5db788e59129a3ca799ab0be9cfdad122249`；远端 `pg_restore --list`、SFTP 完整下载、尺寸和本地校验均通过，prune 未启用 | 仍只是恢复点；未执行 restore/rebuild、维护态、回滚演练、回填或 reconciliation，`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-16 | 第二期 P0 | DATA-01 SFTP 断线续传修复与恢复点刷新 | 修复 `scripts/backup-vps-postgres.py` 的 bounded retry/reconnect/resume：断线保留 partial 并从精确偏移继续，最终失败清理 partial；备份单测 `8 passed`，py_compile/Black/isort/diff-check 通过。当前候选 `45281620a8739ee666a1b20e6c6511c0b8101111` 的 `postgres-20260815T154338Z.dump` 已完整下载，`140279578` bytes，SHA-256 `eddb840c0c15d3041bf29a32471c8b6c03be0bd32a0ccf23d438647c46c2615e`，远端 `pg_restore --list`、尺寸和本地校验均通过 | 仅修复并证明备份下载子步骤；restore/rebuild、维护态 rollback、RTO/RPO、回填和 reconciliation 仍缺，`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-16 | 第二期 P0 | DATA-01 VPS 隔离 restore rehearsal | 当前候选 `e167ab2fc...` 的 `140318641` bytes 归档在 VPS 临时库实际 `pg_restore` 成功；源/恢复库均 `536` 张表、`71` 项迁移，schema SHA 一致；当前源快照有 `10` 张表、`4` 个 sequence 漂移，临时库已清理 | 证明实际 restore 与结构/schema 对比链路，不证明备份时点字节一致、RTO/RPO、维护态 rollback 或 reconciliation；`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-16 | 第二期 P0 | DATA-01 当前候选 backup 下载复核 | `-DownloadLatest` 重新取得远端 `postgres-20260815-184803.dump`；远端 `pg_restore --list`、完整下载 `140318641` bytes 与本地 SHA-256 `4760a38fdfc7ef8570323cfb5dde92ab01eb933cd60d4f6dd08700fc34772752` 均通过 | 仅重复确认备份下载/校验子步骤；restore/rebuild、维护态 rollback、RTO/RPO、回填与 reconciliation 仍缺，`DATA-01` 继续 awaiting，不解锁 DATA-02/03 |
| 2026-08-16 | 第二期 P0 | DATA-02 reconciliation evidence append-only hardening | Reconciliation evidence 改为 append-only immutable model，repository 采用 create + exact replay；同一 `evidence_id` 的不同快照 fail closed，不再用 `update_or_create` 覆写历史；回归覆盖 exact retry/conflict、模型结构与 Data Center architecture guard，增量 mypy 0 regressions | 仅本地持久化/回放合同；Django DB reconciliation 用例在当前全仓测试运行时未取得稳定完成结果，PostgreSQL 生产 reconciliation、维护态与 DATA-01 前置仍缺，`DATA-01/02/03` 状态不变 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant scope read contract | 新增纯 Application `EvidenceScopeGrant`/trusted provider/authorizer 与强制注入 authorizer 的 `ScopedEvidenceReadFacade`；三类 exact read 在 repository 前执行 artifact-level scope gate，缺失、future/stale、revoked、替换和 hash tamper 均 fail closed；unit+facade `12 passed`，mypy/architecture/format/py_compile 通过 | 仅本地授权合同；未接生产 owner/tenant provider、真实 scope source、PostgreSQL current-head/并发或人工授权；API 仍 staff-only，Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source contract | 新增纯 Domain `EvidenceScopeSourceV1`，固定 owner/tenant/account/actor/artifact exact refs、read-only/non-execution、root/successor、PIT 与 revoked/expired no-fallback；scope Domain/Application/facade `30 passed`，增量 mypy/architecture/format/compile 通过 | 仅 immutable source 语义合同；未创建 ledger/provider、未读取 User/session/tenant ORM、未接 API/人工授权/production route，Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source strict codec | strict codec 完整重建 `EvidenceScopeSourceV1`/`ArtifactRef`，exact keys/types、UTC-Z microseconds、root/successor/fixed semantics、identity/content hash roundtrip 与 tamper fail closed；scope/source/facade codec `42 passed`，增量 mypy/architecture/Black/isort/compile 通过 | 仅 canonical persistence contract；未创建 ledger/repository/provider、未接真实 owner/tenant source、API/人工授权或 production route，Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source Application readers | 新增 dormant pure Application exact/current readers；命令仅接受 source ID/version、expected content hash 与 aware PIT，exact 读取保留历史记录，current 先 exact 再要求 final logical head 与 scope identity/content 完全一致，terminal/expired/superseded 不回退；source Application/codec/Domain/facade 合计 `52 passed`，增量 mypy/architecture/Black/isort/compile 通过 | 仅 read contract；未创建 ledger/repository/provider、未读取 mutable User/session/tenant rows、未接人工授权或 production route，真实 owner/tenant source 与 Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source schema-only ledger | 新增零种子 `research_evidence_scope_source_v1` append-only ORM 基座与 `0028` migration；完整 scope/artifact scalar projection、canonical payload、identity/content hashes、root/successor/predecessor、PIT clocks 与固定 read-only/non-execution checks；isolated component `4 passed`，source 读/codec/Domain/facade 合计 `56 passed`，Django check/migration drift/mypy/architecture 通过 | 仅 persistence schema/guard contract；无 repository restore/append composition、可信 owner/tenant provider、mutable User/session/tenant 取证、PostgreSQL 并发或 production route，Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source repository | 新增 public exact/PIT/current-head repository 与 private atomic append store；每次 selector/append 前全表 strict codec/逐列 header/chain restore，root/successor predecessor CAS、exact replay、终态/过期 head 不回退，tamper 与事务 rollback fail closed；repository component `6 passed`，schema+repository `10 passed`，Evidence source 组合回归 `66 passed`，增量 mypy/architecture/Black/isort/compile/diff-check 通过 | 仅本地 repository/schema contract；尚未完成 PostgreSQL 空链并发证据、可信 owner/tenant immutable lifecycle/provider、production composition、人工授权或 route，Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source dormant provider adapter | 新增 `EvidenceScopeSourceV1Provider`，只接受服务端签发的 source ID/version/content hash selector，通过 current reader 严格重验 exact source、artifact、PIT 与 temporal current 后投影 `EvidenceScopeGrant`；缺 selector、source substitution、reader corruption/unavailable、terminal/expired 均 fail closed；provider unit `15 passed`，source 组合回归 `79 passed` | 仅 dormant Application adapter；未读取 User/session/tenant ORM，未创建 selector lifecycle/provider composition，未接 API/人工授权/production route；可信 immutable owner/tenant lifecycle、PostgreSQL 并发、Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority source 下一 slice exit audit | 复核 ScopeSourceV1 Domain/codec/reader/schema/repository/provider 与 Account actor-authority raw ledgers；结论是本地没有可诚实推进的更强 production composition，避免叠加只按 alias 胶水。继续等待 immutable owner/tenant lifecycle selector issuer、同 alias atomic bundle 与 PostgreSQL 空链/current-head 竞争证据 | EVID-01 仍 active；production owner/tenant read、人工授权、Evidence hard gate、写入和 execution 继续关闭，不把 dormant provider 当作 authority source |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant authority provider exception boundary hardening | scope authorizer、selector provider 与 source reader 对未知数据库/RBAC/provider 异常统一脱敏为稳定 `EvidenceScopeUnavailable`，避免内部错误穿透 scoped facade 或导致错误的开放性 fallback；scope-source 单元集 `68 passed`，Black/isort/mypy、architecture/audit delta、diff-check 通过 | 仅本地 fail-closed 异常边界；未创建 immutable owner/tenant lifecycle、selector issuer、production composition、PostgreSQL 并发/人工授权或 route，EVID-01 与 Evidence hard gate、写入、execution 继续关闭 |
| 2026-08-16 | 第一期 P0 | EVID-01 scope grant owner binding hardening | `EvidenceScopeGrant` 现在显式封存并 hash-bind `owner_id`，source→grant projection 逐字段保留 owner；scope/source 单元 `37 passed`，与 Data Center structure/architecture guard 聚合 `46 passed`，增量 mypy 0 regressions | 仅本地 owner identity/contract 加固；没有 immutable owner/tenant lifecycle、真实 provider、人工授权、PostgreSQL 并发或 production route，EVID-01 与 Evidence hard gate、写入、execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant scope grant integrity hardening | `EvidenceScopeAuthorizer` 对 provider grant 重新执行完整 immutable invariant，阻断 permission/identity 替换后重算 hash 的绕过；scope ID/version/actor/tenant/account token 严格限制为无空白、长度不超过 192 的字符串；scope/source/facade 全套 `74 passed`，增量 mypy、Black/isort、diff-check 通过 | 仅本地 fail-closed integrity boundary；没有 immutable owner/tenant lifecycle、真实 provider、PostgreSQL 并发、人工授权或 production route，EVID-01 与 Evidence hard gate、写入、execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | Evidence owner/tenant Application selector boundary hardening | ScopeSourceV1 Application command 与 dormant provider selector 现在同样拒绝空白、空值和超过 192 字符的 `source_id/source_version`；与 Domain/codec/repository/ORM 边界一致，超长 selector 在读取前 fail closed；scope source 聚焦回归 `78 passed`，增量 mypy/Black/isort/治理一致性/diff-check 通过 | 仅本地输入边界加固；未创建 selector issuer、immutable owner/tenant lifecycle、production composition、人工授权或 route，EVID-01 与 Evidence hard gate、写入和 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | AUD-01 authority/query token boundary hardening | System Audit authority snapshot 与直接 reader context 的 actor/tenant/owner/role 统一拒绝空白、空值和超过 192 字符的 token，content hash 在 canonicalization 前同步校验；composition `28 passed`、query `14 passed`，audit composition/query/outbox runtime 聚合 `46 passed`，增量 mypy/Black/isort/diff-check 通过 | 仅本地 composition/query contract；authenticated authority source、tenant/owner lifecycle、durable publisher、production route 与 audit runtime 仍未接线，AUD-01 与 AUD-02/03 继续阻断 |
| 2026-08-14 | 第一期 P0 | Data Center architecture inventory source snapshot | 重新生成并复核 `governance/data_center_architecture_inventory.json`；静态计数为 `cross_app_orm_imports=48`、`current_surface_references=4225`、`data_write_task_decorators=58`、`runtime_parameter_references=49`，Data Center/Provider 外部直连、legacy fact 与待审外部 HTTP 均为 `0`；governance、architecture、catalog、legacy-fact、current-data、Celery guards 通过 | 仅源码治理快照；不代表生产 PostgreSQL/VPS 数据、备份恢复、shadow reconciliation、writer quiescence 或 M9 destructive migration，生产切换继续 DENY |
| 2026-08-14 | 第三期 P1 | AI-Native local release gate / home-chat reuse | 冻结 API/SDK/MCP/TUI provenance/migration/test asset 清单；新增 `check_ai_native_release_gate.py`，本地资产与候选绑定检查通过，`test_ai_native_release_gate.py` `3 passed`；首页迁移到共享 `AgomChatWidget`，Node `33 passed`，本地 Playwright 普通提问/建议执行与取消流通过 | 仅机器门禁与本地 UI 闭环；首页真实候选/生产浏览器 UAT、staging、同候选 owner/reviewer 双签与发布证据仍缺，不能解除 P1 或生产发布闸门 |
| 2026-08-14 | 第一期 P0 | 系统级统一审计日志 M0 事件注册表 | 新增 shadow `governance/audit_event_contracts.json` 与 `check_audit_event_contracts.py`，冻结 7 个顶层 category：20 个 Data Reliability 事件具备 owner/category/write policy/detail/reason 合同，其余 6 类保留 source-file inventory；已接入 consistency workflow 与 governance wiring；定向测试 `5 passed`、治理 wiring `29 passed`，未知 taxonomy、detail、关联字段和状态错配 fail closed | 仅机器合同与静态 inventory；Event Model、migration、outbox、运行写入口、Data Center 双写与生产审计覆盖仍未完成，M1+ 待评审 |
| 2026-08-14 | 第一期 P0 | 系统级统一审计日志 M1 Domain/codec 最小合同 | 新增纯标准库 `SystemAuditEvent` envelope 与 strict codec：typed actor/resource/evidence refs、bounded correlations、source/recorded clocks、stream predecessor/idempotency、敏感字段和未知键 fail closed；定向 `5 passed`、增量 mypy `0 regressions` | 仅Domain/codec；无Model/migration/repository/outbox/业务双写/生产审计，M1账本与Data Center纵向链仍未启用 |
| 2026-08-15 | 治理横切 | Active plan registry v1 | 将 46 个活跃计划文件闭集归入 7 条工作流、16 份主计划、21 份支撑/证据和 9 份限期审查；新增 owner/status/next gate 机器合同与 CI 守卫，未登记、重复、陈旧路径和无截止日审查项统一失败关闭 | 2026-08-22 前清空或正式转化 9 项审查队列；队列只能下降，不得作为长期活跃状态 |
| 2026-08-14 | 第一期 P0 | 系统级统一审计日志 M1 schema-only ledger/outbox 基座 | 新增统一事件表与事务 outbox 表、private append-only/immutable payload guards、迁移 `0011`（恰好两个 `CreateModel`，无数据操作）；事件 component `3 passed`、outbox component `3 passed`、Domain/codec 回归 `10 passed`、增量 mypy `0 regressions`、architecture `2885/0` | 仅zero-seed schema/SQLite软件契约；无repository/query/dispatcher/Data Center双写/业务runtime wiring，PostgreSQL并发、真实迁移回滚、backlog恢复与生产审计覆盖仍未验证 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 ledger repository/query 合同 | 事件仓储完成同-alias private atomic append、exact claim/first-winner、stream predecessor CAS、全表 closed-world restore 与 exact/PIT/head/list；新增 staff-only Application query Protocol/DTO，非授权或 selector substitution fail closed；repository component `5 passed`，与模型合计 `8 passed`，query unit `5 passed`，增量 mypy `0 regressions`、architecture `2889/0` | 仅本地 SQLite/纯契约；outbox dispatcher、Data Center 双写、业务 runtime wiring、staff authority source、PostgreSQL 空链/并发、真实迁移回滚与生产审计覆盖仍未验证 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 staff query actor binding | `SystemAuditReaderContext` 现在要求 `actor_id == django-user:{user_id}` 后才允许触碰 repository；未绑定 staff context fail-closed，query unit `6 passed`、增量 mypy `0 regressions` | 仅请求上下文内部绑定；真实 authenticated user/RBAC authority source、owner scope、PostgreSQL 与生产 composition 仍待完成 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 PostgreSQL 并发证据 harness | 显式 opt-in harness 修正 pytest 隔离库 fixture 顺序，并直接执行 migration `0011` 的 PostgreSQL forward/backward；临时 Docker `postgres:16-alpine` 的 `test_audit_test` 覆盖空 stream first-winner、同 predecessor CAS、outbox claim lease ownership 与 rollback 后重新 claim，`4 passed`（194.64s）；容器已删除 | 这是本机真实 PostgreSQL 隔离库的软件证据，不是生产 VPS/PG 证据；完整生产迁移回滚、backlog/恢复、Data Center 双写、publisher/runtime wiring、staff authority source 与生产审计覆盖仍未完成，registry gate 继续阻断 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 migration rollback 本地证据 | 新增 `test_system_audit_migration.py`，真实执行 migration `0011` 两个 `CreateModel` 的 SQLite forward/backward，确认事件表与 outbox 表可创建并完整删除；系统审计 component 回归 `16 passed` | 仅关闭本地 schema operation 回滚盲区；完整历史 migration chain、PostgreSQL DDL/rollback、outbox backlog recovery、Data Center 双写与生产审计覆盖仍未验证，registry gate 不变 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 outbox transition guard | 现有 outbox 行的 `save/save_base`、QuerySet/manager `update/bulk_update` 不能绕过 repository 私有 state-transition capability；payload/identity 仍不可变，定向模型/repository/dispatcher 回归 `10 passed`、增量 mypy `0 regressions` | 仅关闭本地状态绕过写入；expired lease reclaim、mixed batch accounting、真实 PostgreSQL race/lease、backlog recovery、业务双写与生产 publisher 仍未完成 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 expired-lease recovery | `claim_due()` 以正 lease TTL 回收过期 claimed 行；新 worker 获得新 token、attempt 递增，旧 token 不能 finalize；定向 outbox model/repository/dispatcher 回归 `11 passed`、增量 mypy `0 regressions` | 仅本地 lease 状态机证据；mixed batch accounting、真实 PostgreSQL 双连接 race/lease、backlog 观测、业务双写与生产 publisher 仍待完成 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 dispatcher mixed-batch accounting | dormant dispatcher 现在有成功+失败混合批次的精确计数与 `partial` outcome，以及空批次 `noop`；dispatcher/event unit 回归 `10 passed` | 仅纯 fake Application 证据；真实 publisher、批量事务、PostgreSQL lease race、backlog 观测、业务双写与生产审计仍未完成 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 outbox backlog/recovery observability contract | 新增只读 `SystemAuditOutboxBacklogSnapshot`/use case 与 repository 聚合读取；全表 closed-world restore 后统计 pending/claimed/failed/delivered、due pending、expired claim、oldest age；Application unit `9 passed`、repository component `7 passed`、增量 mypy `0 regressions`，black/isort 通过；PG opt-in harness 已补 backlog 只读断言但本批未运行 | 仅本地读取/聚合契约与未运行的 PG 测试资产；未接 Prometheus/health、publisher/runtime、自动恢复或生产告警；真实 PostgreSQL backlog/恢复观察、生产迁移回滚、Data Center 双写与 authority source 仍待完成 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 outbox backlog health projection wiring | Audit health 读取同 alias 的只读 backlog snapshot，发布 pending/due/claimed/expired/failed/delivered 与 oldest age；recovery work 为 WARNING、异常仅暴露类型；unit/API `16 passed` | 仅 health projection；Prometheus/runtime publisher、自动 reclaim、生产 migration/rollback、真实 PostgreSQL backlog 观察与 Data Center 双写仍缺 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 event/outbox atomic composition contract | 新增 dormant `AppendSystemAuditEventOutboxUseCase` 与同 alias coordinator；event append/outbox enqueue 同 outer transaction，exact retry 与失败回滚；unit `5 passed`、SQLite component `3 passed`、mypy/architecture 通过 | 尚未接 Data Center `data.fetch.*`、publisher/runtime 或生产 route；真实 PostgreSQL 双写竞争、迁移回滚与业务事件仍未验证 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 Data Center fetch-event envelope contract | `DataFetchAuditObservation` 与 canonical `data.fetch.completed/noop/failed` builder 固定 run/ingested-run、RawAudit exact evidence、稳定事件 identity 与显式 stream CAS 输入；unit `8 passed`、增量 mypy `0 regressions`、architecture/audit `0 violations` | 仅 Application envelope；SyncMacro 尚未提供服务端批次 issuer/共同 UOW，registry 仍 `planned/not_wired`，不得宣称双写或生产覆盖 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 Data Center RawAudit identity boundary | `RawAudit` 新增稳定 row identity、run/ingested-run 关联与 canonical content hash；`RawAuditReference` 对历史缺字段 fail-closed；迁移 `0070` 仅 nullable expand、不回填；RawAudit/SyncMacro/Macro publication `15 passed`，makemigrations/check/mypy 通过 | 仅 identity-bearing evidence boundary；尚未接服务端 run issuer、事实/Health/RawAudit/Publication/event/outbox 同 UOW、生产 backfill 或 PostgreSQL 双写，`data.fetch.*` 仍 `planned/not_wired` |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 Data Center SyncExecution identity contract | 纯 Application `SyncExecutionIdentity`/issuer port 固定 server-issued run/ingested-run/batch UUID 与 domain-separated hash；command 不接受 caller identity/clock，纯测试 `8 passed`，增量 mypy/architecture/格式门禁通过 | 仅 dormant identity boundary；尚无 issuer persistence、SyncMacro 同 UOW、事实/Health/RawAudit/Publication/event/outbox 双写、迁移回填或 PostgreSQL 证据，`data.fetch.*` 仍 `planned/not_wired` |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 Data Center SyncExecution identity persistence boundary | 新增 strict `SyncExecutionIdentityRepositoryPort`/persist use case、schema-only migration `0071` 与 private insert-claim/append-only ORM guards；完整 identity exact replay，hash/context/ID 冲突 fail-closed；纯/migration/SQLite component 合计 `16 passed`，`makemigrations --check` 通过 | 仅 owner-issued identity persistence boundary；未接 SyncMacro writer/共同 UOW/事实/Health/RawAudit/Publication/event/outbox 双写、历史回填、生产 PostgreSQL race/rollback，`data.fetch.*` 仍 `planned/not_wired` |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 outbox dispatch task fail-closed contract | Celery dispatch task 在 publisher 未组装时于 claim 前返回 `blocked`，不使用通用 Events/memory/eager fallback；输入、blocked、composition failure `4 passed`，Celery manifest `88 registered / 22 governed files` 通过 | 仅 dormant task/gate；无 durable publisher、claim/retry/requeue、beat schedule、业务双写、生产 broker/PG 或自动恢复证据，system-audit publisher/runtime gate 继续阻断 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 M1 outbox closed-world clock/state hardening | restore 现在闭合 available/claim/delivery/failure/updated 时序，拒绝各状态残留的跨状态字段，并对 future observation cutoff fail-closed；raw-tamper component，outbox model/repository/dispatcher 回归 `32 passed`、增量 mypy `0 regressions`，black/isort 通过 | 仅加强本地 fail-closed；PostgreSQL 双连接竞争、生产 backlog 观察、publisher/runtime、自动恢复、生产迁移回滚与审计覆盖仍待完成 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 AUD-01 composition preflight contract | 新增纯 Application publisher/authority composition boundary：publisher 必须返回保留 event identity、stream predecessor、idempotency、canonical payload 的 exact receipt；authority 只能由注入 provider 提供 authenticated/staff/user binding facts；dispatcher 对 receipt substitution/generic-memory 风格返回值 fail closed；定向 audit 回归 `20 passed`，增量 mypy/architecture/format/compile 通过 | 仅合同与阻断边界；runtime 仍固定 `publisher_not_wired`，没有 durable publisher/receipt sink、authenticated scoped authority lifecycle、beat/retry/PG/VPS 证据，不接 AUD-02 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 AUD-01 authority boundary hardening | authority cutoff 现在先严格拒绝非 `datetime`/naive 输入；注入 provider 返回错误类型或抛出异常时统一脱敏为 `authority_unavailable`，不泄露数据库/RBAC异常；定向 unit `15 passed`，增量 mypy/architecture/Black/isort/diff-check 通过 | 仍只是本地 fail-closed boundary；未接真实 authenticated scoped authority、durable publisher/receipt sink、beat/retry、PostgreSQL/VPS 证据，`AUD-01` 未解除，`AUD-02` 继续等待 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 AUD-01 canonical receipt malformed-payload hardening | 不可 canonical JSON 编码的 publisher receipt payload 统一 fail-closed 为 `publisher_contract_violation`，避免误归类为普通 publisher error；composition/dispatcher 定向回归 `25 passed`，Black/isort、增量 mypy、architecture 与 audit contract checks 通过 | 仅合同加固；runtime 仍固定 `publisher_not_wired`，没有 durable publisher/receipt sink、authenticated scoped lifecycle、beat/retry/PG/VPS 证据，不接 AUD-02 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 AUD-01 canonical receipt exact-tree hardening | receipt 在 JSON 编码前递归校验容器/标量类型、序列顺序与 key 类型；tuple/list、嵌套非原生 mapping 和标量类型替换统一 fail-closed 为 `publisher_contract_violation`；composition/dispatcher/dispatch 定向回归 `30 passed`，audit contract、增量 mypy、architecture、Black/isort、compile/diff-check 通过 | 仅本地 receipt 合同加固（当前环境未安装 ruff 模块）；runtime 仍固定 `publisher_not_wired`，没有 durable publisher/receipt sink、authenticated scoped lifecycle、beat/retry/PG/VPS 证据，`AUD-01` 未解除，`AUD-02` 继续等待 |
| 2026-08-15 | 第二期 P0 | Web→TUI 修复后候选部署与回归证据 | 修复 `terminal_agent_unavailable` provider failure 映射和 AI 结果字段读取契约；固定 TUI Workbench 回归 `250 passed`，terminal/SDK/SSL/actionability 与增量 mypy/architecture 门禁通过；`dev/next-development@96ce6ee43b06e6eb6ad51528ff8ee783a4bf0952` 以标准 `git-clone` 构建部署为 release `20260815144517`，health/ready、迁移、TUI registry、Qlib、Celery/Caddy 均通过，证据已写入 VPS deployment evidence | M5 仍需角色化浏览器 UAT、写后 receipt/refresh、14 日观察、cleanup/rollback 双签；`/api/ready/` 的 Alpha/Qlib 与 workspace freshness warning 不得当作 decision-data gate 完成 |
| 2026-08-15 | 第二期 P0 | AUD-01 修复候选部署与运行取证 | `a76db97d4322fd7f6a2323f4f567873e8c53199c` 以标准 `git-clone` upgrade 部署为 release `20260815152834`，manifest/OCI/source 绑定一致；health/ready、容器、迁移、canonical schema、TUI registry、Qlib、Celery 复核通过，ready 的 Alpha/Qlib 与 workspace freshness warnings 原样保留 | 仅更新候选身份并部署 fail-closed 合同修复；AUD-01 durable publisher/authority/runtime 与 M5 角色 UAT、写后回执、14 日观察、rollback 双签仍缺 |
| 2026-08-15 | 第二期 P0 | 当前候选部署与运行取证（AUD-01/TUI） | 当前 `dev/next-development@cf68dc1e972ecd6e0ae002e4d4f96ff07ef86542` 以标准 `git-clone` code-only upgrade 部署为 release `20260815182857`，image `sha256:e04018272c08ef2dec2ffa98619e99ad689649c06da5964cbe93a9493602a552`；manifest/OCI/source 绑定一致，health/ready、容器、迁移、canonical schema、TUI registry、Qlib、Celery 复核通过，部署前 PostgreSQL 备份成功 | `/api/ready/` 仍有 Alpha/Qlib degraded、workspace recommendation stale 和 market thermometer partial-stale warnings；只更新候选身份与运行证据，不解除 AUD-01 durable publisher/authority/runtime、M5 角色 UAT/14 日观察、DATA-01 restore/rebuild 或 rollback 双签 |
| 2026-08-15 | 第二期 P0 | 当前候选部署与运行取证（EVID-01/AUD-01/TUI） | 当前 `dev/next-development@11594964f589c5f0ec3bf6a541d61d471b79b67f` 以标准 `git-clone` fresh、code-only 部署为 release `20260815200756`，image `sha256:2983bd567f4cb86a52ce48d7a1c3f2162fec4cddbaddb007975375b9448052af`；manifest/OCI/source 绑定一致，HTTPS health/ready、容器、迁移、canonical schema、TUI registry、Qlib、Celery/Caddy 复核通过，部署前 PostgreSQL 备份成功 | `/api/ready/` 仍有 Alpha/Qlib degraded、workspace recommendation stale、Alpha rank source stale 和 market thermometer partial-stale warnings；只更新候选身份与运行证据，不解除 EVID-01 owner/tenant authority、AUD-01 durable publisher/authority/runtime、M5 角色 UAT/14 日观察、DATA-01 restore/rebuild 或 rollback 双签 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 AUD-01 authority snapshot integrity contract | `SystemAuditAuthoritySnapshot.authority_content_hash` 以 domain-separated canonical digest 绑定 actor/user/tenant/owner、认证/staff 标志与 role；scope/身份替换或占位 digest 在 reader context 投影前统一 `authority_unavailable`；composition 定向 `20 passed`，与 query/dispatcher/task 合计 `38 passed`，增量 mypy/architecture/Black/isort/diff-check 通过 | 仅 provider snapshot 的本地完整性边界；未提供 authenticated tenant/owner RBAC/provider、route composition 或 durable publisher，runtime 仍 `publisher_not_wired`，`AUD-01` 保持 `planned`，不接 `AUD-02` |
| 2026-08-15 | 第一期 P1 | AUD-01 provider-issued query context boundary | `SystemAuditReaderContext` 只接受 Audit authority composition 的 provider-issued capability；直接手工构造的同形上下文在 query use case 入口 fail-closed，composition 保留并校验 tenant/owner 与 authority content hash；query/composition 定向回归 `39 passed`，增量 mypy/Black/isort/diff-check 通过 | 仅 Application context boundary；尚无 authenticated authority provider、tenant/owner lifecycle/filter columns、durable publisher、route、beat/retry 或 production runtime，`AUD-01` 仍 planned，AUD-02/03 继续等待 |
| 2026-08-15 | 第二期 P0 | 当前候选部署与运行取证（AUD-01 context boundary） | `dev/next-development@1835ce0ee42f220756066a21890bcec2b8f1f3e9` 以标准 `git-clone` code-only `-Upgrade` 部署为 release `20260815221000`，image `sha256:ef43c80ee8b5130775f152dfea0a7cdc62a93a8853ab4ccb8ba258ae83877ad1`；manifest/OCI/source 绑定一致，health、迁移、canonical schema、TUI registry、Qlib、Celery/Caddy 复核通过，切换前 PostgreSQL 备份成功 | `/api/ready/` warnings 原样保留；该候选不解除 authenticated tenant/owner authority、event scope/linkage、AUD-01 durable publisher/runtime、M5 角色 UAT/14 日观察、DATA-01 restore/rebuild 或 rollback 双签 |
| 2026-08-15 | 第二期 P0 | Web→TUI 当前候选 cutover gate 复核 | `check_web_to_tui_cutover_readiness.py --json` 返回 `decision=DENY`；source consistency 通过（matrix/catalog/evidence SHA 一致），但 stable version/commit window 未绑定、route UAT/cleanup 未绑定候选、production telemetry `0/101`、registry backup/restore 与 owner/reviewer attestations 缺失 | 这是机器拒绝提前 cutover 的证据，不解除 M5 角色化 UAT、14 日观察、生产 registry backup、rollback 或 Classic 清理门禁 |

| 2026-08-16 | 第二期 P1 | Web→TUI candidate identity consistency guard | guard 以已提交 deployment preflight 为候选唯一源，逐项核对 registry、readiness/deployment 最新候选段、cutover evidence 与 matrix/graph/runtime manifest SHA；当前统一到 `e167ab2fc748e4c93d2622f93fa8cc75442b2bb6` / release `20260816004134`，`1 passed`；M5 gate 仍独立返回 DENY | 只防止历史候选或旧 runtime binding 串入当前证据；角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry、registry backup/restore、rollback 与 owner/reviewer 双签仍缺 |
| 2026-08-15 | 第一期 P0 | 系统级统一审计日志 AUD-01 explicit event scope linkage contract | 新增 `AuditScopeRef(tenant_id, owner_id)`，进入 Domain canonical identity/content、strict codec、事件模型 `0012` scope pair 列/约束与 repository/query exact scope selector；历史 NULL scope scoped read、scope tamper、`event.owner` 冒充 scope 和无 scope append 均 fail closed；unit `30 passed`、audit component `8 passed`、Django check/migration drift/mypy/governance 通过 | 仅 scope contract/schema/local repository；没有 immutable tenant/owner lifecycle、server-issued provider、同-alias authority bundle、durable publisher、production backfill 或 route，AUD-01/02 继续阻断 |
| 2026-08-15 | 第一期 P0 | Evidence hard-gate next-slice exit audit | 只读复核 Broker/Evidence 最高优先链：`broker_order_evidence_integrated()` 仍固定 `False`，15 个直接仓位 surface `integrated=0`；本地 Risk ledger、Account zero-seed authority 与 disposable PostgreSQL harness 均不能替代 production composition/alias 证据 | 不新增 alias 胶水或 production writer；下一真实依赖为 immutable tenant/owner lifecycle、authenticated scoped provider、Research/Portfolio/Risk/Broker 同源 exact bundle，以及 staging/production PostgreSQL 并发/回滚证据；EVID-01/EVID-03、AUD-01、写入与 execution 继续关闭 |
| 2026-08-15 | 第一期 P0 | EVID-01 authority lifecycle/provider exit audit | 当前候选 `45281620a8739ee666a1b20e6c6511c0b8101111` / `20260815230537` 远端只读 inventory 显示 auth-context、User/RBAC authority、actor bundle、Evidence scope ledger 均为 `0` 行；scope-provider/actor-authority application 回归 `29 passed`，仅证明 dormant contract | 没有 immutable tenant/owner lifecycle、server-issued selector、同 alias atomic bundle 或生产 authority composition；不得从 mutable User/session/alias/现场 hash 推导 authority，EVID-01、写入与 execution 保持关闭 |

| 2026-08-15 | 第二期 P0 | DATA-01 当前候选 backup refresh failure evidence | 当前候选 `45281620a8739ee666a1b20e6c6511c0b8101111` 的远端归档 `postgres-20260815T154338Z.dump`（`140279578` bytes）在 VPS 端 `pg_restore --list` 通过，但 SFTP 下载约 `4194304` bytes 后连接被关闭；未生成完整本地归档/SHA，partial 已清理 | 这不是新恢复点；`DATA-01` 继续 awaiting，restore/rebuild、维护态 rollback、RTO/RPO、回填与 reconciliation 仍锁定 |

| 2026-08-16 | 第一期 P1 | 系统级统一审计日志 AUD-01 authority freshness/PIT contract | Authority snapshot 新增 provider-issued source identity/version、active/revoked state、`recorded_at/valid_until`，全部纳入 canonical hash；composition 与 query context 在实际 PIT 执行 `recorded_at <= as_of < valid_until`，future/expired/revoked fail-closed；audit composition/query `44 passed`，dispatcher/dispatch 合计 `56 passed`，增量 mypy/Black/isort/py_compile/governance/architecture 通过 | 仅本地 freshness/PIT 合同；无 authenticated tenant/owner lifecycle、source issuer、durable publisher/receipt sink、beat/retry 或生产 PostgreSQL/VPS authority 证据，`AUD-01` 与 `AUD-02` 仍阻断 |
| 2026-08-16 | 第一期 P1 | 系统级统一审计日志 AUD-01 durable delivery receipt contract | Publisher receipt 新增 bounded `sink_id`、`delivery_id` 与 `published_at`；缺失投递证明、publication clock 倒退或 receipt tree/type 替换均在 dispatcher 归类 `publisher_contract_violation`，不会计为 delivered；AUD composition/dispatcher/task `46 passed` | 仅本地 receipt 合同强化；runtime 仍 `publisher_not_wired`，无 durable sink、publisher/beat/retry、authenticated authority provider 或生产 PostgreSQL 投递证据，`AUD-01` 保持阻断，`AUD-02` 继续等待 |
| 2026-08-16 | 第二期 P0 | Web→TUI 当前候选部署与身份复核 | `dev/next-development@e167ab2fc748e4c93d2622f93fa8cc75442b2bb6` 以 code-only、保留数据卷、Celery enabled 的 `-Upgrade` 发布为 `20260816004134`；release/OCI/source、health/ready、迁移、canonical schema、TUI registry、Qlib、Celery 和部署前 PostgreSQL backup/`pg_restore --list` 通过；candidate consistency guard 已同步到新身份 | 仅更新当前候选并证明部署启动；角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry/defect、registry backup/restore、rollback drill 与 owner/reviewer 双签仍缺，M5 继续 DENY |
| 2026-08-16 | 第一期 P0 | EVID-01 当前候选 authority inventory 复核 | 当前 e167 / release `20260816004134` 的只读 PostgreSQL inventory 确认 0050–0053 migration 已应用，但 auth-context、User/RBAC、actor source、Evidence scope、subject/evidence/receipt 账本均为 `0` 行；摘要写入 `evid-01-authority-inventory-2026-08-16.json`，静态一致性回归 `1 passed` | 证明 schema-ready/zero-seed，不证明 authority lifecycle；禁止现场 hash/回填；仍需 immutable owner/tenant lifecycle、authenticated scoped provider、同 alias bundle、人工授权与 PostgreSQL 并发/回滚证据，EVID-01 继续 active |
| 2026-08-16 | 第一期 P0 | 当前候选 VPS 只读运行复核 | 对 e167 / release `20260816004134` 复核 Caddy/TLS、HTTPS health、容器、Django check、migration/canonical schema、TUI registry、Qlib 与 Celery；结构化摘要 `vps-runtime-verification-2026-08-16.json`，candidate consistency `1 passed` | 仅证明现有候选仍在运行且身份一致；未重新部署、未写数据；角色化浏览器 UAT、authority lifecycle、durable publisher、写后 receipt/refresh、14 日 telemetry、rollback 与双签仍缺 |
| 2026-08-16 | 第二期 P1 | Web→TUI M5 observation window binding | 已提交 deployment preflight attestation（SHA `a8bd41a0372bf587239fafc33c4c2e478c6a94a02cce4be8cb3cfa98ed7dd3b`），`start_web_to_tui_observation.py --write` 绑定候选 `20260816004134/e167ab2fc...` 与窗口 `2026-08-15..2026-08-29`；机器 readiness 仍 `DENY`，新候选 UAT `0/108`、telemetry `0/101` | 仅开始不跨候选继承的观察窗口；未完成角色化生产 UAT、写后回执/刷新、14 日 telemetry、cleanup/rollback、registry backup/restore 或 owner/reviewer 双签 |
| 2026-08-16 | 第二期 P1 | Web→TUI 本地写入口与角色 affordance 复核 | 当前候选的可编辑行操作保持“先表单、后提交”，无额外输入的审批/删除/切换动作保持直执行；dashboard `row_actions` 按当前用户可见 action 过滤；TUI actionability `10 passed`、Signal/Strategy 角色回归 `2 passed` | 仅本地交互与权限 affordance 证据；AgomTUI 外部 schema/validator、VPS 角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry、cleanup/rollback、registry backup/restore 与双签仍缺，M5 继续 DENY |
| 2026-08-16 | 第一期 P1 | STRAT-01 readiness mechanism allowlist 防绕过 | `OwnerMechanismAttestation` 现在在 Application 类型边界拒绝 live-data/outcome/snapshot/reconciliation requirement；补充直接组合绕过回归，Research readiness/runtime focused `27 passed` | 仅加强“机制 manifest 不得冒充生产证据”的本地 fail-closed contract；真实 R1–R8 owner/definition/policy/calendar/scope、PIT/OOS、canonical receipts、Promotion 与 consumer/UAT 仍缺，`STRAT-01`/`STRAT-02` 状态不变 |
| 2026-08-16 | 第一期 P1 | STRAT-01 机制 attestation 过期状态收口 | `AttestedMechanismOwnerAdapter` 在直接收集阶段将 `valid_until <= evaluated_at` 的 attestation 显式标记为 `STALE`，并发布稳定原因 `*.runtime.attestation_expired`；Research readiness focused `20 passed`、全分支 focused `53 passed`、增量 mypy `0 regressions`、Black/isort 通过 | 仅修复 Application 中间层可能把过期机制证据暴露为 `VERIFIED` 的 fail-closed 缺口；未登记真实 owner/definition/policy/calendar/scope、PIT/OOS、canonical receipts、Promotion 或生产/UAT 证据，`STRAT-01`/`STRAT-02` 状态不变 |
| 2026-08-16 | 第一期 P1 | AUD-01 scoped authority composition preflight | 新增 exact actor-source + tenant/owner-scope selector 与 typed readers；identity/version/content hash、actor/user/scope 一致性、active/staff/authenticated、PIT 与 validity 全部 fail-closed；unit `6 passed`、增量 mypy/governance 通过 | 仅本地 composition contract；不读取 mutable User/Profile/session，不创建 durable publisher/receipt sink，不接 beat/retry 或 production authority，`AUD-01` 继续阻断 |
| 2026-08-16 | 第二期 P0 | Web→TUI 当前候选部署与只读观测复核 | `dev/next-development@516f4e228699231831222613ffe56b9f6b5f0713` 以 code-only、保留数据卷、Celery enabled 的 `-Upgrade` 发布为 `20260816082603`；release/OCI/source、account.0054、Django deploy check、HTTPS health/ready、容器、TUI registry、Qlib、Celery 和 canonical schema 复核通过；部署报告 `remote-build-report-20260816082603.json` 与 preflight `web-to-tui-deployment-preflight-20260816082603.json` 已绑定 | `/api/ready/` 仍有 Alpha/Qlib provider degraded、workspace recommendation stale、Alpha rank source stale 和 market thermometer partial-stale warnings；角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry/defect、registry backup/restore、rollback 与 owner/reviewer 双签仍缺，M5 继续 DENY；AUD-01/EVID-01 durable authority/publisher 也未解除 |
| 2026-08-16 | 第二期 P0 | Web→TUI 最终代码候选部署与只读观测复核 | `dev/next-development@6c4086231a19005c750c856e78613b766bfd3609` 以 code-only、保留数据卷、Celery enabled 的 `-Upgrade` 发布为 `20260816085250`；release/OCI/source、account.0054、Django deploy check、HTTPS health/ready、容器、TUI registry、Qlib、Celery 和 canonical schema 复核通过；部署报告 `remote-build-report-20260816085250.json` 与 preflight `web-to-tui-deployment-preflight-20260816085250.json` 已绑定 | `/api/ready/` 仍有 Alpha/Qlib provider degraded、workspace recommendation stale、Alpha rank source stale 和 market thermometer partial-stale warnings；角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry/defect、registry backup/restore、rollback 与 owner/reviewer 双签仍缺，M5 继续 DENY；AUD-01/EVID-01 durable authority/publisher 也未解除 |
| 2026-08-16 | 第二期 P1 | Web→TUI 最终候选 observation window binding | `start_web_to_tui_observation.py --write --replace` 严格绑定已提交 preflight SHA `254b1dabe85181cd90120a1d872ed6668a69583fca9c8f2c3f20fb5859acd486` 到 `20260816085250/6c4086231a19005c750c856e78613b766bfd3609`，窗口 `2026-08-16..2026-08-30`；candidate consistency `1 passed` | 仅重置并开始新候选观察，机器 readiness 仍 `DENY`（UAT `0/108`、telemetry `0/101`、rollback/backup/审批缺失）；不代表角色化浏览器 UAT、写后回执、14 日稳定窗口或 cutover 授权 |
| 2026-08-16 | 第二期 P1 | 最终候选 VPS runtime verification 摘要 | 只读摘要 `docs/deployment/vps-runtime-verification-2026-08-16-0109.json` 固定 release/OCI/source、HTTPS health/ready、account migrations、canonical schema、TUI registry、Qlib、Celery 与响应 SHA；无业务写入 | 仅运行身份与启动观察；ready freshness warnings、角色化 UAT、写后 receipt/refresh、14 日 telemetry、registry restore、rollback、双签与 AUD-01/EVID-01 authority/publisher gate 仍未完成 |
| 2026-08-16 | 第一期 P1 | AUD-01 durable publisher composition preflight | `CanonicalSystemAuditPublisherPreflight` 要求 durable sink、exact payload preservation、幂等与 durable receipt；dispatcher 在 claim 前校验，generic/memory publisher 稳定返回 `publisher_contract_unavailable/invalid`；composition/dispatcher/task 定向回归 `49 passed`，增量 mypy、Black、isort 通过 | 仅本地 fail-closed composition contract；runtime 仍 `publisher_not_wired`，没有真实 publisher、beat/retry、authenticated scoped authority 或生产投递证据，AUD-01 继续阻断 |
| 2026-08-16 | 第二期 P0 | 当前候选部署与只读观测（AUD-01/TUI） | `dev/next-development@e29e15b09b47e07d9724b9cbc750ae2882310693` 以 code-only、保留数据卷、Celery enabled 的 `-Upgrade` 发布为 `20260816151607`；image `sha256:7663b1a13f0f6ca61b36cc3f8a673b25b08480b6a0c8c5d62c9eed840a7e40ae`；独立 verify 复核 HTTPS/Caddy、Django check、迁移/schema、TUI registry、Qlib、Celery；health/ready 均 200，运行摘要 `docs/deployment/vps-runtime-verification-2026-08-16-1534.json` | 只读观测，未登录/未写业务；ready 保留 `alpha_qlib_provider_degraded`、`workspace_recommendations_stale`、`workspace_alpha_rank_source_stale`；TUI 角色浏览器 UAT、写后 receipt/refresh、14 日 telemetry、backup/restore、rollback 与双签仍缺，M5 继续 DENY |
| 2026-08-16 | 第二期 P1 | Web→TUI candidate identity refresh | deployment preflight `docs/deployment/web-to-tui-deployment-preflight-20260816151607.json`（SHA `cc9279314b79a6d08ced79b0ce4e34c00985d038fabde6cbb5a433ca6a08966a`）与 registry/readiness/deployment/cutover evidence 统一到 `e29e15b09b47e07d9724b9cbc750ae2882310693` / `20260816151607`；candidate consistency guard 需在提交后复跑 | 仅绑定新候选并准备重置观察窗口；角色化 browser UAT、写后 receipt/refresh、14 日窗口、cleanup/rollback、registry backup/restore 与 owner/reviewer 双签仍未取得 |
| 2026-08-16 | 第二期 P1 | W2/W3/W4 closure review | Data/Strategy 本地 guards 与 contracts 保持 fail-closed，真实 backup/restore、controlled backfill、owner/PIT/OOS/Promotion/UAT 仍依赖生产证据；TUI 本地写表单与 role-affordance regression 已通过，AgomTUI portability、AI staging UAT、QMT Windows XtQuant 仍为外部阻断 | 不把本地 fake、SQLite、代码部署或只读 health 当作生产完成；`DATA-01/02/03`、`STRAT-01/02/03`、`TUI-01/02`、`AI-01`、`QMT-01/02` 状态保持 registry 原值 |
| 2026-08-16 | 第一期 P0 | EVID-02 append rollback bounded slice | Evidence-v3 repository 新增 caller-visible append 失败后的完整回滚、无 orphan、同一 candidate 可重试成功回归；component `5 passed`，Domain/Application `24 passed`，Black 与 diff-check 通过；提交 `6fcb50a97` | 仅本地事务/回滚合同；真实 PostgreSQL 双连接 first-winner、生产 migration/rollback、immutable owner/tenant authority 与人工 approval 仍缺，`EVID-02` 转为 `awaiting_production`，不解锁 `EVID-03` |
| 2026-08-16 | 第二期 P1 | DATA-01 reconciliation evidence immutability slice | reconciliation evidence repository 先 exact replay 再处理插入竞争；同 identity 的不同 snapshot fail-closed，不再覆盖历史；Data Center 定向回归 `13 passed`，增量 mypy `0 regressions`，提交 `07128a457` | 仅本地 append-only/replay 语义；生产 custom-format backup 下载哈希、维护态 rollback、restore/rebuild、RTO/RPO 与 DATA-02/03 仍未完成，`DATA-01` 保持 `awaiting_production` |
| 2026-08-16 | 第二期 P0 | DATA-01 restore evidence input immutability | `scripts/verify_postgres_backup_restore.py` 在格式校验前、恢复前后绑定 dump SHA-256；归档被替换时稳定 fail-closed，unit `14 passed`，增量 mypy 0；同日 VPS custom-format backup 完整下载并校验：`140804438` bytes，SHA `06e52b33c637c17cae4c9f0223246e0e09af84254717196d904f67044e7b2cba` | 只加强恢复工具的输入工件一致性并取得新的可验证恢复点；未执行 restore/rebuild、维护态 rollback、RTO/RPO、回填或 reconciliation，`DATA-01` 继续 `awaiting_production` |
| 2026-08-16 | 第二期 P1 | DATA-02 control-plane identity reuse guard | `SyncRun/SyncBatch/SyncCheckpointRepository` 对 stable key 重试逐字段核对不可变身份，拒绝 dataset/provider/run/checkpoint 替换；并发唯一键只接受 exact identity；identity-reuse 回归通过，增量 mypy `0 regressions`、Black/isort/diff-check 通过 | 仅加强本地回填控制面的幂等/身份合同；未连接生产数据库、未执行 restore/backfill/reconciliation，PostgreSQL 锁/并发、RTO/RPO 与 `DATA-01` 前置仍缺，`DATA-01/02/03` 状态不变 |
| 2026-08-16 | 第二期 P0 | 当前候选 VPS 只读部署与观测复核 | `dev/next-development@07d5d1d338c70ebc1d347663b48b09b38335fce5` 以 code-only、保留数据卷、Celery enabled 的 `-Upgrade` 发布为 `20260816160127`；release/image/source 绑定、迁移/schema、Django deploy check、HTTPS health/ready、TUI registry、Qlib、Celery 与 PostgreSQL archive 可读性复核通过；preflight `docs/deployment/web-to-tui-deployment-preflight-20260816160127.json`，runtime 摘要 `docs/deployment/vps-runtime-verification-2026-08-16-1624.json` | 只读观测，ready 仍有 Qlib/workspace/Alpha 与 market thermometer freshness warnings；未登录/未写业务，角色化 browser UAT、写后 receipt/refresh、14 日 telemetry、restore/rebuild/RTO-RPO、rollback、registry backup/restore 与 owner/reviewer 双签仍缺，M5 继续 `DENY` |
| 2026-08-16 | 第二期 P0 | 当前候选 VPS code-only 部署与只读观测复核 | `dev/next-development@fc145423c4de04cae20c3a6a2e94780505aa5938` 以 code-only、保留数据卷、Celery enabled 的 `-Upgrade` 发布为 `20260816170851`；image `sha256:04d08b5d3e1b1032abfbbefbeb4d9df0f4a6f8c33c706981056c1f36031112eb`；独立 verify 复核 source/image、迁移/schema、Django check、HTTPS/Caddy、TUI registry、Qlib、Celery；health/ready 均 200；runtime 摘要 `docs/deployment/vps-runtime-verification-2026-08-16-1721.json` | 首次部署预备份钩子返回空错误，未切换；随后独立校验恢复点后重试成功。ready 仍有 Qlib/workspace/Alpha 与 market thermometer freshness warnings；未登录/未写业务，角色化 browser UAT、写后 receipt/refresh、14 日 telemetry、restore/rebuild/RTO-RPO、rollback、registry backup/restore 与 owner/reviewer 双签仍缺，M5 继续 `DENY`；AUD-01/EVID-01 authority/publisher gates 不变 |
| 2026-08-16 | 第二期 P1 | Web→TUI 当前候选 observation window binding | `start_web_to_tui_observation.py --write --replace` 已严格绑定 preflight SHA `8639dc3a443e67fd44d51bc74b0f574b93f8a62493078167b1f63d0a2a2b5c7a` 到 `20260816170851/fc145423c4de04cae20c3a6a2e94780505aa5938`，窗口 `2026-08-16..2026-08-30`；candidate consistency `1 passed` | 仅重置并开始不跨候选继承的观察窗口；机器 readiness 仍 `DENY`（UAT `0/108`、telemetry `0/101`、rollback/backup/审批缺失），不代表角色化浏览器 UAT、写后回执、稳定窗口或 cutover 授权 |
| 2026-08-16 | 第二期 P1 | Web→TUI candidate consistency guard hardening | guard 不再硬编码旧 preflight 路径，而是从 cutover candidate 唯一匹配 preflight，并截取 readiness/deployment 最新候选段逐项核对完整 matrix/graph/runtime binding；最新候选 `fc145423c4de04cae20c3a6a2e94780505aa5938` / `20260816170851`，回归 `1 passed` | 仅防止旧章节或旧 preflight 让一致性测试误通过；不新增 UAT/telemetry/写后 receipt/rollback/审批证据，M5 仍 `DENY` |
| 2026-08-16 | 第二期 P1 | EVID-01 nested artifact invariant hardening | scoped grant boundary 重新验证嵌套 `ArtifactRef`，阻断 provider 原地篡改并重算 grant hash 的绕过；scope/source/provider/facade focused `71 passed`，增量 mypy/architecture/governance 通过 | 仅本地 Evidence authority boundary；immutable owner/tenant lifecycle、selector issuer、人工授权、PostgreSQL 生产证据与 hard gate 仍缺，`EVID-01` 状态不变 |
| 2026-08-16 | 第二期 P1 | AUD-01 preflight-to-receipt sink binding | publisher preflight 仅执行一次并返回经验证的 `sink_id`；dispatcher 要求每条 receipt 与该 sink 精确一致，sink substitution fail-closed；composition/dispatcher `47 passed`、task/authority `10 passed`，增量 mypy/architecture/governance 通过 | 仅本地 publisher composition 合同；durable publisher/receipt sink、authenticated authority、Celery beat/retry、Data Center UOW 与 PostgreSQL/VPS 证据仍缺，`AUD-01` 继续阻断 |
| 2026-08-16 | 第二期 P1 | DATA-02 primary-key collision identity guard | 控制面 upsert 在自然键未命中而 primary key 冲突时，按完整 lookup+identity 复核并稳定拒绝 batch/checkpoint 身份替换；新增 collision 回归，`test_control_plane.py --reuse-db --no-migrations` `12 passed`，增量 mypy `0`，Black/isort/py_compile/diff-check 通过 | 仅本地控制面冲突诊断与幂等合同；未连接生产 PostgreSQL，未执行 restore/backfill/reconciliation、维护态 rollback 或 destructive migration，`DATA-01/02/03` 状态不变 |
| 2026-08-16 | 第二期 P1 | EVID-01 critical execution-test contract alignment | 关键 broker/agent 测试对齐当前 Evidence hard gate：create/approve/lease/Fake Agent 在 evidence 未接入时稳定阻断、订单保持 `WAITING_APPROVAL`；`tests/critical` 两文件 `13 passed`，Black/isort/py_compile/diff-check 通过 | 仅修正过时测试合同；未放宽 execution gate、未接入 publisher/authority；全仓 mypy debt 与 module-cycle baseline 仍需独立治理，EVID-01 继续 active |
| 2026-08-16 | 第二期 P1 | CI module-cycle regression repair | 将 Data Fetch audit envelope 归回 Audit owner；Research/Risk 的跨 App 读写改为注入 Protocol；Dashboard→Sentiment 改走 app-neutral readiness registry；本地模块护栏 `206 edges / 0 cycles`、架构/治理合同 `0 violations`、聚焦回归 `32 passed`、增量 mypy `0 regressions` | 未扩大 module-cycle allowlist 或 mypy baseline；全仓 mypy debt ceiling 与远端 CI 仍需独立验证，生产 authority、publisher、execution/TUI 生产门禁不变 |
| 2026-08-16 | 第二期 P1 | CI full-production mypy debt ceiling reduction | 清理 Account authority/codec 与 canonical creation closed-world 类型诊断、append-only model guard 签名、Broker/Portfolio/Risk/Research repository 冗余 cast、serializer 输入收窄与第三方过期 ignore；全量 `mypy --no-incremental` `2881 source files / 0 errors`，CI 同款 debt ceiling `0 errors`，相关回归与格式检查通过 | 未修改 baseline/allowlist，未改变业务/权限/执行语义；GitHub Actions 仍需对新提交重新执行 Fast Feedback，生产 authority/publisher、execution/TUI 生产门禁不变 |
| 2026-08-16 | 第二期 P0/P1 | Web→TUI 当前候选 VPS code-only 部署与只读观测 | `dev/next-development@443658d33159dd80a35b3001ae2c8505113e3fff` 以 `-Upgrade`、保留 PostgreSQL/Redis 数据卷、Celery enabled 发布为 `20260816223921`；health/ready HTTP 200，迁移/schema、Django check、TUI registry、OCI/source、Qlib、Celery、容器与部署前 PostgreSQL archive 复核通过；当前 EVID-01 authority inventory 仍为 `blocked_zero_seed_authority`，12 个 authority/evidence 表为 0 行 | 仅代码部署与只读运行证据；ready freshness warnings、角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry、restore/rebuild/RTO-RPO、rollback、registry backup/restore、owner/reviewer 双签与 AUD-01/EVID-01 authority/publisher 仍缺，M5 继续 `DENY`；详见 `docs/deployment/vps-runtime-verification-2026-08-16-2258.json` 与 `evid-01-authority-inventory-2026-08-16-2258.json` |

## 2026-08-12 整理结果

- 归档 50 份已完成文档：原批次 44 份，加上零循环整改、TUI IA，以及经代码与自动化证据复核完成的 MCP、Capability Catalog 和 Terminal 三条历史任务主线。
- 保留 45 份活跃文件；其中也包括尚未解除生产门禁的本地完成记录。
- Web → TUI M0–M4 历史证据已归档；M5 readiness、生产 preflight、UAT、回滚与 route closure 继续留在活跃区，直到 cutover 门禁真正解除。
- AI-Native 主计划因人工 UAT 签字与 release gate 尚未补齐继续保留；首页聊天复用任务书因前端自动化和浏览器验收资产未闭环继续保留。
- `implementation-progress-summary.md` 暂保留为总体进度入口，不按阶段总结归档。

| 2026-08-16 | 第二期 P0/P1 | Web→TUI 当前候选部署、只读运行复核与观察窗口重绑定 | `dev/next-development@5a13125bb84eb1b20e623d7c1388a0d7632294cb` 发布为 `20260816181141`；health/ready 均 200，迁移/schema、Django check、TUI registry、Qlib、Celery、容器与 preflight 复核通过；preflight SHA `30e7a5a501849ffddca7b372d3c79d5576708e5dbebe459342c3278886fbfdf9` | 仅代码部署与只读证据，须在提交后重绑 `2026-08-16..2026-08-30` 观察窗口；ready freshness warnings、角色化 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback、registry backup/restore、owner/reviewer 双签与 AUD-01/EVID-01 authority/publisher 仍缺，M5 继续 `DENY` |
| 2026-08-16 | 第一期 P0 | AUD-01 runtime composition preflight boundary | 新增同-alias event/outbox/dispatch/durable-publisher/scoped-authority 注入合同；缺组件、generic/memory sink、authority selector 替换或 alias drift 在 claim 前稳定 fail-closed；`tests/unit/audit` `175 passed`，Black/isort、Django check、architecture/governance 与增量 mypy 通过 | 仅本地 composition contract；runtime 仍 `publisher_not_wired`，没有 durable publisher/receipt sink、authenticated authority lifecycle、beat/retry 或生产投递证据，AUD-01 不解除、AUD-02 继续等待 |
| 2026-08-16 | 第二期 P0 | DATA-01 当前候选 PostgreSQL 备份下载复核 | 候选 `443658d33159dd80a35b3001ae2c8505113e3fff` / release `20260816223921` 的远端 custom-format 归档 `/opt/agomtradepro/backups/database/postgres-20260816-164649.dump` 已完成 `pg_restore --list`、SFTP 完整下载、尺寸与 SHA-256 校验；`140977814` bytes，SHA `297d0dc67eb76ff394e2e6e2367a8ba0bc0a0d7ed90af8ce39d3b9f3d86d93b1`，记录见 [`vps-postgres-backup-verification-2026-08-16-2348.json`](../deployment/vps-postgres-backup-verification-2026-08-16-2348.json) | 只新增可验证恢复点；未 restore/rebuild、维护态 rollback、RTO/RPO、回填、切读或 reconciliation，DATA-01 仍 `awaiting_production`，不解锁 DATA-02/03 |
| 2026-08-17 | 第一期 P1 | EVID-01 authority inventory candidate binding guard | guard 从当前 cutover candidate 唯一匹配 deployment preflight、VPS runtime verification 与 authority inventory，校验 registry/release/OCI/image/preflight SHA 及 `blocked_zero_seed_authority`、12 表全零；`tests/unit/test_evid_01_authority_inventory_evidence.py` `1 passed`，Black/isort/diff-check 通过 | 仅防止旧候选或旧 zero-seed inventory 串入当前 release；不创建/回填 authority、不读取 User/Profile/session、不接 production writer；immutable owner/tenant lifecycle、authenticated provider、人工授权与 PostgreSQL production race/rollback 仍缺，EVID-01 保持 `active` |
| 2026-08-17 | 第一期 P1 | AUD-01/DATA-01 CI 收口复核 | 提交 `1e3dd3cfe` 推送后四条必需 GitHub Actions 全部成功：Security Scan、Architecture Layer Guard、Consistency Check、CI Fast Feedback（含 Python 3.11/3.13、增量质量与完整 production mypy debt ceiling）；此前 inventory stale 由确定性生成脚本修复 | CI 通过只证明仓库合同与门禁一致，不解除 AUD-01 durable publisher/authority、EVID-01 authority lifecycle、DATA-01 restore/rollback 或 TUI 生产 UAT 门禁 |
| 2026-08-17 | 第一期 P0 | EVID-01 当前候选 authority inventory 一致性 guard | authority inventory guard 从 active registry/cutover candidate 动态匹配唯一 deployment preflight、VPS runtime verification 与 zero-seed inventory，校验 preflight SHA、OCI revision、runtime image、0050–0053 migration 与 12 表全零；focused `1 passed`，Black/isort、增量 mypy、diff-check 通过 | 仅防旧候选/旧 inventory 串入当前 release；不创建或回填 authority、不读取 User/Profile/session、不接 production writer，EVID-01 仍 `active` |
| 2026-08-17 | 第一期 P1 | 当前候选 VPS 只读部署与 CI 观测 | `3ceafaf193e87626be7458531c66e96b11f7df84` 发布为 `20260817002134`；health、迁移/schema、TUI registry、Qlib、Celery 与容器检查通过；部署报告保留在忽略目录 | 仅证明本候选运行身份与基础服务可用；不解除角色化 UAT、写后回执、14 日 telemetry、restore/rollback、AUD-01/EVID-01 authority/publisher 或 owner/reviewer 双签门禁 |
| 2026-08-18 | 第一期 P1 | TUX-01 metadata fail-safe fallback + batch revalidation | `PublishedTuiMetadataRepository` 对非法 DB published payload 回退 reviewed file payload，发布 `metadata_health=degraded/file` 与结构化 `tui_metadata_fallback` warning；新增只读 `revalidate_tui_metadata_registry --dry-run`，遍历全部 registry 状态并给出修复/归档建议，实跑 `total=8, valid=1, invalid=7, errors=0, writes=0, outcome=partial`；focused `14 passed`，Black/isort、增量 mypy、diff-check 通过 | TUX-01 已完成仓库 exit gate；命令不自动修改非法行，生产 registry 修复、外部 AgomTUI 与 M5 证据链仍未验收 |
| 2026-08-18 | 第一期 P0/P1 | Evidence/Audit closure review | Evidence/Audit scope、dispatcher、publisher preflight 与 task/metrics focused 回归 `112 + 8 passed`；EVID-01 owner/tenant authority、EVID-02 生产 PostgreSQL 竞争、AUD-01 durable publisher/authority 仍保持 fail-closed，未提前接线下游 | EVID-03、AUD-02/03 继续等待依赖；本地 fake、SQLite 或只读部署证据不替代生产 authority/publisher/rollback/UAT |
| 2026-08-18 | 第二期 P0/P1 | DATA/STRAT/TUI production closure audit | DATA-01/02/03、STRAT-01/02/03、TUI-01/02 逐项复核，未发现可在仓库内诚实完成且不冒充生产证据的 bounded slice；保留 `awaiting_production`/`waiting_dependency` 状态和 fail-closed 工具 | 仍需 release owner 在受控生产环境取得 backup/rollback、PIT/OOS/receipt、角色化 HTTPS UAT、写后持久化、观察窗口和签字证据 |
| 2026-08-18 | 第一期 P1 | CI inventory stale correction | 刷新 `governance/data_center_entrypoints.json` 的确定性生成物，提交 `54caffc4a`；该提交的 Consistency、Fast Feedback、Architecture Layer Guard、Security Scan 全部成功 | 仅证明该提交的仓库门禁；Nightly 既有覆盖率失败尚未重新运行，不能据此解除生产计划门禁 |
| 2026-08-18 | 第二期 P1 | TUX-02 metadata source-boundary guard | 新增只读 `scripts/check_tui_metadata_source_consistency.py`，同时读取 published JSON、IA 注册表与真实 runtime loader；校验 IA-owned published screen copy、24 个 runtime screen、action/panel 引用闭合，并显式报告 Python screen patch 在 full-IA payload 上被忽略或未注册的边界；workflow 已接入该 guard，focused `4 passed`，实际检查 `outcome=ok`（12 published screens、24 runtime screens、430/889 actions） | 仅完成三真源的一致性机器校验与 patch 边界取证；尚未删除 legacy Python screen patch、尚未完成 8 处文案双写对账/迁入 publish-review，因此 TUX-02 保持 active，TUX-03 继续等待 TUX-02，TUX-04 已单独进入 runtime contract active 阶段 |
| 2026-08-18 | 第二期 P1 | TUX-04 runtime screen metadata contract | IA `runtime_screens` 的 12 个 runtime screen 已逐项补齐 `summary`、`view_type`、`user_experience`、`default_action_key`；默认入口均经真实 normalized runtime action 集合核对，新增契约测试 `9 passed`，未改变 runtime injection 或外部 AgomTUI | 仅完成 runtime screen 契约字段迁移；研究与工具分组重排、入口消歧、术语统一、普通角色浏览器走查仍未完成，TUX-04 保持 active |
| 2026-08-18 | 第二期 P1 | TUX-02 retired alias screen patch | 删除不可能在 full IA runtime 命中的 `command-center.auto-advisor` Python screen patch；IA alias 仍解析到 `command-center.decision-flow`，新增 alias 不注册回归，source-boundary focused `4 passed`，实际检查 `outcome=ok`（12/24 screens、430/889 actions、0 violations） | 仅收口一个已证实 dead patch；其余 legacy alias patch、8 处漂移双写与 publish/review 迁入仍待完成，TUX-02 保持 `active` |
| 2026-08-18 | 第二期 P1 | TUX-02 execution.audit screen patch | 删除已由 IA canonical screen 完整承载的 `execution.audit` Python screen patch；`execution.events`/`execution.share` alias 仍落到同一 canonical screen，新增 panels 归属回归，source-boundary `5 passed`，实际检查 `outcome=ok`（12/24 screens、430/889 actions、0 violations） | 仅再收口一个已证实 dead patch；其余 legacy alias patch、8 处漂移双写与 publish/review 迁入仍待完成，TUX-02 保持 `active` |
| 2026-08-18 | 第二期 P1 | TUX-02 research.signals copy boundary | 删除 `research.signals` Python patch 中与 IA/published graph 完全重复的 `label`/`summary` 双写，保留行为性 patch；新增 source-boundary 回归 `6 passed`，runtime screen/action 结果不变 | 仅收口两项已证实漂移双写；其余 legacy alias patch、6 处漂移双写与 publish/review 迁入仍待完成，TUX-02 保持 `active` |
| 2026-08-18 | 第二期 P1 | TUX-02 prompt.workbench copy boundary | 删除 `prompt.workbench` runtime injection 中与 IA 完全重复的 `label`、`module_key`、`group`、`audience`、`summary`、`view_type`、`default_action_key`、`user_experience` 双写，保留 workflow、business context 与 dashboard panels；IA/runtime focused `17 passed`、actionability `11 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations） | 仅收口一项已证实 runtime screen 语义双写；其余 legacy alias patch、6 处漂移双写与 publish/review 迁入仍待完成，TUX-02 保持 `active` |
| 2026-08-18 | 第二期 P1 | TUX-02 runtime action copy boundary | 6 个 `replace_existing` action 的 `label`/`description` 现在由 published action 保持，runtime injection 继续提供 endpoint/fields/view_model/task_group 等行为；source consistency `8 passed`、TUI focused `29 passed`、source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations） | 已核实的初始 8 处 copy drift 完成对账；legacy alias patch 清理与 runtime 文案迁入 publish/review 仍待完成，TUX-02 保持 `active`，不新增生产/UAT 证据 |
| 2026-08-18 | 第二期 P1 | TUX-04 execution.audit semantic alignment | `execution.audit` 的 IA 与 published graph summary、business context、checkpoints 已按实际审计健康/事件指标/实盘对账/操作审计 panels 对齐；IA/runtime focused `10 passed`，source guard `outcome=ok` | 仅收口一个已核实的 summary/panel 语义错位；研究与工具分组重排、入口消歧、术语统一、普通角色浏览器走查仍待完成，TUX-04 保持 `active` |
| 2026-08-18 | 第二期 P0 | TAR-01 legacy inline concurrency guard | 旧 Web/TUI inline Agent 路径强制最多 1 个执行槽，即使环境变量尝试放大也只记录 warning 并保持安全上限；service + execution-lease focused `25 passed`，增量 mypy `0 regressions` | 仅完成迁移期事故半径保护；异步 run API、PostgreSQL durable queue、专用 Worker、容量/故障注入、生产观察与 TAR-05 仍未完成，TAR-01 保持 `active` |
| 2026-08-18 | 第二期 P0/P1 | 当前候选 VPS 部署、恢复与稳定观测 | `84293272218725c286aed29db68bd3dae9cb4b16` 以 code-only `-Upgrade` 发布为 `20260818115436`；首轮 `20260818112302` 部署后验证失败并自动回滚超时，随后不删数据卷重建 compose；最终 web `healthy/restarts=0`，Caddy/Celery/数据库/Redis 运行，外部 HTTPS health/ready `200`，镜像 manifest/OCI/source 绑定、迁移预检、TUI registry、pyqlib 检查与部署前 PostgreSQL 备份均有记录 | 仅完成当前候选的恢复后只读运行证据；验证器首轮失败与共享 PID namespace 干扰已如实记录，未取得角色化 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback 双签或 AUD-01/EVID-01 authority/publisher 证据，M5 与相关生产门禁继续 `DENY` |
| 2026-08-18 | 第二期 P0 | TAR-01 queued runtime pure contract slice | 冻结 owner-scoped run selector、aware deadline/digest、有限 dispatch state machine、终态幂等、ID-only broker envelope 与敏感字段拒绝；纯契约 `26 passed`，与现有 terminal service/execution guard 合计 `45 passed`，增量 mypy `0 regressions`，architecture audit `0 violations` | 仅完成 dormant Application/Domain boundary；未接新 Web/TUI intake、PostgreSQL durable run、Celery/专用 Worker、真实 broker、容量/chaos、生产观察或 TAR-02；TAR-01 仍 active |
| 2026-08-18 | 第二期 P0 | TAR-01 admission/queue policy contract | 新增纯 Application `terminal_runtime_queue_policy.py`，冻结 queue/stream 名称、worker/prefetch/深度边界、每用户/全局准入上限、hard/soft/cancel/heartbeat/orphan 截止时间、queued/legacy/emergency feature flags 与受限 inline 上限；纯回归 `15 passed`、增量 mypy `0 regressions`、Black/isort/ruff 通过 | 仅策略合同，不含生产默认值或 ORM/Celery/broker/worker/route；尚无容量/故障注入、真实队列或生产观察，TAR-01 仍 active、TAR-02 不提前启动 |
| 2026-08-18 | 第二期 P0 | TAR-01 migration default contract | 新增 `TerminalRuntimeFeatureFlags.migration_defaults()`，固定 R0 queued intake/worker 关闭、legacy inline 单槽/60 秒上限、queue unavailable 时 `PAUSE`、emergency stop 关闭；focused 回归 `16 passed`，增量 mypy `0 regressions`，Black/isort/ruff/architecture 通过 | 仅命名纯合同 fixture，不改变运行时组合，不接真实队列/Worker/容量证据；TAR-01 仍 active，TAR-02 不提前启动 |
| 2026-08-18 | 第二期 P0 | TAR-01 runtime configuration freeze | `core/settings/base.py` 固化 queued intake/worker、legacy inline、emergency stop、准入上限和 60 秒 inline 上限；production 强制 queued 两闸关闭；VPS compose 显式注入同一组默认值；配置/compose 回归 `20 passed`，TAR contract 回归 `57 passed` | 只冻结配置边界，不接 queued route、PostgreSQL admission、Celery/Worker、broker 或真实容量/生产观察；TAR-01 仍 active，TAR-02 继续等待 |
| 2026-08-18 | 第二期 P0 | TAR-01 queued API composition boundary | 新增纯 `TerminalQueuedRunApplicationBoundary`，未来 API 仅能注入 `SubmitTerminalQueuedRunUseCase`/`TerminalQueuedSubmissionPort`；legacy inline/local-cli 在 adapter 调用前拒绝，AST guard 无 inline Agent service/infrastructure/Celery/ORM；focused `8 passed` | 仅完成 failing-first Application boundary；未接 Web route、durable PostgreSQL admission、broker/Worker、真实 SSE、容量/chaos 或生产观察，现有同步 inline 路径仍保持受限，TAR-01 active、TAR-02 继续等待 |
| 2026-08-18 | 第二期 P0 | TAR-01 machine-readable runtime ADR | 新增 `governance/terminal_agent_runtime_contracts.json`，复用现有 AgentTask/TerminalRun/API/queue/flag contracts，机器冻结职责、状态投影、幂等与敏感数据边界、error codes、API/SDK/MCP/TUI compatibility、Worker/broker/queue、cancel/orphan 与 fail-closed flags；manifest/source consistency `4 passed` | 仅完成 TAR-01 machine-readable ADR/guard；未接 durable PostgreSQL、Web/TUI route、Celery/broker/Worker、SDK/MCP queued client 或生产容量/UAT；reserved codes 仍 `not_runtime`，TAR-01 active、TAR-02 waiting |
| 2026-08-18 | 第二期 P0 | TAR-01 ADR/contract freeze | 新增 [ADR-0008](../architecture/adr-0008-terminal-agent-runtime-boundary.md)，冻结 AgentTask/Run/Port 职责、状态图、owner/client 幂等、prompt/secret retention、稳定错误码与兼容策略；同步更新 `docs/INDEX.md` | 仅完成契约文档冻结；不代表负载基线、durable PostgreSQL admission、broker/Worker、真实 SSE/SDK/TUI、容量/chaos 或生产 UAT，TAR-01 active、TAR-02/TAR-03 继续等待 |
| 2026-08-18 | 第二期 P0/P1 | TAR-01 当前候选 VPS 部署与恢复后稳定观测 | `1b80034dc8cde602274e88265169b826c7962271` 构建 release `20260818130903`；标准 verifier 在 web 重启窗口失败并回滚超时，随后保留数据卷手动切换到已验证 manifest；最终 `current=/opt/agomtradepro/releases/source-20260818130903`，web healthy/restarts=0，Caddy/Celery/数据库/Redis/RSSHub running，HTTPS health/ready `200`，pyqlib `0.9.7`/错误 distribution absent，deployment report 已下载 | 仅当前候选恢复后只读运行证据；没有 queued intake、角色化 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback drill、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据；M5 与 TAR-01/TAR-05 生产门禁继续 `DENY`/active |
| 2026-08-18 | 第一期 P1 | AUD-01 authority-before-publisher preflight ordering | runtime composition 先校验 server-issued scoped authority，再调用未来 publisher capability preflight；缺失或 forged authority 不会触发 publisher 外部能力检查，publisher 缺失仍返回 `publisher_not_wired`；focused `13 passed` | 仅本地 fail-closed ordering guard；未创建 durable publisher、authenticated authority source、Celery beat/retry、Data Center 同 UOW 或生产 PostgreSQL/VPS 投递证据，AUD-01 gate 不变、AUD-02/03 继续等待 |
| 2026-08-18 | 第二期 P1 | VPS Web liveness stabilization after Alpha/Qlib timeout | `56f2f8d4acb4e4510015ee10f6578fb9edc1c698` code-only 部署为 release `20260818185114`、image `sha256:e62c0347848245773dbaa11d47a2bf397f99c6be3a203582fc4ab76fdc999906`；二次只读 verifier（120s remote window）全项通过，Web restart `0`/healthy，HTTPS health 8 次均 `200`（约 `1.09–1.85s`），容器内 inline inference=`false`、pool cap=`120`，近 10 分钟无 inline/长 Alpha 日志；部署 verifier 默认/一键窗口随后加固为 `120s` | 首次标准 verifier 的 30s remote healthcheck timeout 已如实保留；当前仅为短时只读稳定性样本，需继续观察 Alpha latency/restarts/errors；不解除 TUI 角色 UAT、写后回执、14 日 telemetry、AUD-01/EVID-01 或备份恢复门禁；详见 [VPS Web liveness stabilization](vps-web-liveness-stabilization-2026-08-18.md) |
| 2026-08-18 | 第二期 P0/P1 | TAR-01 当前候选 VPS 部署与短窗口只读观测 | `6e217afdd7599086f25f7100a92ae34324e5df73` 以 code-only `-Upgrade` 发布为 release `20260818201455`；数据卷保留、Celery 启用，镜像 `sha256:667b500fdcb4024eb5c63c9e9a6af119d2b4012532683a8f8f861654126df6f1`；迁移/schema、Django check、TUI registry、Qlib、Celery、容器与备份预检通过；部署后 HTTPS health 8 次均 `200`（约 `1.10–1.86s`） | 仅当前候选短窗口只读运行身份/健康证据；queued intake/Worker 仍关闭，未取得角色化 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback drill、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据；M5/TAR-01/TAR-05 生产门禁继续 `DENY`/`active`，报告见 `dist/remote-build-reports/remote-build-report-20260818201455.json` |
| 2026-08-18 | 第二期 P0/P1 | TAR-01 boundary guard 当前候选 VPS 部署与短窗口只读观测 | `d238091d9e7e3aa1324baf92199100e800122ed7` 以 code-only `-Upgrade` 发布为 release `20260818210752`；数据卷保留、Celery 启用，镜像 `sha256:3dd6401b8e8b757087e3d99e1e91dc1ebf539f33bc5148fd7198925717e5cdc3`；迁移/schema、Django check、TUI registry、Qlib、Celery、容器、备份与 TUI JS 预检通过；部署后 HTTPS health 8 次均 `200`（约 `1.17–2.08s`） | 仅当前 boundary guard 候选短窗口只读运行身份/健康证据；queued intake/Worker 仍关闭，未取得角色化 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback drill、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据；M5/TAR-01/TAR-05 生产门禁继续 `DENY`/`active`，报告见 `dist/remote-build-reports/remote-build-report-20260818210752.json` |
| 2026-08-18 | 第二期 P1 | Web→TUI candidate evidence-section consistency guard | 一致性测试固定读取 `config/tui/migration/web_to_tui_cutover_evidence.v1.json` 的不可变 candidate binding，并在 readiness/deployment 文档中选择同时包含完整 candidate/matrix/graph/runtime binding 的匹配章节，避免把较新的 TAR/普通 VPS 观测误当 M5 候选；candidate guard `1 passed`，TUI 相关回归合计 `29 passed` | 仅修正本地证据章节选择与候选绑定校验；没有更新 candidate、没有把普通/TAR 部署提升为 M5 preflight，也未取得角色化 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，M5 继续 `DENY` |
| 2026-08-19 | 第二期 P0/P1 | TUX-02 当前候选 VPS 只读部署与 CI 观测 | `ae332f0390ef67e5ec1386bdfe5307d5cc9bc6e7` 以 code-only 发布为 release `20260819000530`；迁移/schema、TUI registry、Qlib、Celery、容器与 HTTPS health 复核通过；最新 Security Scan、Architecture Layer Guard、Consistency Check、CI Fast Feedback（Python 3.11/3.13）与 Incremental quality gates 全部成功 | 仅记录 TUX-02 runtime copy boundary 的部署身份、短时只读健康与仓库门禁；未取得角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据，M5 与相关生产门禁继续 `DENY` |
| 2026-08-19 | 第二期 P1 | TUX-02 research.signals dead patch cleanup | 删除已由 IA/published graph 完整承载、在 full-IA canonical runtime 中必然被忽略的 `research.signals` Python screen patch；source-boundary focused `8 passed`、TUI metadata/actionability/IA `29 passed`、`npm run check:tui` 与 source guard `outcome=ok` | 仅删除一项已被 full-IA 证据证明无效的 legacy patch；其余非 IA legacy patch、runtime 文案迁入 publish/review、外部 AgomTUI portability 与 M5 角色化生产 UAT仍未完成，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-19 | 第二期 P1 | TUX-02 research.asset-lab dead patch cleanup | 删除已由 IA/published graph 完整承载、且在 full-IA canonical runtime 中明确被忽略的 `research.asset-lab` Python screen patch；保留未注册于 IA 的 `research.alpha-triggers` patch；source consistency `9 passed`、TUI actionability/IA `21 passed`、`npm run check:tui`、TUI JS `34 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations），增量 mypy/ruff/Black/isort 通过 | 仅收口一项已证实无效的 legacy patch；其余非 IA patch、runtime 文案迁入 publish/review、外部 AgomTUI portability、M5 角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 与 owner/reviewer 双签仍未完成，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-19 | 第二期 P0/P1 | TUX-02 `0c50dfafc24f` VPS code-only 部署与短时只读观测 | 四条 CI 全部成功后，以 `-Upgrade` 发布 release `20260819054217`；source/image runtime match，迁移/schema、Django check、TUI registry、Qlib、Celery、容器与 TLS 复核通过；HTTPS `/api/health/` 8/8、`/api/ready/` 3/3 均 200 | 仅证明该 TUX-02 普通部署的运行身份与短时只读健康；未取得角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据，M5 candidate 不重绑，相关门禁继续 `DENY` |
| 2026-08-19 | 第二期 P1 | DATA-01 A 股涨跌停口径显式化 | `CN_A_LIMIT_UP_COUNT`/`CN_A_LIMIT_DOWN_COUNT` 的 Data Center/Pulse 元数据、回滚迁移与 provider 口径统一标明“不含 ST”；Tushare 与 AKShare 计数均过滤名称含 `ST` 的记录，focused unit `37 passed`、component `11 passed`、迁移检查/Django check/mypy/ruff/Black/isort 通过 | 仅完成本地指标口径与实现一致性收口；未取得生产 PostgreSQL 回填、覆盖率、restore/rollback、RTO/RPO 或 DATA-01 生产验收，DATA-01/02/03 状态不变 |
| 2026-08-19 | 第二期 P0/P1 | DATA-01 当前候选 VPS 部署、迁移与只读观测 | `09e8e5ed11fd56c39b1090f28159a59d1ad4c6a4` 发布为 `20260819024316`；data_center.0072/pulse.0007 应用成功，canonical schema、Django check、TUI registry、Qlib、Celery、容器与 HTTPS health 复核通过；本提交四条 CI（Security、Architecture、Consistency、Fast Feedback）全部成功；随后 8 次 `/api/health/` 均 `200` | 仅记录本候选的数据口径部署身份与短时只读健康；未取得生产回填覆盖率、restore/rollback、RTO/RPO、角色化 UAT、业务写后 receipt/refresh、14 日 telemetry、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据，DATA-01/02/03 与相关生产门禁保持原状态 |
| 2026-08-19 | 第二期 P0/P1 | DATA-01 当前最终候选 VPS 部署、fail-closed 修复与只读观测 | `4040d98916dc2527ba5d60ce8b4433c0bdad10f3` 发布为 `20260819034120`；Tushare/AKShare 名称缺失、`NaN` 与 `ST` 行均不计入，schema/Django check/TUI registry/Qlib/Celery/容器与 HTTPS health 复核通过；随后 8 次 `/api/health/` 均 `200`；该提交四条 CI 全部成功 | 仅记录最终候选的 provider 口径、运行身份与短时只读健康；未取得生产回填覆盖率、restore/rollback、RTO/RPO、角色化 UAT、业务写后 receipt/refresh、14 日 telemetry、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据，DATA-01/02/03 与相关生产门禁保持原状态 |
| 2026-08-19 | 第二期 P0 | TAR-01 baseline candidate identity guard | `TerminalRuntimeBaselineCandidate` 将 commit/release、OCI revision、runtime manifest digest 与 test-matrix digest 作为单一候选身份；1/5/10/20 样本必须 exact-equal，缺失/漂移/伪造 artifact binding 在 sample intake 再验证并 fail-closed；baseline/manifest focused `20 passed`，增量 mypy/ruff/Black/isort 通过 | 仅强化未来容量证据的候选绑定，未执行真实 1/5/10/20 压测、容量/SLO、chaos、队列/Worker 或生产观察；TAR-01 仍 active，TAR-02/TAR-05 与生产门禁不变 |
| 2026-08-19 | 第二期 P0 | TAR-01 baseline observation collector boundary | 新增纯 Application `TerminalRuntimeBaselineObservationPort`/`TerminalRuntimeBaselineCollector`；按 1/5/10/20 各调用一次注入 observer，environment/candidate/concurrency exact binding、object-new 伪造 identity 与 unavailable 均 fail-closed；collector focused `6 passed`，增量 mypy/ruff/Black/isort 通过 | 仅冻结未来受控 observer 的注入边界，不执行网络/进程/数据库/broker/Agent 观测，不生成默认容量值；未取得真实 1/5/10/20 负载、SLO、chaos 或生产观察，TAR-01/TAR-05 与生产门禁不变 |
| 2026-08-19 | 第二期 P0 | TAR-01 hard-SLO 与 deterministic matrix gate | 新增纯 Application 硬 SLO 与规范测试矩阵：19 项阈值由观测值计算，unavailable/越限 fail-closed；11 层场景覆盖 10 类 threat，canonical SHA-256 必须与 candidate test-matrix digest exact-match；metric/sample/SLO exact-type 重建，NaN/float-concurrency/object-new 伪造被拒，manifest 锁完整阈值/场景；focused `41 passed`、增量 mypy `0 regressions`、registry `0 violations` | 仅堵住验收门防伪缺口；矩阵仍诚实标记 6 个 planned 场景，尚无 concrete observer、真实 1/5/10/20 负载/进程/Redis/DB/MCP/model、chaos 或生产证据，inline 单槽和 queued 两闸保持不变，TAR-01 active、TAR-02 waiting |
| 2026-08-19 | 第二期 P0 | TAR-01 reserved queued route fail-closed guard | 新增 `TerminalQueuedRuntimeUnavailable` Application guard 与 `/api/terminal/runs/`、`queue/`、`{run_id}`、`events/`、`cancel/` dormant 503 boundary；认证通过后统一返回 `DISPATCH_UNAVAILABLE`/`queued_runtime_not_wired`，不触碰 legacy inline Agent、ORM、Celery 或 broker；route/application/AST focused `8 passed` | 仅显式保持冻结的异步路径不可用，未实现 durable PostgreSQL admission、Celery/专用 Worker、真实 SSE、容量/chaos、生产观察或 TAR-02；queued intake/worker 仍关闭，TAR-01 与生产门禁不变 |
| 2026-08-19 | 第二期 P0 | TAR-01 route guard 当前候选 VPS 部署与短窗口只读观测 | `3ba46b2f06bce4cf11cc0293903a54193be7b4ef` 发布为 release `20260819064907`；镜像 `sha256:232faecb1f69c69778085aee69d90f66dcbfd5c54085ed13f27ab181c0c0e12c`，迁移/schema、Django check、TUI registry、Qlib、Celery、容器、备份与 TLS verifier 通过；HTTPS `/api/health/` 8/8 为 `200`（约 `1.09–1.85s`），`/api/ready/` 3/3 为 `200`（约 `4.61–11.55s`） | 仅证明该候选的短窗口只读运行身份与基础服务稳定；匿名 reserved route 受认证边界返回 `403`，未取得认证后 `503` 的生产 UAT；未做业务写入、角色浏览器 UAT、1/5/10/20 容量/chaos、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，TAR-01/TAR-02 与生产门禁保持 fail-closed |
| 2026-08-19 | 第二期 P1 | TUX-02 execution.events/share legacy alias patch cleanup | 删除已由 IA canonical `execution.audit` 承载、且在 full-IA normalized runtime 中不再注册的两个 legacy screen patch；alias 仍解析到 canonical screen，新增 source-boundary 回归确认旧 key 不进入 runtime；focused metadata/actionability/IA `30 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations；patch 配置 15→13） | 仅收口两个已证实 dead screen patch，不改变 canonical audit/actions，不接数据库修复、外部 AgomTUI 或生产 UAT；其余 non-IA patch、publish/review 迁入、M5 角色化浏览器 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 与双签仍未完成，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-19 | 第二期 P1 | TUX-02 api-library.data-center dead patch cleanup | 删除已由 IA/published graph 完整承载、且在 full-IA normalized runtime 中不再注册的 `api-library.data-center` legacy screen patch 与空 redundant-action 配置；provider columns、update/test row actions 与 receipt panel 由 canonical runtime 保持；data-center/source focused `14 passed`、TUI metadata/actionability/IA/source `35 passed`、完整 Workbench `255 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations；patch 配置 13→12） | 仅收口一项已证实 dead patch，并修正对应 legacy fixture 断言；未宣称外部 AgomTUI portability、角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-19 | 第二期 P1 | TUX-02 macro-regime dead screen patch cleanup | 删除 `macro-regime.beta-gate`、`macro-regime.hedge`、`macro-regime.pulse` 三个已由 IA canonical screens 承载、在 full-IA runtime 中不再生效的 legacy screen patch 与 redundant-action 配置；beta-gate/hedge 仍 alias 到 `macro-regime.strategy`，pulse 仍 alias 到 `macro-regime.overview`；focused metadata/actionability/IA `38 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations；patch 配置 12→9） | 仅收口三项已证实 dead patch并保留 synthetic legacy compatibility；未宣称外部 AgomTUI portability、角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-20 | 第二期 P1 | TUX-02 ai-ops.providers dead screen patch cleanup | 删除已由 IA/published graph 完整承载、在 full-IA normalized runtime 中明确被忽略的 `ai-ops.providers` legacy screen patch；AI 服务商 canonical screen 的语义、三块 panels 与 action 引用保持不变，新增 IA/runtime 与 synthetic legacy-payload 回归；source consistency `2 passed`，完整 source guard 仍为 `outcome=ok`（12/24 screens、430/889 actions、0 violations；patch 配置 9→8） | 仅收口一项已证实 dead patch并保留 legacy payload 自有契约；其余 non-IA patch、runtime 文案迁入 publish/review、外部 AgomTUI portability、M5 角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 与 owner/reviewer 双签仍未完成，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-20 | 第二期 P1 | TUX-02 risk-center.overview dead screen patch cleanup | 删除已由 IA canonical `macro-regime.strategy` 承载、在 full-IA normalized runtime 中不再注册的 `risk-center.overview` legacy screen patch；alias、canonical panels/action 引用不变，新增 IA/runtime 与 synthetic legacy-payload 回归；TUI/source focused `45 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations；patch 配置 8→7） | 仅收口一项已证实 dead patch并保留 legacy payload 自有契约；其余 non-IA patch、runtime 文案迁入 publish/review、外部 AgomTUI portability、M5 角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 与 owner/reviewer 双签仍未完成，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-20 | 第二期 P1 | TUX-02 research.alpha-triggers dead screen patch cleanup | 删除已由 IA canonical `research.signals` 承载、在 full-IA normalized runtime 中不再注册的 `research.alpha-triggers` legacy screen patch；alias、canonical panels/action 引用不变，新增 IA/runtime 与 synthetic legacy-payload 回归 | 仅收口一项已证实 dead patch并保留 legacy payload 自有契约；其余 non-IA patch、runtime 文案迁入 publish/review、外部 AgomTUI portability、M5 角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 与 owner/reviewer 双签仍未完成，TUX-02/TUX-04 继续 active/production_validation |
| 2026-08-20 | 第二期 P1 | TUX-02 execution.account-settings dead screen patch cleanup | 删除已由 IA canonical `execution.accounts` 承载、在 full-IA normalized runtime 中不再注册的 `execution.account-settings` legacy screen patch；旧 alias 仍解析到 canonical screen，legacy payload 自有 contract 保留；TUI metadata/source focused `49 passed`，source guard `outcome=ok`（12/24 screens、430/889 actions、0 violations；patch 配置 6→5） | 仅收口一项已证实 dead patch；未宣称外部 AgomTUI portability、角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，TUX-02/TUX-04 继续 active |
| 2026-08-19 | 第二期 P0/P1 | TUX-02 `550e458fc` VPS code-only deployment and read-only observation | 四条 CI 全部成功后以 `-Upgrade` 发布 release `20260819175810`、镜像 `sha256:d58001e265c70348c9e26a169e73016e83a89020c995c8a11cefa46d3d96abfd`；source/image runtime match，迁移/schema、Django check、TUI registry、Qlib、容器、Caddy domain/TLS、备份与 Celery ping 复核通过，HTTP health `200`；报告见 `dist/remote-build-reports/remote-build-report-20260819175810.json` | 仅记录该不可变候选的部署身份与短时只读健康；未取得角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，TUX-02/TUX-04 与 M5 生产门禁继续 `DENY` |
| 2026-08-19 | 第二期 P0/P1 | TUX-02 `86ed6008b` VPS code-only deployment and read-only observation | 四条 CI 全部成功后发布 release `20260819103557`、镜像 `sha256:05dafc0b8bd95d4c5705edafadd2d2abc63e98ffbaf42085108a70df79d81e3b`；迁移/schema、Django check、TUI registry、Qlib、Celery、容器与 TLS verifier 通过；独立 HTTPS `/api/health/` 8/8、`/api/ready/` 3/3 均 `200`（报告 `dist/remote-build-reports/remote-build-report-20260819103557.json`） | 仅记录该候选部署身份与短时只读健康；ready 仍含 `etf_net_flow` stale/degraded observation，未取得角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/rollback、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据，TUX-02/TUX-04 与相关生产门禁继续 fail-closed |
| 2026-08-19 | 第二期 P0/P1 | 当前 `f9f31700a` 候选 VPS 部署与短窗口只读观测 | 四条 GitHub CI 全部成功后，以 code-only `-Upgrade` 发布 release `20260819080800`；source/image runtime match，迁移/schema、Django check、TUI registry、Qlib、Celery、容器、TLS 与部署前 PostgreSQL backup 复核通过；VPS 独立 HTTPS `/api/health/` `8/8` 为 `200`（约 `0.013–0.200s`），`/api/ready/` `3/3` 为 `200`（约 `3.48–8.17s`）；报告见 `dist/remote-build-reports/remote-build-report-20260819080800.json` | 仅证明该不可变候选的部署身份、短窗口只读健康与运行依赖；ready 仍保留既有 decision-data freshness/degraded-source 观察；未取得角色化浏览器 UAT、业务写后 receipt/refresh、1/5/10/20 容量/chaos、14 日 telemetry、restore/rollback、owner/reviewer 双签或 AUD-01/EVID-01 authority/publisher 证据，相关生产门禁继续 fail-closed |
| 2026-08-19 | 第一期 P0/P1 | AUD-01 runtime authority snapshot preflight | `preflight_system_audit_runtime_authority()` 按同一 dispatch cutoff 读取 provider-issued authority snapshot，重验 hash/PIT/active-staff-authenticated/actor-user/tenant-owner，并将 source identity/version 与 server-issued selector 精确绑定；runtime/authority/composition focused `60 passed`，增量 mypy、Black、isort、Ruff 通过 | 仅新增本地 authority preflight contract，不接 dispatcher claim、durable publisher、authenticated production lifecycle、beat/retry、Data Center 同 UOW 或生产 PostgreSQL/VPS 投递；AUD-01 gate 保持阻断，AUD-02/03 继续等待 |
| 2026-08-19 | 第一期 P0 | AUD-01 authority-before-claim dispatcher wiring | dispatcher 现在以同一 `as_of` 强制执行 authority preflight→durable publisher preflight→`claim_due`；authority 未接线/异常/替换统一 blocked 且 `claimed=delivered=failed=0`，不会触碰 sink 或 outbox；定向 `37 passed`、完整 audit unit `186 passed`，增量 mypy/ruff/Black/isort 通过 | 仅完成本地调用顺序与 fail-closed wiring；生产 authenticated authority、durable publisher/receipt sink、beat/retry、Data Center 同 UOW 与 PostgreSQL/VPS 投递仍缺，AUD-01 保持 `active`，AUD-02/03 继续等待 |
| 2026-08-19 | 第二期 P0 | DATA-01 latest backup + isolated restore evidence | 下载并校验最新已有 VPS 归档 `postgres-20260819-044335.dump`（`141990139` bytes，SHA-256 `b9177563…9cef0c`，不新建/不 prune）；修复 Windows container `.pgpass` 权限与脱敏 stderr；对已验证 `20260816-164649` 归档在独立 disposable PostgreSQL 中完成 source↔restore 自洽演练，`outcome=success`、536 表/71 migrations/schema/table/sequence 全匹配，restore `595.930s`、verification `533.391s`、total `1693.029s`，专项 `15 passed` | 只证明本机隔离恢复工具和归档自洽；该 elapsed 不是生产 RTO/RPO，最新恢复点与演练归档分别绑定；未做 VPS maintenance、生产 DDL/restore、live rollback、回填/reconciliation 或 owner 验收，DATA-01 保持 `awaiting_production`，DATA-02/03 不解锁 |
| 2026-08-19 | 第一期 P0/P1 | AUD-01 preflight candidate VPS deployment and read-only observation | `fbf0901522f6310cb66b2571f5400fede1d2e646` 发布为 release `20260819091201`、image `sha256:24987a71875c1156ced5d796eeb0cda0dec011439d382c7c511e360f409b5272`；四条 GitHub CI、迁移/schema、Django check、TUI registry、Qlib、Celery、容器、TLS 与 PostgreSQL backup 复核通过；独立 HTTPS health `8/8`、ready `3/3` 均 `200` | 仅记录该候选的部署身份与短窗口只读健康；ready 仍保留 `etf_net_flow` stale / market-thermometer degraded 观察；未取得 authenticated authority lifecycle、durable publisher/receipt、dispatcher claim、角色化 UAT、业务写后 receipt/refresh、容量/chaos、14 日 telemetry、restore/rollback 或 owner/reviewer 双签，AUD-01/EVID-01 与相关生产门禁继续 fail-closed |
| 2026-08-19 | 第一期/第二期 P0/P1 | AUD-01/DATA-01 `29cdf1420` 候选部署与短窗口观测 | 四条 push CI 全绿后发布 release `20260819133110`、image `sha256:3748a5aa68dd919e2225894a851c41db88a49cd8b16396974bd64d77ff80a88c`；发布前 PostgreSQL/Redis 备份、迁移/schema、Django check、TUI registry、Qlib、Celery、容器与 TLS verifier 通过；独立 health `8/8`（约 `1.09–3.46s`）、ready `3/3`（约 `4.62–9.94s`）均 `200` | 仅证明当前不可变候选身份、恢复点与短窗口基础健康；ready 如实阻断超过 4 小时的两只行情并保留 `etf_net_flow` stale/degraded；未取得 authenticated audit authority、durable publisher/delivery、生产 restore/RTO/RPO、live rollback、回填/reconciliation、角色 UAT、写后 receipt/refresh、14 日 telemetry 或双签，AUD-01 仍 active、DATA-01 仍 awaiting，依赖门禁不解锁 |
| 2026-08-19 | 第二期 P0 | TAR-01 `da04c053a` 部署与认证 fail-closed 边界验收 | 四条 push CI 全绿后发布 release `20260819145227`、image `sha256:cc6fe35e4e14643223cbb9f97953ef5499ce47f844bdd97eb6e4d319ba952b3b`；备份、迁移/schema、Django/TUI/Qlib/Celery/TLS/容器验证通过。生产认证账号对 reserved route 完成 `1/5/10/20` 共 36 次请求，全部返回预期 `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired / Retry-After=60`，前后 health `10/10` 为 `200`，核心容器 restart=`0`/OOM=`false` | 仅证明不可变候选的认证边界稳定 fail-closed；混合 20 route + 20 health 外部 HTTPS p95 为 `4786.71/4026.08 ms`，既有 Qlib 任务同时占用 Celery 约 `100% CPU`、内存最高 `87.64%`。没有真实 queued admission/worker/SSE/idempotency/cancel/provider-MCP/chaos 数据，不能生成 capacity-ready 报告；TAR-01 active、TAR-02 waiting，queued/worker flags 和 inline 单槽不变 |
| 2026-08-19 | 第二期 P0 | TAR-01 offline snapshot observer/evidence contract | 新增严格 `terminal-runtime-baseline-snapshot.v1` 离线快照解析、`terminal-runtime-baseline-evidence.v1` canonical serializer 与 content-addressed dry-run/append-only recorder；候选、矩阵、UTC、未知字段、secret 字段、有限数值和大小边界均 fail-closed；observer `9 passed`，baseline/SLO/matrix `42 passed`，observer+manifest `14 passed`，architecture/governance 0 violations，增量 mypy/ruff/Black/isort 通过 | 仅提供外部受控快照的离线证据转换，不访问 VPS/网络/ORM/Celery/Agent，不生成真实负载或生产容量结论；manifest 仍 `production_evidence_status=not_runtime`、`runtime_enablement=not_authorized`，六个 repository/Celery/events/local-CLI/load/chaos 场景继续 planned，TAR-01/TAR-02/TAR-05 门禁不变 |
| 2026-08-20 | 第二期 P0 | TAR-01 controlled observer adapter | 新增注入式 `TerminalRuntimeBaselineControlledObserver`，绑定四档 `1/5/10/20` 与一次 hard-SLO 的 environment/candidate/concurrency，receipt、候选/级别替换、缺失/不可用指标均 fail-closed；受控 observer `15 passed`，TAR-01 组合回归 `59 passed`，增量 mypy/ruff/Black/isort 通过 | 仅是受控 staging harness 的 typed composition，不连接 HTTP/process/Redis/PostgreSQL/Celery/Agent，不产生真实容量值；TAR-01/TAR-05 仍 active/fail-closed，queued/worker 不启用 |
| 2026-08-20 | 第二期 P0/P1 | TAR-01 controlled observer candidate VPS deployment and read-only acceptance | `24407af74d4c4c2469f54aff442364ad2de805d5` 发布为 release `20260820035448`、image `sha256:5d1ebbf2ac55a8e91b3dee667eff096fbb698740964e1ce2173b0f6bbc26ed3b`；部署 verifier、迁移/schema、Django/TUI/Qlib/Celery/容器/备份、source/image match 全通过；公网 HTTPS `/api/health/`、`/api/ready/`、`/api/` 各 `5/5` 为 `200`，未认证 reserved route `403`，决策接口保持 `503 decision_runtime_blocked` | 仅证明该候选部署身份、短窗口只读健康和 fail-closed 边界；observer 未接生产运行时，不生成真实 1/5/10/20 容量/SLO/chaos 或业务写入证据，TAR-01/TAR-05 仍 active/fail-closed；报告见 `dist/remote-build-reports/remote-build-report-20260820035448.json` |
| 2026-08-19 | 第一期 P0 | EVID-02 offline PostgreSQL evidence collector/report contract | 新增纯 Application EVID-02 report validator/serializer 与默认 dry-run、显式 `--write` 的 content-addressed recorder；固定本地 disposable PostgreSQL harness、三项逐 case facts、UTC/secret/host/database 边界，明确 `production_claim=false`、`production_ready=false`，head audit/human approval 缺口保持 `not_collected`；focused `15 passed` | 仅完成自动采集与不可变报告合同，不连接 PostgreSQL、Risk Center approval writer、VPS/production database 或真实审核人；不把 EvidenceScopeSourceV1 harness 当 approval proof，EVID-02 仍 `awaiting_production`，EVID-01 authority、生产 current-head/rollback 与 Evidence hard gate 继续 fail-closed |
| 2026-08-19 | 第一期 P0 | EVID-02 current-head/approval link audit contract | 新增只读 `evid-02-head-audit-snapshot.v1` 严格解析、approval/activation 全链 head 与 PIT 审计、exact approval identity 绑定、默认 dry-run/内容寻址报告 CLI；离线 EVID-02 合计 `29 passed`，增量 mypy/Ruff/Black/isort 通过 | 仅完成外部快照到 fail-closed 报告的本地合同；不连接生产 PostgreSQL、approval writer、真实并发/回滚或人工审批，报告固定 `production_claim=false`、`production_ready=false`、`human_approval_status=not_collected`，EVID-02 仍 `awaiting_production` |
| 2026-08-19 | 第一期 P0 | EVID-01 当前 HEAD VPS authority inventory 只读验收 | 当前 HEAD `0ad5df129fbc` 以 code-only、保留数据卷、Celery enabled 发布为 release `20260819193755`，image `sha256:571940d6198e…`；HTTPS/TLS、容器、Django check、迁移/schema、TUI registry、Qlib、Celery、备份与 verifier 全部通过；同一候选 PostgreSQL 只读盘点确认 account 0050–0053 已应用、12 张 authority/evidence 表全为 `0`，证据见 [`evid-01-authority-inventory-2026-08-19-1156.json`](../deployment/evid-01-authority-inventory-2026-08-19-1156.json) 与 [`remote-build-report-20260819193755.json`](../../dist/remote-build-reports/remote-build-report-20260819193755.json) | 仅完成当前候选运行身份与 zero-seed inventory 验收；`EVID-01` 仍 `active`，不得从 mutable User/Profile/session/request 现场推导或回填 authority，authenticated owner/tenant provider、同 alias bundle、人工授权、生产写入及 PostgreSQL race/rollback 证据仍缺，Evidence hard gate 与 execution 继续 fail-closed |
| 2026-08-19 | 第二期 P0/P1 | M5 candidate `20260819195103` / EVID-01 inventory 重绑与只读验收 | `dev/next-development@0ad5df129fbc` 的当前 candidate 绑定为 release `20260819195103`、image `sha256:5580a17c333b…`；最新 verifier 对 HTTPS/TLS、容器、Django check、迁移/schema、TUI registry、Qlib、Celery、备份与 healthcheck 全部通过；同一 candidate 的 runtime verification 与 EVID-01 inventory 已匹配，12 张 authority/evidence 表仍为 `0`，见 [`vps-runtime-verification-2026-08-19-1210.json`](../deployment/vps-runtime-verification-2026-08-19-1210.json)、[`evid-01-authority-inventory-2026-08-19-1210.json`](../deployment/evid-01-authority-inventory-2026-08-19-1210.json) | 仅完成候选一致性与 zero-seed 只读验收；M5 仍需角色化浏览器 UAT、写后 receipt/refresh、14 日观察、restore/rollback 与 owner/reviewer，EVID-01 仍缺 authenticated owner/tenant lifecycle/provider、人工授权、production writer 与 PostgreSQL race/rollback，相关门禁继续 fail-closed |
| 2026-08-19 | 第一期 P0 | EVID-01 offline authority-inventory report contract | 新增纯 Application `apps/research/application/evid_01_authority_inventory.py` 与 `scripts/record_evid_01_authority_inventory.py`；固定 production/PostgreSQL/public、0050–0053 migration、12 张 authority/evidence/root-lock 表、read-only 快照、UTC/commit/hash/secret/unknown-key 边界；报告由 row counts 推导 `blocked_zero_seed_authority` 或 `blocked_unverified_authority`，默认 dry-run、显式 content-addressed append-only `--write`；focused EVID-01 合计 `16 passed`，Ruff/Black/isort、增量 mypy、debt ceiling、governance consistency 通过 | 仅把外部受控快照转换为不可变 fail-closed 本地报告；不连接 PostgreSQL/VPS/User/Profile/session、不回填 authority、不接 owner/tenant lifecycle、人工授权、production writer 或 race/rollback，`EVID-01` 继续 `active`，生产写入与 Evidence hard gate 继续关闭 |
| 2026-08-19 | 第一期 P0/P1 | EVID-01 offline contract candidate VPS deployment and read-only observation | 提交 `0c92cf6357ea2e33877319342745fb4eadde103f` CI 四项全绿后，以 code-only `-Upgrade` 发布 release `20260819210655`、image `sha256:af5bb7953e42…`；HTTPS health/ready `200`、Caddy/TLS、容器、Django check、migrations/schema、TUI registry、Qlib、Celery ping 与 backup 复核通过；同候选只读 PostgreSQL 盘点确认 0050–0053 已应用、12 张表全 `0`，见 [`vps-runtime-verification-2026-08-19-1334.json`](../deployment/vps-runtime-verification-2026-08-19-1334.json)、[`evid-01-authority-inventory-2026-08-19-1332.json`](../deployment/evid-01-authority-inventory-2026-08-19-1332.json) 与 [`evid-01-authority-inventory/9d/9dc07b28dd278133a1f2dab078a0c2c3c06862fa9094a9e2e78c3143bcd5162d.json`](../deployment/evid-01-authority-inventory/9d/9dc07b28dd278133a1f2dab078a0c2c3c06862fa9094a9e2e78c3143bcd5162d.json) | 仅证明该代码候选部署身份、短窗口只读健康与 zero-seed 状态；M5 registry/cutover candidate 尚未因本非-TUI slice 重绑，ready 仍有既有 `etf_net_flow` degraded/stale 观察；不证明 authenticated owner/tenant lifecycle、人工授权、production writer、角色化 UAT、14 日 telemetry、restore/rollback 或 PostgreSQL race，`EVID-01`/M5 继续 fail-closed |
| 2026-08-19 | 第二期 P0 | TAR-02 durable run repository contract slice | 新增 `TerminalAgentRunModel`/0004 migration 与 owner-scoped `TerminalAgentRunRepository`；既有 `AgentTaskModel.created_by` 归属校验、`(actor_user, client_request_id)` first-winner 幂等、digest 冲突拒绝、claim 行锁、外层事务回滚与 message/prompt 不落 run ledger；TAR-01 ports/boundary + repository `15 passed`，PostgreSQL 并发项因本地后端跳过，增量 mypy/ruff/Black/isort 通过，makemigrations 无漂移 | 仅完成 dormant ORM/repository 合同；未接 route、admission/queue、`on_commit` dispatch、Celery/Redis/broker/Worker、SSE/events/cancel、真实 PostgreSQL race/rollback、容量/chaos 或生产观察；`TAR-02` 仍 waiting，queued intake/worker 继续关闭 |
| 2026-08-19 | 第二期 P0 | TAR-02 PostgreSQL 双连接 first-winner/rollback 证据 | 新增显式隔离的 `tests/settings_terminal_agent_run_postgres.py` 与回滚可见性测试；本机 disposable PostgreSQL 18.4 双连接真实运行 `9 passed in 86.11s`，claim first-winner 与 outer rollback 均验证；默认 SQLite/生产设置不会自动启用该证据配置 | 仅证明 repository 的 PostgreSQL 行锁与事务可见性合同；未接 queued admission、`on_commit` dispatch、Celery/Redis/broker/Worker、SSE/events/cancel、容量/chaos 或生产写入，TAR-02 仍 waiting，queued runtime 与生产 gate 继续 fail-closed |
| 2026-08-20 | 第二期 P0 | TAR-01 当前候选只读健康与认证边界复核 | 针对既有 TAR-02 schema/repository 候选（release `20260819223747`，source `2fa7c4c8a4e09ea26e1e50e3e510c20c2bd26cab`）独立复核公网 HTTPS：`/api/health/` 连续 `8/8` 为 `200`（约 `1.09–12.66s`），未认证 `/api/terminal/runs/` 连续 `4/4` 为 `403`；未改生产状态 | 仅证明基础运行健康与认证边界；未取得认证后 reserved-route `503`、真实 1/5/10/20 admission/queue/Worker/SSE/idempotency/cancel/provider-MCP/chaos 指标，不能生成 capacity-ready 报告；TAR-01/TAR-02 与生产 gate 继续 fail-closed |
| 2026-08-20 | 第二期 P1 | STRAT-01 全能力 readiness inventory contract | 新增只读 `EvaluateAllCapabilityReadinessUseCase`，按 canonical R1–R8 顺序逐项调用现有 owner-scoped readiness provider，保留每项完整 evidence/blocker，不跨能力合并或以机制 evidence 推导 live readiness；Research readiness/runtime `29 passed` | 仅完成本地盘点合同与“live requirement 不得被 static mechanism 冒充”的回归；真实 owner/definition/policy/calendar/scope、Publication/PIT/OOS/receipt/lineage、Promotion 与 consumer UAT 仍缺，`STRAT-01/02/03` 状态不变 |
| 2026-08-20 | 第二期 P0 | DATA-01 最新已有 VPS PostgreSQL 归档格式复核 | 下载并校验 `/opt/agomtradepro/backups/database/postgres-20260819-164435.dump`；本地归档 `backups/vps-postgres/postgres-20260819-164435.dump`，`142059273` bytes，远端/本地 SHA-256 均为 `3269f238b1e141de49e723a1f84388c538572d503cd856e0791b735aab8e82a3`；本地 PostgreSQL 18.4 `pg_restore --list` exit `0` | 仅刷新可复核恢复点；未新建备份、prune、生产 restore/RTO/RPO、维护态 rollback、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-20 | 第二期 P0 | Critical Reliability PostgreSQL Nightly 实际取证 | 当前候选 `578064409b8269e440ba7edbf9c480aa7d9917ff` 的 [GitHub Actions run 32276242287](https://github.com/guiyinan/agomTradePro/actions/runs/32276242287) 独立 PostgreSQL 16.15 job 成功；custom-format backup/隔离 restore/schema verifier 与关键分层回归合计 `700 passed + 1 expected SQLite fallback skip`，dump `3600046` bytes，SHA-256 `2b4c7e57e33aa797abfac49d7935d0f0276a0d9616cb123d131c180605a75a75` | 仅解除 CI PostgreSQL Nightly 测试子项；不替代生产数据库 restore/RTO/RPO、维护态 rollback、全市场回填/reconciliation、M9/M10 或真实 QMT，相关生产 gate 继续 fail-closed |
| 2026-08-20 | 第二期 P0/P1 | 当前 `41005ea22` 候选 VPS 部署与只读观测 | 四条 push CI 全绿后以 code-only `-Upgrade` 发布 release `20260820012016`、image `sha256:0834950788c4575ae702f701505697d77c43933a48ca445496a714934b95213e`；source/image runtime match，迁移/schema、Django check、TUI registry、Qlib、Celery、容器、Caddy/TLS 与部署前 PostgreSQL backup 复核通过；独立 HTTPS `/api/health/`、`/api/ready/`、`/api/` 为 `200`，未认证保护接口为预期 `403`，`/api/regime/current/` 保持既有 `503 decision_runtime_blocked` fail-closed | 仅证明当前不可变候选身份、短窗口只读健康和认证边界；未取得角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、生产 restore/RTO/RPO、live rollback、回填/reconciliation、capacity/chaos 或 owner/reviewer 双签，相关门禁继续 fail-closed |
| 2026-08-20 | 第二期 P0/P1 | `41005ea22` VPS 独立 verifier 重跑 | 针对 active release `20260820012016` 以 expected source SHA 重跑 `scripts/deploy_vps_verify.py`，exit `0`；Caddy/TLS、health、容器、Django、迁移/schema、TUI registry、Qlib、release/image identity、backup、healthcheck、Celery worker/beat 与 ping 全部 `[OK]`；独立 HTTPS `/api/health/`、`/api/ready/`、`/api/` 为 `200`，未认证保护接口 `403`，决策接口保持 `503 decision_runtime_blocked` | 只读验收边界进一步取证；不等同角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、生产 restore/RTO/RPO、live rollback、回填/reconciliation、capacity/chaos 或 owner/reviewer 双签，相关门禁继续 fail-closed |
| 2026-08-20 | 第一期 P0 | 当前候选 EVID-01/AUD-03 PostgreSQL 只读盘点 | release `20260820012016` web 容器内复核 account `0050`–`0054`、agent_runtime `0004`、audit `0012` migration 已应用；12 张 authority/evidence/root-lock 表为 `0` 行，`audit_system_outbox`/`audit_system_event` 也为 `0` | 只证明 schema 已部署、authority 仍 zero-seed、当前无 outbox backlog；不证明 durable publisher、dispatcher delivery、authenticated owner/tenant authority、同 UOW 双写、PG race/rollback 或 owner 签字，EVID-01/AUD-01/AUD-03 继续 fail-closed |
| 2026-08-20 | 第二期 P0/P1 | `9341789db` latest-candidate VPS deploy + HTTPS read-only acceptance | 四条 push CI 全绿后发布 release `20260820025103`、image `sha256:02a4a5b7098aec6ba3c152d05442a6316e1428bc3feee652e8f82ff672b4a200`; source/image runtime match，部署内置 verifier、迁移/schema、Django check、TUI registry、Qlib、Celery、容器、Caddy/TLS 与备份复核通过；公网 `/api/health/`、`/api/ready/`、`/api/` 为 `200`，未认证保护接口为预期 `403`，`/api/regime/current/` 保持 `503 decision_runtime_blocked` fail-closed | 仅证明当前不可变候选身份、短窗口只读健康与认证边界；未取得角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、restore/RTO/RPO、live rollback、capacity/chaos 或 owner/reviewer 双签，TUX-02/TUX-04、M5、TAR、AUD、EVID 相关生产门禁继续 fail-closed |
| 2026-08-20 | 第二期 P0/P1 | M5/TAR 最新候选一致性重绑与 VPS 只读验收 | `f3881a04cf0b5d5bff5d2b7e5a6bf25d523667e2` 发布为 release `20260820043710`、image `sha256:ac621fb9cd594045e211e5a4e7cc16c11fea10ca8c34fb5bea148572b4347dc5`；source/image、迁移/schema、Django check、TUI registry、Qlib、Celery、容器、TLS、资源与备份 verifier 全通过；独立 HTTPS `/api/health/`、`/api/ready/`、`/api/` 各 `5/5=200`，未认证 reserved route `403`，决策接口 `503 decision_runtime_blocked`；candidate preflight、cutover JSON、registry next_gate、readiness/deployment 章节已统一，静态一致性回归恢复通过 | 仅完成候选身份与短窗口只读验收；没有角色化浏览器 UAT、业务写后 receipt/refresh、14 日 telemetry、registry backup/restore、rollback、capacity/chaos 或 owner/reviewer 双签，M5/TUI、TAR、AUD、EVID 生产门禁继续 fail-closed |
| 2026-08-20 | 第二期 P0 | TAR-01/M5 当前候选持续只读观测 | 同一 `f3881a04cf0b5d5bff5d2b7e5a6bf25d523667e2` / release `20260820043710` 于 2026-08-19 22:07 UTC 公网复核：health `5/5=200`、ready `3/3=200`、API root `3/3=200`；未认证 `/api/terminal/runs/` `403`，决策接口 `503 decision_runtime_blocked` 且 `must_not_use_for_decision=true` | 仅刷新短窗口基础健康、认证边界与 fail-closed 证据；未取得认证后 reserved-route `503`、真实 1/5/10/20 容量/queue/worker/SSE/chaos、业务写回执、角色 UAT、restore/rollback 或双签，TAR-01/TAR-02/TAR-05 与 M5 继续 fail-closed |

| 2026-08-20 | 第二阶段 P0/P1 | `39992992c` policy-PENDING fix VPS 部署与认证只读验收 | `dev/next-development@39992992cadc1c261f5dd8ffb06b64708a19397f` 部署为 release `20260820075124`，内置 verifier exit 0；Caddy/TLS、health/ready、Django/migrations、TUI registry、Qlib、备份、Celery worker/beat/ping 全部通过。登录态只读探针确认 `/api/tui/operator/home/` 与 `/api/policy/status/` 均 HTTP 200，PENDING 显示“待分类/需人工复核”安全语义；远端日志无 policy traceback 或 home 500 | 仅关闭此前已观测的部署缺陷；未做业务写入、角色矩阵浏览器 UAT、写后 receipt/refresh、14d telemetry、restore/rollback、capacity/chaos 或 owner/reviewer 签署，TAR/TUI/M5/AUD/EVID gates 继续 fail-closed |
| 2026-08-20 | 第一期/第二期 P0 | 当前候选 TAR-01/AUD-01 认证边界与 backlog 只读验收 | 同一 release 的认证 GET 探针确认 `/api/terminal/runs/` 为预期 `503 queued_runtime_not_wired`；`/api/audit/health/` 为 `OK`，失败数与 outbox pending/due/claimed/expired/failed/delivered 均为 0；`/api/audit/metrics/`、通用 metrics 均为 HTTP 200 | 仅证明 queued 与 publisher 仍安全 fail-closed、当前 backlog 为空；不证明 durable publisher/delivery、authenticated authority、queued admission/worker/SSE/idempotency/cancel、PG race 或生产签字，TAR-01/AUD-01/AUD-03 继续 active/fail-closed |
| 2026-08-20 | 第一期/第二期 P0 | 当前候选 verifier 与认证只读复核 | release `20260820075124` / source `39992992c` 的独立 verifier 最终 exit `0`；Celery 瞬时 ping 超时经直接 20 秒 ping 与完整 verifier 重跑确认 `1 node online`。认证 GET 的 TUI/operator/policy/audit/metrics 均健康；决策接口保持 `503 decision_runtime_blocked`，queued runtime 保持 `503 queued_runtime_not_wired`，audit outbox 为空 | 仍不等同业务写入、角色化 UAT、receipt/refresh、真实 queued admission、容量/chaos、restore/rollback、14 日 telemetry 或 owner/reviewer 签署；TAR/TUI/M5/AUD/EVID 生产门禁继续 fail-closed |
| 2026-08-20 | 第二期 P1 | `05970a925` TUX-02 cleanup 候选 VPS 部署与只读验收 | release `20260820091752` / image `sha256:33007a77…`；内置与独立 expected-commit verifier 全通过，TUI preflight 34 JS、migrations/schema、Django/TLS、Qlib、备份、Celery worker/beat/ping 全绿。认证 GET 的 TUI/operator/policy/audit/metrics 200，决策与 queued runtime 继续按 `decision_runtime_blocked` / `queued_runtime_not_wired` fail-closed | 仅当前候选短窗口只读运行证据；不等同角色化 UAT、业务写后 receipt/refresh、外部 portability、14d telemetry、restore/rollback 或 owner/reviewer 双签，TUX-02/TUX-04/M5 继续 active/fail-closed |
| 2026-08-20 | 第二期 P0/P1 | `05970a925` VPS 角色化浏览器写入与读回验收 | 专用 operator/regular 账号 HTTPS Playwright 隔离通过：队列角色边界、策略 create/detail/update/readback、个人 AI provider create/detail/update/readback（敏感 API key 仅在补填参数对话框输入）、确认取消、参数化主任务读取与最小权限 direct-read 矩阵均通过；策略 id `2`、provider id `9` 受控清理后复核为零 | 这是当前活动 release 的真实短窗角色/写入/读回证据；不重绑 registry 中的 M5 `f3881a04...`/`20260820043710` 候选，不替代写后 receipt/refresh、14d telemetry、backup/restore、live rollback、capacity/chaos、外部 portability 或 owner/reviewer 双签，TUI-01/TUI-02/M5 继续 awaiting/fail-closed |
| 2026-08-20 | 第二期 P1 | `28e0c2608` 深链表单 viewport 修复、VPS 部署与角色化写入验收 | `28e0c2608eea1c0a4aed51c3a54eed80220db503` 以 code-only `-Upgrade` 发布 release `20260820114848`；部署/独立 verifier、HTTPS/TLS、迁移/schema、TUI registry、Qlib、备份、Celery 与四条 CI 全绿；修复深链 create/update 表单在滚动面板内不可达，最终 HTTPS Playwright `3 passed`（operator/regular 队列边界、strategy create/detail/update/readback、AI provider create/detail/update/readback），精确清理后 UAT 行为归零 | 仅证明当前 release 的短窗口 TUI 交互/角色/写入读回与部署身份；不重绑正式 M5 `f3881a04...`/`20260820043710`，不替代 receipt/refresh、14d telemetry、backup/restore、live rollback、capacity/chaos、外部 AgomTUI portability 或 owner/reviewer 双签，TUX-02/TUX-04/M5 继续 active/fail-closed |
| 2026-08-20 | 第二期 P0 | TAR-01 local CLI/MCP secret boundary | `OpenAIAgentsTerminalService` 的 MCP stdio 子进程改为显式环境 allowlist，强制 `AGOMTRADEPRO_MCP_ENFORCE_RBAC=true`，不继承 API token、密码、数据库 URL、云端密钥、凭据 URL 或 prompt；`local-cli-mcp-secret-boundary` 矩阵场景标记 `implemented`，canonical matrix digest `8866e24df834b25da8a553675011e431d309a99595b0ea64e0e4dc91a4777888`；focused `29 passed`，Ruff/Black/isort、增量 mypy、governance consistency、architecture/audit 全通过 | 仅完成本地进程边界与 fail-closed 合同；未接 queued route/Worker、生产 CLI、外部 MCP portability、容量/chaos、真实生产 UAT 或 TAR-01/TAR-04/TAR-05 解锁，保持 inline 单槽与 queued 关闭 |
| 2026-08-20 | 第二期 P0/P1 | TAR-01 local CLI/MCP secret-boundary VPS 部署与只读观测 | `93b12f3b8c6cc2ce59c7493ae573afa7ace796eb` 发布为 release `20260820130631`、image `sha256:03604c69…`；source/image runtime match，部署 verifier、迁移/schema、Django、TUI registry、Qlib、备份、资源、Caddy/TLS、Celery worker/beat/ping 全通过；公网 `/api/health/` `8/8=200`、`/api/ready/` `3/3=200`、`/api/` `3/3=200`、未认证 terminal route `3/3=403`，决策接口 `3/3=503` fail-closed | 仅短窗口部署身份与只读边界观测；未证明 queued/worker/本地 CLI UAT、外部 MCP portability、业务写后 receipt/refresh、14d telemetry、restore/rollback、capacity/chaos 或 owner/reviewer，TAR-01/TAR-04/TAR-05 及相关生产 gates 继续 fail-closed |
| 2026-08-20 | 第一期/第二期 P0 | AUD-01 当前候选审计健康与认证边界复核 | 同一 release `20260820130631` 的公网只读探针：`/api/audit/health/` `200`（`overall_status=OK`、failure counter `0`）、`/api/audit/metrics/` `200`、health/ready/API root 均 `200`；未认证 terminal route `403`；决策接口 `503` 且 `must_not_use_for_decision=true` | 仅证明审计健康投影、认证边界与决策 fail-closed；不证明 durable publisher/receipt、authenticated scoped authority、真实 outbox claim/delivery、Data Center 同 UOW 双写、PG 并发/回滚或 owner/reviewer 签字，AUD-01 继续 active，AUD-02/03 继续等待 |
| 2026-08-20 | 第二期 P1 | `7cf7e9843` TUX-02 cleanup candidate VPS deployment and read-only acceptance | 四条 push CI 全部成功后以 code-only `-Upgrade` 发布 release `20260820145119`、image `sha256:6af515cee168cb4a406c158078f73eeab7e7931f331fbbff98b892f9ff701dca`；source/image identity、verifier、迁移/schema、Django/TUI registry、Qlib、备份、Caddy/TLS、Celery worker/beat/ping 通过；独立 HTTPS `/api/health/`、`/api/ready/`、`/api/`、`/api/audit/health/`、`/api/audit/metrics/` 均 200，未认证 terminal/TUI 为预期 403，决策接口保持 503 `decision_runtime_blocked` | 仅记录当前不可变候选的短窗口只读部署与认证边界；未取得角色化浏览器 UAT、写后 receipt/refresh、14d telemetry、restore/rollback、capacity/chaos、外部 portability 或 owner/reviewer 双签，TUX-02/TUX-04 与相关生产门禁继续 active/fail-closed |
| 2026-08-20 | 第一期 P0 | EVID-01 formal candidate acceptance recheck and binding mismatch | 直接以 formal `f3881a04cf0b5d5bff5d2b7e5a6bf25d523667e2` / `20260820043710` 运行 expected-commit verifier，远端明确实际为 `7cf7e984373af71b6f96b234cefb78b5f319d770` / `20260820145119`；对实际版本重新 verifier exit `0`，认证只读 TUI/policy/audit/metrics 200，queued terminal 503、decision 503 fail-closed；同一 PostgreSQL 只读盘点确认 0050–0053 已应用、12 张 authority/evidence/root-lock 表全 0 | 未拼接不同候选证据，未生成伪造 f388 runtime/inventory 工件；EVID candidate guard 仍诚实失败（缺同候选 runtime/inventory），需 release owner 明确重绑并重新采集同一 commit/release/image/matrix，EVID-01/M5/AUD-01/写入与 execution 继续 fail-closed |
| 2026-08-20 | 第二期 P0 | DATA-01 backup evidence collector contract | `scripts/backup-vps-postgres.py --evidence-output` 现在在完整下载与远端/本地 SHA/尺寸复核后生成 `data-backup-evidence.v1`，封存归档年龄、`pg_restore --list` manifest SHA/条数、partial 拒绝、规范化 content hash 和原子幂等写入；focused backup tests `10 passed`，Ruff/Black/isort/增量 mypy/diff-check 通过；远端 manifest 只读校验 exit `0` | 仅完成本地证据格式与 fail-closed 校验合同；当前 VPS SFTP 首个 `1 MiB` 约 `59.64s`，本轮未完成完整下载且未生成 production evidence；未创建/清理/恢复 VPS 备份，未执行生产 restore/RTO/RPO、维护态 rollback、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-20 | 第二期 P0 | DATA-01 VPS latest PostgreSQL archive evidence | 复核并完整下载 VPS 已有 custom-format 归档 `/opt/agomtradepro/backups/database/postgres-20260820-110946.dump`；远端 `pg_restore --list` 通过，远端/本地大小均 `142313231` bytes，SHA-256 均为 `b3f5893f45b0f8aa316307e709450cace3b5c7798bdbe3976b1920f2670c6773`，manifest `7182` entries、manifest SHA `170ca2cd663bd2f1e0f035807c03282e3f4b6e55a2d92dce93978e93973bc394`；结构化证据见 [`data-backup-evidence-2026-08-20.json`](../deployment/data-backup-evidence-2026-08-20.json)，content hash `a7543e05229dc49cb24c44990f59d62200b8fbdd289f969852c1ef570996a518` | 仅新增一个可复核恢复点并证明完整下载/哈希/manifest 校验；未执行生产 restore/rebuild、RTO/RPO、维护态 rollback、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-20 | 第二期 P0 | DATA-01 最新归档本机隔离 restore 验收 | 最新归档在唯一命名 disposable `postgres:16-alpine` 中完成 source↔restore 自洽演练；重跑 `outcome=success`，537/537 public 表、72/72 Data Center migrations、458/458 sequences、schema SHA `f984042e…c6049` 与逐表内容 hash 全匹配，restore `2061.820s`、verification `1017.837s`、total `4866.916s`；精简证据见 [`data01-local-isolated-restore-2026-08-20.json`](../deployment/data01-local-isolated-restore-2026-08-20.json) | 补齐最新恢复点的本机隔离 restore 自洽证据；第一次因 source 预置竞态拍到空快照的失败尝试已丢弃并如实记录。elapsed 不能冒充生产 RTO/RPO；未执行生产 restore/DDL、维护态 rollback、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-20 | 第二期 P0 | TAR-01 current VPS authenticated staircase acceptance | 当前实际 release `7cf7e984373af71b6f96b234cefb78b5f319d770` / `20260820145119` / image `sha256:6af515cee168cb4a406c158078f73eeab7e7931f331fbbff98b892f9ff701dca` 的认证后保留路由完成 `1/5/10/20` 阶梯，共 `36/36` 返回预期 `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired`，`Retry-After=60`；前后健康探针 `10/10=200`；原始 JSON 见 [`tar01-current-candidate-capacity-denied-2026-08-20.json`](../deployment/tar01-current-candidate-capacity-denied-2026-08-20.json) | 证明当前实际版本的认证边界与 queued 关闭时无写入/无接单 fail-closed；不构成 queued admission、Worker、SSE、idempotency/cancel、provider/MCP、chaos 或 capacity-ready 证据，不重绑正式 `f388…/20260820043710` 候选，TAR-01/TAR-02/TAR-05 继续 active/waiting/fail-closed |
| 2026-08-20 | 第二期 P1 | TUX-01 registry next-gate 文案对齐 | TUX-01 非法 published payload fallback、结构化 degraded health 与 dry-run revalidation 已完成，active registry 的 `next_gate` 已移除过期的“待实现 fallback”前置描述，保留 TUX-02/TUX-03 的三真源与 action-density 收口；registry 与 governance consistency 均 `0 violations` | 仅治理索引对齐，不改变 TUX-01/TUX-02 状态，不替代生产 registry repair、外部 AgomTUI portability 或 M5 角色化 UAT |
| 2026-08-20 | 第二期 P0 | TAR-02 bounded admission decision contract | 新增纯 Application `TerminalAdmissionSnapshot`/`TerminalAdmissionDecision`/`evaluate_terminal_admission`；显式校验 actor/counters/worker readiness，严格按 per-user/global cap、queued flags、emergency stop fail-closed，restricted-inline 不冒充 queued；focused `24 passed`，Ruff/Black/isort/增量 mypy/diff-check 通过 | 仅是 admission decision contract，不接 PostgreSQL/锁/route/`on_commit`/Celery/broker/worker/Agent，不创建 run 或真实准入；TAR-01 仍 active、TAR-02 仍 waiting_dependency，需后续并发安全 durable composition 才能推进 |
| 2026-08-20 | 第二期 P1 | TUX-02 execution.accounts dead screen patch cleanup | 删除已由 IA/full-IA runtime 完整承载且明确被忽略的 `execution.accounts` 旧 patch；保留 action dedupe map；新增 canonical semantic/panel/action 与 `execution.account-settings` alias 回归、synthetic legacy payload compatibility；focused source/actionability/IA `46 passed`，source guard `outcome=ok`（12/24、430/889、0 violations，patch 5→4），Ruff/Black/isort/增量 mypy/diff-check 通过 | 仅收口一项仓库内 dead patch，不宣称外部 AgomTUI portability、角色化生产 UAT、写后 receipt/refresh、14d telemetry、restore/rollback 或 M5/owner-reviewer sign-off；TUX-02/TUX-04 继续 active/fail-closed |
| 2026-08-20 | 第二期 P0 | TAR-01 current HEAD VPS deployment and authenticated staircase acceptance | `ecd4e084c3925e1b12228b36c5a504e5fdd895d3` 以 code-only `-Upgrade` 发布为 release `20260820195102`、image `sha256:8c8a078e5bfa5b0737ca82816e66b671ddde362f438be7f8ea965bf052704ff9`；部署 verifier、备份、迁移/schema、Django/TUI/Qlib/Celery、容器与 Caddy/TLS 全通过；认证后 reserved route `1/5/10/20` 共 `36/36` 为预期 `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired / Retry-After=60`，前后 health `10/10=200`，无 run/queue/provider/MCP 副作用；结构化证据见 [`tar01-current-candidate-capacity-denied-2026-08-20-ecd4e084.json`](../deployment/tar01-current-candidate-capacity-denied-2026-08-20-ecd4e084.json) | 仅证明当前 HEAD 的部署身份、健康与认证 fail-closed 边界；不构成 queued admission、Worker、SSE、idempotency/cancel、provider/MCP、chaos 或 capacity-ready 证据，不重绑正式 M5 候选，TAR-01 仍 active、TAR-02/TAR-03 仍 waiting |
| 2026-08-20 | 第二期 P1 | TUX-02 command-center.overview dead screen patch cleanup | 删除已由 IA/published graph 完整承载且在 full-IA runtime 被忽略的 `command-center.overview` 旧 patch；canonical 首页文案、default action、dashboard panels 与 operator action injection 保持不变；新增 IA/runtime、alias、synthetic legacy-payload 回归，source/actionability/IA `48 passed`、完整 Workbench `255 passed`，source guard `outcome=ok`（12/24、430/889、0 violations，patch 4→3），runtime manifest 已刷新 | 仅收口一项已证实 canonical dead patch；保留 3 个仍被 operator deep-link 使用的 non-IA patch，未宣称外部 AgomTUI portability、角色化生产 UAT、写后 receipt/refresh、14d telemetry、restore/rollback 或 M5/owner-reviewer sign-off，TUX-02/TUX-04 继续 active/fail-closed |
| 2026-08-20 | 第二期 P1 | TUX-02 command-center cleanup VPS acceptance | 同一 `2f4554b5192191970a3ccbc98420388881725079` 以 code-only `-Upgrade` 发布 release `20260820211526`；第一次 Docker unpack 在切换前失败，重试成功；source/image、verifier、迁移/schema、TUI registry、Qlib、TLS、web/Celery 全部通过；独立 HTTPS health/ready/API/audit 只读探针通过，未认证 TUI/terminal 为 403，decision 为 503 fail-closed；证据见 `docs/deployment/tui-command-center-cleanup-2026-08-20-2f4554b5.json` | 仅是当前不可变候选的短窗口部署与认证/决策 fail-closed 观测；不替代角色化浏览器 UAT、写后 receipt/refresh、14d telemetry、restore/rollback、capacity/chaos、外部 portability 或 owner/reviewer 双签，TUX-02/TUX-04/M5/TAR/AUD/EVID 继续 active/fail-closed |
| 2026-08-21 | 第二期 P0 | TAR-01 production reserved-route acceptance refresh | 对已部署 `2f4554b5192191970a3ccbc98420388881725079` / release `20260820211526` 重新做认证后 `1/5/10/20` 阶梯：`36/36` 返回 `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired / Retry-After=60`，前后 health `10/10=200`，ready/audit health/metrics 均 `200`；audit health `overall_status=OK`、失败数与 outbox backlog 均 `0`；结构化证据见 [`tar01-current-production-acceptance-2026-08-21.json`](../deployment/tar01-current-production-acceptance-2026-08-21.json) | 仅刷新生产认证与 queued fail-closed 证据；部署候选不是当前分支后续文档提交，且未取得真实 queued admission/Worker/SSE/idempotency/cancel/provider-MCP/chaos/capacity、14d telemetry、restore/rollback 或 owner/reviewer 签署，TAR-01 仍 active、TAR-02/TAR-03 继续 waiting |
| 2026-08-21 | 第二期 P0 | M5 execution-dependency gate | readiness、cleanup guard 与 review snapshot 现在读取 `governance/active_plan_registry.json`，要求 `TUI-01` 及其全部 `depends_on` 单元完成后才允许 M5 进入关闭评估；fixture 回归 `39 passed`，治理/mypy/格式检查与 CI 全绿（commit `d9b987085`）；当前 readiness 明确 `DENY`：`TUI-01=waiting_dependency; TAR-03=waiting_dependency` | 仅补齐计划索引与机器门禁一致性，不改变生产状态或放宽 gate；下一主线仍为 `TAR-01 → TAR-02 → TAR-03`，未取得 queued admission/Worker/SSE、容量/chaos、14 日 telemetry、restore/rollback 或 owner/reviewer 签署 |
| 2026-08-21 | 第二期 P0 | TAR-01 reserved-route evidence contract validation | 新增离线 `terminal_runtime_reserved_route_evidence` 校验器与 CLI；对当前 HEAD 证据严格绑定 commit/release/image，复算 `1/5/10/20` 全部 `503 queued_runtime_not_wired`、health/audit 前后稳定、无副作用与 `capacity_ready=false`；focused `14 passed`，Ruff/Black/isort、增量 mypy 通过，CLI 输出 `runtime_enablement=not_authorized`；baseline recorder 现可直接离线运行 | 只提高现有认证 fail-closed 证据的可重复性；不把 reserved-route 503 冒充容量基线，不实现 queued admission、PostgreSQL/on_commit、Worker/SSE、idempotency/cancel、provider/MCP、chaos、14 日 telemetry、restore/rollback 或 owner/reviewer 验收，TAR-01 仍 active、TAR-02/TAR-03 仍 waiting |
| 2026-08-21 | 第二期 P0 | TAR-01 current HEAD VPS deployment and authenticated acceptance | CI 四门禁全绿后将当前 `4c49dd8a247bf83984346984c1663842e670a2fe` code-only 部署为 release `20260821024537`、image `sha256:e286ac83cf170f08325b48f05ee492aa530c6745ee24b067b767a60e828d93ed`；部署 verifier、备份、迁移/schema、Django/TUI/Qlib/Celery、容器与 TLS 全通过；认证后 reserved route `1/5/10/20` 共 `36/36` 返回 `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired / Retry-After=60`，前后 health/readiness/audit 稳定，审计日志 `541`、失败与 backlog `0`；证据见 [`tar01-current-production-acceptance-2026-08-21-head-4c49dd8a.json`](../deployment/tar01-current-production-acceptance-2026-08-21-head-4c49dd8a.json)，离线 validator 通过 | 仅证明当前 HEAD 的部署身份、健康与认证 fail-closed 边界；ready 的 decision-data freshness warning 已如实记录；不构成 queued admission、Worker、SSE、idempotency/cancel、provider/MCP、chaos、capacity-ready、14d telemetry、restore/rollback 或 owner/reviewer 证据，TAR-01 仍 active、TAR-02/TAR-03 继续 waiting |
| 2026-08-21 | 第二期 P0 | TAR-01 full local regression and gate recheck | `tests/unit/agent_runtime` 全量 `185 passed`；新增事件回放游标/归属/顺序/敏感字段纯合同；reserved-route evidence validator 继续给出 `capacity_ready=false`、`runtime_enablement=not_authorized`、health stable、无 side effects；active-plan registry、governance consistency、mypy debt ceiling 均通过（0 violations/errors） | 仅补强本地可复核回归与 fail-closed 证据；本切片未重新使用 VPS 凭据、未改变 runtime flags，也不把 reserved-route `503` 冒充容量基线；先前 release `20260821024537` / source `4c49dd8a` 的部署证据仍为 VPS 权威观察，TAR-01 继续 active、TAR-02/TAR-03 继续 waiting |
| 2026-08-21 | TAR-01 total exit-gate preflight | `scripts/check_tar01_exit_gate.py` + 3 tests; safety preflight `decision=BLOCKED`, `safety_ready=true`, `capacity_ready=false`; TAR-02/TAR-03 remain waiting and queued/Worker flags remain closed | Read-only guard only; real candidate-bound capacity, Worker/SSE/PostgreSQL/chaos, telemetry, restore/rollback and owner/reviewer evidence remain required |
| 2026-08-21 | 第一期 P0 | EVID-02 disposable PostgreSQL concurrency harness acceptance | 隔离 `postgres:16-alpine` 空库运行固定双连接套件 `3 passed`（empty-root `(1,1,1)`、successor `(1,1,2)`、rollback `(0,0,0)`）；unit/head-audit `29 passed`；canonical artifact `a27e193f53910cdb4395cc88d4d96fb04fcda71f2f191dbda7df2626299e6df8` | 仅是本地 disposable PostgreSQL 软件层证据，`production_claim=false`/`production_ready=false`，head audit 与 human approval 未采集，测试库已删除；EVID-02 仍 `awaiting_production`，真实 VPS race/rollback、approval 与 owner/reviewer 继续缺失 |
| 2026-08-21 | 第二期 P0 | DATA-01 最新已有 VPS PostgreSQL 归档只读读取证 | 只读下载最新归档 `postgres-20260820-205307.dump`；远端 `pg_restore --list` `7182` entries，完整 SFTP 下载 `142314979` bytes，远端/本地 SHA-256 `296311187d72cf6d327be61de55db3205fe139accdef05035c0ec2ed1b9980ac`，结构化 envelope content hash `3a59ff4608736a7445b9961020e78b0625c0924e7f2032d2c50a68200ac32e3a` | 只刷新可复核恢复点与传输完整性；未创建/清理备份、生产 restore/RTO/RPO、维护态 rollback、回填或 reconciliation，`DATA-01` 仍 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-21 | 第一期 P0 | EVID-02/STRAT-01 VPS canonical ledger inventory 只读验收 | 当前运行 `4c49dd8a247bf83984346984c1663842e670a2fe` / release `20260821024537`，health/ready `200`；EVID-02 approval/activation/supporting tables 全部 `0`，head audit 为 empty/no-head、human approval `not_collected`；STRAT R1–R8 `research_r1_`…`research_r8_` `65` 张、Portfolio R4/R5/R8 `7` 张、Equity baseline `4` 张及所需 Data Center actual/PIT/macro-factor/market-structure 表全 `0`，只读工件见 [`evid-02-strat-01-vps-readonly-inventory-2026-08-21.json`](../deployment/evid-02-strat-01-vps-readonly-inventory-2026-08-21.json)（SHA `09d628c3070068e621bc3550bd0d20a70669274c29977c3ddfbc5006fafbf0e5`） | 只证明当前候选健康与 canonical owner/evidence 账本为空；没有创建/更新/删除/回填/人工审批/rollback，不把 Data Center facts 现场提升为 owner evidence；`EVID-02` 仍 `awaiting_production`，`STRAT-01` 仍 `awaiting_production`，`STRAT-02/03`、Evidence hard gate 与 execution 继续 fail-closed |
| 2026-08-21 | 第二期 P0 | TAR-01 `d10c8d9f6` VPS 部署与认证写回读验收 | 当前 `dev/next-development@d10c8d9f65f2bd84a77b94532b91445c4216f9db` 以 code-only `-Upgrade` 发布为 release `20260821051240`；部署 verifier、备份、迁移/schema、Django/TUI/Qlib/Celery、容器与 Caddy/TLS 全通过；公开 health/ready/API/audit/metrics 可读；认证 TUI root `200`，reserved POST route `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired / Retry-After=60`；3 项 HTTPS Playwright UAT（operator/regular 权限、strategy 写回读、user-owned provider 写回读）通过，精确测试行已清理；结构化证据见 [`tar01-current-production-acceptance-2026-08-21-head-d10c8d9f.json`](../deployment/tar01-current-production-acceptance-2026-08-21-head-d10c8d9f.json)（SHA `bfd48f057e13aca3c5e86b99f7fb0f796353575b53867511c41be6838354627d`） | 证明当前候选部署身份、角色边界、业务写后读回与 queued fail-closed；ready 的 decision-data freshness 仍 `warning/blocked` 且 stale quotes `must_not_use_for_decision`；不构成 queued admission/Worker/SSE/idempotency/cancel/provider-MCP、1/5/10/20 capacity/chaos、14d telemetry、restore/rollback 或 owner/reviewer 签署，`check_tar01_exit_gate.py` 仍 `BLOCKED/safety_ready=true/capacity_ready=false`，TAR-01 active、TAR-02/TAR-03 waiting |
| 2026-08-21 | 第二期 P0 | TAR-01 当前 HEAD `a428edaad` VPS 部署与认证阶梯/写回读验收 | 当前 `dev/next-development@a428edaad5cf70e0c47a5649c5f867ae6aeabdd5` code-only 部署为 release `20260821060037`、image `sha256:0b83684e05c77a0371e223f2b3250246307f17f3da7cc626608c29839cf01d7f`；verifier、备份、迁移/schema、Django/TUI/Qlib/Celery、容器/TLS 全通过；认证后 reserved route `1/5/10/20` 共 `36/36` 为预期 `503 DISPATCH_UNAVAILABLE / queued_runtime_not_wired / Retry-After=60`，前后 health `5/5=200`，ready/audit 稳定且 audit `541` logs、`0` failures/backlog；3 项 HTTPS Playwright（operator/regular、strategy 写回读、user-owned provider 写回读）重跑通过，测试行清理为零；证据 [`tar01-current-production-acceptance-2026-08-21-head-a428edaad.json`](../deployment/tar01-current-production-acceptance-2026-08-21-head-a428edaad.json)，SHA `9360fa15e8c41348d436a50d4e475c869615e8f5873caf3385e09e965d2f2c16` | 证明最新 HEAD 部署身份、角色边界、业务写后读回与 queued fail-closed；初次策略 UAT 的一次确认弹窗超时已清理残留并在同候选重跑通过；decision-data freshness 仍 `warning/blocked` 且 stale quotes `must_not_use_for_decision`；不构成 queued admission/Worker/SSE/idempotency/cancel/provider-MCP、capacity/chaos、14d telemetry、restore/rollback 或 owner/reviewer 签署，`check_tar01_exit_gate.py` 仍 `BLOCKED/safety_ready=true/capacity_ready=false`，TAR-01 active、TAR-02/TAR-03 waiting |
| 2026-08-21 | 第一期 P0 | AUD-01 current HEAD audit read-only acceptance | 同一 `a428edaad` / release `20260821060037` 的 authenticated `/api/audit/health/` 与 `/api/audit/metrics/` 快照均 `200`、`overall_status=OK`；operation logs `541`、failures `0`、pending/due/claimed/expired/failed backlog 全 `0`，与候选/镜像绑定在 [`tar01-current-production-acceptance-2026-08-21-head-a428edaad.json`](../deployment/tar01-current-production-acceptance-2026-08-21-head-a428edaad.json)（SHA `9360fa15e8c41348d436a50d4e475c869615e8f5873caf3385e09e965d2f2c16`） | 只证明审计健康投影和空 backlog；不证明 durable publisher/receipt、authenticated authority、真实 claim/delivery、Data Center 同 UOW、PG race/rollback 或 owner/reviewer 签字，`AUD-01` 继续 active，`AUD-02/03` 继续 waiting |
| 2026-08-21 | 第一期 P0 | EVID-02/STRAT-01 current HEAD canonical ledger inventory 重绑 | 在 `a428edaad` / release `20260821060037` / image `sha256:0b83684e…cf01d7f` 上重新执行 default alias 纯 `SELECT`：EVID-02 approval/activation/supporting tables 全 `0`、head `empty/no-head`、human approval `not_collected`；Research R1–R8 `65` 张、Portfolio R4/R5/R8 `7` 张、Equity baseline 与 Data Center actual/PIT/macro-factor/market-structure 依赖全 `0`；工件 [`evid-02-strat-01-vps-readonly-inventory-2026-08-21-head-a428edaad.json`](../deployment/evid-02-strat-01-vps-readonly-inventory-2026-08-21-head-a428edaad.json) SHA `e93fdcef3591aece3d9d9412a9e58b288e9514a759e3a2fe048d2d85bf56f95b` | 只更新当前候选绑定并确认真实 owner/evidence ledger 仍为空；不执行 seed/backfill/promotion/审批/rollback，`EVID-02`、`STRAT-01` 继续 `awaiting_production`，后续依赖与 execution gate 不变 |
| 2026-08-21 | 第二期 P0 | TAR-01/AUD-01 current VPS read-only probe refresh | 同一 `a428edaad` / release `20260821060037` 新鲜只读探针：HTTPS health/ready/db/audit/metrics 全 `200`，decision-ready `503` fail-closed（stale、`must_not_use_for_decision=true`），容器/Caddy/TLS、PostgreSQL/Redis、Celery worker/beat、`pyqlib==0.9.7` 与错误 `qlib` distribution 缺失复核通过；审计 `541` logs、`0` failures、全部 outbox gauges `0`；匿名 TUI `403`；工件 [`tar01-current-vps-readonly-probe-2026-08-21-head-a428edaad.json`](../deployment/tar01-current-vps-readonly-probe-2026-08-21-head-a428edaad.json) SHA `d3a2ac6dedb47b33fe1d76196075bbe904a06e5d972ed41e56825aec5ca87f7b` | 只刷新健康/认证/决策 fail-closed 证据；不构成 queued admission、Worker/SSE、capacity/chaos、14d telemetry、restore/rollback、durable publisher/authority 或 owner/reviewer 签署，`TAR-01` 仍 `BLOCKED`、`AUD-01` 仍 `active` |
| 2026-08-21 | 第二期 P0 | TAR-01 queued runtime VPS 容量/SSE/重启观测（`fec65c802`） | release `20260821181242` 的独立 verifier 全通过；受控 Worker 停止期间 `1/5/10/20` 得到 `202×1`、`202×3+429×2`、`429×10`、`429×20`，拒绝原因为 `per_user_queued_limit`；重复首请求精确重放，Worker 重启后队列归零；SSE `200 text/event-stream`、终态取消 `409 RUN_NOT_CANCELLABLE`；证据见 [`tar01-current-production-queued-runtime-acceptance-2026-08-21-fec65c80.json`](../deployment/tar01-current-production-queued-runtime-acceptance-2026-08-21-fec65c80.json) | 证明 bounded admission/idempotency/Worker restart/SSE negotiation，不证明成功 provider 执行、多用户 global capacity、持续 chaos/14d telemetry、restore/rollback、AUD/EVID/STRAT/DATA 或 owner/reviewer 签署；`/api/decision-ready/` 仍 `503` 且 `must_not_use_for_decision=true`，不改 fail-closed 状态 |
| 2026-08-21 | TAR-01 bounded observation governance rebinding | 机器合同改为 `decision_status=runtime_observed_not_exit_ready`，明确候选级 queued/worker 授权与 repository 默认分离；`check_tar01_exit_gate.py` 全部检查通过并报告 `BLOCKED/safety_ready=true/capacity_ready=false`，保留 provider、global capacity、chaos、telemetry、restore/rollback 与 owner/reviewer 缺口 | 仅修正已部署短窗观测与旧 dormant 合同的治理不一致；不改变运行时数据、不解除 TAR-01 退出门禁、不签署 AUD/EVID/STRAT/DATA |
| 2026-08-21 | TAR-01 event cursor/replay contract guard | 纯 Application 新增 owner-scoped `TerminalRunEventReplayQuery`、有界 `TerminalRunEventReplay` 与严格批次校验；拒绝负/布尔游标、超限批次、run 替换、重复/倒序 sequence 与敏感 payload，并保留 terminal event 回放；API contract `16 passed`，`tests/unit/agent_runtime` `185 passed`，Ruff/Black/isort/增量 mypy/diff-check 通过 | 仅完成本地事件回放契约，不改 route/ORM/Celery/SSE serializer/runtime flags，也未部署；`events-reconnect-and-owner-scope` 仍需真实 component/chaos/reconnect 观察，TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false` |
| 2026-08-21 | TAR-01 event replay component evidence + VPS worker identity guard | 新增真实 Django component 回放套件：ordered/bounded replay、Last-Event-ID、terminal event、owner-scope、SSE frame 与敏感字段拒绝，`4 passed in 196.52s`；部署脚本同步保证队列关闭时清理旧 `terminal_agent_worker`、开启时校验 worker 镜像与 release exact match；Black/isort/Ruff/diff-check 通过 | 仅完成本地 component evidence 与待部署的候选一致性 guard，不把 component/短窗观测冒充生产 chaos、容量或 14 日 telemetry；CI 与后续 VPS guard 验证完成前，`events-reconnect-and-owner-scope` 仍 `planned`、TAR-01 仍 `BLOCKED` |
| 2026-08-21 | TAR-01 worker identity guard VPS acceptance | CI 四门禁全绿（`f0f9b9c67`，inventory refresh `e24f743c6`）后，候选 `e24f743c6a2c887c6fcb7fcac4f8b22f453e59f6` 发布为 release `20260821213436`、image `sha256:650e905ebb43db55cca1048db9ffd594f61173c10e0f68bb42a718856314b73b`；部署 verifier、备份、迁移/schema、Django/TUI/Qlib/Celery、容器/TLS 通过；web/beat/Celery image exact，队列关闭时无 stale `terminal_agent_worker`；health `200`、audit health/metrics `200`、匿名 terminal/TUI `403` | 证明当前候选部署身份与 worker 不串镜像；`/api/decision-ready/`、`/api/regime/current/` 仍 `503` `must_not_use_for_decision=true`；无认证 actor 账号，未取得新一轮 production queued capacity/chaos、14d telemetry、restore/rollback 或 owner/reviewer 证据，TAR-01 继续 `BLOCKED` |
| 2026-08-21 | TAR-01 event scenario matrix reconciliation | `tests/component/agent_runtime/test_terminal_agent_run_events.py` 的 ordered/bounded replay、Last-Event-ID、terminal retention、owner-scope 与 SSE 安全回归已纳入 canonical matrix；matrix digest 更新为 `6272ea6606ebbf3c0791e48d807b733cbc6d9a4ce7d945d95c5e3a16c22aea64`，矩阵/manifest/gate/controlled-observer + component focused `37 passed`；`check_tar01_exit_gate.py` 仍为 `BLOCKED/safety_ready=true/capacity_ready=false` | 仅修正事件组件证据与机器矩阵状态的一致性；`load-1-5-10-20`、`chaos-worker-stream-recovery`、生产容量/故障注入、14d telemetry、restore/rollback 与 owner/reviewer 仍未完成，TAR-01/TAR-02/TAR-03 保持 fail-closed |
| 2026-08-21 | TAR-01 dormant load/chaos contract paths | 新增 `tests/load/agent_runtime/test_terminal_agent_capacity.py` 与 `tests/chaos/agent_runtime/test_terminal_agent_recovery.py`，覆盖 1/5/10/20 observer 绑定、不可用指标 fail-closed、orphan 恢复边界、终态不可覆写和 ID-only broker；与 matrix 回归 `7 passed`，Ruff/Black/isort/diff-check 通过 | 仅是 dormant deterministic contract tests，不是 `implemented` runtime scenario；未执行 HTTP/Redis/broker/Worker fault、生产容量、chaos、SLO、14d telemetry、restore/rollback 或 owner/reviewer 验收，`load-1-5-10-20`/`chaos-worker-stream-recovery` 及 TAR-01 退出门仍保持 planned/BLOCKED |
| 2026-08-21 | TAR-01 `ec864da4b6` VPS deployment/public observation | CI-green candidate `ec864da4b6f1f96da65ea5e6aa61ccc76c811ddc` deployed as release `20260821230203`, image `sha256:ed710b134de44de5aba074f48abc5c0013f2481a578e787643b4d49f98d72600`; runtime match, migrations/schema, Django checks, web/beat/Celery image binding passed; public health/ready/audit/metrics `200`, anonymous terminal/TUI `403`; decision-ready/regime `503` with `must_not_use_for_decision=true`; evidence [`tar01-current-vps-observation-2026-08-21-ec864da4.json`](../deployment/tar01-current-vps-observation-2026-08-21-ec864da4.json) | 只读公网观测，未写生产数据、未覆盖 decision fail-closed；TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false`，生产 capacity/chaos、14d telemetry、restore/rollback、provider/MCP 成功与 owner/reviewer 签署仍未证明 |
| 2026-08-21 | TAR-01 `ec864da4b6` authorized production canary capacity/recovery | 在已校验 release 上临时开启 `TERMINAL_RUNTIME_AUTHORIZED` + queued intake/worker；停 worker 期间真实认证 `1/5/10/20` 得到 `202×1`、`202×1+429×4`、`429×10`、`202×2+429×18`，共 `4` accepted/`32` `per_user_queued_limit`，同 key replay `202` 且无第二 durable row；worker start+restart 后队列归零，SSE 协商 `200 text/event-stream`，canary 最终为 `terminal_agent_execution_failed`；证据 [`tar01-current-production-capacity-2026-08-21-ec864da4.json`](../deployment/tar01-current-production-capacity-2026-08-21-ec864da4.json) | 首次真实 candidate-bound capacity/write/recovery observation；不宣称 provider 成功、multi-user/global capacity、持续 chaos/reconnect、14d telemetry、restore/rollback 或 owner/reviewer exit，决策状态未改写，`check_tar01_exit_gate.py` 仍 `BLOCKED/safety_ready=true/capacity_ready=false` |
| 2026-08-21 | TAR-01 `ec864da4b6` valid queued canary provider diagnosis | 完整 `terminal_agent` 载荷真实认证创建 task=`201`、run=`202`，SSE=`200 text/event-stream`；Agents SDK/MCP 初始化成功，唯一启用的 DeepSeek `deepseek-chat` 返回上游 `402 Insufficient Balance`，应用按既有合同写入脱敏 `terminal_agent_execution_failed`，无 fallback/密钥落盘；证据 [`tar01-current-production-provider-block-2026-08-21-ec864da4.json`](../deployment/tar01-current-production-provider-block-2026-08-21-ec864da4.json) | 这是 provider 配置/余额阻断，不是 VPS 网络故障；不宣称 provider 成功、capacity/chaos/14d telemetry、restore/rollback 或 owner/reviewer 签署，决策接口保持 `503 must_not_use_for_decision=true`，TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false` |
| 2026-08-22 | TAR-01 worker-side emergency-stop claim guard | 修正 queued Celery task 在已派发但紧急停止切换后的窗口：`TERMINAL_EMERGENCY_STOP=true` 时在 repository claim 前稳定返回 `submissions_paused`，不抢新 run；新增回归并通过 Agent Runtime unit `186 passed`、增量 mypy `0 regressions`、Ruff/Black/isort/diff-check | 只加强 fail-closed 安全边界，不改变默认开关、已运行任务或生产候选；TAR-01 capacity/provider/chaos/telemetry/restore/rollback 与人工签署门禁保持不变 |
| 2026-08-22 | TAR-01 current HEAD VPS deployment and post-deploy observation | CI 四门禁在 `71e62773e` 全绿；code-only/fresh 发布为 release `20260822012308`、image `sha256:8f3715e90bc8967e7c5a09ec9ade690787161a9fa6d8709eb28e7fd32c790916`；备份、迁移/schema、Django check、Caddy/TLS、容器、Celery、TUI registry、Qlib 全通过；公网 health/ready=`200`，ready critical data=`ok`、decision data=`warning`，decision-ready=`503 blocked` 且 `must_not_use_for_decision=true`；证据 [`tar01-current-vps-observation-2026-08-22-71e62773.json`](../deployment/tar01-current-vps-observation-2026-08-22-71e62773.json) | 仅证明当前 HEAD 部署身份与短窗健康/决策 fail-closed；未提交 terminal-agent 业务 canary，不证明 provider/MCP 成功、capacity/chaos、14d telemetry、restore/rollback 或 owner/reviewer 签署；TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false` |
| 2026-08-22 | TAR-01 authorized current-candidate capacity and worker-restart observation | 用户明确授权后，在 `71e62773e` / release `20260822012308` 受控临时开启 queued runtime：Worker 停止期间 `1:202×1`、`5:202×3+429×2`、`10:429×10`、`20:429×20`，拒绝均为 `per_user_queued_limit`；4 条 durable rows 入队，幂等重放不增行，队列 `4→0`；另 2 条在 Worker 重启/短故障窗口中最终进入 `failed`，SSE=`200 text/event-stream`；证据 [`tar01-current-production-capacity-2026-08-22-71e62773.json`](../deployment/tar01-current-production-capacity-2026-08-22-71e62773.json) | 证明 candidate-bound queue admission、per-user backpressure、幂等、Worker restart 后恢复与 SSE 错误事件持久化；provider/MCP 执行仍为 `terminal_agent_execution_failed`，不证明多用户/global capacity、硬 SLO、持续 chaos/reconnect、14d telemetry、restore/rollback 或 owner/reviewer 签署；观测后 flags 全部恢复 `false`、专用 Worker 移除、临时 token 删除；decision-ready 仍 `503 must_not_use_for_decision=true`，TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false` |
| 2026-08-22 | TAR-01 candidate contract rebinding and event-evidence index | 机器 TAR-01 合同已从旧 `ec864da4b6` 重绑到最新观测候选 `71e62773e` / release `20260822012308` / image `sha256:8f3715e90bc8967e7c5a09ec9ade690787161a9fa6d8709eb28e7fd32c790916`；`current_evidence` 直接列出事件组件测试与当前容量工件，manifest focused 回归 `6 passed` | 只修正候选身份与证据索引的一致性；事件组件已实现，但 sustained chaos、完整多用户/global hard-SLO、provider 成功、restore/rollback、telemetry 与人工签署仍缺失，TAR-01 继续 `BLOCKED/safety_ready=true/capacity_ready=false`，不推进 TAR-02/TAR-03 |
| 2026-08-22 | TAR-01 candidate capacity-evidence validator | 新增纯 Application validator、离线 CLI 与 gate integrity check；当前容量工件的候选身份、`1/5/10/20` 计数、队列归零、幂等、Worker restart、SSE、cleanup、provider/gate 字段均严格校验，focused `22 passed`，mypy regressions/debt ceiling 均为 `0` | 只提高 candidate-bound 证据可复核性；任何篡改 fail-closed，TAR-01 gate 仍 `BLOCKED/safety_ready=true/capacity_ready=false`，不把单用户观测升级成容量就绪，也不启用 TAR-02/TAR-03 |
| 2026-08-22 | DATA-01 VPS 新建 PostgreSQL custom-format 备份 | 按授权运行 `scripts/backup-vps-postgres.py` 创建并下载 `/opt/agomtradepro/backups/database/postgres-20260821T195155Z.dump`；远端 `pg_restore --list` 通过，远端/本地大小均 `142507268` bytes，远端/本地 SHA-256 均为 `8aadc5e9aff1f50c142b03dd219153c01b25d5110533195ec3670de36e44157e`，manifest `7204` entries、manifest SHA `e761f7d394605238509885d28e8536c6115d1528c5b8a1c261dd929d753886b4`；本地归档为 `backups/vps-postgres/postgres-20260821T195155Z.dump`，结构化 envelope [`data-backup-evidence-2026-08-22.json`](../deployment/data-backup-evidence-2026-08-22.json)，content hash `4942f47d84dd52a18b5b7f7aabf1ab589f52150f51f1bfe0ac6ee0b689c05d5a`，prune 未启用 | 只新增一个已下载、哈希与格式复核的生产恢复点；未执行生产 restore/rebuild、RTO/RPO、维护态 rollback、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-22 | DATA-01 最新归档本机隔离 restore 验收 | 新归档在唯一命名 disposable `postgres:16-alpine` source↔restore 自洽通过：539/539 public 表、72/72 Data Center migrations、460/460 sequences、schema/逐表内容/sequence 差异均为 `0`；`pg_restore` `1144.211s`、verification `945.14s`、total `3358.717s`；证据 [`data01-local-isolated-restore-2026-08-22.json`](../deployment/data01-local-isolated-restore-2026-08-22.json)，dump SHA `8aadc5e9aff1f50c142b03dd219153c01b25d5110533195ec3670de36e44157e`，报告 SHA `33b7217d839b3036934ef0d3d2fbb45b61fd412af44ec354680e9a260b9c50a0` | 只证明本机隔离恢复自洽；elapsed 不能冒充生产 RTO/RPO，未执行生产 restore/DDL、维护态 rollback、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-22 | 第二期 P0 | TAR-01 worker lease/authority hardening | delivery-scoped worker identity；started/heartbeat/event/finish 丢 lease 全部 fail-closed；worker 从 User/Profile 重建 role/admin/MCP 并拒绝伪造 payload；Agent Runtime unit `217 passed`，component 覆盖 stale-event/authority/missing-profile | 仅本地安全 hardening，不关闭 production capacity/provider/chaos/14d telemetry/restore-rollback/human sign-off；TAR-01 继续 `BLOCKED/safety_ready=true/capacity_ready=false` |
| 2026-08-22 | 第二期 P0 | TAR-01 dormant chaos evidence contract | 新增纯 Application candidate-bound chaos evidence 与 injected observer；UTC timeline、worker/run/stream/recovery、reconnect/terminal/duplicate/cross-user counters、observed/unavailable/failed 状态均严格校验；focused `14 passed` | 不执行 HTTP/Redis/Celery/Docker I/O，`load-1-5-10-20` 与 `chaos-worker-stream-recovery` 仍 `planned`，不冒充生产 chaos/capacity/14d/restore/rollback 证据 |
| 2026-08-22 | 第二期 P0 | TAR-01 VPS Celery/QLib memory containment | 远端观测到 Celery cgroup 在 `1500m` 限制下反复 SIGKILL/OOM；VPS compose 默认 worker limit 调整为 `2g`，并加入 `CELERY_WORKER_MAX_TASKS_PER_CHILD=1` 防止子进程累积；待重建后观察 | 只解决候选运行稳定性配置，不改 decision-ready fail-closed、不启用 queued runtime；需重部署、health/ready、容器内存与 QLib/worker 日志观测后再形成 production evidence |
| 2026-08-22 | 第二期 P0 | TAR-01 VPS worker memory containment deployment and observation | CI 四门禁在 `5108689606fd` 全绿；code-only `-Upgrade` 发布为 release `20260822065344`、image `sha256:f7124069a72a6557a30245232338bf1e276ac9b400705d74a733ad90a5559898`；备份、迁移/schema、Django/TUI/Qlib/Celery、容器/TLS 全通过；Celery worker 实际 `2GiB`、`max-tasks-per-child=1`，首个 15 分钟窗口 `1.076GiB/2GiB`、无 `WorkerLostError`/SIGKILL/OOM/Traceback；公网 health/ready `200` | 仅证明新候选部署身份、短窗口 Worker 内存稳定性与决策 fail-closed；`/api/decision-ready/` 仍 `503` 且 `must_not_use_for_decision=true`；不构成多用户/global capacity、持续 chaos/reconnect、provider/MCP 成功、14d telemetry、restore/rollback 或 owner/reviewer 签署；TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false`；证据见 [`tar01-current-vps-observation-2026-08-22-510868960.json`](../deployment/tar01-current-vps-observation-2026-08-22-510868960.json) |
| 2026-08-22 | 第二期 P0 | TAR-01 VPS worker memory remediation and re-observation | 发现旧 `2GiB` release 实际触发 `30` 次历史 cgroup OOM（含 WorkerLost/SIGKILL）；commit `274600e3c9a9` 仅将通用 QLib Worker 上限提高到 `4GiB`，保留并发 `1`、`max-tasks-per-child=1`，终端 Worker 仍为 `2GiB`；code-only upgrade 发布 release `20260822073022`、image `sha256:995f1e2b…`，备份/迁移/schema/Django/TUI/Qlib/TLS/health 全通过；15m15s 后 Worker `1.868GiB/4GiB`、restart=0、OOMKilled=false，新增日志错误计数全为 0，公网 health/ready `200` | 这是对前一候选错误稳定性结论的纠正与单 Worker 短窗复验；decision-ready 仍 `503` + `must_not_use_for_decision=true`，不构成 global capacity、持续 chaos、provider/MCP、14d telemetry、restore/rollback 或人工签署证据；TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false`；证据见 [`tar01-worker-memory-remediation-2026-08-22-274600e3c.json`](../deployment/tar01-worker-memory-remediation-2026-08-22-274600e3c.json) |
| 2026-08-22 | 第二期 P0 | TAR-01 QLib prediction batching and VPS re-observation | 4GiB 候选后续 OOM 使其首个 15m15s 样本失效；`c7ea5a9fc914` 新增 `QLIB_PREDICTION_BATCH_SIZE=500`，每批显式释放 DatasetH/handler/prediction，并将变量明确注入 `celery_worker`；CI 四门禁全绿，code-only upgrade 发布 release `20260822091112`、image `sha256:b6b5db33…`，备份/迁移/schema/Django/TUI/Qlib/TLS/health 全通过；真实 QLib 任务完成 `12/12` 分片、最后批 `33` instruments，`outcome=success`、`stored=1`，耗时 `1256.763726s`；22m34s 窗口 Worker 约 `835.6MiB..1.03GiB/4GiB`，restart=0、OOMKilled=false、post-release kernel OOM=0、无 WorkerLost/SIGKILL/Traceback；公网 health/ready=`200` | 这是 bounded 单 Worker QLib 内存回归与任务成功证据，不是 global capacity、持续 chaos/reconnect、provider/MCP 成功、14d telemetry、restore/rollback 或 owner/reviewer 签署；decision-ready 仍 `503` + `must_not_use_for_decision=true`，TAR-01 仍 `BLOCKED/safety_ready=true/capacity_ready=false`；证据见 [`tar01-qlib-batch-memory-remediation-2026-08-22.json`](../deployment/tar01-qlib-batch-memory-remediation-2026-08-22.json) |
| 2026-08-22 | 第二期 P0 | TAR-01 QLib worker-memory evidence validator | 新增纯 Application candidate-bound validator，严格校验现有 [`tar01-qlib-batch-memory-remediation-2026-08-22.json`](../deployment/tar01-qlib-batch-memory-remediation-2026-08-22.json) 的 commit/release/OCI、UTC 窗口、4GiB/concurrency=1/max-tasks-per-child=1、batch=500、12/12 分片、内存上限、OOM/重启/错误计数、`outcome=success`/`stored=1`、health/ready 与 CI/gate 字段；focused `16 passed`，Ruff/Black/isort 与增量 mypy `0 regressions` | 仅提高既有单 Worker bounded evidence 的离线可复核性；validator 不访问 VPS/HTTP/Redis/Celery/Docker、不启用生产运行时、不改变 flags；强制 `capacity_ready=false`、decision-ready `503` + `must_not_use_for_decision=true`，TAR-01 仍 `BLOCKED`，global capacity/持续 chaos/reconnect/provider/14d telemetry/restore-rollback/owner-reviewer 仍未证明 |
| 2026-08-22 | 第二期 P0 | TAR-02 dispatch reconciliation | 新增 `reconcile_queued_terminal_agent_dispatch`：只扫描已提交且超过 grace window 的 queued run，重派发仅携带 `run_id/task_id`，限量 `1..1000`，Celery Beat 每分钟调度；补 repository 过滤/边界测试、任务 `success/partial/failed/blocked/noop` 统计及 Celery contract 注册；unit `16 passed`，Ruff/Black/isort、增量 mypy、Celery contract guard 通过 | 关闭提交后 broker 短暂失败导致 queued 行无人重派的本地合同缺口；仍未启用 VPS queued flags，未声称真实 broker/Worker、PostgreSQL 生产并发、容量/chaos、14d telemetry、restore/rollback 或人工签署；TAR-02 继续 active，生产路径保持 fail-closed |
| 2026-08-22 | 第二期 P0 | TAR-01 current candidate VPS deployment observation | CI 四门禁在 `4cef9040c` 全绿；`dev/next-development` 以 code-only upgrade 发布为 release `20260822134658`、image `sha256:cfaf1756…`，source/runtime identity match；PostgreSQL/Redis volumes 保留，部署前备份、迁移/schema、Django、TUI registry、容器与 Celery worker/beat 通过；公网 `/api/health/`、`/api/ready/` 均 `200`，Qlib=`pyqlib 0.9.7` | `/api/decision-ready/` 实际为 `503 blocked` 且 `must_not_use_for_decision=true`；queued intake/worker/runtime authorization/emergency-stop 保持关闭；本次不重复部署、不新增 capacity/chaos/recovery/restore/rollback/14d telemetry/provider-success 或 owner/reviewer sign-off，TAR-01 继续 `BLOCKED/safety_ready=true/capacity_ready=false`；结构化证据见 [`tar01-current-vps-observation-2026-08-22-4cef9040.json`](../deployment/tar01-current-vps-observation-2026-08-22-4cef9040.json) |
| 2026-08-22 | 第二期 P0 | TAR-02 durable admission/dispatch repository exit gate | Agent Runtime/terminal unit `251 passed`；SQLite component `21 passed`；本地 disposable PostgreSQL component `18 passed, 5 skipped`，并发 admission 专项 `1 passed`，覆盖 first-winner claim、outer rollback visibility、per-user/global bounded admission、idempotent replay、post-commit ID-only dispatch 与 stable queue/cancel/event semantics；execution focus 已推进 TAR-03 | TAR-02 repository gate 已关闭；VPS queued intake/worker/runtime authorization 仍关闭，不宣称生产 broker/Worker/provider、global capacity、chaos/recovery、14d telemetry、restore/rollback 或 owner/reviewer 签署；PostgreSQL 证据来自本地 disposable container，不改变生产数据；下一主线为 TAR-03 |
| 2026-08-22 | 第二期 P0 | TAR-03 Worker/event/SDK repository exit gate | Agent Runtime/terminal unit `251 passed`；ordered event/SSE component `4 passed`；isolated PostgreSQL repository `18 passed, 5 skipped`，并发 admission 专项 `1 passed`；SDK queued facade `2 passed`；专用 `terminal_agent` queue、worker lease/heartbeat/reaper、cancel、ordered cursor replay、owner scope 和 exact release-image worker isolation 均有本地代码/测试证据；execution focus 已推进 TAR-04 | TAR-03 repository gate 已关闭；未重新部署 VPS，queued/worker 默认仍关闭；不宣称 provider success、multi-user/global hard-SLO capacity、sustained chaos/reconnect、Redis outage、14d telemetry、restore/rollback 或 owner/reviewer 签署；下一主线为 TAR-04 |
| 2026-08-22 | 第二期 P0 | TAR-04 server-side CLI enforcement | `sdk/agomtradepro/local_cli.py` 保留兼容模块名但收口为服务器薄客户端：只读取 server URL/scoped API token，`run` 提交 `/api/prompt/agent/execute`；删除 provider key、本地 Agents SDK、本地 turn loop 和本地 Agent 配置。历史 `local_cli` runtime mode 对新请求返回 `local_cli_disabled`，CLI 使用 server-owned `web_queued`/API；CLI/MCP/server API/queued result/policy focused `35 passed`，增量质量门禁通过 | 未部署 VPS、不启用 queued/worker、不声称 provider/MCP 成功、容量/chaos、生产 UAT 或 owner/reviewer 签字；TAR-04 仍 active，下一步是 MCP/TUI queued integration 与 browser/TUI streamed-result UX |
| 2026-08-22 | 第二期 P0 | TAR-04 TUI queued server-run result integration | CLI TUI 新增 `cli.agent_queue`，只提交已有任务 ID/任务说明到服务器 `/api/terminal/runs/`；结果只保留 `run_id`、状态和事件游标，Workbench 以有限轮询回放服务器事件并展示终态；队列关闭时稳定显示 `queued_runtime_not_wired`，不创建本地 Agent 路径。Python projection/actionability 与 Playwright harness 回归通过 | 仅完成仓库内 B/S 客户端契约；不部署 VPS、不启用 queued/worker、不声称 provider 成功、容量/chaos、14d telemetry、恢复/回滚或 owner/reviewer 签字；用户侧不安装 Agent |
| 2026-08-22 | 第二期 P0 | TAR-04 capability discovery/schema/call/confirmation resume | 新增 `sdk/agomtradepro/local_mcp.py` 与 `sdk/tests/test_sdk/test_local_mcp_contract.py`；通过标准 MCP `ClientSession` 复用 canonical `agom_capability_search`、`agom_capability_schema`、`agom_capability_call`、`agom_confirmation_resume`，严格保留服务端 role/审批/audit 边界；新增测试 `4 passed`，CLI/MCP 合计 `11 passed`，strict mypy、增量回归、full debt ceiling、Ruff/Black/isort 与治理一致性均通过 | 仅完成远程 registry facade；用户安装包不属于 B/S 产品，真实 provider/MCP UAT、queued/worker/capacity/chaos/14d telemetry、restore/rollback 和 owner/reviewer 仍未完成；不部署 VPS、不改变 decision-ready fail-closed，TAR-04 继续 active |
| 2026-08-22 | 第二期 P0 | TAR-04 bounded explicit reconnect/token rotation | `RemoteMcpConnection` 支持显式有界重连与 fresh token-provider 读取；连接尝试限制 `1..3`，能力调用不自动重试以避免 mutation 重复；CLI/MCP 合计 `13 passed`，Ruff/Black/isort、strict mypy、增量回归与 debt ceiling 通过 | 仅完成远程 SDK 重连/轮换合同；用户安装包不属于 B/S 产品，真实 provider/MCP UAT、queued/worker/capacity/chaos/14d telemetry、restore/rollback 和人工门禁仍未完成；不部署 VPS、不改变 decision-ready fail-closed，TAR-04 继续 active |
| 2026-08-22 | 第二期 P0 | TAR-04 B/S 服务端 Agent 决策修正 | 用户确认 CLI/API 调用由服务端 Agent Runtime 执行；provider/MCP/确认/审计留在服务端，用户侧不安装 provider-backed Agent。已移除 SDK `agomtradepro-agent` 发布入口与 `[agent]` 安装 extra，治理 manifest/计划/index 已改为 server-side CLI/API contract；未部署 VPS、未改变 queued/worker/decision-ready fail-closed | 这是架构与计划边界修正，不是生产运行时验收；服务端 CLI/API、queued Worker、provider 成功、capacity/chaos、14d telemetry、restore/rollback 与 owner/reviewer 仍待独立门禁 |
| 2026-08-22 | 第二期 P0 | TAR-04 服务端 API 提交契约 | SDK `PromptModule.agent_execute` 明确提交 `/api/prompt/agent/execute`，队列 facade 覆盖 create/status/events/cancel；新增 provider-secret-free payload 契约测试，组合回归 `22 passed`，治理 manifest 纳入证据。用户侧仍不安装 Agent/provider；未启用 queued Worker、未做生产 UAT 或 provider 成功验证 | 仅关闭服务端 API 提交与 JSON facade 契约；`iter_events`/`wait_for_run`、浏览器/TUI queued UX、候选运行时启用、capacity/chaos、恢复、telemetry 与 owner/reviewer 仍未完成 |
| 2026-08-22 | 第二期 P0 | TAR-04 薄客户端结果消费契约 | `TerminalAgentRunsModule.iter_events` 支持显式事件游标回放，`wait_for_run` 支持有限时状态等待；覆盖终态、超时、非法游标/轮询参数和不重复提交，SDK focused `6 passed`，治理与计划同步；不安装本地 Agent/provider | 仅关闭 SDK 结果消费合同；浏览器/TUI queued UX、候选运行时启用、capacity/chaos、恢复、telemetry、生产 UAT 与 owner/reviewer 仍未完成 |
| 2026-08-22 | 第二期 P1 | TUX-02 operations deep-link dead screen patch cleanup | 删除 `ai-ops.agent-runtime`、`api-library.runtime`、`api-library.config-center` 三个已由 IA canonical screens 承载的 non-IA screen patch；新增三组 alias/source-boundary 回归，focused metadata/actionability/IA `52 passed`、完整 Workbench `257 passed`，source guard `outcome=ok`（12/24 screens、430/890 actions、0 violations，patch 配置 3→0），生成 TUI runtime manifest 已刷新，`npm run check:tui`、Ruff/Black/isort/diff-check 通过 | 仅收口本地 canonical runtime 的最后三项 screen patch；未部署 VPS，未宣称外部 AgomTUI portability、角色化生产 UAT、写后 receipt/refresh、14d telemetry、restore/rollback、capacity/chaos 或 owner/reviewer 双签；CLI 仍为服务端 AI 薄客户端，用户不安装 Agent；TUX-02/TUX-04 继续 active/fail-closed |
| 2026-08-22 | 第二期 P1 | TAR-04/TUX-02 server-side CLI metadata boundary | CLI screen 注入移除重复的 IA-owned 语义字段；`cli.agent_chat`/`cli.agent_stream`/`cli.agent_queue` 明确提交 `/api/terminal/*` 到服务器端 Agent Runtime，客户端只提交请求并消费服务端结果/事件，不安装或运行本地 Agent；metadata/actionability/IA focused `53 passed` | 仅完成本地 B/S 薄客户端契约与文案边界；未部署 VPS、未启用 queued/worker/provider/MCP，不声称生产 UAT、容量/chaos、14d telemetry、restore/rollback 或 owner/reviewer 签署；TAR-04/TUX-02/TUX-04 继续 active/fail-closed |
| 2026-08-23 | 第二期 P1 | TUX-02/TUX-04 current source-boundary audit | 重新运行 `check_tui_metadata_source_consistency.py`：`outcome=ok`、12 published/24 runtime screens、430/890 actions、configured/ignored patches 均为 0、violations=0；registry 仍以 `planned` 作为 TUX-02/TUX-04 的机器状态 | 仅对齐当前 runtime 快照与机器注册表口径；历史 `430/889` 仅保留为历史证据，不改 gate、不新增 repository scope；publish/review 迁入、IA/术语重排、外部 AgomTUI portability、普通角色浏览器 UAT、写后 receipt/refresh、14d telemetry、restore/rollback 与 owner/reviewer 仍未完成；不部署 VPS，CLI/TUI 仍是服务器端 AI 薄客户端 |
| 2026-08-23 | 第二期 P0 | Web-to-TUI readiness read-only observation | `check_web_to_tui_cutover_readiness.py --json` 返回 `decision=DENY`；source consistency 与依赖顺序通过，但 immutable candidate binding、108 路由 UAT、101 任务 telemetry、cleanup/rollback、registry backup 与 owner/reviewer attestations 均缺失 | 仅记录当前机器采集结果，不部署、不写生产、不创建 backup、不执行 rollback 或角色 UAT；`TUI-01` 继续 `awaiting_production`，TUX-02/TUX-04 继续按 registry 为 `planned`，M5/TAR 门禁保持 fail-closed |
| 2026-08-23 | 第二期 P0 | TAR-01 current public health read-only observation | 既有公网候选无写入探针：`/api/health/`、`/api/ready/` 均 `200`；database/Redis/Celery/critical data=`ok`，decision data=`warning` 且 `must_not_use_for_decision=true`；响应 SHA 与未绑定候选范围见 [`tar01-current-public-health-readonly-2026-08-23.json`](../deployment/tar01-current-public-health-readonly-2026-08-23.json) | 仅是未绑定 commit/release 的短窗健康观察；未部署、未生产写入、未创建备份、未回滚、未做角色 UAT 或签署，TAR-01/TAR-05 与 decision-ready 继续 fail-closed |
| 2026-08-23 | 第二期 P0 | TAR-01 public health read-only recheck | 新鲜只读 HTTPS 复核：`/api/health/`、`/api/ready/` 均 `200`，database/Redis/Celery/critical data=`ok`；`/api/decision-ready/` 为 `503 blocked`，`must_not_use_for_decision=true`，阻断原因为 `MCP audit evidence write failed during final acceptance`；结构化响应与 SHA 见 [`tar01-public-health-readonly-recheck-2026-08-23.json`](../deployment/tar01-public-health-readonly-recheck-2026-08-23.json) | 仅证明公网可达与决策 fail-closed；候选 commit/release/OCI 未绑定，未部署、未写生产、未启用队列/Worker、未创建备份、未回滚、未做角色 UAT 或 owner/reviewer 签署；AUD-01/EVID-01 的证据写入/authority、TAR-01/TAR-05 capacity/chaos/telemetry/restore/rollback 仍未解除 |
| 2026-08-23 | 第二期 P0 | TAR-01 public audit health read-only observation | 单次无写入 HTTPS GET `/api/audit/health/` 返回 `200/OK`；失败计数、数据库/表 probe 与 pending/due/claimed/expired/failed outbox 均为 `0`，operation logs=`555`；结构化工件 [`tar01-public-audit-health-readonly-2026-08-23.json`](../deployment/tar01-public-audit-health-readonly-2026-08-23.json)，body SHA=`db83db31700811c465d9b7ef76918aae5ef7e8f5c6a686f474e183a6c0cb8d83` | 仅证明当前审计健康投影可达，不证明 durable publisher、authenticated authority、final-acceptance receipt 或 decision-ready；未部署、未写生产、未审批/激活、不改变 runtime block，AUD-01/EVID-01/TAR-01 继续 fail-closed；B/S CLI/API 仍只向服务器提交请求，用户不安装本地 Agent |
| 2026-08-22 | 第二期 P1 | TAR-04 client-package boundary guard | SDK 包元数据回归现在禁止 `agomtradepro-agent`、`[agent]` extra、`openai-agents` 与 provider-key 安装路径；服务端主包仍可持有 Agent Runtime 依赖，用户侧 B/S/薄客户端不运行本地 Agent | 仅保护客户端打包边界，不是 VPS/provider/生产 UAT 证据；可选 SDK/MCP 仅是远程传输，TAR-04/TAR-05 运行时与人工门禁仍 fail-closed |
| 2026-08-22 | 第二期 P1 | TAR-04 no-local-install documentation guard | SDK README 明确浏览器/TUI 与普通 CLI 用户无需安装 SDK、Agent 或本地模型；`pip install` 仅适用于有明确需求的远程集成宿主，且只提供 API/MCP 薄传输 | 仅收口用户文案与静态契约，不改变服务端执行边界；未部署 VPS、未声称 provider/MCP、容量/chaos、生产 UAT 或人工门禁完成 |
| 2026-08-22 | 第二期 P1 | TAR-04 packaging scope correction | 计划明确撤销“跨平台用户安装包”作为产品下一门；只有受控集成宿主可选用远程 SDK/MCP 传输，普通用户继续直接使用 B/S/服务器端 CLI | 仅修正计划范围，不构建或发布本地 Agent 安装包；真实 provider/MCP、容量/chaos、生产 UAT、恢复/回滚和人工门禁仍未完成 |
| 2026-08-22 | 第二期 P1 | TAR-04 browser/TUI server-side result contract verification | Workbench 浏览器契约回归 `23 passed`：一次服务器队列提交、事件游标回放、终态轮询、行编辑表单、权限/确认、脱敏、失败恢复与 `queued_runtime_not_wired` 均通过；客户端不启动本地 Agent/Worker，也不重复提交 mutation | 仅为仓库内浏览器/TUI B/S 契约证据；未部署 VPS、未取得生产角色化 UAT、provider/MCP 成功、容量/chaos、telemetry、恢复/回滚或 owner/reviewer 签字，用户侧不安装 Agent |
| 2026-08-22 | 第二期 P0 | TAR-01 current VPS read-only recheck | 对既有 `4cef9040c` / release `20260822134658` 做无部署复核：Web/Worker/Beat/PostgreSQL/Redis 健康，health/readiness=`200`，critical data=`ok`，queued/runtime/emergency-stop flags 关闭；decision-ready=`503 blocked` 且 `must_not_use_for_decision=true`；未认证 Agent Runtime/TUI 探针均为 `403 authentication_required`；结构化证据见 [`tar01-current-vps-readonly-recheck-2026-08-22-4cef9040.json`](../deployment/tar01-current-vps-readonly-recheck-2026-08-22-4cef9040.json) | 只读状态证据，不代表角色化浏览器/TUI UAT、provider/MCP、capacity/chaos、恢复回滚、14 日 telemetry 或 owner/reviewer 签字；TAR-01 继续 `BLOCKED/safety_ready=true/capacity_ready=false`，不重复部署 |
| 2026-08-23 | 第二期 P0 | TAR-01/EVID-02/STRAT-01/DATA-01 current candidate P0 read-only inventory | 在 `4cef9040c` / release `20260822134658` 上以同一 PostgreSQL alias 只读复核：audit event/outbox 均 `0`、outbox backlog/failure 均 `0`、audit health=`200/OK`、operation logs=`555`；Account owner-assignment `9` 张目标表、Research R1–R8 `65` 张表、Portfolio R4/R5/R8 `8` 张表均 `0` 行；Data Center `59` 张表共 `3,653,282` 行，publication=`47 published/2051 superseded`，但最新 `equity.core.backfill` 为 blocked；结构化工件见 [`tar01-p0-readonly-ledger-inventory-2026-08-23-4cef9040.json`](../deployment/tar01-p0-readonly-ledger-inventory-2026-08-23-4cef9040.json)，SHA-256=`7f4e859915e7e0a8399ee75558a12e660b34ef04000f29988291f59d47eaaa55` | 只读事实不等于 publisher/authority/approval、owner strategy、full reconciliation/backfill、生产 restore/rollback 或决策可用；未写生产、未审批、未 promotion，`AUD-01` active、`EVID-02/STRAT-01/DATA-01` 仍 awaiting_production，`DATA-02/03` waiting_dependency，decision-ready 仍 `503 must_not_use_for_decision=true` |
| 2026-08-23 | 第二期 P0 | DATA-01 existing VPS backup read-only integrity observation | 复核已有 `/opt/agomtradepro/backups/database/postgres-20260822-075316.dump`，远端 `pg_restore --list` exit `0`；完整下载副本两端尺寸均 `142825371` bytes、SHA-256 均为 `f028ec2fe986be3c0f56f529e3fc44332ece472000c6e43f917d42b9ac2ffc55`；结构化工件见 [`tar01-readonly-backup-observation-2026-08-23.json`](../deployment/tar01-readonly-backup-observation-2026-08-23.json)，content hash=`715191dde263ebe59dc9c10f381f0926c94366b64daf5af55b48296a2e22fe77` | 只读复核既有归档，未创建新备份、未 prune、未部署、未生产 restore/DDL/backfill/reconciliation/rollback；本机无 `pg_restore` 客户端，未冒充本地格式验证；`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-23 | 第一期 P0 | EVID-01 Research Evidence composition fail-closed | 默认 Research Evidence composition 现始终注入 `ScopedEvidenceReadFacade` 与未接线 scope provider；缺少 authenticated owner/tenant grant 时不触碰 repository，staff permission 不再被提升为 owner scope；composition/scope/read/API focused 回归 `58 passed`，增量 mypy、debt ceiling、Ruff/Black/isort 通过；同时明确 B/S 与 CLI/SDK 只传输请求，AI/provider/tool execution 在服务器端 | 仅本地 fail-closed composition 边界；authenticated owner/tenant authority source、同 alias bundle、人工授权、production provider/rollback、PostgreSQL race 与生产 UAT 仍缺，EVID-01 及 Evidence hard gate 继续 active/fail-closed，不部署 VPS |
| 2026-08-23 | 第一期 P0 | EVID-01 same-alias authorized Evidence composition contract | 新增 `make_authorized_evidence_read_facade(selector_provider, using)`，同一 alias 组装 scope-source reader/provider 与 Evidence repository；默认 factory 仍无 selector、在 repository 前 fail-closed；composition/scope/read/API focused `72 passed`，增量 mypy、Ruff/Black/isort、diff-check 通过 | 仅完成可注入 composition contract；selector issuer、authenticated owner/tenant lifecycle、atomic multi-source bundle、人工授权、production provider、PostgreSQL production race/rollback 与 Evidence hard gate 仍缺，authority 继续 zero-seed，CLI/SDK 仍为服务器端 AI 的薄客户端 |
| 2026-08-23 | 第一期 P0 | EVID-01 Research scope ledger disposable PostgreSQL concurrency evidence | 在本地专用 `agomtradepro-tar02-pg` 测试容器中新建、运行并清理 `evidence_scope_test_disposable`；显式 PostgreSQL harness `3 passed in 66.82s`，覆盖空链 root 竞争唯一 winner、同 predecessor successor 唯一 winner、外层异常 rollback 无孤儿行 | 这是隔离 disposable PostgreSQL 的 repository/事务证据，不是 VPS/生产数据库、owner/tenant lifecycle、selector issuer、production writer、容量或人工授权证据；未部署 VPS、未回填或写生产库，EVID-01/Evidence hard gate 继续 active/fail-closed；CLI/API 仍只向服务器传输请求，AI/provider/MCP/tool execution 在服务器端 |
| 2026-08-23 | 第一期 P0 | EVID-01 same-alias composition CI candidate verification | commit `91b18e0c4fadbd31989b0afd75f1550ea32f3bae` 的 [CI Fast Feedback](https://github.com/guiyinan/agomTradePro/actions/runs/32591510019)、Architecture Layer Guard、Security Scan、Consistency Check 全部成功；Python 3.11/3.13 targeted suite、mypy debt ceiling、TUI/runtime guards 均通过 | 只证明仓库候选的自动化门禁与服务器端边界；未取得 authenticated owner/tenant lifecycle、production selector issuer、atomic bundle、人工授权、VPS production UAT、PG production race/rollback 或 hard-gate sign-off |
| 2026-08-23 | 第二期 P0 | TAR-04 server Agent portfolio authority preflight | 默认服务器端 Agent composition 在上下文、工具、模型/provider 之前注入 authority gate；未接 immutable owner/tenant authority provider 时，caller-supplied `portfolio_id`、portfolio scope/tool 稳定返回 `agent_authority_not_wired`，API 映射 `503` 且不触发模型；focused Agent/API 回归 `33 passed`，增量 mypy/debt ceiling/Ruff/Black/isort 通过 | 仅本地 fail-closed 安全切片；CLI/API 仍为 B/S 薄客户端，provider/model/MCP/tool/确认/审计均在服务器端，用户不安装或运行本地 Agent；owner/tenant authority、同 alias bundle、scoped UAT、provider/queue/worker/capacity/chaos/telemetry/restore/rollback 与 owner/reviewer 仍未完成，不部署 VPS |
| 2026-08-23 | 第二期 P0 | TAR-04 all-runtime portfolio authority default | `AgentRuntime` 直接构造、terminal runtime 与 strategy runtime composition 均继承同一默认 fail-closed authority gate；portfolio scope/tool 在 owner/tenant authority 未接入前统一返回 `agent_authority_not_wired`，不会调用模型/provider；focused runtime/authority 回归 `30 passed` | 关闭内部 composition 绕过 public API gate 的本地安全缺口；仍未接 immutable authority/same-alias bundle、scoped UAT、provider/queue/worker/capacity/chaos、telemetry、restore/rollback 或人工门禁；CLI/API 继续由服务器调用 AI，用户不安装本地 Agent，不部署 VPS |
| 2026-08-23 | 第二期 P0 | TAR-04 strategy/internal Agent authority closure | AI-driven 与含 AI 的 HYBRID strategy 在共享上下文读取 portfolio 前执行 authority preflight；未接 authority 时不读 portfolio，`agent_authority_not_wired` 不再回退 legacy Prompt/Chain；rule-only/script-only 保持原路径；strategy focused `40 passed` | 关闭内部 read-before-gate 与 fallback 绕过；immutable owner/tenant authority、同 alias bundle、scoped UAT、provider/queue/worker/capacity/chaos、telemetry、restore/rollback 与人工门禁仍未完成；不部署 VPS，CLI/API 仍由服务器调用 AI，用户不安装本地 Agent |
| 2026-08-23 | 第一期 P1 | EVID-01 Research Evidence ledger future-clock guard | 两个 append-only Research repository 现在都用 repository clock 校验 `recorded_at <= server_now()`；未来 timestamp、naive/unavailable server clock 均 fail-closed 且不落库。scope/evidence component `20 passed`，Ruff/Black/isort、增量 mypy、full debt ceiling、diff-check 通过 | 仅完成本地写入时钟边界；未完成 authenticated owner/tenant authority、atomic multi-source bundle、production provider、PostgreSQL production race/rollback、VPS UAT 或 Evidence hard-gate sign-off；CLI/SDK 继续只向服务器提交请求，AI/provider/tool execution 在服务器端，不要求用户安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope selector authority binding | `EvidenceScopeSourceV1Selector` 现在必须携带并逐字段匹配 immutable source 的 owner/tenant/account/actor；scope provider/composition focused `85 passed`，Ruff/Black/isort、增量 mypy、full debt ceiling、CI 四门禁通过。B/S 与 CLI/SDK 仍只传输请求，AI/provider/tool execution 留在服务器端 | 仅完成本地 selector fail-closed 合同；authenticated owner/tenant lifecycle issuer、同 alias atomic bundle、人工授权、production provider、PostgreSQL race/rollback、VPS/UAT 与 Evidence hard-gate sign-off 仍缺，EVID-01 继续 active/fail-closed |
| 2026-08-23 | 第一期 P0 | EVID-01 PostgreSQL actor-authority bundle read contract | 新增 dormant `DjangoAccountActorAuthorityInputBundleProviderV3`：同一显式 alias 的外层事务先设置 PostgreSQL `REPEATABLE READ READ ONLY`，再读取 authentication-context、Account-user、RBAC 三个 immutable raw-source current reader，并投影 `ExactCurrentActorAuthorityInputBundleV3`；SQLite/嵌套事务/空或非-current ledger/alias、identity、hash drift 均 fail-closed；focused `7 passed`，Ruff/Black/isort、增量 mypy、architecture `0 violations` | 仅完成本地 snapshot composition contract，未接 Evidence/HTTP/CLI/production writer，不读取 mutable User/Profile/session、不现场 hash、不改变 zero-seed；authenticated owner/tenant lifecycle、生产 seed/provider、PG race/rollback、VPS/UAT 与 hard-gate sign-off 仍缺，EVID-01 继续 active/fail-closed；CLI/SDK 仍是服务器端 AI 的薄传输客户端，用户不安装本地 Agent |
| 2026-08-23 | 第一期 P0 | EVID-01 owner/tenant authority composition gate audit | 只读核对确认 actor-authority bundle 仅覆盖 authentication-context、Account user、RBAC 三源，不含 Evidence scope 所需 `owner_id/tenant_id/account_id`；当前无可验证 owner/tenant lifecycle issuer、selector receipt 或 production write route，mutable grant/User/Profile/session 不能现场 hash 成 authority | 不新增 scope 映射或 Evidence route，默认 composition 继续在 repository 前 fail-closed；解除门禁仍需 immutable owner/tenant/scope source、同 alias PostgreSQL 写入/回滚/并发、角色化 UAT 与 owner/reviewer 签署；CLI/API 继续只向服务器传输请求，AI/provider/MCP/Agent 均在服务器端，用户不安装本地模型或 provider 软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 dormant scope-source winner-first replay | `IssueEvidenceScopeSourceV1` 现在先恢复 immutable winner；已有 winner 重试不再读取当前 owner/tenant observation、不要求仍为 logical head/current，过期 winner 仍可历史重放；无 winner 才做 observation 双读与 predecessor/CAS；纯 Application `4 passed` | 仅修正 dormant scope-source 幂等顺序；未接 owner/tenant lifecycle issuer、Evidence/HTTP/CLI/Agent route、production writer、PG race/rollback、UAT 或人工签署；EVID-01 继续 active/fail-closed，CLI/API 仍为服务器端 AI 薄传输客户端，用户不安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope observation content-hash selector | `IssueEvidenceScopeSourceV1Command` 增加 `expected_observation_content_hash`，provider 与 Application 在 Domain/CAS 前逐项核对 observation ID/version/hash；同 ID/version 替换、伪造 hash、未来/无效 observation fail-closed；scope/lifecycle/provider/application focused `55 passed` | 仅收紧 dormant owner/tenant observation selector；未创建 owner/tenant lifecycle issuer，未接 mutable User/Profile/session、Evidence/HTTP/CLI/Agent route、production writer、PG race/rollback、UAT 或人工签署；EVID-01 继续 active/fail-closed，CLI/API 仍只在服务器端调用 AI，用户不安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope-source repository winner read port | `DjangoEvidenceScopeSourceV1Repository.get_winner()` 先 full-world restore/chain 校验，再按 source ID/version 与 PIT 返回 immutable 首赢家；过期 winner 可重放且不回退 current head；isolated repository component `10 passed` | 仅补齐 dormant lifecycle 与 Django repository 的读契约；未接 owner/tenant lifecycle issuer、mutable User/Profile/session、Evidence/HTTP/CLI/Agent route、production writer、PG race/rollback、UAT 或人工签署；EVID-01 继续 active/fail-closed |
| 2026-08-23 | 第一期 P0 | EVID-01 scope-source lifecycle Django append seam | 私有 Django scope-source store 对齐 dormant lifecycle 的 `expected_predecessor_hash`/`recorded_at` append 端口并提供 validated `now()`；完整 ledger predecessor/CAS、root/successor、winner-first 历史重放可由真实隔离 store 执行；component `12 passed`，Research 组合回归 `70 passed`，Ruff/Black/isort、增量 mypy、full debt ceiling、governance/architecture/diff checks 通过 | 仅 dormant/local SQLite contract；未接 owner/tenant authority issuer、HTTP/CLI/Agent route、mutable User/Profile/session、production writer、VPS 或 PostgreSQL production race/rollback；EVID-01 继续 active/fail-closed，CLI/API 仍是服务器端 AI 的薄传输客户端，用户不安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope-source lifecycle same-unit guard | lifecycle observation provider 与 append repository 必须声明并匹配同一 `unit_of_work_key`；alias 缺失/替换/不一致在任何读取或写入前 fail-closed；alias mismatch 回归纳入 focused suite，scope lifecycle/application/provider/repository `71 passed`，Ruff/Black/isort、增量 mypy、diff-check 通过 | 仅 dormant composition transaction-boundary contract；不创建 owner/tenant authority、不读 User/Profile/session、不接 HTTP/CLI/Agent/production writer，不替代 PostgreSQL production race/rollback；EVID-01 继续 active/fail-closed，CLI/API 仍为服务器端 AI 薄客户端 |
| 2026-08-23 | 第一期 P0 | EVID-01 expired final predecessor successor policy | active 但已过 `valid_until` 的最终 head 仍可作为新的 CAS predecessor；`revoked` 仍 terminal，future head fail-closed，current reader 继续独立执行 TTL/head 语义；lifecycle focused `7 passed`，Ruff/Black/isort、增量 mypy 通过 | 仅澄清 dormant chain continuation 与 current-read 分离；未创建 owner/tenant authority、不读 User/Profile/session、不接 HTTP/CLI/Agent/production writer，不替代 PostgreSQL race/rollback；EVID-01 继续 active/fail-closed，CLI/API 仍为服务器端 AI 薄客户端 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope-source lifecycle unit liveness guard | 除构造时 alias 校验外，execute 入口、atomic 边界、每次关键读写之间及 append 前后都重新验证 provider/repository 的 canonical `unit_of_work_key`；运行期间 key 漂移、空白/非规范 key、append 后漂移均 fail-closed，真实事务 fake 回归确认异常回滚；Research lifecycle/application/provider/composition/repository `76 passed` | 仅增强 dormant transaction-identity liveness contract；不创建 owner/tenant authority、不读 User/Profile/session、不接 HTTP/CLI/Agent/production writer，不替代 PostgreSQL race/rollback；EVID-01 继续 active/fail-closed，CLI/API 仍为服务器端 AI 薄客户端，用户不安装本地 Agent、模型或 provider 软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 authorized Evidence composition same-alias guard | `make_authorized_evidence_read_facade()` 现在要求 selector provider、scope-source repository 与 Evidence repository 都声明并匹配同一 `django:{using}`；缺失、非法或跨 alias selector 在构造任何 repository 前 fail-closed；composition/provider/scope/repository focused `77 passed`，增量 mypy、Ruff/Black/isort、governance/architecture checks 通过 | 仅收紧 dormant composition wiring；不创建 authenticated owner/tenant authority、selector issuer、User/Profile/session reader、production writer 或 HTTP/CLI/Agent route，不替代 PostgreSQL race/rollback、UAT 或人工签署；EVID-01 继续 active/fail-closed，CLI/API 仍为服务器端 AI 薄客户端 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope observation schema-only ledger | 新增 `EvidenceScopeSourceV1ObservationModel` 与 `0029` schema-only migration；zero-seed、observation identity/content hash 唯一、status/artifact/clock DB 约束及 private exact-insert/save-update-bulk-raw-delete guards；isolated component `5 passed`，scope model/repository/lifecycle/provider/composition focused `52 passed`，`makemigrations --check` 无漂移，增量 mypy/格式化/架构门禁通过 | 仅建立 dormant owner/tenant observation 持久化边界；不创建 authority/lifecycle issuer，不读取 User/Profile/session，不接 provider/writer/HTTP/CLI/Agent route，不回填或部署 VPS；EVID-01 继续 active/fail-closed。CLI/API 仍只把请求发送到服务器，AI、模型、provider、MCP/tool execution 在服务器端，用户不安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope observation strict codec | 新增 `EvidenceScopeSourceV1Observation` strict canonical codec；exact keys/types、UTC-Z microseconds、nested ArtifactRef、content-hash recomputation、round-trip 与 tamper/unknown/secret-field fail-closed 回归；scope/model/repository/lifecycle/provider/composition 聚合 `61 passed`，增量 mypy、Ruff、Black、isort 通过 | 仅完成 dormant observation DTO 的 canonical serialization boundary；不创建 owner/tenant authority 或 selector issuer，不接 User/Profile/session、provider/writer/HTTP/CLI/Agent route，不回填或部署 VPS；EVID-01 继续 active/fail-closed，CLI/API 仍是服务器端 AI 的薄传输客户端，用户不安装本地 Agent、模型或 provider 软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 scope observation strict read repository/provider seam | 新增 `DjangoEvidenceScopeSourceV1ObservationRepository` 只读 provider；每次 selector 前完整恢复 observation ledger、逐行 codec/标量/canonical hash 校验，按 exact observation ID/version/content hash 与 PIT 返回 active current；future/expired/revoked/missing、clock 不可用、无关行 tamper 均 fail-closed；isolated component `11 passed`，Ruff/Black/isort、增量 mypy regression 通过 | 仅完成 dormant observation 的服务器端 read seam；没有 capture/append、owner/tenant lifecycle issuer、User/Profile/session 读取、HTTP/CLI/Agent route、生产 authority 或 VPS 部署，不解除 EVID-01/Evidence hard gate；CLI/API 仍只向服务器传输请求，AI/provider/MCP/tool execution 在服务器端，用户不安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-01 next-slice exit audit | 复核 ScopeSourceV1 Domain/codec/Application readers、zero-seed ledger、closed-world repositories、observation read seam、same-alias facade 与 Account actor bundle；确认当前本地 read contract 已闭环，默认 composition 继续在 repository 前 fail-closed | 下一步不能再加 alias glue 或把 Account actor bundle 映射成 Evidence owner/tenant；仍缺独立 immutable owner/tenant/scope lifecycle（root/successor/revocation/expiry/receipt）、server-issued selector provider、生产写入与 authenticated route。EVID-01 保持 active/fail-closed，需外部业务语义/授权后才能继续；CLI/API 仍为服务器端 AI 薄客户端，用户不安装本地软件 |
| 2026-08-23 | 第一期 P0 | EVID-02 SELECT-only current-head snapshot normalizer | 新增严格 `evid-02-select-only-ledger-snapshot.v1` 外部采集 envelope 与 pure Application normalizer；绑定 candidate commit/release、database alias、query digest、UTC/PIT 与 `read_mode=select_only`，重建既有 `evid-02-head-audit-snapshot.v1`，拒绝未知/mutation/human-approval/secret 字段；`record_evid_02_head_audit.py --input-format select-only` 默认 dry-run、报告仍 content-addressed；head-audit focused `18 passed`，Ruff/Black/isort、增量 mypy、full debt ceiling 通过 | 当前没有新的 VPS 快照落盘，未接数据库、未写 approval/activation、未收集人工签署；输出固定 `production_claim=false`/`production_ready=false`/`runtime_enablement=not_authorized`，`EVID-02` 继续 `awaiting_production`，真实 production current-head/rollback、approval、owner/reviewer 与 Evidence hard gate 不解锁；CLI/API 仍是服务器端 AI 薄客户端 |
| 2026-08-23 | 第一期 P0 | EVID-02 VPS current-head SELECT-only observation | 对候选 `4cef9040cccc2127c3f8128c8d858bc7958df2a4` / release `20260822134658` 通过同一 `default` PostgreSQL alias 执行一次 `REPEATABLE READ READ ONLY` 查询；canonical approval/activation 两账本均为 `0` 行，head=`empty`、无未来行/链问题；原始 envelope [`evid-02-select-only-vps-snapshot-2026-08-23-4cef9040.json`](../deployment/evid-02-select-only-vps-snapshot-2026-08-23-4cef9040.json) 的 query SHA=`0735a5b16fee05f9b3fc07f564d56bea85467981b97bfc53234cf23bc9e00b6c`，content-addressed report [`dc03b94b2a454a3db500960d2abc19fd1f14f40affc1c045f40c77760fbd5c12`](../deployment/evid-02-head-audit/dc/dc03b94b2a454a3db500960d2abc19fd1f14f40affc1c045f40c77760fbd5c12.json) | 仅采集/下载/本地验证，不部署、不迁移、不写生产、不创建 approval/activation、不执行 rollback，`production_claim=false`、`production_ready=false`、`human_approval_status=not_collected`、`runtime_enablement=not_authorized`；这份空账本观察不能替代真实审批、生产并发/rollback、owner/reviewer 签署，`EVID-02` 仍 `awaiting_production`，Evidence hard gate 不解锁；CLI/API 仍为服务器端 AI 薄客户端 |
| 2026-08-23 | 第一期 P0 | EVID-01 当前 VPS authority inventory 只读复核 | 在未部署、未迁移、未写库的前提下，对运行候选 `4cef9040cccc2127c3f8128c8d858bc7958df2a4` / release `20260822134658` 读取 PostgreSQL `django_migrations` 与 12 张 authority/evidence/root-lock 表；0050–0053 已应用，12 张表均为 `0` 行，报告 [`3900c08b9054620f9b969f4bd5aab8097bb00ad1e3692e9aaeee66bda5cdf9b4`](../deployment/evid-01-authority-inventory/39/3900c08b9054620f9b969f4bd5aab8097bb00ad1e3692e9aaeee66bda5cdf9b4.json) 由快照 [`evid-01-authority-inventory-snapshot-2026-08-23-0810.json`](../deployment/evid-01-authority-inventory-snapshot-2026-08-23-0810.json) 生成 | 仅证明 schema 已部署但 authority 仍 zero-seed；`blocked_zero_seed_authority`、`authority_ready=false`、`production_claim=false`、`runtime_enablement=not_authorized`。不把 Account actor bundle 映射为 Evidence owner/tenant，不接 route、不写 production；EVID-01 仍需独立 immutable owner/tenant/scope lifecycle、selector issuer、同 alias production composition、人工授权与 PostgreSQL race/rollback，继续 active/fail-closed |
| 2026-08-23 | 第一期 P0 | EVID-02 disposable PostgreSQL concurrency recheck | 在现有本地 `agomtradepro-tar02-pg` disposable PostgreSQL 16 容器中显式使用专用 Django settings 与空库 `evidence_scope_test_20260823` 重跑 scope-source harness，空 root、同 predecessor successor、rollback/no-orphan `3 passed in 12.20s`；运行后数据库已清理，既有 content-addressed report dry-run digest `a27e193f…e6df8` 校验通过 | 仅本地 PostgreSQL 软件合同复核，不是 VPS/生产 PostgreSQL、owner/tenant lifecycle、approval/current-head/rollback 或人工签署证据；不部署、不写生产，`EVID-02` 继续 `awaiting_production`，CLI/API 仍只向服务器提交请求 |
| 2026-08-23 | 第一期 P0 | EVID-01 Research unit acceptance recheck | 完整 `tests/unit/research` 回归 `860 passed in 178.19s`，覆盖 Evidence composition/scope/lifecycle/read facades 与 R1–R8 contracts；无生产代码改动、无 authority 组合变更 | 仅仓库 unit contract 证据，不把 fixtures 当 owner/tenant authority；EVID-01 仍 active/fail-closed，production selector/writer、VPS/UAT、PG production race/rollback 与人工签署仍缺 |
| 2026-08-23 | 第二期 P1 | Web→TUI/EVID 候选证据链分离与身份复核 | EVID-01 只读 inventory 单测改为校验当前 `4cef9040…/20260822134658` 快照及 content-addressed report；Web→TUI candidate consistency 补齐正式 `2f4554b5192191970a3ccbc98420388881725079` / `20260820211526` preflight 在 readiness/deployment 文档的完整 binding，定向回归 `2 passed` | 仅修复验收证据的候选耦合与文档索引，不部署、不写生产、不新增角色 UAT；M5 仍 `DENY`，EVID-01 仍 `blocked_zero_seed_authority`，角色化 UAT、写后回执、14 日 telemetry、backup/restore、rollback 与 owner/reviewer 双签仍缺 |
| 2026-08-23 | 第二期 P1 | Web→TUI/EVID 候选 guard 全套验收复核 | `e73930f66` 将 EVID-01 authority-inventory 合同与 M5 candidate 合同解耦；EVID-01/candidate/readiness focused 回归 `36 passed`，完整 `tests/unit/test_tui_workbench.py` `257 passed`；source guard `outcome=ok`（12/24 screens、430/890 actions、patch boundary 0、0 violations），active-plan/governance consistency 均 `0`，四条 push CI 全绿 | 仅仓库与 CI 验收，不部署 VPS、不写生产；Web→TUI readiness 仍 `DENY`，EVID-01 仍 zero-seed/fail-closed；角色化生产 UAT、写后 receipt/refresh、14 日 telemetry、backup/restore、rollback 与 owner/reviewer attestations 仍未完成，B/S 客户端继续只向服务器提交请求 |
| 2026-08-23 | 第二期 P0 | DATA-01 最新已有 VPS 归档结构化证据复核 | 只读下载既有 `/opt/agomtradepro/backups/database/postgres-20260822-075316.dump`，远端/本地大小 `142825371` bytes、SHA-256 `f028ec2fe986be3c0f56f529e3fc44332ece472000c6e43f917d42b9ac2ffc55`；远端 `pg_restore --list` 与本地容器复核均为 `7204` entries，manifest SHA `7a75c9afffd87ed2aaa9bdade115a1898f5219075ded466f7e411ae3a18ddba7`；`data-backup-evidence.v1` content hash `6566a973…17f38`，备份/restore verifier `25 passed` | 仅自动取证与传输/格式完整性；未创建备份、未部署、未 restore 生产库、未维护态 rollback、RTO/RPO、回填或 reconciliation，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-23 | 第二期 P0 | DATA-01 最新归档本地隔离 restore 验收 | 在 disposable PostgreSQL 16 中以同一归档建立 source 基线并完成第二次隔离 restore；`7189` restore entries、`539` public tables、`72` 个 Data Center migrations、`460` 个 sequences，逐表内容/schema/sequence 差异全为零；报告 [`data01-latest-backup-restore-2026-08-23.json`](../deployment/data01-latest-backup-restore-2026-08-23.json)，report SHA `7b8ee34b…58af8e`，归档 SHA 仍为 `f028ec2f…ffc55` | 仅本地隔离恢复与精确一致性证据；临时 source/restore 库已清理，未触碰 VPS/生产库，未建立生产 RTO/RPO、维护态 rollback、backfill/reconciliation 或 owner/reviewer 签署，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-23 | 第二期 P0 | TAR-01/AUD-03 公网健康与审计只读复核 | 对 `https://demo.agomtrade.pro` 一次性 GET：`/api/health/`=`200`、`/api/ready/`=`200`（DB/Redis/Celery/critical_data 均 ok，worker=1，decision_data=warning）、`/api/audit/health/`=`200/OK`（operation logs=555、outbox pending/claimed/failed/delivered 全为 0）；`/api/decision-ready/` 仍 `503 blocked`，`must_not_use_for_decision=true`，原因仍为 `MCP audit evidence write failed during final acceptance`；结构化工件 [`tar01-public-health-readonly-recheck-2026-08-23-1219.json`](../deployment/tar01-public-health-readonly-recheck-2026-08-23-1219.json)，content hash=`4354ca3094ff03df6935275275492d40a740188315a41fb69eb28106b4a3e670` | 仅新鲜只读连通性/审计健康事实，公网响应未提供 immutable candidate identity（bound=false）；未部署、未写库、未启用队列/Worker、未审批/激活、未备份/回滚、未做角色 UAT 或签署，不解除 TAR-01/TAR-05/AUD-01/EVID-01 或 decision-ready fail-closed 门禁 |
| 2026-08-23 | 第二期 P0 | TAR-01/AUD-03 公网健康与审计低频复核 | 低频无写入 HTTPS GET 于 `2026-08-23T14:12:58Z` 再次确认 `/api/health/`、`/api/ready/`=`200`（DB/Redis/Celery/critical_data=`ok`、worker=1、decision_data=`warning`），`/api/audit/health/`=`200/OK`（operation logs=555、failure/outbox 各计数=0）；`/api/decision-ready/`=`503`、`must_not_use_for_decision=true`，阻断原因为 `MCP audit evidence write failed during final acceptance`。工件 [`tar01-public-health-readonly-recheck-2026-08-23-1413.json`](../deployment/tar01-public-health-readonly-recheck-2026-08-23-1413.json)，SHA-256=`0f6473dbfa0ec11cb934a49af4097804a4f68e6198b9832ce5d20491579f3fee` | 仅刷新公网可达性、审计健康与决策 fail-closed 事实；公共端点仍未提供 immutable candidate identity（bound=false），未部署、未写生产、未启用队列/Worker、未创建备份、未回滚、未做角色 UAT 或 owner/reviewer 签署，TAR-01/TAR-05/AUD-01/EVID-01 继续 fail-closed |
| 2026-08-23 | 第二期 P1 | 决策报价 freshness 语义验收澄清 | 复核发现非交易日报价 `age_minutes` 较大但 `freshness_status=latest_completed_session`、`is_stale=false`；实现仅对 CN-listed、最新已完成中国交易日且收市后的快照清除年龄阻断，`test_accepts_latest_completed_session_quote_on_weekend`、旧交易日与盘中 stale 反例测试均覆盖，因此不是 freshness bug | 该语义不解除 `/api/decision-ready/`=`503`、`must_not_use_for_decision=true`；审计证据写入、市场 thermometer/coverage 与 provider 门禁仍阻断；本次仅更新验收说明，未改生产代码、未部署、未写生产 |
| 2026-08-23 | 第二期 P0 | TAR-01 当前 VPS 候选只读 verifier 验收 | 对现有 VPS 候选 `4cef9040…` / release `20260822134658` 执行一次只读 SSH verifier：Caddy/TLS、health、Django、迁移、Data Center schema、TUI registry、Qlib、容器、资源、healthcheck、Celery worker/beat/ping 全通过（exit=0）；结构化工件 [`tar01-current-vps-readonly-verification-2026-08-23.json`](../deployment/tar01-current-vps-readonly-verification-2026-08-23.json)，SHA-256=`d92285f87677571d3cc75d0ca78bd50b16933137f2df15bcd3d0eabe18cf7b51` | 只读确认已有候选运行身份；快照采集时分支为 `ca920849…`，随后仅新增 docs-only 提交 `17319c24…`，两者均未与 VPS 候选绑定；未部署、未切换 release、未迁移/回滚、未启用队列、未写生产；`/api/decision-ready/` 仍 `503` fail-closed，TAR-01/TAR-05 的 capacity/provider/chaos/telemetry/restore/rollback/owner-reviewer 门禁不解除 |
| 2026-08-24 | 第二期 P0 | TAR-01 当前 VPS 候选低频只读复核 | 同一现有候选再次执行只读 SSH verifier，exit=0；Caddy/TLS、Django、迁移、Data Center schema、TUI registry、Qlib、容器、资源、healthcheck、Celery worker/beat/ping 均通过。新工件 [`tar01-current-vps-readonly-verification-2026-08-24.json`](../deployment/tar01-current-vps-readonly-verification-2026-08-24.json)，SHA-256=`7e364af02b2e72e5ffed55fb417923d8fa99a44e914ae092fd654f964c8d1eba`；health/ready/audit 均 `200`，decision-ready=`503` 且 `must_not_use_for_decision=true` | 候选仍为 `4cef9040…` / release `20260822134658`，本地 HEAD=`8a755c3…`，binding=false；仅确认运行身份与低频可达性，未部署、未写生产、未启用队列、未回滚、未做角色 UAT/容量/混沌/14 日观测或 owner/reviewer 签署，相关门禁继续 fail-closed |
| 2026-08-23 | 第二期 P1 | TUX-02/TUX-04 source/actionability 当前回归 | source guard=`ok`（12 published/24 runtime、430/890 actions、0 violations），TUI actionability `12 passed`，EVID composition/provider focused `24 passed`，candidate consistency `1 passed`；无 metadata publish、VPS 部署或生产写入 | 仅补充仓库回归证据，不改变 registry 的 `TUX-02`/`TUX-04`=`planned`、EVID-01 execution focus、M5/TAR/AUD/EVID 生产门禁；角色化 UAT、写后回执、外部 portability、telemetry、restore/rollback 与 owner/reviewer 签署仍缺 |
| 2026-08-23 | 第一期 P0 | EVID-01 当前 HEAD bounded-slice 再审定 | 当前 HEAD `eb57eddf0` 上 EVID-01 composition/scope/lifecycle/observation/repository/authority-inventory targeted regression `150 passed in 9.61s`；active-plan 与 governance consistency 均 `0` violations；生产快照仍为 0050–0053 已应用、12 张 authority/evidence/root-lock 表全零，报告固定 `blocked_zero_seed_authority`/`authority_ready=false`/`production_claim=false`/`runtime_enablement=not_authorized` | 审定没有新的安全本地 slice；默认 composition 继续在 repository 前 fail-closed，禁止把 Account actor bundle 映射为 Evidence owner/tenant，也不新增 alias glue；仍需外部定义独立 immutable owner/tenant/scope lifecycle、server-issued selector issuer、production writer/authenticated route、同 alias production composition、PostgreSQL race/rollback 与 owner/reviewer 签署，故 EVID-01 保持 `active`/fail-closed，未部署、未写生产，B/S 与 CLI/API 仍为服务器端 AI 薄传输客户端 |
| 2026-08-23 | 第二期 P0 | STRAT-01 当前运行候选 owner-ledger 只读复核 | 当前镜像 `agomtradepro-web:20260822134658` / source `4cef9040c` 上，R1–R8 `65` 张表全零，Portfolio R4/R5/R8 `7` 张表全零，Account owner-assignment `9` 张表及显式 owner/policy/operator registry 全零；工件 [`strat-01-owner-ledger-readonly-recheck-2026-08-23.json`](../deployment/strat-01-owner-ledger-readonly-recheck-2026-08-23.json)，SHA-256=`20e1d1c23ad00ab89879c1d2b2a4c93c051b07f9af0286c6df2c4f459c8d5ab6` | 仅 SELECT-only 自动取证，未写生产、未创建 owner/definition/policy/calendar/scope/qualification、未 promotion/审批；`STRAT-01` 继续 `awaiting_production`，`STRAT-02/03`、PIT/OOS、canonical receipts、Promotion、consumer UAT、rollback 与 owner/reviewer 签署不解锁 |
| 2026-08-24 | 第一期 P0 | EVID-02 当前候选新鲜 SELECT-only 只读复核 | 只读 SSH verifier 再次确认现有候选 `4cef9040…` / release `20260822134658` / image `sha256:cfaf17560df2…`; 同一 `default` alias PostgreSQL `REPEATABLE READ READ ONLY` 于 `2026-08-23T23:19:50Z` 读取 approval/activation，两账本均为 `0` 行、head=`empty`；快照 [`evid-02-select-only-vps-snapshot-2026-08-24-4cef9040.json`](../deployment/evid-02-select-only-vps-snapshot-2026-08-24-4cef9040.json)，input SHA-256=`962eb52c3a93fa39848777f12cf6acaad653d64c7b49a07cc5e889ffa0b2234c`，报告 [`894cce56837ad938da268b6b0c43f4f0dde01374d9efbba0176cfb1896d92225.json`](../deployment/evid-02-head-audit/89/894cce56837ad938da268b6b0c43f4f0dde01374d9efbba0176cfb1896d92225.json) | 仅刷新候选绑定与空账本事实；local `dev/next-development` HEAD 未部署，本次没有迁移、approval/activation 写入、rollback 或人工签署；报告固定 `production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，真实 first-winner/current-head、生产并发/rollback、owner/reviewer 与 Evidence hard gate 仍锁定；B/S 客户端与 CLI/API 继续只向服务器提交请求 |
| 2026-08-24 | 第二期 P0 | STRAT-01 当前候选新鲜 owner-ledger 只读采集 | 同一候选 `4cef9040…` / release `20260822134658` / image `sha256:cfaf17560df2…`，同一 `default` alias 执行 `REPEATABLE READ READ ONLY`；Research `65`、Portfolio `7`、Account authority/assignment 广义 `15`、owner/policy/operator/assignment 广义 `34` 张表全部为 `0` 行；快照 [`strat-01-owner-ledger-readonly-recheck-2026-08-24-2341.json`](../deployment/strat-01-owner-ledger-readonly-recheck-2026-08-24-2341.json)，input SHA-256=`4a8163e816196cc364ab315b94c269f5a3e4d47f7cc6d67c9d47cbf498ec7ec9`，报告 [`4427a38da21d5da577de577f6bb4072eb3361c735453e887a224bf018a2ebc20.json`](../deployment/strat-01-owner-ledger-inventory/44/4427a38da21d5da577de577f6bb4072eb3361c735453e887a224bf018a2ebc20.json) | outcome=`zero_seed`；只读采集，没有创建/更新/删除/回填/promotion/审批，也未把 Data Center facts 变成 owner evidence；`STRAT-01` 继续 `awaiting_production`，`STRAT-02/03`、PIT/OOS、receipts、Promotion、UAT、rollback 与 owner/reviewer 签署不解锁；B/S 与 CLI/API 仍为服务器端 AI 薄传输客户端 |
| 2026-08-24 | 第二期 P0 | AUD-03 当前候选 SELECT-only 运营观察 | 同一候选 `4cef9040…` / version `20260822134658` / OCI `sha256:cfaf17560df2…` 上，VPS `default` alias migration 只读查询 applied=`493`、pending/failed=`0`，公网 `/api/audit/health/`=`200/OK`，operation logs=`555`、failures=`0`、outbox pending/due/claimed/expired/failed/delivered 全为 `0`；快照 [`aud03-operational-observation-select-only-2026-08-24-2358.json`](../deployment/aud03-operational-observation-select-only-2026-08-24-2358.json)，报告 [`56e1ef25819bc2f73df2c78975090494317f1f918bc4238a24d24ff7cec90f82.json`](../deployment/aud03-operational-observation/56/56e1ef25819bc2f73df2c78975090494317f1f918bc4238a24d24ff7cec90f82.json) | `missing_section_count=4` 的 alerts/TUI/recovery/archive 明确 unavailable；只读派生，无 migration/claim/publish/Celery/fault/archive/restore/生产写入；`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，`AUD-03` 继续 `waiting_dependency`，AUD-01/AUD-02 与 owner/reviewer 签署不解锁 |
| 2026-08-24 | 第二期 P0 | DATA-03 当前候选双 readiness 只读采样 | 对既有 verifier 已绑定的候选 `4cef9040…` / version `20260822134658` / OCI `sha256:cfaf17560df2…` 低频执行公开 HTTPS `GET`：`/api/ready/`=`200`、`/api/decision-ready/`=`503 blocked` 且 `must_not_use_for_decision=true`；匿名 `/api/data-center/providers/`=`403`，canonical smoke 明确为 `unknown`。原始 envelope [`data03-readiness-http-get-2026-08-24-0015.json`](../deployment/data03-readiness-http-get-2026-08-24-0015.json)，source SHA=`1a1211d8bbd6dd029b50d554cfb4d012f01549447a486e1077414b965229c267`；只读 recorder 报告 [`2e48775c3400fd35407265123f43acf4f7d3302be8b1407d0f20448b7c6e0782.json`](../deployment/data03-readiness/2e/2e48775c3400fd35407265123f43acf4f7d3302be8b1407d0f20448b7c6e0782.json)，`service_failure=0`、`decision_blocker=1`、`smoke_failure=1`、`production_ready=false` | 仅完成一次候选绑定的双 readiness/health 只读观察与 append-only 证据；未部署、未写生产、未切换维护态、未取得认证 canonical Data Center smoke、未完成 DATA-02 reconciliation、M9/M10 observation window 或 owner/reviewer 签署，`DATA-03` 继续 `waiting_dependency`，decision-ready 保持 fail-closed |
| 2026-08-24 | 第二期 P0 | Web→TUI/TUI-01 当前机器 readiness 只读快照 | `python scripts/check_web_to_tui_cutover_readiness.py --json` 返回 `DENY`（`as_of=2026-08-24`）；source consistency 与 execution dependency 通过，stable candidate/version window、108 路由 UAT、cleanup scope、101 项 production telemetry、rollback、registry backup 与 owner/reviewer attestations 均缺失。快照 [`web-to-tui-readiness-observation-2026-08-24.json`](../deployment/web-to-tui-readiness-observation-2026-08-24.json)，SHA-256=`f60e19b683f7f31d900dd1964403d8bbd162f27398991757878b8b319dd037b5` | 只读记录当前机器 gate，不部署、不重绑候选、不写生产、不创建 backup、不执行 cleanup/rollback 或角色 UAT；`TUI-01` 继续 `awaiting_production`，TUX-02/TUX-04 按 registry 暂停，M5/TAR 保持 fail-closed，用户侧 B/S/CLI 仍不安装本地 Agent/provider |
| 2026-08-24 | 第二期 P0 | TAR-01/EVID-01 当前 HEAD 一次性 VPS 部署与 post-0055 只读验收 | 以 `94abd76e46eeef4a8e21853799c7d69bcd9bbe3b` 做一次 code-only `-Upgrade`，绑定 release `20260824133504` / image `sha256:1c560b5fed14964a008c278a88d9f3e3b144444a172ecc239d06cedbd76d6a3e`；部署前 PostgreSQL 备份，迁移/schema、Django/TUI registry、Qlib=`pyqlib 0.9.7`、Caddy/TLS、Web/Worker/Beat/Celery ping 全通过；`account.0055` 与 `research.0029` 已应用，同 alias 的 13 张 authority/evidence 表全为 `0` 行；公网 health/db/ready/audit=`200`，匿名 `/api/tui/`=`403`，decision-ready=`503` 且 `must_not_use_for_decision=true`；结构化工件 [`tar01-current-vps-deployment-acceptance-2026-08-24-94abd76e.json`](../deployment/tar01-current-vps-deployment-acceptance-2026-08-24-94abd76e.json)，EVID 报告 [`55bb41f6129c30d42c8ec3041bc4e90b4ae5064e29409b83d8df6ea86bb7d680.json`](../deployment/evid-01-authority-inventory/55/55bb41f6129c30d42c8ec3041bc4e90b4ae5064e29409b83d8df6ea86bb7d680.json) | 这是一次受控部署与低频只读运行验收，不是 owner/tenant authority seed/approval 或业务写入；EVID-01 报告固定 `blocked_zero_seed_authority`、`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，未做 PostgreSQL production first-winner/revocation/rollback、角色化浏览器 UAT、queued/capacity/chaos、14 日 telemetry、restore/rollback 或 owner/reviewer sign-off；TAR-01 仍 `CONTRACT_COMPLETE/safety_ready=true/capacity_ready=false`，decision gate 与全局执行保持 fail-closed；不再重复部署 |
| 2026-08-24 | 第一期 P0 | EVID-02 当前 HEAD approval/current-head SELECT-only 只读复核 | 同一已部署候选 `94abd76e46eeef4a8e21853799c7d69bcd9bbe3b` / release `20260824133504`，同一 `default` PostgreSQL alias 内以 `REPEATABLE READ READ ONLY` 查询三张 canonical ledger；`research_evidence_operator_spec_approval`、`research_activated_evidence_operator_spec`、`research_r6_activation_authorization` 均为 `0` 行，approval/activation head=`empty`；query digest=`8553cd8217b7e1e03b3140c1cc7eed9d33f07899650b7a5f4f933bb7cff47d40`，快照 input SHA=`69814588d331087160224ac16758840f1b833c1787863a54f4c2418763bfb64f`，报告 [`3513ea4582a73d2afccd5c2967008c4a8c17aa70d3a7039473947724c289e4fd.json`](../deployment/evid-02-head-audit/35/3513ea4582a73d2afccd5c2967008c4a8c17aa70d3a7039473947724c289e4fd.json) | 只读事实重新绑定到当前候选，未创建/审批/激活、未执行生产并发/rollback；报告固定 `human_approval_status=not_collected`、`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，EVID-02 继续 `awaiting_production`，真实 first-winner/current-head/rollback、owner/reviewer 与 Evidence hard gate 不解锁；B/S、CLI/API 仍只向服务器提交请求，用户不安装本地 Agent/provider |
| 2026-08-24 | 第二期 P0 | DATA-01 latest existing VPS custom-format backup 只读刷新 | 通过 `scripts/backup-vps-postgres.py --download-latest --evidence-output` 发现并下载既有 `/opt/agomtradepro/backups/database/postgres-20260824-074227.dump`；远端/本地 `142813695` bytes、SHA-256=`7eb67da66bb6d3c550bc35f96abbc2c38ea403f776c56602316e83b912b4fd6d`，远端 `pg_restore --list`=`7204` entries、manifest SHA=`795d83b33400407596991f92523a5b15b2148bbf5e4e77fc52682194875f3886`；结构化工件 [`data-backup-evidence-2026-08-24.json`](../deployment/data-backup-evidence-2026-08-24.json)，content hash=`423387ef6125233f4257694935beef0cea9c8543993803b3ef58bc896758e9f9` | 仅读取/下载与格式完整性校验，未创建备份、未 prune、未 restore 生产库、未维护态 rollback、未做 RTO/RPO/backfill/reconciliation；`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-24 | 第二期 P0 | STRAT-01 当前部署候选新鲜 owner-ledger 只读采集 | 在当前受控候选 `94abd76e46eeef4a8e21853799c7d69bcd9bbe3b` / release `20260824133504` / image `sha256:1c560b5fed14964a008c278a88d9f3e3b144444a172ecc239d06cedbd76d6a3e` 上，同一 `default` alias 执行 `REPEATABLE READ READ ONLY`；Research `65`、Portfolio `7`、Account authority/assignment 广义 `16`、owner/policy/operator/assignment 广义 `35` 张表全部 `0` 行；快照 [`strat-01-owner-ledger-readonly-recheck-2026-08-24-0707.json`](../deployment/strat-01-owner-ledger-readonly-recheck-2026-08-24-0707.json)，input SHA-256=`c1f06ea97f7991e0fbb7ce3c481fa7382b153b0ddb8809c4f1965dcd0fb45ec3`，报告 [`0bed8c11f20f7b93b1b5cb424270f98419f61e6c948f61f46a9c775a9ed3c0ca.json`](../deployment/strat-01-owner-ledger-inventory/0b/0bed8c11f20f7b93b1b5cb424270f98419f61e6c948f61f46a9c775a9ed3c0ca.json) | outcome=`zero_seed`；仅只读采集，没有创建/更新/删除/回填/promotion/审批，也未把 Data Center facts 变成 owner evidence；`STRAT-01` 继续 `awaiting_production`，`STRAT-02/03`、PIT/OOS、canonical receipts、Promotion、consumer UAT、rollback 与 owner/reviewer 签署不解锁 |
| 2026-08-24 | 第二期 P0 | AUD-03 当前部署候选新鲜 SELECT-only 运营观察 | 当前受控候选 `94abd76e46eeef4a8e21853799c7d69bcd9bbe3b` / version `20260824133504` / OCI `sha256:1c560b5fed14964a008c278a88d9f3e3b144444a172ecc239d06cedbd76d6a3e` 上，公网 `/api/audit/health/`=`200/OK`，operation logs=`555`、failures=`0`、failure rate=`0`、outbox pending/due/claimed/expired/failed/delivered 全为 `0`；同一 `default` alias migration applied=`495`、pending/failed=`0`、graph SHA=`142da62cb866ee9ef2a291bd0f1f0edf527615c3d76cf15a1aae02d1cb191c2a`；快照 [`aud03-operational-observation-select-only-2026-08-24-0736.json`](../deployment/aud03-operational-observation-select-only-2026-08-24-0736.json)，报告 [`00d9f623008af7d51286e6620fad6dee6e87a741f9439156d7830ab8918cee53.json`](../deployment/aud03-operational-observation/00/00d9f623008af7d51286e6620fad6dee6e87a741f9439156d7830ab8918cee53.json) | `missing_section_count=4` 的 alerts/TUI/recovery/archive 明确 unavailable；只读派生，无 migration/claim/publish/Celery/fault/archive/restore/生产写入；`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，`AUD-03` 继续 `waiting_dependency`，AUD-01/AUD-02 与 owner/reviewer 签署不解锁 |
| 2026-08-24 | 第二期 P0 | DATA-03 当前部署候选双 readiness 只读复核 | 对当前受控候选 `94abd76e46eeef4a8e21853799c7d69bcd9bbe3b` / version `20260824133504` / OCI `sha256:1c560b5fed14964a008c278a88d9f3e3b144444a172ecc239d06cedbd76d6a3e` / matrix `6272ea6606ebbf3c0791e48d807b733cbc6d9a4ce7d945d95c5e3a16c22aea64` 执行低频 HTTPS GET：`/api/ready/`=`200/ok`、`/api/decision-ready/`=`503/blocked` 且 `must_not_use_for_decision=true`；`/api/health/`=`200`，匿名 `/api/data-center/providers/`=`403`，canonical smoke=`unknown`；快照 [`data03-readiness-http-get-2026-08-24-0757.json`](../deployment/data03-readiness-http-get-2026-08-24-0757.json)，source SHA=`57ae566d61aa95c7848e8f5b8c1bbd0ae70f10bff6d9cdf43a580780ba728707`，报告 [`8144f224cce8840a8284c64517fbf49b646e56d600235f976f7e423b4b35bf5a.json`](../deployment/data03-readiness/81/8144f224cce8840a8284c64517fbf49b646e56d600235f976f7e423b4b35bf5a.json) | `service_failure_count=0`、`decision_blocker_count=1`、`check_defect_count=17`、`smoke_failure_count=1`；只读观察，不切换、不写生产、不做认证 canonical smoke，`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，`DATA-03` 继续 `waiting_dependency`，仍需 DATA-02 reconciliation、维护窗口和 owner 批准 |
| 2026-08-24 | 第二期 P1 | TUX-02 runtime screen IA copy ownership closure / CI acceptance | 10 个 runtime injection fragment 移除 IA-owned 顶层语义字段，保留 panels/workflow/business behavior；manifest/source guard、metadata/actionability/IA `63 passed`、完整 Workbench `257 passed`、TUI JS `35 passed` 与本地质量门禁通过；首次 push 暴露的 deterministic inventory 漂移与 manifest hash gitleaks 误报已在 `162255e5e` 修正，Architecture/Security/Consistency/Fast Feedback 四条 CI 全绿 | TUX-02 repository exit gate 已在 registry 标记 `completed`；不代表 VPS 部署、生产写入、角色浏览器 UAT、外部 AgomTUI portability、写后 receipt/refresh、14d telemetry、restore/rollback 或 owner/reviewer 双签；EVID-01、M5/TUI-01、TUX-03/TUX-04 仍按各自 fail-closed 门禁推进 |
| 2026-08-24 | 第一期 P0 | EVID-01 当前仓库合同复核 | 当前 HEAD 的 owner/tenant authority、core authenticated composition、Research scope lifecycle/provider/observation、Evidence composition guard 与 v2 inventory 定向回归 `89 passed`；Account/Research component repository/model 合同 `36 passed in 156.20s`；composition guard 扫描 `2931` 个生产文件通过，registry/governance `0` violations，`git diff --check` 通过 | 未发现新的安全 repository slice；本地合同不等于生产 authority。post-0055 VPS 账本仍 zero-seed，真实独立 root approval、生产 PG first-winner/successor/revocation/rollback、same-alias 端到端写读回执及 owner/reviewer sign-off 仍缺，EVID-01 继续 `active`/fail-closed；不重复部署 VPS，global execution/decision deny 不变 |
| 2026-08-24 | 第一期 P0 | EVID-01 VPS 候选与当前 HEAD 漂移审定 | 只读比较部署候选 `94abd76e46eeef4a8e21853799c7d69bcd9bbe3b` 与当前 HEAD `5c6e4da5087c39ccdbaa612f6599a0b3b392f0a8`：`git diff --name-only` 共 `31` 个文件，全部为 `docs/`、`governance/` 或 `.gitleaks.toml`，`apps/`、`config/`、`core/`、`scripts/`、`tests/` 均为 `0`；当前分支与 origin 同步，四条 push CI 全绿 | 证明当前 HEAD 相对 VPS 候选没有生产/runtime 代码漂移；不重新部署、不写生产、不改变候选绑定。EVID-01 仍因 13 张账本 zero-seed、独立 root approval、生产 PG first-winner/successor/revocation/rollback、same-alias 端到端回执和 owner/reviewer sign-off 缺失而 `active`/fail-closed |
| 2026-08-24 | 第二期 P0 | TAR-01/AUD-03 当前公网只读健康复核（09:35 UTC） | 低频 HTTPS `GET` 仅观察：`/api/health/`=`200`、`/api/ready/`=`200`（database/Redis/Celery/critical data=`ok`、decision-data=`warning`）、`/api/audit/health/`=`200/OK`、匿名 `/api/tui/`=`403`；`/api/decision-ready/`=`503`、`must_not_use_for_decision=true`、`decision_runtime_blocked`；工件 [`tar01-public-health-readonly-recheck-2026-08-24-0935.json`](../deployment/tar01-public-health-readonly-recheck-2026-08-24-0935.json)，SHA-256=`41f4604302b1ab4b5a9d2425fcb9713833636fb91436453722694ecfc9aeaa4e` | 未部署、未迁移、未启用队列/Worker、未回滚、未写生产，公共响应未提供候选绑定；不构成 TAR-01/TAR-05 capacity/provider/chaos/角色 UAT/14 日 telemetry/restore-rollback/owner-reviewer 证据，相关门禁继续 fail-closed |
| 2026-08-24 | 第二期 P0 | DATA-01 最新归档本机隔离 restore 验收 | 只读下载既有归档后在 disposable `postgres:16-alpine` 中完成 source→restore 自洽校验：`142813695` bytes、SHA-256=`7eb67da66bb6d3c550bc35f96abbc2c38ea403f776c56602316e83b912b4fd6d`、`7189` 个非注释 restore entries；`539/539` public 表、`72/72` Data Center migrations、`460/460` sequences，schema/逐表内容/sequence 差异均为 `0`；restore `689.563s`、verification `628.355s`、total `2214.035s`；精简证据 [`data01-local-isolated-restore-2026-08-24.json`](../deployment/data01-local-isolated-restore-2026-08-24.json)，canonical recorder artifact [`e7af4216ed86cdd63a62d84d5a38ef5bcc28ee255e82490611f673bb945ebe9d.json`](../deployment/data01-isolated-restore/e7/e7af4216ed86cdd63a62d84d5a38ef5bcc28ee255e82490611f673bb945ebe9d.json)，artifact SHA-256=`e7af4216ed86cdd63a62d84d5a38ef5bcc28ee255e82490611f673bb945ebe9d` | 仅证明本机隔离恢复自洽；restore/源库及容器临时归档已清理，recorder 为离线读取且 `production_claim=false`，耗时不能冒充生产 RTO/RPO；未执行生产 restore/DDL、维护态 rollback、controlled backfill、reconciliation 或 owner/reviewer 签署，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-24 | 第一期 P0 | EVID-01 post-0055 候选低频只读复核（13:04–13:05 UTC） | SSH 只读复核同一 `94abd76e…` / `agomtradepro-web:20260824133504`：Web、Celery worker/beat、PostgreSQL、Redis、Caddy 运行；公网 `/api/health/`、`/api/ready/`、`/api/audit/health/` 均 `200`，`/api/decision-ready/`=`503` 且 `must_not_use_for_decision=true`；Account `0050–0055`、Research `0029` 已应用，同一 `default` alias 的 13 张 authority/evidence 表逐表 `SELECT COUNT(*)` 均为 `0`；快照 [`evid-01-authority-inventory-snapshot-2026-08-24-recheck-2105.json`](../deployment/evid-01-authority-inventory-snapshot-2026-08-24-recheck-2105.json)，报告 [`39760173ab5aa8e4adfab03d088c62519e22e5b6cea40d78eaf2d5d0befd6372.json`](../deployment/evid-01-authority-inventory/39/39760173ab5aa8e4adfab03d088c62519e22e5b6cea40d78eaf2d5d0befd6372.json) | 新鲜只读运行事实，报告固定 `blocked_zero_seed_authority`、`authority_ready=false`、`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`；未 seed/审批/写库/rollback，不解除 EVID-01、决策总闸或 owner/reviewer sign-off |
| 2026-08-24 | 第一期 P0 | EVID-01 当前 HEAD 与受控候选代码漂移更正 | 当前 HEAD `3900a064cf1e…` 相对 `94abd76e…` 含 `apps/` `9`、`config/` `1`、`tests/` `2` 个 TUX-02 repository 文件，另有文档/治理/生成物变化；因此 TUX-02 虽已通过仓库与 CI 验收，尚未在 VPS 候选上重新部署。按用户“不频繁部署”约束保持候选不变 | 不把本地 TUI 代码冒充生产版本；EVID-01、TAR-01、M5/TUI-01 的生产 UAT、写回执、容量/恢复/14 日 telemetry、rollback、owner/reviewer 仍按 fail-closed 规则等待 |
| 2026-08-24 | 第二期 P0 | DATA-03 当前候选认证 canonical Data Center smoke | 同一受控候选 `94abd76e…` / `20260824133504` / OCI `sha256:1c560b5…d6a3e` / matrix `6272ea66…aea64` 上，认证只读 `GET /api/ready/`=`200/ok`、`GET /api/decision-ready/`=`503/blocked` 且 `must_not_use_for_decision=true`；认证 `GET /api/data-center/providers/`=`200` 返回 2 条脱敏 provider，status=`200` 返回 15 条 capability，其中 8 条 `must_not_use_for_decision=true` 且含 `stale/degraded`，canonical provider-status smoke 按失败记录。快照 [`data03-readiness-authenticated-smoke-2026-08-24-1335.json`](../deployment/data03-readiness-authenticated-smoke-2026-08-24-1335.json)，source SHA=`c1df5cf05b81b26cdba86d51acb9a74c4836b986548adae1f5177d6154f121f`，报告 [`55f20b1348564daf6dea93f23aecc229953954e6fcc1859f40180fbebea84d98.json`](../deployment/data03-readiness/55/55f20b1348564daf6dea93f23aecc229953954e6fcc1859f40180fbebea84d98.json) | 只完成认证 canonical smoke 的低频 SELECT/HTTPS 观察，报告固定 `service_failure_count=0`、`decision_blocker_count=1`、`smoke_failure_count=1`、`production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`；未写生产、未切换维护态、未做 DATA-02 reconciliation/M9/M10/观察窗口或 owner/reviewer 签署，`DATA-03` 继续 `waiting_dependency` |
| 2026-08-24 | 第二期 P0 | DATA-02 当前候选 `fund.nav` SELECT-only reconciliation | 同一受控候选 `94abd76e…` / `20260824133504` / OCI `sha256:1c560b5…d6a3e` / matrix `6272ea66…aea64` 内，SSH 只读 ORM 查询 `fund_net_value` 与 canonical `data_center_fund_nav_fact(source=fund_legacy_repo)` 各 `7,648` 条；两侧 snapshot hash 均为 `c733f383…ddbed92`，报告 `same=7,648`、其余差异/缺陷均为 `0`。原始 envelope [`data02-reconciliation-candidate-2026-08-24.json`](../deployment/data02-reconciliation-candidate-2026-08-24.json)，source SHA=`e0232b4587be7188103c4a1a51b3947a1da97baa7226c6a010896d10e2407b73`，canonical artifact [`65935870cc4002c1e96fb0ab2473ee679b6b1540318aa72f2155a95d47db43dc.json`](../deployment/data02-reconciliation/65/65935870cc4002c1e96fb0ab2473ee679b6b1540318aa72f2155a95d47db43dc.json) | 仅证明 `fund.nav` 当前快照一致；artifact 固定 `production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`，未执行 backfill/写表/维护态/生产 restore-rollback，不覆盖全量 coverage/freshness、DATA-01 前置或 owner/reviewer 门禁，`DATA-02` 继续 `waiting` |
| 2026-08-24 | 第二期 P0 | TAR-05 当前候选认证 reserved-route staircase | 同一受控候选 `94abd76e…` / release `20260824133504` / image `sha256:1c560b5…d6a3e` 以 account session+CSRF 做预检与 `1/5/10/20` 阶梯，共 `36` 次；全部返回 `503 DISPATCH_UNAVAILABLE`、`queued_runtime_not_wired`、`Retry-After=60`；前后 health/ready/audit 均稳定，audit=555 logs、0 failures、backlog 全 0，TUI catalog=`886/890/23`。工件 [`tar01-current-reserved-route-observation-2026-08-24-94abd76e.json`](../deployment/tar01-current-reserved-route-observation-2026-08-24-94abd76e.json)，SHA=`23fc37fee8d54ab9f8f53252ca4d37753db83afbc52a336dfe8f68e235e6b8cd` | 仅证明当前候选的认证边界与安全 fail-closed；`capacity_ready=false`、`runtime_enablement=not_authorized`，未启用 queued/Worker、未测 durable admission/SSE/idempotency/cancel/provider-MCP/capacity/chaos/14d telemetry/restore-rollback、未做角色 UAT 或 owner/reviewer 签署；TAR-01/TAR-05 继续 fail-closed，不重复部署 |
| 2026-08-24 | 第二期 P0 | DATA-02 当前候选 coverage/freshness 只读复核 | 同一受控候选 `94abd76e…` / release `20260824133504` / image `sha256:1c560b5…d6a3e` 上认证只读读取 `active_a_share`：active stock=`5,533`，universe quality=`ok`；price/valuation/financial fact coverage 均 `5,533/5,533`，但 canonical publication 分别为 `0/5,533`、缺失、`1/5,533`，全部 `must_not_use_for_decision=true`；`/api/ready/`=`200 warning`，`/api/decision-ready/`=`503 blocked`。工件 [`data02-coverage-freshness-observation-2026-08-24-94abd76e.json`](../deployment/data02-coverage-freshness-observation-2026-08-24-94abd76e.json)，SHA=`bf78a00e45357b6e61f46e1f96f68ee7fed4fac9648c90dca55387fe4f9fdfeb` | 只完成覆盖/新鲜度事实采集，未回填、未改配置、未写生产、未切维护态、未做 DATA-02 全量 reconciliation/M9/M10 或 owner/reviewer 签署；publication 不完整使 DATA-02 继续 `waiting_dependency`，decision-ready 与 runtime enablement 保持 fail-closed |
| 2026-08-24 | 第一期 P0 | EVID-01 authority/composition focused revalidation | 当前 HEAD 的 Account owner/tenant authority、Research scope selector/lifecycle/provider、same-alias Evidence facade 与 audit-authority focused unit contracts `93 passed in 2.83s`；覆盖 root/successor/current-head、身份替换和缺 provider fail-closed | 仅证明本地合同稳定，不创建 authority seed、不部署/写生产；VPS 13 张 authority/evidence ledger 仍 zero-seed，生产 PostgreSQL first-winner/successor/revocation/rollback、独立 root approval、端到端回执与 owner/reviewer 签署仍缺，`EVID-01` 继续 `active`/fail-closed |
| 2026-08-24 | 第一期 P0 | EVID-01 受控候选与当前 HEAD 漂移最新复核 | 当前 HEAD=`271513f2a` 与部署候选=`94abd76e…` 的文件级差异共 `59` 个，其中非文档/治理仅 `apps/` `9`、`config/` `1`、`tests/` `2`，均为 TUX-02 TUI runtime injection/manifest/test；Evidence、Account、Research、Data Center 和 runtime backend 无生产代码漂移 | 不把当前 HEAD 冒充已部署版本，按“不频繁部署”约束不重部署；该比较不解除 EVID-01 zero-seed authority、root approval、PG race/revocation/rollback、端到端回执或双签门禁 |
| 2026-08-25 | 第一期 P0 | EVID-01 owner/tenant authority same-alias guard | `GetCurrentOwnerTenantAuthorityV1` 暴露 repository `unit_of_work_key`；authenticated owner/tenant → Evidence bridge 在组装 selector/Evidence repositories 前拒绝 authority reader 与请求 `django:{using}` 不一致；新增 alias-mismatch 回归，Account/Research/Core/Audit focused `94 passed`，Black/isort/Ruff、增量 mypy 与 debt ceiling 通过 | 仅完成本地 typed composition/fail-closed contract；未 seed authority、未生产写入/部署、未做人工批准；VPS 13 张 authority/evidence ledger 仍 zero-seed，EVID-01 继续 `active`，global execution/decision deny 与 decision-ready 503 不变 |
| 2026-08-25 | 第一期 P0 | TAR-01 当前 legacy inline runtime gate observation | 授权后在候选 `94abd76e…` / release `20260824133504` / OCI `sha256:1c560b5…d6a3e` / runtime manifest SHA=`1988746a1b333810981705a3d34c83a64760fb87354f763125f592a7862ea08f` 上用认证会话发送 2 次 bounded `POST /api/terminal/chat/`；inline enabled、concurrency=`1`、queued intake/worker 与 runtime authorization=`false`，health/ready=`200`，web restart=`0`；两次均由 `DecisionRuntimeGateMiddleware` 在 view 前返回 `503 decision_runtime_blocked`，provider/MCP 未调用；15:12 UTC 的追加只读 GET 复核仍为 health/ready=`200`、decision-ready=`503 blocked`，且 core coverage=`incomplete`；证据 [`tar01-current-inline-runtime-gate-observation-2026-08-25-94abd76e.json`](../deployment/tar01-current-inline-runtime-gate-observation-2026-08-25-94abd76e.json)，SHA=`db0a6fe9db1585d2d649fafe98f827c8a7b9931040ed44fa83baa25885de5fb9` | 这是全局 fail-closed 与候选开关的真实观察，不是容量基线；真实 inline 延迟、Daphne/Redis/DB/model/MCP 指标 unavailable，5/10/20 阶梯及完整单槽 baseline 未执行；`capacity_ready=false`、safe baseline=`unknown_not_zero`，不放宽 global deny、不启用队列/Worker、不提升并发、不推进 TAR-02 |
| 2026-08-26 | 第一期 P0 | AUD-01 / TAR-01 decision-ready 恢复前置修复 | 修复 `sdk/agomtradepro_mcp/audit.py`：嵌入式 audit sink 与网络路径统一携带 `delivery_id`，SDK audit focused `12 passed`；恢复路线同步写入审计、数据可靠性与 terminal runtime 计划，registry 版本更新为 `2026-08-26.v1` | 仅完成本地 delivery identity 契约，不等于 durable final-acceptance receipt、authority、DATA-02/03 publication/reconciliation 或 decision-ready 恢复；未部署、未写生产、未清除 runtime blocker，inline=`1`、queued/worker disabled、global deny/fail-closed 保持 |
| 2026-08-26 | 第一期 P0 | TAR-01 production blocker 只读复核 | 只读 SSH/容器 GET 复核候选 `94abd76e…`：web healthy；health/ready=`200`，audit health=`200/OK` 且四项检查 OK；decision-ready=`503`，runtime blocker 仍为 `decision_runtime_blocked` / `MCP audit evidence write failed during final acceptance`，core coverage=`incomplete`、provider capabilities=`blocked` | 近 72 小时日志无新的 audit failure；这是持久化 readiness/证据链 blocker，不是容量数据；本地 delivery-id 修复尚未部署，未清除状态、未写生产、未重启、未扩流，`capacity_ready=false`、safe baseline=`unknown_not_zero` |
| 2026-08-26 | 第一期 P0 | TAR-01 current branch deployment and post-deploy verification | 已授权将 `dev/next-development@45d7616d…` 以 upgrade 发布为 release `20260826135953`、image `sha256:c481bb88…a1ce66`；release identity、web/worker/beat 同镜像、health、schema、备份和 deployment-owned catalog sync 通过；迁移计划为 `No planned migration operations`，未做 authority seed/backfill/decision repair；结构化证据见 [`tar01-current-vps-deployment-acceptance-2026-08-26-45d7616d.json`](../deployment/tar01-current-vps-deployment-acceptance-2026-08-26-45d7616d.json) | post-deploy `/api/health/`、`/api/ready/`、`/api/audit/health/`=`200`，审计 failures/outbox=`0`，inline=`1`、queued/runtime authorization 仍关闭；`/api/decision-ready/` 仍 `503`（`decision_runtime_blocked`、core coverage incomplete、provider capabilities blocked）；这是部署与 fail-closed 复核，不是实际 inline 容量基线，`capacity_ready=false`、safe baseline=`unknown_not_zero`，不推进 TAR-02 |
| 2026-08-27 | 第一期 P0 | AUD-01 canonical publisher/runtime/authority repository exit | 完成 append-only durable delivery receipt、唯一 production publisher、same-alias outbox UOW、Config Center fail-closed runtime selector、Account authenticated scoped authority 与 production composition；最终 Audit/Config unit `290 passed`、system-audit component `39 passed`，11 个生产文件增量 mypy 与 debt ceiling、Black/isort/Ruff、migration check、Celery/audit contract、架构及治理门禁全绿；发现并消除 `core/integration` 对 Infrastructure 的 6 个新增导入，未抬高治理基线 | 未获得专用 PostgreSQL 并发测试库，PG first-winner/concurrency 与 migration/rollback/backlog/告警/TUI/archive/restore 仍归 AUD-03；未部署、未写生产。机器注册表与本索引已把 AUD-01 置为 completed、AUD-02 置为唯一 active repository focus |
| 2026-08-27 | 第一期 P0 | AUD-02 Data Reliability same-UOW/replay repository exit | Macro/Price/Quote 以 server-issued identity 在同一 UOW 提交 fact、RawAudit、publication/member、20 类 typed event/outbox；补齐 validation/failover exhaustion、provider recovery、freshness、conflict、rollback、quality、decision-read、repair parent 与 exact replay，degraded quality 缺事件按 silent loss 拒绝；审计 registry 一次性晋级 `20 active/wired`。最终 Audit/Data Center unit `1449 passed`，component `231 passed, 8 PG opt-in skipped`，59 个生产文件增量 mypy、full debt、2987 文件架构、governance、Celery/current-data/audit contracts 与 migration dry-run 全绿 | 未设置专用 `DATABASE_URL`，migration graph 与生产 PostgreSQL/migration/rollback/backlog/recovery/alerts/TUI/archive/restore/双签未验证，均归 AUD-03；未部署、未写生产。机器注册表将 AUD-02 置为 completed、AUD-03 置为 awaiting_production，并把唯一 repository focus 切换至 TUX-04 |
| 2026-08-28 | 第二期 P1 | TUX-04 IA/术语/runtime screen/普通角色 repository exit | `research-tools` 拆分为投资研究、AI 工作台、个人服务接入、个人设置；入口消歧并固化宏观象限/提示词/观测时间/数据基准日词汇；12 个 runtime screen 与 `execution.audit` 契约对齐。隔离普通角色浏览器显示 15 screens，4 个代表入口可达，直达 `capability-router.admin-access` 明确 403/无权访问；证据 [`tux04-repository-closure-evidence-2026-08-28.json`](../testing/tux04-repository-closure-evidence-2026-08-28.json)。TUI JS `35 passed`、focused `64 passed`、Workbench `258 passed`、Terminal Agent `20 passed`，source guard `12/24 screens`、`430/890 actions`、0 violations，mypy/debt/格式门禁全绿 | 本地隔离 SQLite 走查不等于生产 UAT；published graph/manifest 哈希已改变但未部署、未写生产、未做外部 portability 或 M5 candidate rebind。TUX-04 置为 completed，唯一 repository focus 推进至 TUX-03，TUX-05 继续等待 |
| 2026-08-28 | 第二期 P1 | TUX-03 action copy/density/主任务可达 repository exit | 430-action 基线全部对账：17 条冗余注册退役，413 published / 871 runtime actions 保留；route key 暴露、样板/机翻文案、重复 label、route default/panel 引用与 screen/group density debt 全部为 0，runtime normalization 只改 published-screen action tier、真实降级 143 条且幂等。普通角色隔离浏览器走查 15 screens 与 6 个代表工作区；P0 创建账户 panel 的 advanced 表单可见聚焦且未误发写请求，required-read panel 亦有零请求回归。证据 [`tux03-repository-closure-evidence-2026-08-28.json`](../testing/tux03-repository-closure-evidence-2026-08-28.json)。TUI JS `37 passed`、focused `125 passed`、Workbench `258 passed`、Terminal Agent `20 passed`，source/density/validator/static/migration/mypy/debt/格式门禁全绿 | 本地 SQLite 首页并发读取曾产生可追踪的 503 恢复态；未翻译字段、broker-order 占位和 freshness/状态栏/内部 key 继续归 TUX-05。未部署、未写生产、未做外部 portability 或 M5 candidate rebind。TUX-03 置为 completed，唯一 repository focus 推进至 TUX-05 |
| 2026-08-28 | 第二期 P1 / 第一期 P0 corrective | TUX-05 presentation repository exit；AUD-02 regression reopen | 表格局部滚动/日期与行操作单行、空 pager、重复创建说明、broker alias、状态栏、19 个字段翻译、可见内部 key 与 freshness 投影全部闭合；8 个代表 screen 截图及 SHA 固化于 [`tux05-repository-closure-evidence-2026-08-28.json`](../testing/tux05-repository-closure-evidence-2026-08-28.json)。TUI JS `41 passed`、focused `130 passed`、Workbench `308 passed`、Terminal Agent `20 passed`，presentation `19/34/0/0`，mypy/debt/架构/治理全绿。Windows current-data runner 增加 28K 顺序分批后，首批 242 nodeid 展开 300 项暴露 `268 passed / 29 failed / 3 errors` 的既有 AUD-02 eager audit-runtime 配置回归；TUX 新增 4 nodeid 独立全绿 | TUX-05 completed，未部署/写生产/重绑 M5；为避免保留虚假的完成状态，AUD-02 重新 active、AUD-03 回到 waiting_dependency。下一步只在 AUD-02 内补失败测试与最小 owner-boundary 修复，完整 298-nodeid registry 全绿后才重新验收 |
| 2026-08-28 | 第一期 P0 corrective | AUD-02 cross-App regression corrective exit | On-Demand sync use case 改为 hydration 时解析 typed factory，纯读 Equity 构造不再提前加载 Audit runtime；Account 测试 helper 显式补齐三项 critical Audit 定义，无 production fallback。完整 current-data registry 298 nodeid 展开 `301 + 55 passed`，原 Audit/Data Center unit `1449 passed`、component `231 passed, 8 PG opt-in skipped`；证据 [`aud02-corrective-repository-closure-evidence-2026-08-28.json`](../testing/aud02-corrective-repository-closure-evidence-2026-08-28.json) | AUD-02 completed、AUD-03 awaiting production；无 dependency-ready repository unit，focus 显式为 null。未部署/生产写/runtime 激活/外部调用；EVID-03 继续等待 EVID-01/02 |
| 2026-08-29 | 第三期 P0 | `main` source candidate 冻结与生产只读 preflight | 冻结 `origin/main@07d96d6c…`，matrix/graph/runtime manifest SHA 分别为 `e3027671…69cd5d`、`63be10ee…532a2`、`bfa8eeb8…e6fe`；VPS verifier 证明当前生产仍为 `45d7616d…` / `20260826135953` / image `sha256:c481bb88…a1ce66`，TLS、web、迁移、schema、TUI registry、Qlib、Celery 均绿；公网 health/ready/audit=`200`，decision-ready=`503 blocked`；证据 [`release-candidate-preflight-2026-08-29-07d96d6d.json`](../deployment/release-candidate-preflight-2026-08-29-07d96d6d.json)，SHA=`a739324bf672fe68b15f60c4a767c3075444ed97d2d9859d6ff4a736244061d8` | source candidate 尚未部署/重绑/启动观察；M5 readiness=`DENY`。Python 版本门禁发现 `docker/Dockerfile.qlib-train` 仍为 3.10，须先获批有界 repository remediation、CI/合并并重新冻结；未部署、重启、迁移、备份、生产写入、负载、故障注入或 rollback，TUI-01/TAR-05 保持 `awaiting_production` |
| 2026-08-29 | 第三期 P0 remediation | Qlib 训练镜像 Python 3.11 repository exit | `docker/Dockerfile.qlib-train` 升级为 `python:3.11-slim`，运行时文档同步收敛到 3.11，新增 Python/`pyqlib`/`libgomp1` 与 Docker context 回归断言；版本一致性、Compose config、BuildKit `--check` 全绿，聚焦测试 `45 passed`；隔离容器用 Python 3.11.14 成功安装 `pyqlib=0.9.7` CPython 3.11 wheel，错误的 `qlib` distribution 不存在。证据 [`release-candidate-remediation-2026-08-29-py311.json`](../deployment/release-candidate-remediation-2026-08-29-py311.json)，SHA=`1156d941e429e79262bef23ab327d3492033e436bc8c741e1cf0d1bbcb45437d` | 完整依赖安装因 Docker exit `137`、完整仓库 context build 因本地未跟踪数据超过 `1.04 GB` 未完成，保留给 CI/受控构建。未访问/部署/写生产；`07d96d6d…` 不再是最终候选，须提交、CI、review、合并并重新冻结新 `main`，TUI-01/TAR-05 仍 `awaiting_production`、M5 仍 `DENY` |
| 2026-08-29 | 第一期 P0 auto-collect | 当前生产候选 EVID-01/02 SELECT-only 账本重绑定 | 当前运行身份复核为 `45d7616d…` / `20260826135953` / image `sha256:c481bb88…a1ce66`；同一 `default` PostgreSQL alias 的 `REPEATABLE READ READ ONLY` 事务确认 Account `0050–0055` 已应用，13 张 authority/evidence 表和 EVID-02 operator/approval/activation 三表均为 `0` 行。原始 bundle [`evid-01-evid-02-select-only-bundle-2026-08-29-45d7616d.json`](../deployment/evid-01-evid-02-select-only-bundle-2026-08-29-45d7616d.json)，SHA=`c1952a52ed9702cd06d6793aff1f6e99e45c1af04530d22db6293e421ec4386d`；派生报告 SHA 分别为 `6bb8ff13…0959d`、`3a831184…644a` | EVID-01 仍 `blocked_zero_seed_authority`，EVID-02 approval/activation head 仍 `empty`、human approval 未采集；未部署/迁移/写生产/执行并发或 rollback/代签。EVID-01/02 保持 `awaiting_production`，下一门仍需真实 root/reviewer approval、生产 PG first-winner/successor/revocation/rollback、same-alias 回执与双签，全局 deny 不变 |
| 2026-08-29 | 第二期 P0 auto-collect | 当前生产候选 STRAT-01 owner-ledger 重绑定 | `45d7616d…` / `20260826135953` / image `sha256:c481bb88…a1ce66` 上以 `REPEATABLE READ READ ONLY` 盘点：Research R1–R8 `65` 表、Portfolio R4/R5/R8 `7` 表、Account authority/assignment `16` 表、owner/policy/operator/assignment `35` 表均为零行。快照 [`strat-01-owner-ledger-readonly-recheck-2026-08-29-45d7616d.json`](../deployment/strat-01-owner-ledger-readonly-recheck-2026-08-29-45d7616d.json)，SHA=`7361711a11d252250c4bf523fbb4741cf41bdd5ab7785df69ef818fb9b7ac9c6`；派生报告 SHA=`1f9d9730…98d97`、outcome=`zero_seed` | 未创建/修改/删除/回填/promotion/审批，也未将 Data Center facts 反充 owner evidence；STRAT-01 继续 `awaiting_production`，真实 owner/definition/policy/calendar/scope/qualification 仍需业务 owner，STRAT-02/03 依赖不变 |
| 2026-08-29 | 第一期 P0 auto-collect | 当前生产候选 AUD-03 operational observation 重绑定 | `45d7616d…` / `20260826135953` / image `sha256:c481bb88…a1ce66` 上单个 PostgreSQL `REPEATABLE READ READ ONLY` 事务确认 495 项迁移全部 applied、pending/failed=`0/0`，migration graph SHA=`c552b6fe…559d`；operation logs/failures=`563/0`，outbox 六类 backlog 均为 0。原始 envelope [`aud03-operational-observation-select-only-2026-08-29-45d7616d.json`](../deployment/aud03-operational-observation-select-only-2026-08-29-45d7616d.json)，source SHA=`017ff085…a0d7`；派生报告 SHA=`526973c4…b68f` | 仅完成身份、迁移图、audit/outbox 与公网 health 的 SELECT/HTTPS 观察；alerts/TUI/recovery/archive 明确 unavailable，报告保持 `production_claim=false`、`production_ready=false`、`runtime_enablement=not_authorized`。未部署/写生产/故障注入/recovery/archive/restore/代签，`AUD-03` 继续 `awaiting_production` |
| 2026-08-29 | 第二期 P0 auto-collect | DATA-01 当前生产恢复点与本地隔离 restore 重绑定 | 只读下载 8 月 26 日部署已有 `/opt/agomtradepro/backups/database/postgres-20260826-081700.dump`，远端/本地 `144285484` bytes、SHA=`dc000ab2…d4fa`，远端 manifest=`7235`；证据 [`data-backup-evidence-2026-08-29.json`](../deployment/data-backup-evidence-2026-08-29.json)。同一 dump 在 disposable PostgreSQL 16 做 source→restore 精确复核，source/restore 均为 `541` 表、`72` 项 Data Center migrations、`462` sequences，schema SHA=`028087c0…8d81`，所有 missing/extra/changed 为 0；原始报告 [`data01-current-backup-restore-2026-08-29-45d7616d.json`](../deployment/data01-current-backup-restore-2026-08-29-45d7616d.json)，canonical artifact SHA=`e13d1058…6870` | 只证明现有恢复点的传输、格式与本地隔离自洽；未新建/prune 远端备份、未触碰生产库、维护态、live rollback、RTO/RPO、backfill/reconciliation 或人工签字。临时库/文件已清理，`DATA-01` 继续 `awaiting_production`，`DATA-02/03` 不解锁 |
| 2026-08-29 | 第三期 P0 | 合并后 `main` source candidate 重新冻结 | Qlib Python 3.11 修复 `86498b1f9` 与 canonical evidence PR #10 均完成 CI/review/merge；新 `main@09269c14…` 自身 Architecture/Security/Consistency/Fast Feedback 四条 push CI 全绿，版本一致性恢复 `PASSED`。source binding 的 matrix/graph/runtime manifest SHA 为 `e3027671…69cd5d`、`63be10ee…532a2`、`bfa8eeb8…e6fe`；只读 VPS verifier 仍识别生产 `45d7616d…` / `20260826135953` / `sha256:c481bb88…a1ce66`，health/ready/audit=`200`、decision-ready=`503 blocked`。证据 [`release-candidate-preflight-2026-08-29-09269c14.json`](../deployment/release-candidate-preflight-2026-08-29-09269c14.json)，SHA=`466b06878229fdead920ad5cf31de5a09bcd18d79cb5a1321271fa432b095ff3` | source freeze 已完成但尚无该提交的 release/OCI identity；M5 readiness 仍 `DENY`，TUI-01/TAR-05 仍 `awaiting_production`。未部署/重启/迁移/备份创建/生产写入/UAT 回执/观察/负载/fault/rollback；下一步仅在精确授权后做保留 PostgreSQL/Redis 的受控 code-only deployment 与 post-deploy candidate binding |
| 2026-08-29 | 第三期 P0 production checkpoint | `09269c14` candidate deployment and canonical binding | 精确授权下先创建并下载校验 PostgreSQL backup `postgres-20260829T083336Z.dump`（`146273315` bytes，SHA=`a5c77b8c…2b30a`，未 prune），再从 detached clean worktree 以 source-upload `upgrade` 部署 `09269c14…` 为 release `20260829163806` / image `sha256:08650701…884da`；应用 `audit.0013_systemauditdeliveryreceipt`，TUI registry `30` 发布，独立 expected-commit verifier 的 TLS/health/Django/migration/schema/TUI/Qlib/Celery/backup/resource 检查全绿。证据 [`release-candidate-deployment-2026-08-29-09269c14.json`](../deployment/release-candidate-deployment-2026-08-29-09269c14.json)，SHA=`8e7646b373812739d621bc2afdac5a9ed648936d9e48d07f1618f2e18d7108d6`；M5 preflight SHA=`c28f3ebc…80d0` | canonical cutover evidence 已重绑新 commit/release/OCI/matrix/graph/runtime identity，旧候选 UAT/telemetry/approval 不继承；`/api/ready/`=`200` 且 decision-data=`ok`，但 `/api/decision-ready/` 仍 `503`、`must_not_use_for_decision=true`。未启用 queued runtime、未做 role UAT/write receipt、load/chaos、restore/live rollback 或代签；TUI-01、TAR-05、AUD-03、DATA-01 均继续 `awaiting_production`，M5 继续 `DENY` |
| 2026-08-29 | 第一期 P0 auto-collect | `09269c14` EVID-01/02 SELECT-only 重绑定 | release `20260829163806` 上同一 `default` PostgreSQL `REPEATABLE READ READ ONLY` 快照确认 Account `0050–0055` 已应用，13 张 authority/evidence 表与 operator/approval/activation 表仍为零；EVID-01 artifact [`23bbda2b…7693f.json`](../deployment/evid-01-authority-inventory/23/23bbda2b41c8fc22e6cb27b74b9f3f35ea1ad59dfdb9a69125aca9702787693f.json) outcome=`blocked_zero_seed_authority`，EVID-02 artifact [`67edbdc6…00dad.json`](../deployment/evid-02-head-audit/67/67edbdc604cd60e25980b0bf89566f99e9314e2b937919bdeba88a7cfc900dad.json) 的 approval/activation head=`empty` | 仅候选绑定 SELECT 事实；未 seed/approval/activation、未做生产 PG race/rollback 或代签，EVID-01/02 继续 `awaiting_production`，全局 execution/decision deny 不变 |
| 2026-08-29 | 第二期 P0 auto-collect | `09269c14` STRAT-01 owner-ledger 重绑定 | release `20260829163806` 上 Research R1–R8 `65` 表、Portfolio R4/R5/R8 `7` 表、Account authority/assignment `16` 表、owner/policy/operator/assignment `35` 表仍全部零行；原始 SHA=`263f9813…53c7`，canonical artifact [`65fb8e2f…64a79.json`](../deployment/strat-01-owner-ledger-inventory/65/65fb8e2f45db6ddcea7bc19e4b96accf41764598644fd8b37d9abcec0e864a79.json) outcome=`zero_seed` | 未创建/修改 owner definition、policy、calendar、scope、qualification、promotion 或 approval；STRAT-01 继续 `awaiting_production`，须由命名业务 owner 提供并批准真实输入 |
| 2026-08-29 | 第一期 P0 auto-collect | `09269c14` AUD-03 部署后只读观察 | release `20260829163806` 上 496 项 migration 全 applied、pending/failed=`0/0`，graph SHA=`02406e0a…0496`；operation logs/failures=`563/0`，outbox 六类 backlog 均为 0。原始 SHA=`969a2313…60ff`，canonical artifact [`34f3b1a7…2262.json`](../deployment/aud03-operational-observation/34/34f3b1a70283c491a9fbc8a20c4686e5d51315bac8be3cb5ead34bc2f07d2262.json) | alerts/TUI/recovery/archive 明确 unavailable；未做 rollback/fault/recovery/archive/restore 或代签，AUD-03 继续 `awaiting_production`，单点健康观察不构成 production readiness |
| 2026-08-29 | 第二期 P0 auto-collect | `09269c14` DATA-01 新恢复点本地隔离 restore | 预部署 dump `146273315` bytes、SHA=`a5c77b8c…2b30a`、restore entries=`7220`；disposable PostgreSQL 16 中 source/restore 均为 `541` tables、`72` Data Center migrations、`462` sequences，schema SHA=`028087c0…8d81` 且全部差异为零；原始报告 [`data01-current-backup-restore-2026-08-29-09269c14.json`](../deployment/data01-current-backup-restore-2026-08-29-09269c14.json) SHA=`d7c438eb…20ca`，canonical artifact [`a8da9c32…b121.json`](../deployment/data01-isolated-restore/a8/a8da9c326c130ec5be19acbc525382178c258075c6aa55531e86c560552bb121.json) | 临时库/容器已清理；仅证明本地隔离恢复自洽，719.811s restore 与 1864.578s total 不冒充生产 RTO/RPO。未做 maintenance/live rollback/backfill/reconciliation/代签，DATA-01 继续 `awaiting_production`，DATA-02/03 不解锁 |
| 2026-08-29 | 第三期 P0 production checkpoint | release `20260829163806` role browser UAT 与精确 cleanup receipts | 候选 commit/release/image/OCI 无漂移；现有 regular/operator/admin 角色通过 108/108 route-page 解析、动态最小权限 direct-read、operator 边界与 9/9 参数化 read。策略 `#8` 与 inactive personal provider `#16` 均经 create/update/readback，随后按 run/name/owner/PK 各精确删除 1 条且剩余 0；provider fixed test 通过。结构化证据 [`web_to_tui_production_uat_checkpoint_20260829163806.v1.json`](../../config/tui/migration/evidence/web_to_tui_production_uat_checkpoint_20260829163806.v1.json)，SHA=`970193d0…28ab` | 固定账户 create/cancel 链路连续两次按钮不可点击；策略写回成功但 UI 在固定窗口内仍停留“读取数据”。未运行 active RSS/shared quota/pending approval/factor/backtest/external AI/Terminal 批次，未启用 queued runtime、load/fault/maintenance/live rollback；role owner 确认仍缺。canonical `uat` 保持空，TUI-01 继续 `awaiting_production`、M5=`DENY` |
| 2026-08-29 | 第二期 P1 corrective reopen | TUX-05 action/settlement/production-safe recorder | 用户授权 TUX-05 重新成为唯一 repository focus；范围固定为账户 action panel 可见/点击、confirmed write 完成态与 TUI-01 production-safe recorder，仍要求 108 routes、三角色、唯一写回执、readback、exact cleanup/零残留 | 完成代码/测试/真实本地浏览器/治理后再 commit/CI/review/merge；新提交必须从独立 clean worktree 部署并重跑生产 UAT。external AI、queued runtime、authority/approval、active RSS/shared quota、load/fault/maintenance/live rollback 仍禁止 |
| 2026-08-29 | 第二期 P1 corrective exit | TUX-05 action/settlement/production-safe recorder | F9 展开 support/advanced task 并验证账户 create/cancel submit 可见可点；confirmed mutation 真实返回后发布“操作完成”；strategy/provider delete 的未渲染模板响应在 adapter 边界显式 render。首轮完整本地 profile 因显式 `signal.list` 与被动 dashboard reads 竞争 6-slot gate 为 `9/10`，随后以显式非沉浸式 deep-link 优先收敛为单请求，未放大并发、重试或延长超时；最终 Chromium `10/10`、108/108、三角色、9/9 参数化读取、两条同 run receipt 与 exact cleanup/零残留。证据 [`tux05-corrective-repository-closure-evidence-2026-08-29.json`](../testing/tux05-corrective-repository-closure-evidence-2026-08-29.json)；TUI JS `44 passed`、Workbench/Terminal Agent `328 passed`、recorder `7 passed`、adapter `3 passed`，mypy/debt/架构/治理全绿 | TUX-05 重新 completed，机器 focus 回到 null。未部署/写生产/调用外部 AI/启用 queued runtime/修改 authority/approval/执行 load/fault/live rollback；下一步仅按精确授权完成 CI/review/merge、新 main clean-worktree 候选部署和 candidate-bound production-safe UAT，真实 role owner 确认仍独立缺失 |
| 2026-08-29 | 第二期 P1 production read settlement corrective | TUX-05 operator role assertion | release `20260829220843` 的 candidate-bound profile 严格为 `9/10`：operator queue 在默认 `5s` 断言刚超时后完成并显示命名 grid、7 行与“读取完成”，canonical UAT 未写入。修正仅让该只读角色断言使用既有 `60s` production settlement ceiling；operator/regular 正反边界仍完整，不改写 SLO、不加 retry/concurrency。隔离本地 fixed profile `10/10`、108/108、三角色、两条回执、cleanup residual=0；证据 [`tux05-production-read-settlement-corrective-evidence-2026-08-29.json`](../testing/tux05-production-read-settlement-corrective-evidence-2026-08-29.json) | TUX-05 暂为唯一 repository focus。下一步 CI/Sol review/merge、新 main clean-worktree 重部署并重跑完整生产 recorder；external AI、queued runtime、authority/approval、load/fault、maintenance/live rollback 仍禁止，role owner 确认仍缺 |
| 2026-08-30 | 第二期 P1 / 第三期 P0 production corrective exit | TUX-05 merge/deploy/canonical production-safe UAT | PR #13 经 Sol/Luna review 与全量 CI 通过后合并为 `c826f741…`；从独立 clean worktree 新建并校验 PostgreSQL backup `postgres-20260829T153008Z.dump`（`146649635` bytes，SHA=`40448647…fe825`，未 prune），部署 release `20260829233430` / image `sha256:7fbf039a…df14`，migration/schema、web/Celery/PostgreSQL/Redis 全绿且自动 rollback 未触发。canonical production-safe recorder `10/10`、108/108、regular/operator/admin、两条同 run write/readback receipt、cleanup residual=0；证据 [`release-candidate-deployment-2026-08-30-c826f741.json`](../deployment/release-candidate-deployment-2026-08-30-c826f741.json) 与 [`web_to_tui_uat_candidate.production-safe.v2.json`](../../config/tui/migration/evidence/web_to_tui_uat_candidate.production-safe.v2.json) | `TUX-05=completed`、repository focus=`null`；TUI-01 保持 `awaiting_production`，真实 role owner 确认、14 日窗口、108 路由 cleanup/rollback、缺陷快照、101-task telemetry、rollback drill、registry backup 与 owner/reviewer attestations 仍缺，readiness=`DENY`。external AI、queued runtime、authority/approval、流量、load/fault、maintenance/live rollback 均未执行 |
| 2026-08-30 | 第三期 P0 production checkpoint | 最终 `36b72d2f` release、TUI cleanup/rollback 与 registry recovery point | deep-link compatibility fix 随 commit `36b72d2f…` 部署为 release `20260830071422` / image `sha256:09f64914…1c94b`；canonical run `tui01-36b72d2f-20260830-01` 通过 10/10、108/108、三角色、两条 receipt/readback、zero residual。六类 cleanup scope 均 108/108，local isolated rollback drill PASS；生产 registry generation 30 的 root-only bundle/sidecar 已创建，SHA=`1fe6b01f…fb882`，restore dry-run 与 active graph hash 对账通过 | readiness 为 `5/10`、整体 DENY。真实 role-owner、截至 `2026-09-12` 的稳定窗口、structured defect/101-task telemetry、正式 payload-free registry attestation 和 owner/reviewer 双签仍缺；raw bundle 不冒充 attestation，未执行 Classic cleanup wave/live rollback |
| 2026-08-30 | 第二期 P0 production checkpoint | DATA-02 dry-run 与 Audit typed authority 停止线 | 最终 release 上 DATA-02 默认 dry-run 保持无写入：universe 5,533；financial eligible `288,409` rows / `3,750` assets、unresolved/future=0/0；price eligible=0、stale/invalid=5,533；financial Publication `1,923/5,533`。写前 backup `postgres-20260829T220625Z.dump` 的 SHA=`434903ac…3d95`；幂等 definition reconcile 后三项 Audit definition 存在，但 active production profile 缺三项真实值，七张 authority root/ledger 表均为 0 | 未执行事实回填、Publication replacement 或 DATA-03 activation。`DATA-02=awaiting_production`、`DATA-03=waiting_dependency`、`AUD-03=awaiting_production`；下一门是命名 production owner 与独立 root/reviewer 提供真实 typed selector/profile，自动化不得代签或生成默认 authority |
| 2026-08-30 | 第一期 P0 production auto-collect | 最终 `36b72d2f` EVID-01/02 SELECT-only 重绑定 | 对 release `20260830071422` / image `sha256:09f64914…1c94b` 的 `default` PostgreSQL alias 执行单个 `REPEATABLE READ READ ONLY` 快照；Account 0050–0055 已应用，13 张 authority/evidence 表和 EVID-02 operator/approval/activation 三表均为 0。原始 bundle SHA=`f7a26a5e…a0cd`；EVID-01 report SHA=`63c08dcb…4d85`、outcome=`blocked_zero_seed_authority`；EVID-02 report SHA=`c2fec726…6275`、两条 head=`empty`、human approval=`not_collected` | 未创建 authority/approval/activation、未执行生产并发或 rollback、未代签。`EVID-01/02` 保持 `awaiting_production`，全局 deny 不变；下一真实门是独立 root/reviewer 决策与获批的 PG first-winner/successor/revocation/rollback、same-alias 回执和双签 |
| 2026-08-30 | 第二期 P0 production auto-collect | 最终 `36b72d2f` STRAT-01 owner-ledger 重绑定 | release `20260830071422` 的同一 `default` PostgreSQL alias 上，四个固定 selector 分别命中 Research R1–R8 `65` 表、Portfolio R4/R5/R8 `7` 表、Account authority/assignment `16` 表、owner/policy/operator/assignment `35` 表；全部 `row_count_total=0`、`nonzero_table_count=0`。snapshot SHA=`6f8dac57…5814`，canonical report SHA=`71bd1af3…99d4`、outcome=`zero_seed` | 未创建/修改/删除/回填/promotion/审批，未把 Data Center facts 反充 owner evidence。`STRAT-01` 保持 `awaiting_production`，真实 owner/definition/policy/calendar/scope/qualification 仍需命名业务 owner，`STRAT-02/03` 依赖不变 |
| 2026-08-30 | 第一期 P0 production auto-collect | 最终 `36b72d2f` AUD-03 SELECT-only 重绑定 | release `20260830071422` 上 496 项 migration 全 applied、pending/failed=`0/0`，graph SHA=`02406e0a…0496`；operation logs/failures=`563/0`，outbox 六类 backlog 均为 0，公网 Audit health=`200/OK`。原始 SHA=`6d16a9ff…864a`，canonical artifact [`963e4efd…6950.json`](../deployment/aud03-operational-observation/96/963e4efd8527916d4bbbe5a5b0923868f3be043a1d63834014b9e0fa97a86950.json) | alerts/admin TUI/recovery/archive 明确 unavailable；未做 migration/write/fault/recovery/archive/restore 或代签，`AUD-03` 保持 `awaiting_production`。候选、ledger 或授权范围不变时不重复探针 |
| 2026-08-30 | 第二期 P0 governance checkpoint | AUD-03/DATA-02 no-fake-values 逐项授权 preflight | 精确 profile/snapshot、三项 Audit definition、authority reader 零账本、provider 选择、四阶段命令/停止/回滚边界已固化为 [`aud03-data02-production-authorization-preflight-2026-08-30-36b72d2f.json`](../deployment/aud03-data02-production-authorization-preflight-2026-08-30-36b72d2f.json)，SHA=`25dc78fd…096d1`；确认 profile v2/snapshot hash 内部一致但 current catalog invalid、loader=`mode_invalid`，且 DATA-02 facts 先写、四 Publication 后续原子切换 | 未执行生产写入。仍须真实 production owner/root/reviewer 身份与 receipt、六字段 ledger heads、mode/outbox 决策、activation actor/reason、DATA-02 operator/source/batch 和恢复点接受；四阶段逐项授权，AUD-03/DATA-02 保持 `awaiting_production`、DATA-03 保持 `waiting_dependency` |
| 2026-08-30 | 第三期 P0 production auto-collect | TUI M5 observation source 与监控 remediation preflight | 公网 exporter `200`，本次 `203` 条 TUI migration series 覆盖全部 `101` 个可比较 task key；但全部为 TUI surface，且 exporter 不是 14 日 query-range source。VPS 容器/进程/端口、env keys、volumes、systemd、常见 agents、Caddy routes 和标准路径均无本地 retained source 线索，外部 query origin/retention 未提供。source 证据 [`tui-m5-observation-source-preflight-2026-08-30-36b72d2f.json`](../deployment/tui-m5-observation-source-preflight-2026-08-30-36b72d2f.json)，SHA=`b8b22c64…660bcf`；条件化授权包 [`tui-m5-monitoring-remediation-preflight-2026-08-30-36b72d2f.json`](../deployment/tui-m5-monitoring-remediation-preflight-2026-08-30-36b72d2f.json)，SHA=`c386ea45…c1b76`；同步修正 checked-in readiness 的旧空 UAT 断言，三组合同 `48 passed` | 正式 builder 均拒绝 2026-09-12 前的最终快照。先由 operations 证明既有外部 source；若没有，须显式 repository focus 和逐项生产授权完成 pinned collector/retention，再 fresh re-attestation 并重置 14 日窗口。禁止瞬时 scrape/零值回填；测试仍严格断言 5/10 DENY，TUI-01/02 均不晋级 |
| 2026-08-30 | P0 governance handoff | AUD-03/DATA-02 与 TUI M5 审核团队交接 | 可直接转发的 [`closure-review-team-handoff-2026-08-30-36b72d2f.md`](../deployment/closure-review-team-handoff-2026-08-30-36b72d2f.md) 当前 SHA=`4ba887ba…dc36`；AUD/DATA 模板 SHA=`bebe0575…d6c7`，TUI 模板 SHA=`1cd47973…3fd5` | 初始模板不是审批；后续 single-owner 动态状态已写入 handoff 顶部和最新 checkpoint，未发生的执行结果与 14 日终审仍不可预签 |
| 2026-08-30 | P0 governance handoff | EVID-01/02 与 STRAT-01 补充审核入口 | 候选绑定 preflight SHA=`8518c165…87ae`；EVID 五阶段模板 SHA=`e7055af3…69a2`；STRAT R1–R8 模板 SHA=`c21b14ed…2c24`。补充动态清单登记 10 个 work order；加入 single-owner 授权后的 28 项离线包 SHA=`93c7540d…75fc6` | 回传已作为真实 DEFER 收录；取消多人签字不生成 authority/definition。`EVID-01/02`、`STRAT-01` 保持 `awaiting_production`，后续生产事实仍逐阶段形成 |
| 2026-08-30 | P0 governance handoff | TAR-05 Terminal Runtime 补充审核入口 | 最终 release identity/basic health 已绑定，但专用 Terminal Worker、runtime manifest/flags/staging/provider/retained metrics 仍缺；加入 single-owner 授权后的 23 项离线包 SHA=`684bd75a…ec196` | P1 回传已作为真实 DEFER 收录；不再要求多人签字，但未启动 Worker/load/fault/model、未改 flag/部署/rollback。`TAR-05=awaiting_production`、capacity false、queued/并发>1 fail-closed |
| 2026-08-30 | P0 governance simplification | 个人项目 single-owner 合同与五份回传处理 | 唯一真人项目所有者授权固化为 [`personal-project-single-owner-authorization-2026-08-30-36b72d2f.json`](../deployment/personal-project-single-owner-authorization-2026-08-30-36b72d2f.json)，SHA=`d9c6e9f4…5fd7`；同一 owner 可承担治理角色，不再要求第二名自然人。五份报告的 JSON/sidecar/候选与缺失证据已校验：AUD/DATA、TUI source、EVID、STRAT、TAR 均为有效 DEFER；TUI role-owner 对既有 run 的范围内决定为 APPROVE | `TUI-01=completed`；其余技术门禁没有被签字替代。authority zero-seed、业务定义、staging/runtime manifest、retention/负载/模型结果仍保持真实 DEFER/fail-closed |
| 2026-08-30 | P0 repository exit | `TUI-03` retained monitoring source | 新增 manifest-digest pinned Prometheus、21d/4GB 双上限、`prometheus_data`、`web:8000/metrics/` 唯一真实 target、健康检查、精确 M5 rules、host-only credential 和 Caddy HTTPS read-query allowlist；VPS/本地 runtime 打包投影同步。结构化证据 [`tui03-retained-monitoring-repository-closure-evidence-2026-08-30.json`](../testing/tui03-retained-monitoring-repository-closure-evidence-2026-08-30.json)，SHA=`8fda7913…91a7`；聚焦 TUI 回归 `48 passed`，compose/promtool/Caddy/alerts/PowerShell/mypy/registry 均通过 | `TUI-03=completed`、focus 回到 null、`TUI-02=awaiting_production`。本 checkpoint 未部署、未创建生产 volume/credential、未生成 retained sample、未重置或回填观察窗口；下一步是 clean successor 部署与真实 post-deploy evidence |
| 2026-08-30 | P0 production Day 0 | `TUI-02` successor deployment、UAT、cleanup、rollback 与 registry backup | commit `80ea002b…` 已部署为 release `20260830215638` / image `sha256:54cb9646…8c39fc`；Prometheus `up=1`、21d/4GB、17 rules/0 unhealthy、认证 query=200，窗口重置为 `2026-08-30..2026-09-13`。正确域名 `demo.agomtrade.pro` 的 TLS/health 通过；正式 run `tui02-80ea002b-20260830-01` 为 10/10、108/108、三角色、两 receipt、零残留，cleanup 8/8、六 scope 108/108、isolated rollback PASS。generation 30 backup SHA=`c3cc3a05…48b8d` 通过 sidecar/restore dry-run。汇总 [`tui02-production-day0-checkpoint-2026-08-30-80ea002b.json`](../deployment/tui02-production-day0-checkpoint-2026-08-30-80ea002b.json)，SHA=`1cff7915…e4d85`；single-owner 授权 SHA=`f675c446…300e1` | `TUI-02=active`、readiness=`5/10 DENY`、repository focus=`null`。第二名真人/职责分离要求已取消；自然窗口、structured defect/101-task telemetry、post-window registry attestation、review snapshot 和同一 owner 的两个 role-bound attestations仍须真实完成，不回填历史或提前签发 |
| 2026-08-31 | P0 repository exit | `TAR-06` fail-closed staging load/metrics collector | 新增 non-production URL/production denylist、exact candidate/preflight/matrix、20 个互异 stdin actor、barrier 同步 1/5/10/20、HTTP percentile 与 exact Prometheus query collector；missing/failure 保留 unavailable。CLI 默认 validation-only，显式 execute 才写 secret-free raw source、snapshot 与 canonical `controlled_staging_observation` content-addressed JSON/SHA sidecar，canonical artifact 直接绑定 raw source digest。Terminal runtime focused `84 passed`、高风险 TUI/SDK/SSL `356 passed`，增量/全量 mypy、3002-file architecture、governance/registry、Black/isort/Ruff 全绿；[closure evidence](../testing/tar06-staging-collector-repository-closure-evidence-2026-08-31.json) SHA=`c7af18fa…958f2` | `TAR-06=completed`、repository focus=`null`。本 checkpoint 未访问 staging/VPS、未生成真实负载、未调用 provider/MCP、未修改 flag 或形成容量结论；`TAR-05` 仍须真实 staging/chaos/provider/canary/观察证据，queued/并发>1 保持 fail-closed |
| 2026-08-31 | P0 repository corrective exit | `TAR-06` semantic approval 与 live Worker preflight | adversarial review 发现首版只核对所谓 approved-preflight 文件 SHA，任意字节可冒充决定，且 queue 布尔值不能证明 Worker 存活；corrective 改为严格 `terminal-runtime-staging-preflight.v1` APPROVE、owner/authorization identity、最长 24h 时效、全 candidate/target/runtime/workload/query envelope SHA 与 staging-only action scope。runtime 必须 legacy inline=1、专用 Worker/queue、prefetch=1、non-billable stub、MCP disabled；每个阶梯重验 approval/heartbeat 并一次性消费内部授权，外部 receipt 不能绕过，部分失败不能重复授权。失败优先依次为 `27 failed / 2 passed`、`4 failed / 29 passed` 和 retry 专项 `1 failed`；最终 Agent Runtime/load/recovery `302 passed`、TUI/SDK/SSL `356 passed`，增量/全量 mypy、3003-file architecture、governance/registry、Black/isort/Ruff 全绿；[corrective evidence](../testing/tar06-staging-preflight-corrective-closure-evidence-2026-08-31.json) SHA=`926f1105…36746` | `TAR-06` 同日有界重开后重新 completed、focus=`null`；未执行任何 staging/VPS/生产网络、真实负载、provider/MCP、fault、flag 或审批代签，`TAR-05` P1–P7 与 capacity gate 不变 |
| 2026-08-31 | P0 production read-only + repository guard | `TUI-02` retained sample 精确时间门 | 生产只读 checkpoint 证明 candidate/image 无漂移、Prometheus 无重启、21d/4GB volume、target、17 rules、auth query 全部健康，首个 raw sample=`2026-08-30T15:09:35.034Z`；[checkpoint](../deployment/tui02-production-observation-checkpoint-2026-08-31-80ea002b.json) SHA=`db055a18…b0d2a`。新增 hash-bound retained binder，telemetry/defect snapshot 升级 v2 UTC timestamp，readiness 按 exact 1209600 秒而非日历日判定 | 当前仍 `5/10 DENY`；精确 eligible=`2026-09-13T15:09:35.034Z`，未部署/重启/写生产/backup/load/cleanup，TAR-05 与所有 decision/data gate 不变 |
| 2026-08-31 | P0 production auto-collect | `AUD-03` successor alerts/admin TUI 重绑定 | successor `80ea002b…` / `20260830215638` 保持无漂移、Web restart=0；migration `496/0/0`，Audit health=`200/OK`、logs/failures=`563/0`、outbox 六项为 0；Prometheus 2 groups/12 rules/unhealthy=0、active/critical=0，同候选 108/108 UAT 中 10 个 Audit route 全通过。[checkpoint](../deployment/aud03-successor-production-readonly-checkpoint-2026-08-31-80ea002b.json) SHA=`a5ce4fbd…182d`，canonical artifact SHA=`6022067c…efaf` | `missing_section_count` 从 4 降至 2，alerts/admin TUI 由 unavailable 收敛为 available；瞬时无告警不冒充留存窗口，未执行 recovery/archive、fault、migration/write/runtime activation，`AUD-03` 保持 awaiting production |
| 2026-08-31 | P0 repository exit | `AUD-04` candidate-bound archive / isolated restore rehearsal | 新增 authority-gated、candidate 前后双重验真的半开 ledger window；事件/source/manifest/stream anchor/replay hash 全部派生，strict codec 与 append-only SHA artifact/sidecar 拒绝替换；restore 仅进入 `memory_only` namespace 并 exact replay。focused `32 passed`，Audit 回归 `531 passed / 5 skipped`，mypy/debt、Black/isort/Ruff、3005-file architecture 与 31-unit registry 全绿；[closure evidence](../testing/aud04-audit-archive-rehearsal-repository-closure-evidence-2026-08-31.json) SHA=`1c64e66a…4e317` | `AUD-04=completed`、focus=`null`；未读写生产、未执行 archive/restore/delete/fault/runtime/deploy，结果固定 non-production。`AUD-03` 仍须真实 authority/profile、writer smoke、rollback/recovery 与逐动作获批的 canary archive/restore |
| 2026-08-31 | P0 repository corrective exit | `DATA-04` ASGI database connections / SELECT-only preview | successor-bound dry-run 在 preview 前因 PostgreSQL client exhaustion 停止；只读证据为 max=100/reserved=3、database/ready/Audit health=503、Web idle client 按约 30s 累积，生产 Daphne 仍为 `CONN_MAX_AGE=600`。修复固定 ASGI age=0、删除读路径 `get_or_create`、缺配置 `MISSING_CONFIG`、显式 PUT/save 初始化。focused `18 passed`，相关回归 DATA-04 行为 `69 passed`；mypy/debt、53-surface current-data、Black/isort/Ruff、3005-file architecture、Django/migration/governance 全绿；[closure evidence](../testing/data04-asgi-db-select-only-preview-repository-closure-evidence-2026-08-31.json) SHA=`aaaa675e…ea8d` | `DATA-04=completed`、focus=`null`，DATA-02 新增该依赖。未终止 session、重启、部署、写库或回填；唯一扩大回归失败是未改动 HEAD 中 `financial_fact_repository.py` 243/200 行既有债务。下一门是另行授权 clean deploy，重验多 scrape 周期连接稳定、readiness 与无写 dry-run；candidate 变化重置 TUI-02 观察 |
| 2026-08-31 | P0 repository corrective exit | `DATA-05` bounded financial repository owner | DATA-04 扩大回归暴露的 HEAD 243/200 行失败独立登记；availability preview/backfill 移入 65/100 owner，原 financial owner 降至 189/200，公开 class/facade identity 与行为不变，不提高预算/基线。新 source 纳入 current-data markers；focused `12 passed`，扩大相关回归收敛为 `70 passed`，mypy/debt、53-surface current-data、Black/isort/Ruff、3006-file architecture、Django/migration/governance 全绿；[closure evidence](../testing/data05-financial-repository-owner-closure-evidence-2026-08-31.json) SHA=`7c535f2a…8a56a` | `DATA-05=completed`、focus=`null`；未读取/写入生产、重启、部署或回填。DATA-02/03 与 DATA-04 clean-deploy/revalidation 前置不变 |
| 2026-09-01 | P0 Git checkpoint | closure corrective stack PR #15 merge 与 main CI | `dev/next-development@e38ebdcf8…` 的七个 coherent commits 经 Sol/Luna review 后由 PR #15 merge commit `1546ca0ecfaa0d2d9a004720065a3acac9240d87` 进入 main；候选 SHA 与 review head 一致，Architecture `33513058174`、Security `33513058194`、Consistency `33513058305`、Fast Feedback `33513058369` 均 completed/success。[canonical evidence](../testing/pr15-closure-corrective-merge-main-ci-evidence-2026-09-01.json) SHA=`2febac54…f48e6a` | 仅固化 GitHub 合并与 CI 终态；未部署、未重启、未迁移、未读写生产、未执行外部调用/load/fault/runtime enablement。`execution_focus=null`，下一真实生产门仍是 clean successor deployment 后的 DATA-02 连接/readiness/SELECT-only dry-run 与获批 reconciliation |
| 2026-09-02 | P0 production deployment + read-only acceptance | successor `aa7127ff4` / release `20260901232812` | PR #16/main CI 完成后，从候选独立 clean worktree 执行底层部署；predeploy PostgreSQL custom dump 已创建、远端 `pg_restore --list` 验证并下载，remote/local SHA=`c9f7cf87…1caf`。release/image/OCI revision 精确一致，migration pending=0，三轮 PostgreSQL client backends 恒为 2。DATA-02 两个 SELECT-only dry-run 都成功执行但 overall ready=false：5,533 completed-session price 全不合格、5,533 quote/price/valuation 全 stale、financial 仅 1,923/5,533。EVID-01/02 与 STRAT-01 同候选快照继续 zero-seed；AUD migration=496、outbox/failure=0、logs=563、alerts=0 | 部署/备份/切换/重启/自动回滚保护已验证，回滚未触发；DATA-02 未执行 `--execute`、未回填/切换 publication，EVID/STRAT 未写 authority/policy，AUD 未 archive/restore/fault。`DATA-02/AUD-03/EVID-01/EVID-02/STRAT-01` 保持 awaiting production，`DATA-03/EVID-03` 保持 dependency-blocked |
| 2026-09-02 | P0 candidate-bound production UAT + retained source | `TUI-02` successor window reset | production-safe run `tui02-aa7127ff-20260902-01` 通过 `10/10`、`108/108`、regular/operator/admin；ai_provider/strategy 两条同 run create/update/readback/delete receipt 均在 60s SLO 内且 residual=0。cleanup recorder `8/8`，六 scope 均 `108/108`；candidate-bound local isolated rollback PASS。后续受控 web-only restart 于 `2026-09-02T15:38:21.178433901Z` 恢复同一候选，故旧窗口作废；旧 checkpoint 仅保留历史，reset artifact 记录 web/prometheus healthy、public health/ready=200、decision-ready=503 fail-closed | readiness=`5/10 DENY`，retained source 已清空，等待重启后首个真实 raw sample 及新的 exact 14-day eligible instant。不得回填/合成历史；未生成 post-window defect/101-task telemetry、registry attestation 或 final approval。未启用 external AI/queued runtime，未改 authority/approval，未执行 load/fault/live rollback/Classic cleanup |
| 2026-09-02 | P1 repository corrective exit | `EVID-04` cross-platform content-addressed fixture | 复现确认 Git LF snapshot SHA=`d789b48e…28fb` 在 Windows `text=auto` checkout 后变为 CRLF SHA=`bf648f39…4692`，使历史 report 从 tracked `3900c08b…f9b4` 误重算为 `387772ae…28cd`。fixture test 现读取 Git-canonical LF text bytes 后调用原 strict parser；未改 serializer/parser/recorder/snapshot/artifact | 原失败节点 `1 passed`，Black/isort/Ruff、registry/governance 全绿；`EVID-04=completed`、focus=null。生产 raw-byte provenance 与 EVID-01/02 zero-seed/approval gate 不变 |
| 2026-09-02 | P0 Git/CI checkpoint | `dev/next-development` 候选同步与 deterministic inventory 修复 | 本地分支追平并推送至 `140bf23fcdacdd92160ec59286eabba88e156252`；因合并后的源码行号/来源计数变化，刷新 `data_center_architecture_inventory.json` 与 `data_center_entrypoints.json`。Architecture、Security、Consistency、Fast Feedback 四条 CI 均 completed/success | 仅固化分支与 CI 终态，不部署/重启/迁移/写生产；VPS 仍运行既有 `aa7127ff4` 候选，EVID/DATA/STRAT/AUD/TAR 生产门禁和 TUI-02 观察窗口按注册表继续 fail-closed/awaiting |
| 2026-09-02 | P0 production incident + local corrective | candidate-bound VPS read-only availability observation and decision readiness short-circuit | 当前候选 `aa7127ff4d9f71555b0d0486314da5518bd2ac20` / release `20260901232812` / image `sha256:55d2b1d8…f9519c` 无漂移；但 `2026-09-02T09:24:13Z` Web `running/unhealthy`、restart=`0`，公网 health/ready/decision 全为 `502`，Caddy `web:8000` upstream timeout；Prometheus `healthy`/restart=`0`、target=`up`，`HighAPILatency` `/api/ready/` P95=`4.875s` pending。只读工件 [`tui02-production-readonly-refresh-2026-09-02-aa7127ff.json`](../deployment/tui02-production-readonly-refresh-2026-09-02-aa7127ff.json) SHA=`8992083e…eedb6c16`。本地 `core/health_checks.py` 在持久化 decision gate blocked 时直接返回四键 fail-closed projection，避免重复扫描 5,533 资产；`tests/component/test_health_checks.py` 全量 `25 passed`，mypy regression/Black/isort/Ruff 通过，提交 `ac95af184`/`efe301941` 已推送；Architecture、Security、Consistency、Fast Feedback 四条 CI 均 success | 生产候选尚未包含修复；未重启、未部署、未写库/改配置，decision gate 仍 `DENY`/`must_not_use_for_decision=true`。下一步需另行批准一次受控 restart 或 deploy 并重取 Web/Caddy/Prometheus 证据；任何 restart 都会按规则重绑 TUI-02 观察窗口。DATA/EVID/STRAT/AUD/TAR 及 TUI-02 14 日、capacity/chaos、restore/rollback、owner/reviewer 门禁不解锁 |
| 2026-09-02 | P1 repository corrective exit | VPS Web liveness host watchdog | 提交 `efdbb63c6c9ccc2b108ab3e5f3155404dc0758bf` 新增 server-side `scripts/vps-web-watchdog.sh` 与 systemd service/timer 模板：Docker `web` 连续 3 次 unhealthy 后才允许只重启 `web`，15 分钟 cooldown、1 小时最多 2 次、120 秒恢复确认；healthy/decision-ready blocked 清零，不 kill 共享 PID 或触碰 runtime_ns/Celery/DB。五项行为测试、`sh -n` 与打包投影通过；Architecture `33619653200`、Security `33619653029`、Consistency `33619653074`、Fast Feedback `33619653085` 均 success | 仅完成仓库合同，未安装 timer、未重启/部署 VPS、未形成恢复/容量/14 日证据；安装为一次显式运维动作，任何实际 restart 按 TUI-02 规则重新绑定 retained sample，所有生产门禁保持原状态 |
| 2026-09-02 | P1 repository corrective exit | TUI-02 retained checkpoint 跨平台哈希护栏 | retained validator 与 binding 统一使用 Git canonical LF 字节，避免 Windows CRLF checkout 将有效 checkpoint 误判为缺失；新增 CRLF 回归，retained/readiness focused `41 passed`，dry-run 识别 `2026-09-01T16:56:29.796000Z` 首样本与 `2026-09-15T16:56:29.796000Z` eligible | 仅修复本地/CI 证据读取一致性，不改变生产 raw-byte provenance、候选身份、观察窗口或门禁；当前 readiness 仍 `5/10 DENY` |
| 2026-09-02 | P1 repository corrective exit | VPS bundle verifier watchdog 资产护栏 | `verify-vps-bundle.ps1` required-file 合同现在强制包含 watchdog service/timer/script，避免部署包缺件却通过验收；watchdog 行为回归 `5 passed`，PowerShell parser 通过 | 仅收紧本地打包验收，不安装 timer、不部署/重启 VPS，不改变候选、观察窗口或生产门禁 |
| 2026-09-03 | P1 governance projection correction | active registry v40 baseline / 40 canonical closure units | 按当时的 `governance/active_plan_registry.json` 重新核对 `docs/plans/README.md` 与 `docs/INDEX.md`：同步 v40、40 units 以及 2026-09-02 web restart 后的 TUI-02 observation reset；未改变其他机器状态或生产门禁 | 仅修正人读计划/索引投影并记录 reset evidence；后续已由 v41 候选 next-gate alignment 取代，`execution_focus=null`，当前无可执行 repository unit，生产/外部门禁继续 fail-closed |
| 2026-09-03 | P1 governance projection correction | active registry v41 candidate next-gate alignment | 将 AI-01 与 TUI usability workstream 的机器 next_gate 从历史 successor 对齐到当前 `aa7127ff4…` / `20260901232812`，保留明确的 TAR-05/TUI-02 依赖与 fail-closed 条件；同步 registry 版本 v41 | 仅修正机器门禁的候选投影，不改变 unit 状态、不部署/重启 VPS、不授权 AI UAT 或 TUI 观察提前通过 |
| 2026-09-03 | P1 repository acceptance recheck | TUI Workbench full regression after dead-patch cleanup | 完整 `python -m pytest tests/unit/test_tui_workbench.py -q -p no:cacheprovider` 通过 `308 passed in 283.47s`；此前 data-center dead-patch cleanup 的 stale fixture 未再出现；未改生产代码、未部署或重启 VPS | 仅补强本地 TUI 验收证据；TUI-02 仍等待真实 post-reset retained sample/精确 14 日 telemetry，其他生产/外部门禁不变 |
| 2026-09-03 | P0 production read-only probe | TUI-02 post-reset authenticated first-sample attempt | 服务器端只读探针登录页 `200`/CSRF 存在，但 login POST `403`；后续 TUI GET `200` 未形成已认证 migration metric，Prometheus raw `web_to_tui_migration_events_total` 仍 `0 series`，未提交表单、写业务库、部署或重启 | 认证/角色 UAT 仍不可用，不能把该页面 `200` 当作首个 retained sample；TUI-02 继续等待可用的受控生产账号、真实样本及精确 14 日窗口 |
| 2026-09-03 | P1 repository corrective exit | DATA-02 successor recorder operator-safe blocked output | `record_data02_successor_checkpoint.py` 在 CLI 边界将预期的 `Data02SuccessorCheckpointError` 稳定输出为 `blocked` JSON、`reason_code=invalid_successor_checkpoint`、退出码 `2`；底层 parser 仍 fail-closed，focused recorder 回归 `12 passed`，Black/isort/Ruff、增量 mypy 与 debt ceiling 全绿 | 仅改善服务器端工具的阻断可读性，不连接 VPS/数据库、不写生产、不执行 backfill 或切换 publication；缺少四个 immutable publication identity 的真实 checkpoint 仍不能作为 DATA-02 证据，`DATA-02` 继续 `awaiting_production` |
| 2026-09-03 | P1 documentation projection correction | current candidate/status cross-document alignment | 将 `docs/INDEX.md`、Web→TUI 计划、发布阻塞计划、VPS evidence、M5 binding guide 与 Audit 计划中的旧候选/未部署/未恢复叙述标为历史或对齐到 `aa7127ff4…` / `20260901232812`；明确 AUD-scoped 12 条与全局 17 条 Prometheus rules 的范围；active-plan、governance、route/link consistency 全通过 | 仅修正文档投影，不修改 registry 状态、不部署/重启 VPS、不改变 TUI-02、DATA-02、AUD-03、EVID、STRAT、TAR 生产门禁；真实 retained window、authority、数据回填与容量证据仍待外部输入 |
| 2026-09-03 | P0 production read-only probe | Public health/readiness recheck after current candidate recovery | 服务器端对 `https://demo.agomtrade.pro` 仅执行 HTTPS `GET`：`/api/health/`=`200`、`/api/ready/`=`200`（database/Redis/Celery/critical data=`ok`，decision data=`warning`），`/api/decision-ready/`=`503 blocked` 且 `must_not_use_for_decision=true`；原始响应 hash 与阻断字段见 [`tar01-public-health-readonly-recheck-2026-09-03.json`](../deployment/tar01-public-health-readonly-recheck-2026-09-03.json) | 公开端点不暴露 candidate identity，本次不是 candidate-bound 证明；未部署、未重启、未迁移、未写生产，decision/data/TUI/AUD/STRAT/TAR 门禁保持 registry 原状态 |
| 2026-09-03 | P1 documentation projection correction | Historical candidate labels in active plans | 将发布阻塞、M5 readiness、关键可靠性和 Data Center 计划中的旧候选/初始缺口明确标为历史，并把发布阻塞计划当前事实对齐到 `aa7127ff4…` / `20260901232812`；不改变 registry 状态、不部署/重启 VPS、不改变任何生产门禁 |
| 2026-09-03 | P1 documentation projection correction | release-blocker execution checkpoint chronology | 将第 13 节明确为自 2026-08-30 起的时间序列，并注明旧小节按日期解释、当前状态以计划顶部/registry/最新检查点为准；不改变 registry 状态、不部署/重启 VPS、不改变任何生产门禁 |
| 2026-09-02 | P0 repository corrective exit | DATA-02 successor checkpoint recorder contract | 新增纯 Application `data02_successor_checkpoint.py` 与服务器端 `record_data02_successor_checkpoint.py`；严格验证候选/source/release/image、连接增长、health/ready/decision probe、5,533 universe、source time、dry-run/side-effect flags，以及四个 core dataset 的唯一 `publication_id`/`publication_hash`。当前候选 checkpoint 因缺 publication identities 按预期 fail-closed；补充 11 项单测、entrypoint inventory `1152` 且 `candidate-review=0`、增量 mypy 与 debt ceiling `0`、Black/isort/Ruff/governance 全绿 | 仅补齐 DATA-02 只读 successor 证据的自动采集合同，不连接 VPS/DB、不执行 backfill/写库/publication switch，不把 synthetic publication IDs 当生产事实；`DATA-02` 继续 `awaiting_production`、`DATA-03` 继续 dependency-blocked，下一步仍须真实 provider refresh/backfill、before/after reconciliation 与 data-owner tolerance decision |
