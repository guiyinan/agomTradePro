# Personal Investment Readiness Check - 2026-06-30

> Scope: local `dev/next-development` readiness for using AgomTradePro as a personal investment research and decision-support system. This is not approval for unattended real-money auto execution.

## Result

Status: **usable for personal research and assisted decision workflow**

Verified at: `2026-06-30 22:39 Asia/Shanghai`

The local environment now passes Django checks, production deployment checks, strict readiness checks, decision quote freshness gates, Qlib freshness checks, and Alpha/workspace consistency checks.

Update `2026-06-30 23:00 Asia/Shanghai`: added a repeatable daily evidence collector for the continuous-run trial:

```bash
python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --include-weekly-advisor
```

The command completed with exit code 0 and wrote:

- `var/readiness-evidence/2026-06-30-personal-readiness.json`
- `var/readiness-evidence/2026-06-30-personal-readiness.md`

The first collected status was `warning`: system readiness and Qlib were `ok`, auto-advisor payload generation was `ok`, but the two discovered active accounts had `account_equity=0.0`, so risk-center daily reports were not yet decision-useful.

Update `2026-06-30 23:23 Asia/Shanghai`: account readiness and workspace evidence were repaired and rerun:

```bash
python manage.py repair_personal_account_readiness --json
python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --include-weekly-advisor --run-workspace-refresh
python manage.py validate_personal_readiness_window --json
```

The final collected status is `ok`: system readiness, Qlib, Regime, Pulse, action recommendation, risk-center daily reports, and auto-advisor payloads are all `ok`. Rotation now records three successful configs and three explicit `skipped` configs with `no_valid_allocation`, which remains visible but no longer counts as a failed workspace refresh.

Current continuous-run status after the `2026-07-03` scheduler evidence is `accepted_days=4`, `remaining_days=16`, `scheduler_clean_suffix_days=3`, `scheduler_clean_remaining_days=17`, and `status=in_progress`. The next required trading-day evidence is `2026-07-06`; projected evidence-window completion remains `2026-07-27`, while the scheduler-clean final gate projects `2026-07-28`.

Update `2026-06-30 23:45 Asia/Shanghai`: added a single daily pipeline command for scheduled continuous-run evidence:

```bash
python manage.py run_personal_readiness_daily --target-date 2026-06-30 --json
```

The command runs account readiness preflight, daily evidence collection, and 20-trading-day window validation in one step. It runs the workspace refresh and weekly auto-advisor payload by default. Missing positive-equity accounts are reported as `action_required`; add `--repair-accounts` only when account creation is intended.

Update `2026-06-30 23:58 Asia/Shanghai`: tightened the acceptance validator so `accepted_days` now means the current continuous accepted trading-day streak ending at the latest evidence date, not a historical total. A missing trading-day evidence file or a failed trading day in the current streak breaks the window and appears in `blocking_issues`. The validator now supports `--calendar-source auto|qlib|weekday`; `auto` prefers the local Qlib `calendars/day.txt` trading calendar and falls back to weekdays only when Qlib calendar coverage is unavailable.

Update `2026-07-01 00:10 Asia/Shanghai`: added scheduled continuous-run support. Celery beat can now run `apps.task_monitor.application.tasks.run_personal_readiness_daily_task`, and database-backed scheduler initialization includes `setup_personal_readiness_daily`. The initial default schedule was `mon-fri 23:50 Asia/Shanghai`, after the post-close Alpha/Qlib recovery window. Scheduled runs keep `repair_accounts=false` by default so account creation remains an explicit operator action.

Update `2026-07-01 00:25 Asia/Shanghai`: scheduled daily runs now resolve their default target date to the latest closed trading day instead of blindly using the current calendar date. Explicit `--target-date` / task `target_date` still overrides this behavior. The target-date resolver deliberately does not roll back to the latest local Qlib data date; if Qlib data is stale for the latest closed trading day, the daily evidence must surface that as a freshness failure instead of silently reusing older evidence.

Update `2026-07-01 00:40 Asia/Shanghai`: tightened the continuous window validator to accept an expected latest readiness date. The daily pipeline now passes its `target_date` into window validation, so a missing evidence file for the day that should have been checked is reported as `blocking_issues[].reason = "evidence is missing"` instead of silently validating only up to the newest old evidence file.

Update `2026-07-01 00:55 Asia/Shanghai`: added `next_required_date` and `next_required_reason` to the window validator output. When the current streak is healthy but incomplete, the field points to the next trading day that needs evidence. When a day is blocking, it points to the blocking date. This makes the 20-day run easier to operate from either Celery beat logs or manual JSON output.

Update `2026-07-01 01:05 Asia/Shanghai`: added `show_personal_readiness_status`, a read-only operational summary command. It reports the continuous window, scheduler periodic-task configuration, latest evidence summary, and the next command to run. This gives a single daily inspection entrypoint without generating or mutating readiness evidence.

Update `2026-07-01 01:15 Asia/Shanghai`: refined `show_personal_readiness_status` with structured `next_action`. When the next required date is later than the latest closed trading day, the status reports `wait_for_post_close` instead of emitting a premature run command. Once the target day is closed, it reports `run_daily`; if a blocking issue exists, it reports `inspect_blocking_issue`.

Update `2026-07-01 01:25 Asia/Shanghai`: added a default guard to `run_personal_readiness_daily` so explicit `--target-date` values later than the latest closed trading day fail before evidence collection. The override `--allow-unclosed-target-date` exists only for diagnostics and is also disabled by default in `run_personal_readiness_daily_task`.

Update `2026-07-01 01:40 Asia/Shanghai`: made the scheduled readiness safety posture observable. Static Celery beat config and `setup_personal_readiness_daily` now write `allow_unclosed_target_date=false` explicitly. `show_personal_readiness_status --json` now returns scheduler `effective_kwargs` and `safety`, including warnings if scheduled evidence can run before close or create accounts automatically.

Update `2026-07-01 01:55 Asia/Shanghai`: expanded scheduler safety checks so `show_personal_readiness_status` warns when the periodic task is disabled, has no verifiable crontab, or is scheduled before `15:30 Asia/Shanghai`. This makes continuous-run readiness depend on both evidence quality and the scheduler actually being able to produce the next evidence record after market close.

Update `2026-07-01 02:05 Asia/Shanghai`: scheduler safety issues now affect the top-level operational status and `next_action`. If the continuous-run scheduler is unsafe while the 20-day window is still incomplete, `show_personal_readiness_status` reports `status=warning` and `next_action.action=fix_scheduler` with `python manage.py setup_personal_readiness_daily` as the repair command.

Update `2026-07-01 02:20 Asia/Shanghai`: daily evidence now records `operation_context` with `target_date_closed`, `latest_closed_date`, `allow_unclosed_target_date`, and `mode`. The split `collect_personal_readiness_evidence` command now rejects unclosed target dates by default, matching `run_personal_readiness_daily`; `--allow-unclosed-target-date` remains available only for diagnostics and is recorded in evidence.

Update `2026-07-01 02:30 Asia/Shanghai`: the 20-trading-day validator now rejects evidence marked with diagnostic `operation_context`. Diagnostic unclosed-target evidence can still be written for troubleshooting, but it cannot increase `accepted_days` or satisfy the formal continuous-run gate.

Update `2026-07-01 02:45 Asia/Shanghai`: `show_personal_readiness_status --json` now exposes the latest evidence `operation_context` and `formal_evidence` flag. Old evidence without context remains visible as `formal_evidence=null`; new formal evidence reports `true`, and diagnostic evidence reports `false`.

Update `2026-07-01 03:00 Asia/Shanghai`: `show_personal_readiness_status --json` now reports both `latest_evidence` and `latest_formal_evidence`. If a future diagnostic evidence file exists, it remains visible as the latest file, but the latest formal evidence candidate remains visible separately for acceptance-window review.

Update `2026-07-01 03:10 Asia/Shanghai`: `show_personal_readiness_status --json` now includes an `acceptance_gate` summary with accepted/remaining days, latest formal evidence date, next required date, next action, and whether the next formal daily evidence can be generated immediately. This gives scripts and operators a single stable field for the 20-trading-day gate.

Update `2026-07-01 03:20 Asia/Shanghai`: the split `collect_personal_readiness_evidence` command now defaults omitted `--target-date` to the latest closed trading day, matching `run_personal_readiness_daily`. This prevents early-morning manual evidence collection from accidentally targeting the current unclosed calendar day.

Update `2026-07-01 status follow-up`: `show_personal_readiness_status --json` now classifies evidence with `acceptance_candidate` and `evidence_mode`. Legacy evidence without `operation_context` remains visible as `formal_evidence=null`, but it is now explicitly marked as `acceptance_candidate=true` and `evidence_mode=legacy_without_operation_context`; diagnostic unclosed-target evidence remains `acceptance_candidate=false`.

Update `2026-07-01 diagnostic-mode follow-up`: readiness evidence now treats any run using `--allow-unclosed-target-date` as diagnostic mode in `operation_context`. A closed target date with the override is recorded as `diagnostic_override`; an unclosed target date remains `diagnostic_unclosed_target`. Both stay excluded from formal acceptance.

Update `2026-07-01 scheduler-acceptance follow-up`: `show_personal_readiness_status --json` now treats scheduler safety as part of acceptance. Even if the 20-trading-day evidence window is accepted, an unsafe or disabled scheduler keeps the top-level status at `warning`, sets `acceptance_gate.accepted=false`, and returns `next_action.action=fix_scheduler`.

Update `2026-07-01 scheduler-timezone follow-up`: scheduler safety now validates the crontab timezone. The daily readiness task must use `Asia/Shanghai`; otherwise `show_personal_readiness_status --json` reports `unexpected_scheduler_timezone`, because the post-close time check is only meaningful in the intended market timezone.

Update `2026-07-01 scheduler-weekday follow-up`: scheduler safety now validates the crontab weekday expression. The daily readiness task must use `mon-fri`; otherwise `show_personal_readiness_status --json` reports `unexpected_scheduler_day_of_week`, because missing a weekday can break the 20-trading-day evidence streak and weekend runs can overwrite the latest closed-day evidence.

Update `2026-07-01 scheduler-calendar follow-up`: scheduler safety now includes `day_of_month` and `month_of_year` in the reported schedule and requires both to be `*`. Restricted month/day crontab fields now produce `unexpected_scheduler_day_of_month` or `unexpected_scheduler_month_of_year`, preventing a partial-month schedule from being treated as continuous-run ready.

Update `2026-07-01 scheduler-run-control follow-up`: scheduler safety now exposes PeriodicTask run controls and rejects continuous-readiness tasks configured as `one_off`, with `expires`, or with `expire_seconds`. These settings can stop the daily evidence task before the 20-trading-day window completes.

Update `2026-07-01 scheduler-args follow-up`: scheduler safety now parses PeriodicTask positional `args`, exposes `effective_args`, and requires it to be empty. Invalid args JSON or non-empty positional args now produce `invalid_scheduler_args` or `unexpected_scheduler_args`, preventing Celery argument binding drift from breaking scheduled evidence collection.

Update `2026-07-01 scheduler-delivery follow-up`: scheduler safety now exposes PeriodicTask delivery controls and requires the daily readiness task to use the default Celery delivery path. Custom `queue`, `exchange`, `routing_key`, `priority`, invalid headers JSON, or non-empty headers now produce scheduler warnings so evidence collection cannot be silently routed away from the available worker.

Update `2026-07-01 scheduler-repair follow-up`: `setup_personal_readiness_daily` now clears unsafe PeriodicTask controls when updating an existing task. It resets positional args, custom delivery controls, headers, one-off mode, and expiration fields, so the status command's `fix_scheduler` action can actually repair the scheduler drift it reports.

Update `2026-07-01 missing-evidence-action follow-up`: `show_personal_readiness_status --json` now distinguishes missing evidence from failed evidence. If a closed required date is missing its evidence file, `next_action.action=run_daily`; if an existing evidence file failed validation, the action remains `inspect_blocking_issue`.

Update `2026-07-01 evidence-inspection follow-up`: added `inspect_personal_readiness_evidence` as a read-only evidence diagnosis command. It expands one evidence JSON into acceptance status, blockers, non-blocking observations, and the next operator command. `show_personal_readiness_status --json` now points failed existing evidence to this inspection command instead of suggesting an immediate rerun.

Update `2026-07-01 strict-monitor follow-up`: `show_personal_readiness_status` now supports `--strict-monitor`. This mode stays green while the system is correctly waiting for post-close, but exits non-zero when operator action is required: unsafe scheduler, failed evidence requiring inspection, or missing closed-date evidence requiring a daily run. It is intended for lightweight external monitors during the 20-trading-day trial.

Update `2026-07-01 strict-acceptance follow-up`: `show_personal_readiness_status` now supports `--strict-acceptance`. This is the final promotion gate: it exits non-zero until `acceptance_gate.accepted=true`, which requires the 20-trading-day evidence window, scheduler safety/activity, quote pre-readiness proof, risk/weekly evidence, and local scheduler runtime proof to be accepted. Unlike `--strict-monitor`, normal pre-close waiting still fails this final gate because the system is not yet proven usable for production reliance.

Update `2026-07-01 evidence-quality follow-up`: `validate_personal_readiness_window --json` and `show_personal_readiness_status --json` now include `evidence_quality`. This records total/trading/non-trading evidence counts, accepted/rejected counts, acceptance-candidate counts, formal/legacy/diagnostic counts, non-trading dates, and rejected evidence reasons. The current chain has 1 record, 1 accepted record, 0 rejected records, 0 diagnostic records, and 1 legacy record.

Update `2026-07-01 evidence-fingerprint follow-up`: `inspect_personal_readiness_evidence --json` now reports the inspected evidence file path, byte size, and SHA-256 hash. The current `2026-06-30` evidence file is 77,627 bytes with SHA-256 `419d657ab182837489f93e707af02fc3a77f932b5d4f456d78b11782d7987fe2`, giving the continuous-run record a stable fingerprint for later audit.

Update `2026-07-01 accepted-evidence-manifest follow-up`: `validate_personal_readiness_window --json` and `show_personal_readiness_status --json` now include `accepted_evidence`. Each evidence file counted toward the continuous window is listed with target date, path, byte size, SHA-256, evidence mode, acceptance-candidate flag, and acceptance reason. The current accepted evidence manifest contains the `2026-06-30` legacy evidence file with SHA-256 `419d657ab182837489f93e707af02fc3a77f932b5d4f456d78b11782d7987fe2`.

Update `2026-07-01 accepted-manifest-hash follow-up`: `validate_personal_readiness_window --json` and `show_personal_readiness_status --json` now include `accepted_evidence_manifest`. It hashes the normalized list of evidence files counted in the current continuous window. The current manifest has 1 record, target date `2026-06-30`, and SHA-256 `81c8cdd5b452159be77ee7038dc6b4d7f4db14e21fdc936fc6aaa73d649489ae`.

Update `2026-07-01 acceptance-gate-proof follow-up`: `show_personal_readiness_status --json` now mirrors `accepted_evidence_manifest` and `evidence_quality` into `acceptance_gate`. External scripts can now read the stable acceptance object for accepted/remaining days, next action, scheduler-aware acceptance, evidence quality, and the accepted evidence chain hash.

Update `2026-07-01 schedule-expectation follow-up`: `show_personal_readiness_status --json` now reports `schedule_expectation` at the top level and inside `acceptance_gate`. For the current next required date, it reports `scheduled_for=2026-07-01T23:50:00+08:00` on the `personal-readiness-daily-evidence` task. This distinguishes normal waiting from a post-schedule missed evidence run.

Update `2026-07-01 schedule-due-status follow-up`: `schedule_expectation` now includes `now`, `due_status`, `seconds_until_due`, and `seconds_overdue`. The current state reports `due_status=pending`, so the system is still waiting for the scheduled `2026-07-01T23:50:00+08:00` run rather than missing evidence.

Update `2026-07-01 overdue-monitor follow-up`: `show_personal_readiness_status --strict-monitor` now fails if `schedule_expectation.due_status=overdue`, even when `next_action` is still `wait_for_post_close`. This prevents a missed scheduled evidence run from being hidden behind the normal waiting state.

Update `2026-07-01 schedule-grace follow-up`: `schedule_expectation` now includes a 30-minute default overdue grace window with `grace_deadline`, `grace_minutes`, and `seconds_until_grace_deadline`. Runs between the scheduled time and grace deadline report `due_status=grace_period`; `--strict-monitor` fails only after the grace window expires. The current next run has `grace_deadline=2026-07-02T00:20:00+08:00`.

Update `2026-07-01 monitor-gate follow-up`: `show_personal_readiness_status --json` now includes `monitor_gate`, a machine-readable version of the `--strict-monitor` decision. It exposes `ok`, `state`, `reason`, `next_action`, `next_check_after`, and `due_status`. The current state reports `monitor_gate.ok=true`, `state=wait_for_post_close`, and `next_check_after=2026-07-01T23:50:00+08:00`.

Update `2026-07-01 completion-projection follow-up`: `validate_personal_readiness_window --json` and `show_personal_readiness_status --json` now report `projected_completion_date` and `projected_remaining_calendar_days`. These fields estimate the earliest date the 20-trading-day gate can complete if every remaining trading day produces accepted evidence. The current chain projects `2026-07-27`, 27 calendar days after the latest accepted target date `2026-06-30`.

Update `2026-07-01 completion-from-today follow-up`: `show_personal_readiness_status --json` now adds `status_date` and `acceptance_gate.projected_remaining_calendar_days_from_today`, while the non-JSON output prints the projected completion date. This separates the audit-relative remaining days from the operator's current-day countdown.

Update `2026-07-01 accepted-chain-quality follow-up`: `validate_personal_readiness_window --json` now reports `accepted_evidence_quality`, and `show_personal_readiness_status --json` mirrors it into `acceptance_gate`. This summarizes only the evidence files currently counted in the continuous acceptance chain, including date range and formal/legacy/diagnostic composition.

Update `2026-07-01 scheduler-run-metadata follow-up`: `show_personal_readiness_status --json` now exposes scheduler `run_metadata` for the daily readiness PeriodicTask, including `last_run_at`, `total_run_count`, and `date_changed`. This gives the 20-trading-day trial an audit trail for whether Celery beat has actually dispatched the scheduled evidence task.

Update `2026-07-01 scheduler-activity-acceptance follow-up`: final acceptance now requires scheduler activity evidence after the 20-trading-day evidence window is complete. `acceptance_gate.scheduler_activity` compares PeriodicTask `total_run_count` with the required trading-day count; if the evidence window is accepted but dispatch history is insufficient, `acceptance_gate.accepted=false` and `issue=insufficient_dispatch_history`.

Update `2026-07-01 acceptance-requirements follow-up`: `show_personal_readiness_status --json` now includes `acceptance_gate.requirements`, a machine-readable checklist for final promotion. It reports evidence window progress, scheduler safety, and scheduler activity as separate booleans so automation can explain why `--strict-acceptance` is still failing.

Update `2026-07-01 failed-requirements follow-up`: `show_personal_readiness_status --json` now includes `acceptance_gate.failed_requirements`, derived from the requirements checklist. `--strict-acceptance` also includes the failed requirement names in its CommandError message, so automation can route the next operator action without parsing the full JSON payload.

Update `2026-07-01 operator-actions follow-up`: `show_personal_readiness_status --json` now includes `acceptance_gate.operator_actions`. Each failed final-acceptance requirement is mapped to a concrete operator action, command, target date when relevant, and optional next check time. The current state maps the missing evidence window to `wait_for_post_close` for `2026-07-01`, with `python manage.py run_personal_readiness_daily --target-date 2026-07-01` as the eventual daily command.

Update `2026-07-01 scheduler-trigger-proof follow-up`: daily readiness evidence now records `operation_context.trigger_source`. Manual command runs default to `manual`; Celery beat runs default to `scheduler`; `setup_personal_readiness_daily` writes `trigger_source=scheduler` into the PeriodicTask kwargs. Final acceptance now requires accepted non-legacy evidence to be scheduler-triggered, so manually generated formal evidence cannot satisfy the scheduler activity proof. Because the accepted evidence manifest now includes `trigger_source`, the current manifest hash changed to `81c8cdd5b452159be77ee7038dc6b4d7f4db14e21fdc936fc6aaa73d649489ae`.

Update `2026-07-01 trigger-source-hardening follow-up`: `trigger_source` is now a Django management command stealth option instead of a public CLI flag on `collect_personal_readiness_evidence` or `run_personal_readiness_daily`. Shell users cannot pass `--trigger-source scheduler`; only internal `call_command` paths such as the Celery beat task can set the scheduler trigger marker.

Update `2026-07-01 scheduler-last-run-proof follow-up`: final scheduler activity acceptance now requires PeriodicTask `last_run_at` to exist and to be on or after the latest scheduler-triggered accepted evidence date. This prevents a sufficient `total_run_count` from passing final acceptance when the scheduler metadata is missing or stale relative to the evidence chain.

Update `2026-07-01 readiness-status-service follow-up`: scheduler activity acceptance calculation moved from the large status management command into `apps.task_monitor.application.readiness_status_services`. The command remains responsible for Django I/O and payload assembly, while the pure application helper owns the final scheduler proof calculation. This also restored the governance large-file check to 0 violations.

Update `2026-07-01 markdown-trigger-audit follow-up`: readiness Markdown evidence now prints `operation_context.trigger_source` and `operation_context.allow_unclosed_target_date` alongside mode, target-date closure, and latest closed date. This keeps the human-readable evidence file aligned with the JSON acceptance proof, so manual review can distinguish scheduler evidence from manual or diagnostic evidence without opening the raw JSON.

Update `2026-07-01 scheduler-task-provenance follow-up`: Celery-scheduled daily evidence now records `operation_context.trigger_task_id` and `operation_context.trigger_task_name`. The fields are internal Django management-command stealth options, passed only through the Celery task wrapper. The validator, status command, accepted evidence manifest, accepted-chain quality, and latest-evidence summary now expose this task provenance. Legacy evidence has these fields as `null`; the current accepted manifest hash is now `70df764851b9f5f36d652cb9df36f1d7ead76282cc5e15a84cf356a60c83e14b`.

Update `2026-07-01 scheduler-task-provenance-acceptance follow-up`: final scheduler activity acceptance now requires each non-legacy scheduler evidence record in the accepted window to include a Celery task id and the expected task name `apps.task_monitor.application.tasks.run_personal_readiness_daily_task`. Evidence that only has `trigger_source=scheduler` is no longer enough for final acceptance; `acceptance_gate.scheduler_activity.status=insufficient_scheduler_task_provenance` explains this failure mode.

Update `2026-07-01 scheduler-task-id-uniqueness follow-up`: final scheduler activity acceptance now requires Celery task ids to be unique across the accepted scheduler evidence records. Reusing one task id for multiple trading-day evidence files now fails with `acceptance_gate.scheduler_activity.status=duplicate_scheduler_task_ids`, so the final gate proves a sequence of distinct scheduled task executions rather than repeated evidence stamped with one task identity.

Update `2026-07-01 scheduler-activity-operator-action follow-up`: `acceptance_gate.operator_actions` now routes scheduler activity failures by failure type. Dispatch metadata failures still point to `python manage.py show_personal_readiness_status --json`; evidence provenance failures such as manual evidence, missing scheduler provenance, wrong task provenance, or duplicate task ids now point to `python manage.py validate_personal_readiness_window --json` with `action=inspect_scheduler_evidence_provenance`.

Update `2026-07-01 no-legacy-final-acceptance follow-up`: final scheduler activity acceptance now rejects accepted windows that still contain `legacy_without_operation_context` evidence. Legacy evidence remains visible during the current in-progress trial, but a final "truly usable" acceptance requires all counted days to be formal scheduled evidence with operation context and task provenance. The failure mode is `acceptance_gate.scheduler_activity.status=legacy_evidence_in_accepted_window`.

Update `2026-07-01 fixed-target-scheduler-safety follow-up`: scheduler safety now rejects PeriodicTask kwargs that contain a fixed `target_date`. The scheduled daily task must resolve the latest closed trading day at runtime; otherwise it could repeatedly write the same date and break the continuous 20-trading-day proof. `setup_personal_readiness_daily` rewrites kwargs and removes this drift.

Update `2026-07-01 scheduler-calendar-source-safety follow-up`: scheduler safety now requires `calendar_source=auto` in the daily readiness PeriodicTask kwargs. This keeps continuous validation anchored to the local Qlib trading calendar when available, while still allowing controlled fallback; a fixed `weekday` scheduler calendar now reports `unexpected_scheduler_calendar_source`. `setup_personal_readiness_daily` repairs the drift back to `auto`.

Update `2026-07-01 scheduler-main-chain-safety follow-up`: scheduler safety now rejects PeriodicTask kwargs that disable `run_workspace_refresh`, disable `include_weekly_advisor`, or relax `max_qlib_staleness_days` beyond 5 days. These settings are part of the personal investment main-chain proof: Regime/Pulse/workspace refresh, auto-advisor payload generation, and Alpha/Qlib freshness must remain active for scheduled daily evidence.

Update `2026-07-01 scheduler-kwargs-safety-extraction follow-up`: scheduler kwargs safety calculation moved into the pure Application-layer readiness status service. The management command still owns PeriodicTask I/O and payload assembly, while kwargs safety issues and metadata are now shared pure logic. This restored the governance large-file check after the main-chain scheduler safety rules pushed `show_personal_readiness_status.py` over the 1200 non-empty-line threshold.

Update `2026-07-01 pre-close-readiness-audit follow-up`: before the `2026-07-01` post-close scheduler window, read-only checks still pass. `healthcheck --json` reports `healthy=true`, Redis/database/Celery are ok with 1 worker, decision quotes for `510300.SH` and `000300.SH` remain decision-usable as the latest completed session, and Alpha/workspace consistency remains `ok`. `build_qlib_data --check-only --target-date 2026-06-30` confirms local Qlib data is fresh through the latest closed trading day. Formal acceptance still correctly fails at `1/20` accepted days because `2026-07-01` has not closed yet.

Update `2026-07-01 local-beat-runtime follow-up`: verified the local runtime needed for the scheduled continuous-run proof. The original `celery_beat_windows` command failed because it attempted to start beat through `app.worker_main`; it now uses `app.start(["beat", ...])` and has regression coverage. A local beat process is running with `django_celery_beat.schedulers:DatabaseScheduler`, and a second local solo worker was started after beat dispatched a backlog of due tasks. Decision quote snapshots briefly crossed the 4-hour freshness threshold during this runtime startup; they were refreshed synchronously through `refresh_decision_quote_snapshots`, restoring `healthcheck --json` to `healthy=true` with fresh `510300.SH` and `000300.SH` quotes.

Update `2026-07-01 local-runtime-monitor-gate follow-up`: `show_personal_readiness_status` now supports `--require-local-scheduler-runtime`. This optional local trial gate scans for Celery beat and worker processes and adds `scheduler_runtime` to the JSON payload. `--strict-monitor --require-local-scheduler-runtime` now fails before the scheduled evidence window if the local beat or worker runtime is missing, preventing a valid PeriodicTask configuration from hiding a stopped scheduler process.

Update `2026-07-01 local-worker-ping-gate follow-up`: the local scheduler runtime gate now also verifies Celery worker responsiveness with `inspect ping`. A running worker process is no longer enough; `scheduler_runtime` reports `responsive_worker_count`, `worker_ping_status`, and `worker_ping_error`, and strict monitoring fails with `local_celery_worker_not_responsive` if worker processes exist but none respond. The current local runtime reports two responsive workers.

## Fixes Applied

1. Qlib builder now falls back to existing local `instruments/<universe>.txt` members when Tushare `index_weight` is rate-limited or returns empty data.
2. Added unit coverage for the local-universe fallback path.
3. Refreshed decision-grade quote snapshots for `510300.SH` and `000300.SH`.
4. Started a local Celery worker for readiness verification.
5. Rebuilt recent Qlib data for `csi300` through `2026-06-30`.
6. Refreshed default-account workspace recommendations from the latest `2026-06-30` Qlib Alpha ranking.
7. Added `collect_personal_readiness_evidence` as a daily continuous-run evidence command.
8. Added `repair_personal_account_readiness` and created two readiness simulated accounts:
   - `613` for user `182`, `admin_投研验收模拟仓`, total value `1000000.00`
   - `614` for user `222`, `codex_user_投研验收模拟仓`, total value `1000000.00`
9. Rotation workspace refresh now treats expected empty-allocation configs as `skipped` instead of failed, while preserving the skip reason in evidence.
10. Added `validate_personal_readiness_window` as the executable 20-trading-day acceptance gate.
11. Added `run_personal_readiness_daily` as the daily scheduler-friendly wrapper around account preflight, evidence collection, and window validation.
12. Tightened `validate_personal_readiness_window` to require a continuous accepted trading-day streak instead of accepting non-contiguous historical evidence.
13. Added Qlib trading-calendar support to the readiness window validator and daily wrapper, with explicit `--calendar-source` control.
14. Added `run_personal_readiness_daily_task` and `setup_personal_readiness_daily` so continuous evidence collection can be run by Celery beat instead of relying on manual command execution.
15. Updated the daily readiness wrapper so omitted target dates resolve to the latest closed trading day instead of blindly using the current calendar date or the latest available local Qlib data date.
16. Added expected-latest-date anchoring to `validate_personal_readiness_window` and wired daily runs to validate through their own target date, making missed scheduled evidence visible.
17. Added `next_required_date` / `next_required_reason` to the window validator so operators can see the exact next trading day or blocking date required to advance the acceptance window.
18. Added `show_personal_readiness_status` as a read-only operational status command for continuous-run monitoring.
19. Added structured `next_action` to the status command so operators do not run readiness evidence before the next required trading day has closed.
20. Added an unclosed target-date guard to `run_personal_readiness_daily` and its Celery task wrapper so manual or scheduled runs cannot create formal evidence for a trading day before it has closed unless explicitly run in diagnostic mode.
21. Added explicit scheduler safety kwargs and status reporting so `show_personal_readiness_status` proves scheduled daily evidence has `allow_unclosed_target_date=false` and `repair_accounts=false`.
22. Extended scheduler safety reporting to flag disabled periodic tasks, missing/unverifiable crontab schedules, and schedules before `15:30 Asia/Shanghai`.
23. Wired scheduler safety into the top-level readiness status and `next_action`, so scheduler drift is treated as an operational issue before manual daily evidence commands.
24. Added evidence `operation_context` and aligned the split evidence collector with the unclosed target-date guard used by the daily wrapper.
25. Updated the continuous window validator so diagnostic or unclosed-target evidence cannot count toward the 20-trading-day formal acceptance gate.
26. Exposed latest evidence `operation_context` and `formal_evidence` in the status command for daily operator review.
27. Added `latest_formal_evidence` to the status command so diagnostic future evidence cannot hide the latest formal acceptance candidate.
28. Added `acceptance_gate` to the status command as the stable machine-readable summary for continuous-run acceptance progress.
29. Aligned split evidence collection with the daily wrapper so omitted target dates resolve to the latest closed trading day.
30. Added evidence classification fields to the status command so the first legacy accepted day, new formal evidence, and diagnostic evidence are distinguishable during the 20-day review.
31. Tightened evidence `operation_context.mode` so the unclosed-target override can never produce a formal-looking evidence file, even when the requested target date is already closed.
32. Made scheduler safety a hard part of the status acceptance rollup, preventing a completed evidence window from hiding a broken continuous-run scheduler.
33. Added scheduler timezone safety validation so a non-`Asia/Shanghai` crontab cannot be treated as post-close-ready.
34. Added scheduler weekday safety validation so the continuous-run task must stay on the expected `mon-fri` evidence cadence.
35. Added scheduler day/month safety validation so the continuous-run task cannot silently run only on a subset of calendar days or months.
36. Added PeriodicTask run-control safety validation so one-off or expiring scheduler settings cannot be treated as continuous-run ready.
37. Added PeriodicTask positional-args safety validation so the scheduled readiness task must be driven only by explicit kwargs.
38. Added PeriodicTask delivery-control safety validation so the scheduled readiness task cannot silently route to a custom queue/exchange/routing key or carry custom delivery headers.
39. Updated `setup_personal_readiness_daily` to repair unsafe PeriodicTask controls on existing tasks, closing the scheduler safety remediation loop.
40. Updated readiness status next-action handling so a missing closed-date evidence file directly returns the daily evidence command, while failed evidence still requires inspection.
41. Added `inspect_personal_readiness_evidence` so failed daily evidence can be diagnosed from a stable JSON command before rerunning or repairing accounts/data.
42. Added `show_personal_readiness_status --strict-monitor` so external scripts can detect operator-action states without treating normal pre-close waiting as a failure.
43. Added `show_personal_readiness_status --strict-acceptance` as the final automated gate for claiming the personal investment system is truly usable.
44. Added window-level `evidence_quality` output so the acceptance run can prove which evidence files were counted, rejected, legacy, diagnostic, or non-trading.
45. Added file fingerprints to `inspect_personal_readiness_evidence` so daily evidence files can be referenced by SHA-256 in audit notes.
46. Added `accepted_evidence` to the readiness validator/status payload so every accepted day in the current window has a file-level audit manifest.
47. Added `accepted_evidence_manifest` so the full accepted evidence chain can be verified with one normalized SHA-256 hash.
48. Mirrored evidence manifest and quality fields into `acceptance_gate` so the final acceptance object carries both progress and proof metadata.
49. Added `schedule_expectation` to readiness status and `acceptance_gate` so monitoring can see when the next scheduled evidence run should occur.
50. Added due/overdue timing fields to `schedule_expectation` so monitors can distinguish normal pre-run waiting from missed scheduled evidence.
51. Updated strict monitor behavior so overdue scheduled evidence is treated as an operator-action failure.
52. Added a configurable schedule overdue grace window so monitor checks allow normal Celery beat/worker delay before failing.
53. Added `monitor_gate` so external monitors can read the same health decision as `--strict-monitor` without relying only on process exit status.
54. Added continuous-window completion projection fields so operators can see the earliest possible final acceptance date from the same JSON payload used by monitors.
55. Added status-date-relative completion countdown output for day-to-day readiness monitoring.
56. Added accepted-chain evidence quality output so final acceptance can distinguish counted evidence quality from all files in the evidence directory.
57. Added scheduler run metadata output so continuous-run monitoring can audit PeriodicTask dispatch history.
58. Made scheduler dispatch history part of final acceptance so a manually generated 20-day evidence window cannot bypass continuous-run proof.
59. Added acceptance requirement breakdown fields so the final gate exposes which proof component is still missing.
60. Added failed acceptance requirement output and strict-acceptance error details for direct operator routing.
61. Added acceptance operator actions so external monitors can route each failed final-acceptance requirement to a concrete next command or wait state.
62. Added scheduler trigger-source proof so accepted formal evidence must come from the scheduled daily task before it can satisfy final scheduler activity acceptance.
63. Hid the trigger-source marker from public CLI parsing so the scheduler marker is only available to internal `call_command` execution paths.
64. Added scheduler `last_run_at` coverage proof so final acceptance requires the recorded scheduler run date to cover the latest scheduler-triggered accepted evidence.
65. Extracted scheduler activity status calculation into an Application-layer pure helper to keep the status command below the governance large-file threshold.
66. Added trigger-source and unclosed-target override fields to the human-readable readiness Markdown evidence.
67. Added Celery task provenance (`trigger_task_id`, `trigger_task_name`) to scheduler evidence, accepted evidence manifests, latest evidence summaries, and evidence quality counters.
68. Made Celery task provenance a hard final-acceptance requirement for non-legacy scheduler evidence in the accepted window.
69. Made Celery task id uniqueness a hard final-acceptance requirement so repeated task ids cannot satisfy the continuous-run proof.
70. Split scheduler activity operator actions so dispatch-history failures and evidence-provenance failures route to the right diagnostic command.
71. Made legacy evidence a final-acceptance blocker so the 20-day production proof must be fully formal scheduled evidence.
72. Added fixed `target_date` scheduler safety detection and verified setup repair removes the drift from PeriodicTask kwargs.
73. Added scheduler `calendar_source=auto` safety detection and verified setup repair removes unsafe `weekday` scheduler drift.
74. Added scheduler safety checks for workspace refresh, weekly advisor payload, and Qlib staleness threshold drift.
75. Extracted scheduler kwargs safety logic into the Application layer so the status command remains below the governance large-file threshold while preserving the same scheduler safety payload.
76. Fixed the Windows Celery beat management command so local continuous-run scheduling can actually start with the database scheduler.
77. Started local Celery beat and a second local solo worker for the continuous-run trial, then refreshed decision quote snapshots after they crossed the freshness threshold during runtime startup.
78. Added an optional local scheduler runtime gate to readiness status so strict monitoring can require both Celery beat and worker processes during the local continuous-run trial.
79. Extended the local scheduler runtime gate with Celery worker ping responsiveness, so a stuck or non-responsive worker process cannot pass the local continuous-run monitor.

## Evidence

### Code And Migration Baseline

| Check | Result |
| --- | --- |
| `git status --short` before code fix | clean |
| `python manage.py check` | passed, no issues |
| `python manage.py showmigrations --plan | Select-String "\\[ \\]"` | no unapplied migrations |
| `python manage.py check --deploy` with production settings and required env | passed, no issues |

### Readiness

`python manage.py healthcheck --json` returned `healthy: true`.

Key sections:

| Section | Status | Evidence |
| --- | --- | --- |
| database | ok | connection and `SELECT 1` passed |
| redis | ok | Redis/cache ping passed |
| celery | ok | 1 worker responded |
| critical_data | ok | `macro_indicator=true`, `regime_state=true` |
| decision_data | ok | quotes fresh and `must_not_use_for_decision=false` |
| alpha_workspace_consistency | ok | no issues |

Decision quote freshness:

| Asset | Snapshot UTC | Source | Status |
| --- | --- | --- | --- |
| `510300.SH` | `2026-06-30T14:05:50.221723+00:00` | akshare | fresh |
| `000300.SH` | `2026-06-30T14:06:07.223286+00:00` | akshare | fresh |

### Qlib / Alpha

`python manage.py build_qlib_data --check-only --target-date 2026-06-30` passed:

| Field | Value |
| --- | --- |
| provider_uri | `D:\githv\agomTradePro\data\qlib\cn_data` |
| target_date | `2026-06-30` |
| latest_trade_date | `2026-06-30` |
| result | data freshness satisfied |

`python manage.py build_qlib_data --universes csi300 --lookback-days 90 --target-date 2026-06-30` completed:

| Field | Value |
| --- | --- |
| universe | `csi300` |
| latest_after | `2026-06-30` |
| effective_target_date | `2026-06-30` |
| instrument_files_written | 2 |
| feature_series_written | 1617 |
| stock_count | 300 |

Alpha/workspace consistency after refresh:

| Field | Value |
| --- | --- |
| status | ok |
| alpha.latest_trade_date | `2026-06-30` |
| alpha.provider_source | `qlib` |
| qlib provider status | available |
| workspace.latest_updated_at | `2026-06-30T14:35:17.249247+00:00` |
| issues | none |

### Continuous-Run Evidence Collector

`python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --include-weekly-advisor --run-workspace-refresh` completed:

| Section | Status | Evidence |
| --- | --- | --- |
| system | ok | readiness rollup healthy |
| qlib | ok | `build_qlib_data --check-only` passed |
| workspace | ok | Regime, Pulse, action recommendation and rotation refresh completed |
| account targets | ok | 2 decision-ready simulated accounts discovered |
| auto advisor | ok | console and weekly report payloads generated |
| risk-center daily report | ok | post-investment reports generated for accounts `613` and `614` |

This command is the preferred daily record for the 20-trading-day trial. Files under `var/readiness-evidence/` are local runtime evidence and are intentionally ignored by Git.

Scheduled execution:

| Field | Value |
| --- | --- |
| Celery task | `apps.task_monitor.application.tasks.run_personal_readiness_daily_task` |
| Periodic task | `personal-readiness-daily-evidence` |
| Quote pre-refresh task | `decision-quote-pre-readiness-refresh` at `mon-fri 15:35 Asia/Shanghai` |
| Daily evidence task | `personal-readiness-daily-evidence` at `mon-fri 16:10 Asia/Shanghai` |
| Weekly auto-advisor task | `dashboard-auto-advisor-weekly-report` at `fri 17:30 Asia/Shanghai` |
| Default calendar source | `auto` (`qlib` when local calendar coverage is available) |
| Scheduler calendar source safety | scheduled kwargs must keep `calendar_source=auto`; setup repair restores it |
| Scheduler main-chain safety | scheduled kwargs must keep workspace refresh and weekly advisor enabled, with Qlib staleness threshold no looser than 5 days |
| Account repair | disabled by default (`repair_accounts=false`) |
| Unclosed target-date override | disabled by default (`allow_unclosed_target_date=false`) |
| Scheduler safety status | `ok`, no issues |
| Fixed target date safety | scheduled kwargs must not include `target_date`; setup repair removes it |
| Scheduler run metadata | exposed as `last_run_at`, `total_run_count`, `date_changed` |
| Scheduler task provenance | accepted evidence records expose `trigger_task_id` and `trigger_task_name`; current legacy record has both as `null` |
| Scheduler activity acceptance | requires the accepted window to contain no legacy evidence; all counted days must have `trigger_source=scheduler`, expected `trigger_task_name`, unique Celery `trigger_task_id` values, and scheduler `last_run_at` coverage once the 20-trading-day evidence window is complete |
| Acceptance requirement breakdown | `evidence_window`, `scheduler_safety`, `scheduler_activity` |
| Failed requirement output | current failed requirement is `evidence_window` |

Workspace detail:

| Component | Status | Evidence |
| --- | --- | --- |
| macro_sync | success | 11 synced, 54 skipped, 8 no-data indicators |
| regime_snapshot | success | `Recovery`, confidence `0.3285361890491151` |
| pulse_snapshot | success | composite `0.151`, strength `moderate`, reliable `true` |
| action_recommendation | success | risk budget `0.85`, sectors `消费 / 科技 / 金融` |
| rotation_signals | success | 6 configs: 3 successful, 3 skipped, 0 failed |

Continuous window detail:

| Field | Value |
| --- | --- |
| command | `python manage.py validate_personal_readiness_window --json` |
| status | `in_progress` |
| required_days | 20 |
| accepted_days | 4 |
| remaining_days | 16 |
| scheduler_clean_suffix_days | 3 |
| scheduler_clean_remaining_days | 17 |
| next_required_date | `2026-07-06` |
| next_required_reason | `next_trading_day` |
| projected_completion_date | `2026-07-27` |
| projected_scheduler_completion_date | `2026-07-28` |
| projected_remaining_calendar_days | 24 |
| projected_remaining_calendar_days_from_today | 24 |
| projected_scheduler_remaining_calendar_days | 25 |
| projected_scheduler_remaining_calendar_days_from_today | 25 |
| accepted_evidence_quality.record_count | 4 |
| accepted_evidence_quality.legacy_record_count | 1 |
| accepted_evidence_quality.formal_record_count | 3 |
| accepted_evidence_quality.diagnostic_record_count | 0 |
| accepted_evidence_quality.task_provenance_record_count | 3 |
| accepted_evidence_quality.missing_task_provenance_record_count | 1 |
| accepted_evidence_manifest.sha256 | `2417bd2d161d682937c4f5602ae7053e812d401f90de1c2159fd12033e5ca161` |
| latest_target_date | `2026-07-03` |
| blocking_issues | none |

`accepted_days` is a continuous accepted trading-day streak ending at `latest_target_date`. The default calendar source is `auto`, which uses the local Qlib trading calendar when available and reports the effective `calendar_source` in JSON output. Non-contiguous evidence does not accumulate toward the 20-day gate.

### Tests

| Command | Result |
| --- | --- |
| `pytest tests\unit\test_qlib_builder.py -q` | 6 passed |
| `pytest tests\unit\test_health_checks.py tests\guardrails\test_alpha_workspace_consistency_guardrail.py -q` | 19 passed |
| `pytest tests\unit\test_qlib_builder.py tests\unit\test_health_checks.py tests\guardrails\test_alpha_workspace_consistency_guardrail.py -q` | 25 passed |
| `pytest tests\guardrails\test_no_501_on_primary_paths.py tests\unit\test_scheduler_initialization.py -q` | 12 passed |
| `pytest tests\unit\test_personal_readiness_evidence_command.py -q` | 4 passed |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` | 10 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` | 4 passed |
| `pytest tests\unit\test_personal_readiness_daily_command.py -q` | 3 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_daily_command.py -q` after Qlib calendar support | 12 passed |
| `pytest tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py -q` | 21 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_daily_command.py -q` after expected-date anchoring | 17 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after next-required-date output | 13 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` | 2 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after next-action output | 4 passed |
| `pytest tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_daily_task.py -q` after unclosed target-date guard | 11 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` | 45 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py -q` after scheduler safety output | 12 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler safety output | 46 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py -q` after disabled/early scheduler warnings | 14 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after disabled/early scheduler warnings | 48 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler top-level action | 8 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler top-level action | 49 passed |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py -q` after evidence operation context | 14 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after evidence operation context | 51 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after diagnostic evidence rejection | 14 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after diagnostic evidence rejection | 52 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after latest evidence context output | 10 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after latest evidence context output | 54 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after latest formal evidence output | 11 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after latest formal evidence output | 55 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after `acceptance_gate` output | 11 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after `acceptance_gate` output | 55 passed |
| `pytest tests\unit\test_personal_readiness_evidence_command.py -q` after split collector default-date alignment | 8 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after split collector default-date alignment | 57 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after evidence classification output | 11 passed |
| `ruff check apps\alpha\infrastructure\qlib_builder.py tests\unit\test_qlib_builder.py` | passed |
| `black --check apps\alpha\infrastructure\qlib_builder.py tests\unit\test_qlib_builder.py` | passed |
| `isort --check-only apps\alpha\infrastructure\qlib_builder.py tests\unit\test_qlib_builder.py` | passed |
| `ruff check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_command.py` | passed |
| `black --check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_command.py` | passed |
| `isort --check-only apps\task_monitor\management\commands\collect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_command.py` | passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `python manage.py show_personal_readiness_status --json` after evidence classification output | `accepted_days=1`, `remaining_days=19`, `next_action=wait_for_post_close`, latest legacy evidence `acceptance_candidate=true` |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py -q` after diagnostic override mode | 35 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler acceptance rollup | 12 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler timezone safety | 13 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler weekday safety | 14 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler day/month safety | 15 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler run-control safety | 16 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler args safety | 17 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler delivery safety | 18 passed |
| `pytest tests\unit\test_scheduler_initialization.py tests\unit\test_personal_readiness_status_command.py -q` after scheduler repair defaults | 24 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after missing-evidence action split | 19 passed |
| `pytest tests\unit\test_personal_readiness_evidence_inspection.py -q` | 5 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_evidence_inspection.py -q` after evidence inspection next-action wiring | 24 passed |
| `python manage.py inspect_personal_readiness_evidence --json` | accepted current `2026-06-30` evidence with no blockers; legacy missing `operation_context` reported as an observation |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after strict monitor mode | 21 passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor` | exit code 0 while correctly waiting for `2026-07-01` post-close |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after strict acceptance mode | 23 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after evidence quality output | 15 passed |
| `python manage.py show_personal_readiness_status --json` after evidence quality output | `evidence_quality.record_count=1`, `accepted_record_count=1`, `rejected_record_count=0`, `diagnostic_record_count=0` |
| `pytest tests\unit\test_personal_readiness_evidence_inspection.py -q` after evidence fingerprint output | 5 passed |
| `python manage.py inspect_personal_readiness_evidence --json` after evidence fingerprint output | current `2026-06-30` evidence SHA-256 `419d657ab182837489f93e707af02fc3a77f932b5d4f456d78b11782d7987fe2` |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after accepted evidence manifest output | 15 passed |
| `python manage.py show_personal_readiness_status --json` after accepted evidence manifest output | `accepted_evidence[0].target_date=2026-06-30`, SHA-256 `419d657ab182837489f93e707af02fc3a77f932b5d4f456d78b11782d7987fe2` |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after accepted manifest hash output | 15 passed |
| `python manage.py show_personal_readiness_status --json` after accepted manifest hash output | `accepted_evidence_manifest.sha256=81c8cdd5b452159be77ee7038dc6b4d7f4db14e21fdc936fc6aaa73d649489ae` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after acceptance gate proof metadata | 23 passed |
| `python manage.py show_personal_readiness_status --json` after acceptance gate proof metadata | `acceptance_gate.accepted_evidence_manifest.sha256=81c8cdd5b452159be77ee7038dc6b4d7f4db14e21fdc936fc6aaa73d649489ae` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after schedule expectation output | 24 passed |
| `python manage.py show_personal_readiness_status --json` after schedule expectation output | `schedule_expectation.scheduled_for=2026-07-01T23:50:00+08:00` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after schedule due-status output | 25 passed |
| `python manage.py show_personal_readiness_status --json` after schedule due-status output | `schedule_expectation.due_status=pending`, `seconds_overdue=0` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after overdue strict-monitor guard | 26 passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after overdue strict-monitor guard | exit code 0 while current `schedule_expectation.due_status=pending` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after schedule grace window | 27 passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after schedule grace window | exit code 0 with `schedule_expectation.grace_deadline=2026-07-02T00:20:00+08:00` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after monitor gate output | 28 passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after monitor gate output | exit code 0 with `monitor_gate.ok=true`, `state=wait_for_post_close` |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py -q` after completion projection output | 43 passed |
| `ruff check apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after current-day completion countdown output | 28 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py -q` after accepted-chain quality output | 43 passed |
| `ruff check apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler run metadata output | 28 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler activity acceptance gate | 30 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after acceptance requirement breakdown output | 30 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after failed requirement output | 30 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after acceptance operator actions output | 30 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after acceptance operator actions output | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after acceptance operator actions output | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after acceptance operator actions output | 85 passed |
| `python manage.py check` after acceptance operator actions output | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after acceptance operator actions output | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after acceptance operator actions output | exit code 0 with `monitor_gate.ok=true`, `operator_actions[0].action=wait_for_post_close` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after acceptance operator actions output | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py -q` after scheduler trigger-source proof | 72 passed |
| `ruff check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py apps\task_monitor\management\commands\run_personal_readiness_daily.py apps\task_monitor\management\commands\setup_personal_readiness_daily.py apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py apps\task_monitor\application\tasks.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py` after scheduler trigger-source proof | passed |
| `black --check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py apps\task_monitor\management\commands\run_personal_readiness_daily.py apps\task_monitor\management\commands\setup_personal_readiness_daily.py apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py apps\task_monitor\application\tasks.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py` after scheduler trigger-source proof | passed |
| `python manage.py setup_personal_readiness_daily` after scheduler trigger-source proof | configured `personal-readiness-daily-evidence` with `trigger_source=scheduler` |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler trigger-source proof | 85 passed |
| `python manage.py check` after scheduler trigger-source proof | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler trigger-source proof | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler trigger-source proof | exit code 0 with scheduler safety `trigger_source=scheduler` and `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler trigger-source proof | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py -q` after trigger-source hardening | 18 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after trigger-source hardening | 85 passed |
| `ruff check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py apps\task_monitor\management\commands\run_personal_readiness_daily.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py` after trigger-source hardening | passed |
| `black --check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py apps\task_monitor\management\commands\run_personal_readiness_daily.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py` after trigger-source hardening | passed |
| `python manage.py help collect_personal_readiness_evidence` and `python manage.py help run_personal_readiness_daily` after trigger-source hardening | `trigger-source` help matches: `0` for both commands |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after trigger-source hardening | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after trigger-source hardening | exit code 0 with scheduler safety `trigger_source=scheduler` and `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after trigger-source hardening | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler last-run proof | 30 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler last-run proof | 85 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler last-run proof | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler last-run proof | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after readiness status service extraction | 30 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after readiness status service extraction | 85 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py apps\task_monitor\application\readiness_status_services.py tests\unit\test_personal_readiness_status_command.py` after readiness status service extraction | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py apps\task_monitor\application\readiness_status_services.py tests\unit\test_personal_readiness_status_command.py` after readiness status service extraction | passed |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after readiness status service extraction | 0 violations |
| `python manage.py check` after readiness status service extraction | passed, no issues |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after readiness status service extraction | exit code 0 with `monitor_gate.ok=true` and scheduler safety ok |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after readiness status service extraction | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_evidence_command.py -q` after markdown trigger audit output | 10 passed |
| `ruff check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_command.py` after markdown trigger audit output | passed |
| `black --check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_command.py` after markdown trigger audit output | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after markdown trigger audit output | 85 passed |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after markdown trigger audit output | 0 violations |
| `python manage.py check` after markdown trigger audit output | passed, no issues |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after markdown trigger audit output | exit code 0 with `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after markdown trigger audit output | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py -q` after scheduler task provenance output | 66 passed |
| `ruff check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py apps\task_monitor\management\commands\run_personal_readiness_daily.py apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py apps\task_monitor\application\tasks.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py` after scheduler task provenance output | passed |
| `black --check apps\task_monitor\management\commands\collect_personal_readiness_evidence.py apps\task_monitor\management\commands\run_personal_readiness_daily.py apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py apps\task_monitor\application\tasks.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py` after scheduler task provenance output | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler task provenance output | 85 passed |
| `python manage.py check` after scheduler task provenance output | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler task provenance output | 0 violations |
| `python manage.py help collect_personal_readiness_evidence` and `python manage.py help run_personal_readiness_daily` after scheduler task provenance output | `trigger-source` / `trigger-task*` help matches: `0` for both commands |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler task provenance output | exit code 0 with `accepted_evidence_manifest.sha256=70df764851b9f5f36d652cb9df36f1d7ead76282cc5e15a84cf356a60c83e14b`, `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler task provenance output | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler task provenance final gate | 30 passed |
| `ruff check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler task provenance final gate | passed |
| `black --check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler task provenance final gate | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler task provenance final gate | 85 passed |
| `python manage.py check` after scheduler task provenance final gate | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler task provenance final gate | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler task provenance final gate | exit code 0 with `scheduler_activity.status=pending_window`, `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler task provenance final gate | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler task id uniqueness gate | 30 passed |
| `ruff check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler task id uniqueness gate | passed |
| `black --check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler task id uniqueness gate | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler task id uniqueness gate | 85 passed |
| `python manage.py check` after scheduler task id uniqueness gate | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler task id uniqueness gate | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler task id uniqueness gate | exit code 0 with `unique_scheduler_task_id_count=0`, `duplicate_scheduler_task_id_count=0`, `monitor_gate.ok=true` while still waiting for post-close |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler task id uniqueness gate | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after scheduler activity operator-action routing | 30 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler activity operator-action routing | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after scheduler activity operator-action routing | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler activity operator-action routing | 85 passed |
| `python manage.py check` after scheduler activity operator-action routing | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler activity operator-action routing | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler activity operator-action routing | exit code 0 with `monitor_gate.ok=true` while still waiting for post-close |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler activity operator-action routing | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after no-legacy final acceptance gate | 30 passed |
| `ruff check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after no-legacy final acceptance gate | passed |
| `black --check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after no-legacy final acceptance gate | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after no-legacy final acceptance gate | 85 passed |
| `python manage.py check` after no-legacy final acceptance gate | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after no-legacy final acceptance gate | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after no-legacy final acceptance gate | exit code 0 with `scheduler_activity.status=pending_window`, `legacy_record_count=1`, `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after no-legacy final acceptance gate | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py -q` after fixed target-date scheduler safety | 36 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after fixed target-date scheduler safety | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after fixed target-date scheduler safety | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after fixed target-date scheduler safety | 85 passed |
| `python manage.py check` after fixed target-date scheduler safety | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after fixed target-date scheduler safety | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after fixed target-date scheduler safety | exit code 0 with scheduler safety `status=ok` and no `fixed_scheduler_target_date` issue |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after fixed target-date scheduler safety | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py -q` after scheduler calendar source safety | 36 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after scheduler calendar source safety | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after scheduler calendar source safety | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler calendar source safety | 85 passed |
| `python manage.py check` after scheduler calendar source safety | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler calendar source safety | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler calendar source safety | exit code 0 with scheduler safety `calendar_source=auto`, `status=ok`, no issues |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler calendar source safety | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py -q` after scheduler main-chain safety | 36 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after scheduler main-chain safety | passed |
| `black --check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after scheduler main-chain safety | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler main-chain safety | 85 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py -q` after scheduler kwargs safety extraction | 36 passed |
| `ruff check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after scheduler kwargs safety extraction | passed |
| `black --check apps\task_monitor\application\readiness_status_services.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py` after scheduler kwargs safety extraction | passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_daily_task.py tests\unit\test_scheduler_initialization.py tests\unit\test_macro_periodic_tasks.py tests\unit\test_personal_readiness_daily_command.py tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_account_readiness_repair.py tests\unit\test_rotation_readiness_degradation.py -q` after scheduler kwargs safety extraction | 85 passed |
| `python manage.py check` after scheduler kwargs safety extraction | passed, no issues |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after scheduler kwargs safety extraction | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor` after scheduler kwargs safety extraction | exit code 0 with scheduler safety `status=ok`, `monitor_gate.ok=true` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after scheduler kwargs safety extraction | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `python manage.py healthcheck --json` during `2026-07-01` pre-close audit | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py build_qlib_data --check-only --target-date 2026-06-30` during `2026-07-01` pre-close audit | Qlib freshness satisfied; latest trade date `2026-06-30` |
| `python manage.py show_personal_readiness_status --json --strict-monitor` during `2026-07-01` pre-close audit | exit code 0 with `monitor_gate.ok=true`, `next_action=wait_for_post_close`, `due_status=pending` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` during `2026-07-01` pre-close audit | expected exit code 1: `1/20` days accepted, `failed_requirements=evidence_window` |
| `pytest tests\unit\test_celery_beat_windows_command.py -q` after local beat startup fix | 1 passed |
| `ruff check core\management\commands\celery_beat_windows.py tests\unit\test_celery_beat_windows_command.py` after local beat startup fix | passed |
| `black --check core\management\commands\celery_beat_windows.py tests\unit\test_celery_beat_windows_command.py` after local beat startup fix | passed |
| `python manage.py celery_beat_windows --loglevel=info` through hidden `Start-Process` after local beat startup fix | running process observed; stderr log contains `beat: Starting...` and `DatabaseScheduler: Schedule changed.` |
| `python manage.py shell -c "... refresh_decision_quote_snapshots() ..."` after decision quote freshness crossed threshold | `status=success`, `synced_count=2`, `must_not_use_for_decision=false` |
| `python manage.py healthcheck --json` after local beat/worker startup and quote refresh | `healthy=true`; Celery workers responsive, decision quotes fresh, Alpha/workspace consistency ok |
| `pytest tests\unit\test_celery_beat_windows_command.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_scheduler_initialization.py -q` after local beat startup fix | 37 passed |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_celery_beat_windows_command.py tests\unit\test_scheduler_initialization.py -q` after local runtime monitor gate | 39 passed |
| `ruff check apps\task_monitor\management\readiness_runtime.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after local runtime monitor gate | passed |
| `black --check apps\task_monitor\management\readiness_runtime.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after local runtime monitor gate | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime` after local runtime monitor gate | exit code 0 with `scheduler_runtime.status=ok`, `beat_process_count=1`, `worker_process_count=2` |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_celery_beat_windows_command.py tests\unit\test_scheduler_initialization.py -q` after local worker ping gate | 40 passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after local worker ping gate | exit code 0 with `scheduler_runtime.status=ok`, `worker_process_count=2`, `responsive_worker_count=2`, `worker_ping_status=ok` |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after final local runtime gate audit | 0 violations |
| `ruff check apps\task_monitor\management\readiness_runtime.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after final local runtime gate audit | passed |
| `python -m black --check apps\task_monitor\management\readiness_runtime.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after final local runtime gate audit | passed |
| `python manage.py check` after final local runtime gate audit | passed, no issues |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_celery_beat_windows_command.py tests\unit\test_scheduler_initialization.py -q` after final local runtime gate audit | 40 passed |
| `python manage.py healthcheck --json` after final local runtime gate audit | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after final local runtime gate audit | exit code 0 with `scheduler_runtime.status=ok`, `beat_process_count=1`, `worker_process_count=2`, `responsive_worker_count=2`, `worker_ping_status=ok` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` after final local runtime gate audit | expected exit code 1: `1/20` days accepted, `remaining=19`, `failed_requirements=evidence_window`, next check after `2026-07-01T23:50:00+08:00` |
| `python manage.py build_qlib_data --check-only --target-date 2026-06-30` during `2026-07-01` main-chain follow-up | Qlib freshness satisfied; latest trade date `2026-06-30` |
| `python manage.py warmup_cache --only regime,macro,alpha` during `2026-07-01` main-chain follow-up | regime, macro, and alpha caches warmed successfully |
| `pytest tests\unit\test_auto_advisor_outputs.py tests\unit\test_auto_advisor_decision_sheet.py tests\guardrails\test_advisor_sheet_api.py tests\unit\test_pulse_use_cases.py tests\unit\test_regime_orchestration.py tests\unit\account\test_account_risk_periodic_tasks.py tests\e2e\test_decision_workspace.py -q` during `2026-07-01` main-chain follow-up | 63 passed |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` during `2026-07-01` main-chain follow-up | exit code 0, `status=ok`, `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, `target_count=2`, both account checks ok |
| `python manage.py sync_market_thermometer_inputs --as-of-date 2026-07-01` during market thermometer follow-up | partial data refresh: turnover, margin balance, and market news succeeded; new investor accounts and ETF net flow remained unavailable/timed out |
| `python manage.py calculate_market_thermometer --as-of-date 2026-07-01` during market thermometer follow-up | generated a blocked snapshot with `valid_component_count=2`, `must_not_use_for_decision=true`; this exposed the need to fall back to the latest usable thermometer snapshot for decision readiness |
| Decision readiness fallback patch after market thermometer follow-up | `get_decision_data_readiness_payload()` now uses the latest usable thermometer snapshot when the newest snapshot is blocked, and exposes the skipped blocked snapshot as `skipped_latest_market_thermometer` |
| `pytest tests\unit\data_center\test_decision_data_readiness.py tests\unit\data_center\test_repositories.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_celery_beat_windows_command.py tests\unit\test_scheduler_initialization.py -q` after decision readiness fallback patch | 47 passed |
| `python manage.py healthcheck --json` after decision readiness fallback patch | `healthy=true`, `decision_data.status=ok`; current blocked `2026-07-01` thermometer snapshot is retained under `skipped_latest_market_thermometer` while the latest usable `2026-06-26` snapshot remains active for decision readiness |
| `collect_personal_readiness_evidence(...)` application call after decision readiness fallback patch | diagnostic no-file run returned `status=ok`; `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, `target_count=2`; accounts `613` and `614` both had `risk_status=ok`, `advisor_status=ok`, `weekly_status=ok`, and `weekly_data_health_blocked=false` |
| Periodic task audit during `2026-07-01` pre-close follow-up | decision quote refresh tasks are enabled at weekdays `09:45`, weekdays `15:20`, and every 6 hours; market thermometer post-close refresh is enabled at `17-19:20` Asia/Shanghai; `personal-readiness-daily-evidence` remains enabled at weekdays `23:50` Asia/Shanghai |
| `python -m celery -A core inspect active reserved scheduled --timeout=8` during `2026-07-01` pre-close follow-up | both local workers responded; active/reserved/scheduled queues were empty |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` during `2026-07-01 10:56 +08:00` pre-window monitor | exit code 0 with `monitor_gate.ok=true`, `next_action=wait_for_post_close`, `scheduled_for=2026-07-01T23:50:00+08:00`; local runtime ok with 1 beat process, 2 worker processes, and 2 responsive workers |
| `python manage.py healthcheck --json` during `2026-07-01 10:56 +08:00` pre-window monitor | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok; decision readiness used the latest usable `2026-06-26` market thermometer and retained the blocked `2026-07-01` snapshot under `skipped_latest_market_thermometer` |
| `python manage.py show_personal_readiness_status --json --strict-acceptance` during `2026-07-01 10:57 +08:00` pre-window monitor | expected exit code 1: `1/20` days accepted, `remaining=19`, `failed_requirements=evidence_window`, next check after `2026-07-01T23:50:00+08:00` |
| `python -m celery -A core inspect active reserved scheduled --timeout=8` during `2026-07-01 10:56 +08:00` pre-window monitor | both local workers responded; active/reserved/scheduled queues were empty |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` during `2026-07-01 10:59 +08:00` main-chain monitor | diagnostic no-file run returned `status=ok`; `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, `target_count=2`, and both account checks ok. The run refreshed decision workspace snapshots, retained known Rotation skips as non-fatal, and did not create formal scheduler evidence for `2026-07-01` |
| `python manage.py healthcheck --json` after the `2026-07-01 10:59 +08:00` no-file main-chain monitor | `healthy=true`; decision quotes remained fresh, decision data stayed usable, and Alpha/workspace consistency remained `ok` |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after the `2026-07-01 10:59 +08:00` no-file main-chain monitor | exit code 0 with `monitor_gate.ok=true`, `scheduler_runtime.status=ok`, 1 beat process, 2 worker processes, and `next_check_after=2026-07-01T23:50:00+08:00` |
| Market thermometer command date safety patch during `2026-07-01` pre-window follow-up | `sync_market_thermometer_inputs` and `calculate_market_thermometer` now reuse the Celery task's market-thermometer as-of-date resolver when `--as-of-date` is omitted, so manual default runs before the post-close cutoff target the previous business day instead of writing an incomplete same-day snapshot |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py -q` after market thermometer command date safety patch | 4 passed |
| `ruff check apps\data_center\application\tasks.py apps\data_center\management\commands\sync_market_thermometer_inputs.py apps\data_center\management\commands\calculate_market_thermometer.py tests\unit\data_center\test_market_thermometer_tasks.py` after market thermometer command date safety patch | passed |
| `black --check apps\data_center\application\tasks.py apps\data_center\management\commands\sync_market_thermometer_inputs.py apps\data_center\management\commands\calculate_market_thermometer.py tests\unit\data_center\test_market_thermometer_tasks.py` after market thermometer command date safety patch | passed |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py tests\unit\data_center\test_decision_data_readiness.py tests\unit\test_scheduler_initialization.py -q` after market thermometer command date safety patch | 32 passed |
| `python manage.py check` after market thermometer command date safety patch | passed, no issues |
| Governance baseline sync after market thermometer command date safety patch | static test function count updated from `6,019` to `6,021` to account for two new command-date safety tests |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after market thermometer command date safety patch and baseline sync | 0 violations |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after market thermometer command date safety patch | exit code 0 with `monitor_gate.ok=true`, `scheduler_runtime.status=ok`, and `next_check_after=2026-07-01T23:50:00+08:00` |
| `python manage.py healthcheck --json` after market thermometer command date safety patch | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| Real command verification for `python manage.py calculate_market_thermometer` during `2026-07-01 11:15 +08:00` | confirmed the default date resolver targeted `2026-06-30` instead of same-day `2026-07-01`; this also exposed that a standalone calculate command can still produce a blocked snapshot when inputs have not been refreshed enough |
| Market thermometer blocked-write safety patch during `2026-07-01` pre-window follow-up | `CalculateMarketThermometerUseCase.execute()` now supports `persist_blocked=False`; `calculate_market_thermometer` defaults to syncing inputs first and skips persisting `must_not_use_for_decision=True` snapshots unless `--allow-blocked-write` is explicitly supplied |
| `python manage.py calculate_market_thermometer --skip-sync` after market thermometer blocked-write safety patch | returned a blocked `2026-06-30` calculation with `persisted=False` and `blocked_write_skipped=True`, confirming the manual command no longer persists blocked snapshots by default |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py tests\unit\data_center\test_decision_data_readiness.py tests\unit\test_scheduler_initialization.py -q` after market thermometer blocked-write safety patch | 34 passed |
| `ruff check apps\data_center\application\market_thermometer.py apps\data_center\management\commands\calculate_market_thermometer.py tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py` after market thermometer blocked-write safety patch | passed |
| `black --check apps\data_center\application\market_thermometer.py apps\data_center\management\commands\calculate_market_thermometer.py tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py` after market thermometer blocked-write safety patch | passed |
| Governance baseline sync after market thermometer blocked-write safety patch | static test function count updated from `6,021` to `6,023`; `apps/data_center/application/market_thermometer.py` large-file allowance updated from 1342 to 1351 non-empty lines for this production safety guard |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after market thermometer blocked-write safety patch and baseline sync | 0 violations |
| `python manage.py check` after market thermometer blocked-write safety patch | passed, no issues |
| `python manage.py healthcheck --json` after market thermometer blocked-write safety patch | `healthy=true`; decision data stayed usable through latest usable market thermometer fallback and Alpha/workspace consistency remained `ok` |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after market thermometer blocked-write safety patch | exit code 0 with `monitor_gate.ok=true`, `scheduler_runtime.status=ok`, and `next_check_after=2026-07-01T23:50:00+08:00` |
| Diff review after market thermometer blocked-write safety patch | confirmed Celery task still calls `execute(as_of_date=target_date)` with default `persist_blocked=True`, preserving formal scheduler failure evidence; manual `calculate_market_thermometer` defaults to `persist_blocked=False` for operator safety |
| Provider fetch closure cleanup after diff review | replaced loop-variable lambdas in market thermometer provider calls with `functools.partial` so bounded provider calls keep the intended provider and request arguments without late-binding risk |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py tests\unit\data_center\test_decision_data_readiness.py tests\unit\test_scheduler_initialization.py -q` after provider fetch closure cleanup | 34 passed |
| `ruff check apps\data_center\application\market_thermometer.py apps\data_center\management\commands\calculate_market_thermometer.py tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py` after provider fetch closure cleanup | passed |
| `black --check apps\data_center\application\market_thermometer.py apps\data_center\management\commands\calculate_market_thermometer.py tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py` after provider fetch closure cleanup | passed |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after provider fetch closure cleanup | 0 violations |
| `python manage.py check` after provider fetch closure cleanup | passed, no issues |
| `python manage.py healthcheck --json` after provider fetch closure cleanup | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after provider fetch closure cleanup | exit code 0 with `monitor_gate.ok=true`, local scheduler runtime ok, and `next_check_after=2026-07-01T23:50:00+08:00` |
| `docs/modules/data-center-market-thermometer.md` update after command safety changes | documented default as-of-date resolution, manual command default sync, blocked-write protection, `--skip-sync`, `--allow-blocked-write`, and the difference between manual safety behavior and formal Celery scheduler evidence semantics |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after market thermometer docs update | 0 violations |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py tests\unit\data_center\test_decision_data_readiness.py tests\unit\test_scheduler_initialization.py -q` after market thermometer docs update | 34 passed |
| `ruff check apps\data_center\application\market_thermometer.py apps\data_center\application\tasks.py apps\data_center\management\commands\calculate_market_thermometer.py apps\data_center\management\commands\sync_market_thermometer_inputs.py tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py` after market thermometer docs update | passed |
| `black --check apps\data_center\application\market_thermometer.py apps\data_center\application\tasks.py apps\data_center\management\commands\calculate_market_thermometer.py apps\data_center\management\commands\sync_market_thermometer_inputs.py tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py` after market thermometer docs update | passed |
| `python manage.py healthcheck --json` after market thermometer docs update | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after market thermometer docs update | exit code 0 with `monitor_gate.ok=true`, local scheduler runtime ok, and `next_check_after=2026-07-01T23:50:00+08:00` |
| `python manage.py check` after market thermometer docs update | passed, no issues |
| `docs/development/quick-reference.md` update after market thermometer command safety changes | refreshed the document date, module/test-count summary, and quick commands for `sync_market_thermometer_inputs` / `calculate_market_thermometer`; documented default Celery-style as-of-date resolution, pre-16:00 previous-business-day behavior, default input sync, `--skip-sync` diagnostic semantics, and blocked-write protection |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after quick-reference update | 0 violations |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py tests\unit\data_center\test_market_thermometer.py tests\unit\data_center\test_decision_data_readiness.py tests\unit\test_scheduler_initialization.py -q` after quick-reference update | 34 passed |
| `python manage.py check` after quick-reference update | passed, no issues |
| `python manage.py healthcheck --json` after quick-reference update | `healthy=true`; decision quotes fresh, decision data usable, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after quick-reference update | exit code 0 with `monitor_gate.ok=true`, `scheduler_runtime.status=ok`, 1 beat process, 2 worker processes, 2 responsive workers, and `next_check_after=2026-07-01T23:50:00+08:00`; acceptance remains `1/20` days and is waiting for the scheduled post-close evidence window |
| Rotation scheduler skipped-count alignment during `2026-07-01` pre-window follow-up | `RotationSignalScheduler.generate_all_signals()` now reports `skipped` separately and no longer mixes `status=skipped` / `no_valid_allocation` payloads into `successful`, keeping infrastructure scheduler summaries aligned with the application-facing rotation provider |
| `pytest tests\unit\test_rotation_readiness_degradation.py -q` after Rotation scheduler skipped-count alignment | 3 passed |
| `ruff check apps\rotation\infrastructure\services.py tests\unit\test_rotation_readiness_degradation.py` after Rotation scheduler skipped-count alignment | passed |
| `black --check apps\rotation\infrastructure\services.py tests\unit\test_rotation_readiness_degradation.py` after Rotation scheduler skipped-count alignment | passed |
| Governance baseline sync after Rotation scheduler skipped-count alignment | static test function count updated from `6,023` to `6,024` |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after Rotation scheduler skipped-count alignment and baseline sync | 0 violations |
| `python manage.py check` after Rotation scheduler skipped-count alignment | passed, no issues |
| `python manage.py healthcheck --json` after Rotation scheduler skipped-count alignment | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after Rotation scheduler skipped-count alignment | exit code 0 with `monitor_gate.ok=true`, local scheduler runtime ok, and `next_check_after=2026-07-01T23:50:00+08:00`; formal acceptance remains `1/20` days pending the scheduled post-close window |
| Rotation scheduler repository state cleanup during `2026-07-01` pre-window follow-up | `RotationSignalScheduler` now keeps a repository dependency on `self.config_repo` instead of replacing that attribute with the active-config list during `generate_all_signals()`, avoiding state pollution in the scheduled rotation generation helper |
| `pytest tests\unit\test_rotation_readiness_degradation.py -q` after Rotation scheduler repository state cleanup | 3 passed |
| `ruff check apps\rotation\infrastructure\services.py tests\unit\test_rotation_readiness_degradation.py` after Rotation scheduler repository state cleanup | passed |
| `black --check apps\rotation\infrastructure\services.py tests\unit\test_rotation_readiness_degradation.py` after Rotation scheduler repository state cleanup | passed |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after Rotation scheduler repository state cleanup | 0 violations |
| `python manage.py check` after Rotation scheduler repository state cleanup | passed, no issues |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after Rotation scheduler repository state cleanup | exit code 0 with `monitor_gate.ok=true`, local scheduler runtime ok, and `next_check_after=2026-07-01T23:50:00+08:00` |
| `python manage.py healthcheck --json` after Rotation scheduler repository state cleanup | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| Readiness evidence inspection observation enhancement during `2026-07-01` pre-window follow-up | `inspect_personal_readiness_evidence` now keeps accepted evidence accepted while surfacing non-blocking yellow items as structured `observations`, including macro sync skipped indicators, Rotation skipped configs, degraded market thermometer components, and skipped latest blocked thermometer snapshots |
| `python manage.py inspect_personal_readiness_evidence --target-date 2026-06-30 --json` after inspection observation enhancement | returned `status=accepted`, `blockers=[]`, and observations for legacy operation context, macro sync skipped `54` indicators after syncing `11`, Rotation skipped `3` configs, and degraded market thermometer stale components `new_investor_accounts,etf_net_flow` |
| `pytest tests\unit\test_personal_readiness_evidence_inspection.py -q` after inspection observation enhancement | 6 passed |
| `ruff check apps\task_monitor\management\commands\inspect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_inspection.py` after inspection observation enhancement | passed |
| `black --check apps\task_monitor\management\commands\inspect_personal_readiness_evidence.py tests\unit\test_personal_readiness_evidence_inspection.py` after inspection observation enhancement | passed after formatting the inspection command file |
| Governance baseline sync after inspection observation enhancement | static test function count updated from `6,024` to `6,025` |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after inspection observation enhancement and baseline sync | 0 violations |
| `python manage.py check` after inspection observation enhancement | passed, no issues |
| `python manage.py healthcheck --json` after inspection observation enhancement | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| Auto-advisor macro regime fix during `2026-07-01` pre-window follow-up | fixed dashboard auto-advisor `_current_regime_payload()` to call `resolve_current_regime(as_of_date=...)` with a keyword argument, eliminating the `resolve_current_regime() takes 0 positional arguments but 1 was given` degradation seen in readiness evidence |
| `pytest tests\unit\test_auto_advisor_dashboard_console.py -q` after auto-advisor macro regime fix | 12 passed |
| `ruff check apps\dashboard\application\query_services.py tests\unit\test_auto_advisor_dashboard_console.py` after auto-advisor macro regime fix | passed |
| `black --check apps\dashboard\application\query_services.py tests\unit\test_auto_advisor_dashboard_console.py` after auto-advisor macro regime fix | passed after formatting both focused files |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` after auto-advisor macro regime fix | diagnostic no-file run returned `status=ok`, `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, and `target_count=2`; the run refreshed the main chain without creating a formal scheduler evidence file |
| Direct auto-advisor console verification after macro regime fix | `build_auto_advisor_console_payload(account_id=613)` returned `macro_regime.status=ok`, `current=Recovery`, and a populated Regime distribution instead of `unavailable` |
| Governance baseline sync after auto-advisor macro regime fix | static test function count updated from `6,025` to `6,026` |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after auto-advisor macro regime fix and baseline sync | 0 violations |
| `python manage.py check` after auto-advisor macro regime fix | passed, no issues |
| `python manage.py healthcheck --json` after auto-advisor macro regime fix | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after auto-advisor macro regime fix | exit code 0 with `monitor_gate.ok=true`, local scheduler runtime ok, and `next_check_after=2026-07-01T23:50:00+08:00`; formal acceptance remains `1/20` days pending the scheduled post-close window |
| `python manage.py calculate_market_thermometer --as-of-date 2026-07-01` during `2026-07-01` pre-window follow-up | refreshed market thermometer inputs and persisted a usable `2026-07-01` snapshot: score `43.48`, band `warm`, `valid_component_count=4`, `must_not_use_for_decision=false`; news count and sentiment refreshed for the target date, while `new_investor_accounts` and `etf_net_flow` remain stale |
| `python manage.py healthcheck --json` after `2026-07-01` market thermometer refresh | `healthy=true`; decision data now consumes the `2026-07-01` market thermometer directly with no `skipped_latest_market_thermometer`; quotes, critical data, Celery, and Alpha/workspace consistency remain ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after `2026-07-01` market thermometer refresh | exit code 0 with `monitor_gate.ok=true`; scheduler runtime remains ok and the next formal evidence is still scheduled for `2026-07-01T23:50:00+08:00` |
| Risk-center pre-trade evidence follow-up during `2026-07-01` pre-window | daily readiness account evidence now runs a no-order `EvaluatePreTradeRiskUseCase` probe and stores `pre_trade_check` alongside the risk-center daily report; new formal evidence requires `pre_trade_check.status=ok`, while legacy evidence remains classified as legacy |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_window_validation.py -q` after pre-trade evidence follow-up | 33 passed |
| `ruff check` and `black --check` on readiness evidence command, inspection command, validation command, and related tests after pre-trade evidence follow-up | passed |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` after pre-trade evidence follow-up | diagnostic no-file run returned `status=ok`, `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, `target_count=2`, `account_status_counts.ok=2`; account evidence includes `pre_trade_check.status=ok` and `post_investment_check` in the risk-center section |
| `python manage.py check` after pre-trade evidence follow-up | passed, no issues |
| `python manage.py healthcheck --json` after pre-trade evidence follow-up | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| Governance baseline sync after pre-trade evidence follow-up | static test function count updated from `6,026` to `6,028` |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after pre-trade evidence follow-up and baseline sync | 0 violations |
| SQLite write-lock resilience follow-up during `2026-07-01` pre-window | moved transient SQLite lock retry into `apps.data_center.infrastructure.orm_retry` and routed `MacroFactRepository.bulk_upsert()` through it, so local scheduled macro writes retry short `database is locked` contention without hiding non-lock `OperationalError` failures |
| `pytest tests\unit\data_center\test_repositories.py -q` after SQLite write-lock resilience follow-up | 8 passed |
| `ruff check apps\data_center\infrastructure\repositories.py apps\data_center\infrastructure\orm_retry.py tests\unit\data_center\test_repositories.py` after SQLite write-lock resilience follow-up | passed |
| `black --check apps\data_center\infrastructure\repositories.py apps\data_center\infrastructure\orm_retry.py tests\unit\data_center\test_repositories.py` after SQLite write-lock resilience follow-up | passed |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` after SQLite write-lock resilience follow-up | diagnostic no-file run returned `status=ok`, `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, `target_count=2`, `account_status_counts.ok=2`; workspace refresh completed successfully while Celery beat/workers were running |
| `python manage.py healthcheck --json` after SQLite write-lock resilience follow-up | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| Governance baseline sync after SQLite write-lock resilience follow-up | static test function count updated from `6,028` to `6,030`; large-file growth gate remains clean after moving retry code out of `repositories.py` |
| `python scripts\check_governance_consistency.py --baseline governance\governance_baseline.json --format text` after SQLite write-lock resilience follow-up and baseline sync | 0 violations |
| Rotation momentum coverage follow-up during `2026-07-01` pre-window | fixed partial-period momentum scoring so assets with usable 20/60-day coverage are not discarded only because 120-day history is still short; also preserved DB-backed `momentum_periods` in `RotationConfigModel.to_domain()` |
| `pytest tests\unit\domain\test_rotation_services.py tests\unit\test_rotation_readiness_degradation.py -q` after Rotation momentum coverage follow-up | 53 passed |
| `ruff check` and `black --check` on Rotation domain/service/model tests after Rotation momentum coverage follow-up | passed |
| `generate_rotation_signals(date(2026, 6, 30))` after Rotation momentum coverage follow-up | real database run returned `total_configs=6`, `successful=6`, `skipped=0`, `failed=0`; previously skipped configs now produce allocations from available `510300` and `159915` coverage |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` after Rotation momentum coverage follow-up | diagnostic no-file run returned `status=ok`, `workspace_status=ok`; `rotation_signals` returned `successful=6`, `skipped=0`, `failed=0` |
| `python manage.py healthcheck --json` after Rotation momentum coverage follow-up | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| Governance baseline sync after Rotation momentum coverage follow-up | static test function count updated from `6,030` to `6,032` |
| Market thermometer stale-component follow-up during `2026-07-01` pre-window | added `--json` output to `sync_market_thermometer_inputs` and `calculate_market_thermometer`, so stale provider diagnostics can be captured by automation instead of relying on Python repr/log parsing |
| `python manage.py sync_market_thermometer_inputs --as-of-date 2026-07-01` during stale-component follow-up | real sync stored `new_investor_accounts=2`, `margin_balance=5`, `market_news=3`, and Tushare-backed `turnover=5`; `etf_net_flow` remained unavailable because AKShare/EastMoney timed out and Tushare returned no data |
| Macro fact inspection after market thermometer stale-component follow-up | latest `CN_A_NEW_INVESTOR_ACCOUNTS` remains `2023-08-31`; latest `CN_A_ETF_NET_FLOW` remains `2026-06-22`, confirming the remaining stale components are source-data gaps rather than scoring fallback bugs |
| `python manage.py calculate_market_thermometer --as-of-date 2026-07-01 --skip-sync --json` after stale-component follow-up | JSON output parsed by command contract; snapshot stayed usable with `score=43.48`, `band=warm`, `valid_component_count=4`, `must_not_use_for_decision=false`, stale components `new_investor_accounts` and `etf_net_flow` |
| `pytest tests\unit\data_center\test_market_thermometer_tasks.py -q` after stale-component follow-up | 7 passed |
| `ruff check` and `black --check` on market thermometer commands and command tests after stale-component follow-up | passed |
| Governance baseline sync after market thermometer stale-component follow-up | static test function count updated from `6,032` to `6,034` |
| Auto-advisor weekly persistence evidence follow-up during `2026-07-01` pre-window | readiness collection now records persisted weekly report history, delivered notifications, matched report id, and `audit_log_id` under `auto_advisor.weekly_report_persistence`; evidence inspection reports missing persistence proof as a yellow observation when a weekly payload exists |
| `pytest tests\unit\test_personal_readiness_evidence_command.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_window_validation.py -q` after weekly persistence evidence follow-up | 35 passed |
| `ruff check` and `black --check` on weekly persistence evidence command/checker tests after follow-up | passed |
| `python manage.py collect_personal_readiness_evidence --target-date 2026-06-30 --run-workspace-refresh --include-weekly-advisor --no-file --json` after weekly persistence evidence follow-up | diagnostic no-file run returned `status=ok`, `system_status=ok`, `qlib_status=ok`, `workspace_status=ok`, `target_count=2`, `account_status_counts.ok=2`; both readiness accounts had `weekly_report_persistence.status=ok` with persisted weekly reports and delivered dashboard notifications |
| `python manage.py healthcheck --json` after weekly persistence evidence follow-up | `healthy=true`; database, Redis, Celery, critical data, decision data, and Alpha/workspace consistency ok |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` during `2026-07-01` pre-window | monitor gate ok; current action remains `wait_for_post_close`, next scheduled evidence is `2026-07-01T23:50:00+08:00`, grace deadline `2026-07-02T00:20:00+08:00`; accepted evidence remains `1/20` |
| Governance baseline sync after weekly persistence evidence follow-up | static test function count updated from `6,034` to `6,036` |
| Window-level weekly persistence quality follow-up during `2026-07-01` pre-window | `validate_personal_readiness_window` now reports weekly report persistence coverage in `accepted_evidence_quality` and `evidence_quality`; current accepted legacy evidence remains accepted but is visible as `weekly_report_persistence_missing_record_count=1` / `weekly_report_persistence_missing_account_count=2` |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py -q` after window-level weekly persistence quality follow-up | 50 passed |
| `ruff check` and `black --check` on window validation command/tests after weekly persistence quality follow-up | passed |
| `python manage.py validate_personal_readiness_window --calendar-source auto --json` after weekly persistence quality follow-up | `status=in_progress`, `accepted_days=1`, `remaining_days=19`, `next_required_date=2026-07-01`; new weekly persistence quality fields surfaced missing proof in the accepted legacy `2026-06-30` evidence without blocking it |
| Governance baseline sync after window-level weekly persistence quality follow-up | static test function count updated from `6,036` to `6,037` |
| Auto-advisor weekly scheduler gate follow-up during `2026-07-01` pre-window | `show_personal_readiness_status` now reports `auto_advisor_weekly_scheduler` and includes it as an acceptance-gate requirement; real DB status is ok with task `dashboard.generate_auto_advisor_weekly_reports`, enabled Friday `17:30` Asia/Shanghai, all active accounts scope, and no safety issues |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_window_validation.py -q` after weekly scheduler gate follow-up | 52 passed |
| `ruff check` and `black --check` on status command, weekly scheduler helper, scheduler utils, and status tests after weekly scheduler gate follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after weekly scheduler gate follow-up | monitor gate remains ok and waiting for `2026-07-01T23:50:00+08:00`; weekly scheduler requirement is ok |
| Governance baseline sync after weekly scheduler gate follow-up | static test function count updated from `6,037` to `6,039`; large-file gate remains clean after extracting scheduler helpers |
| Final weekly persistence gate follow-up during `2026-07-01` pre-window | acceptance gate now includes `auto_advisor_weekly_persistence`; it stays `pending_window` before the 20-day window is complete, but once the evidence window is accepted it requires at least one accepted evidence record/account with `weekly_report_persistence.status=ok` |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_window_validation.py -q` after final weekly persistence gate follow-up | 53 passed |
| `ruff check` and `black --check` on status command/tests after final weekly persistence gate follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after final weekly persistence gate follow-up | monitor gate remains ok and waiting for `2026-07-01T23:50:00+08:00`; `auto_advisor_weekly_persistence` is `pending_window` with current legacy proof counts `ok_record_count=0`, `missing_record_count=1` |
| Governance baseline sync after final weekly persistence gate follow-up | static test function count updated from `6,039` to `6,040` |
| Final weekly scheduler activity gate follow-up during `2026-07-01` pre-window | acceptance gate now includes `auto_advisor_weekly_activity`; it stays `pending_window` before the 20-day window is complete, but once the evidence window is accepted it requires weekly scheduler run history (`last_run_at` and positive `total_run_count`) and does not double-report while scheduler safety is already failing |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_window_validation.py -q` after final weekly scheduler activity gate follow-up | 54 passed |
| `ruff check` and `black` on status command/tests after final weekly scheduler activity gate follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after final weekly scheduler activity gate follow-up | monitor gate remains ok and waiting for `2026-07-01T23:50:00+08:00`; accepted evidence remains `1/20`; `auto_advisor_weekly_activity` is `pending_window` with current scheduler run history still empty |
| Governance baseline sync after final weekly scheduler activity gate follow-up | static test function count updated from `6,040` to `6,041`; `show_personal_readiness_status.py` added to large-file baseline at 1,235 non-empty lines pending later extraction |
| Formal post-investment risk gate follow-up during `2026-07-01` pre-window | formal readiness evidence inspection and window validation now require each account's `risk_center_daily_report.post_investment_check.passed` to be `true`, matching the final acceptance requirement that both pre-trade and post-investment risk-center checks are present in every new formal daily evidence record |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal post-investment risk gate follow-up | 64 passed |
| `ruff check` and `black` on readiness inspection/window validation command/tests after formal post-investment risk gate follow-up | passed |
| Governance baseline sync after formal post-investment risk gate follow-up | static test function count updated from `6,041` to `6,043` |
| Formal risk coverage quality gate follow-up during `2026-07-01` pre-window | window validation now reports `formal_risk_*` accepted-evidence quality counters, and the final acceptance gate includes `risk_center_formal_evidence`; after the 20-day window is accepted it requires formal account-level risk report, pre-trade, and post-investment evidence coverage before top-level status can be `accepted` |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_evidence_inspection.py -q` after formal risk coverage quality gate follow-up | 65 passed |
| `ruff check` and `black` on readiness status/window validation command/tests after formal risk coverage quality gate follow-up | passed |
| Governance baseline sync after formal risk coverage quality gate follow-up | static test function count updated from `6,043` to `6,044`; `show_personal_readiness_status.py` large-file baseline updated to 1,294 non-empty lines pending later helper extraction |
| Formal decision-data evidence gate follow-up during `2026-07-01` pre-window | formal readiness evidence now requires `system.checks.decision_data` with `status=ok`, `readiness_status=ok`, and `must_not_use_for_decision=false`; window validation reports `formal_decision_data_*` counters, inspection surfaces missing/blocked decision-data blockers, and final acceptance gate includes `decision_data_formal_evidence` |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal decision-data evidence gate follow-up | 68 passed |
| `ruff check` and `black` on readiness validation/inspection/status command/tests after formal decision-data evidence gate follow-up | passed |
| Governance baseline sync after formal decision-data evidence gate follow-up | static test function count updated from `6,044` to `6,047`; `show_personal_readiness_status.py` large-file baseline updated to 1,348 non-empty lines pending later helper extraction |
| Formal Alpha/workspace evidence gate follow-up during `2026-07-01` pre-window | formal readiness evidence now requires `system.checks.alpha_workspace_consistency.status=ok`; window validation reports `formal_alpha_workspace_*` counters, inspection surfaces missing/degraded Alpha-workspace blockers, and final acceptance gate includes `alpha_workspace_formal_evidence` before decision-data/risk gates |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal Alpha/workspace evidence gate follow-up | 71 passed |
| `ruff check` and `black` on readiness validation/inspection/status command/tests after formal Alpha/workspace evidence gate follow-up | passed |
| Governance baseline sync after formal Alpha/workspace evidence gate follow-up | static test function count updated from `6,047` to `6,050`; `show_personal_readiness_status.py` large-file baseline updated to 1,401 non-empty lines pending later helper extraction |
| Formal workspace core evidence gate follow-up during `2026-07-01` pre-window | formal readiness evidence now requires successful `regime_snapshot`, reliable `pulse_snapshot`, and successful `action_recommendation` under `workspace.result.components`; window validation reports `formal_workspace_core_*` counters, inspection surfaces missing/degraded workspace core blockers, and final acceptance includes `workspace_core_formal_evidence` before Alpha, decision-data, and risk gates |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal workspace core evidence gate follow-up | 74 passed |
| `ruff check` and `black` on readiness validation/inspection/status command/tests after formal workspace core evidence gate follow-up | passed |
| Governance baseline sync after formal workspace core evidence gate follow-up | static test function count updated from `6,050` to `6,053`; `show_personal_readiness_status.py` large-file baseline updated to 1,454 non-empty lines pending later helper extraction |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after formal workspace core evidence gate follow-up | exit code 0 with `monitor_gate.ok=true`, `scheduler_runtime.status=ok`, 1 beat process, 2 worker processes, 2 responsive workers, `workspace_core_formal_evidence.status=pending_window`, and next scheduled evidence `2026-07-01T23:50:00+08:00`; formal acceptance remains `1/20` days |
| Formal quote freshness evidence gate follow-up during `2026-07-01` pre-window | formal readiness evidence now requires fresh, usable decision quotes under `system.checks.decision_data.quotes`; window validation reports `formal_quote_freshness_*` counters, evidence inspection surfaces missing/stale/blocked quote blockers, and final acceptance includes `decision_quote_freshness_formal_evidence` after the decision-data gate |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal quote freshness evidence gate follow-up | 75 passed |
| `ruff check` and `black` on readiness validation/inspection/status command/tests after formal quote freshness evidence gate follow-up | passed |
| Governance baseline sync after formal quote freshness evidence gate follow-up | static test function count updated from `6,053` to `6,054`; `show_personal_readiness_status.py` large-file baseline updated to 1,511 non-empty lines pending later helper extraction |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after formal quote freshness evidence gate follow-up | exit code 0 with `monitor_gate.ok=true`, local scheduler runtime ok, `decision_quote_freshness_formal_evidence.status=pending_window`, and the next scheduled evidence still `2026-07-01T23:50:00+08:00`; accepted evidence remains `1/20` days |
| Formal Qlib freshness evidence gate follow-up during `2026-07-01` pre-window | formal readiness evidence now requires an auditable `qlib` section with `status=ok` and `check_only=true`; window validation reports `formal_qlib_*` counters, evidence inspection surfaces missing/non-check-only Qlib blockers, and final acceptance includes `qlib_formal_evidence` before workspace, Alpha, decision-data, quote freshness, and risk gates |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal Qlib freshness evidence gate follow-up | 78 passed |
| `ruff check` and `black` on readiness validation/inspection/status command/tests after formal Qlib freshness evidence gate follow-up | passed |
| Governance baseline sync after formal Qlib freshness evidence gate follow-up | static test function count updated from `6,054` to `6,057`; `show_personal_readiness_status.py` large-file baseline updated to 1,570 non-empty lines pending later helper extraction |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after formal Qlib freshness evidence gate follow-up | exit code 0 with `monitor_gate.ok=true`, `qlib_formal_evidence.status=pending_window`, local scheduler runtime ok, and next scheduled evidence `2026-07-01T23:50:00+08:00`; accepted evidence remains `1/20` days |
| `python manage.py run_personal_readiness_daily --target-date 2026-07-01 --json` during the `2026-07-01` post-close window | generated formal evidence `var/readiness-evidence/2026-07-01-personal-readiness.json`; run status `ok`; validation advanced to `accepted_days=2`, `remaining_days=18`, next required date `2026-07-02`; formal gates were present and ok for workspace core, Qlib, Alpha/workspace, decision data, quote freshness, and risk evidence |
| `python manage.py validate_personal_readiness_window --json` after the `2026-07-01` formal daily run | `accepted_days=2`, `remaining_days=18`, accepted manifest SHA-256 `a6d2a55aa58d2c944080940bccf8eae0df5552f2e8f19b04ad644b71f84518d1`; `formal_record_count=1`, `formal_qlib_ok_record_count=1`, `formal_workspace_core_ok_record_count=1`, `formal_alpha_workspace_ok_record_count=1`, `formal_decision_data_ok_record_count=1`, `formal_quote_freshness_ok_record_count=1`, and formal risk counts all ok for 2 accounts |
| `python manage.py inspect_personal_readiness_evidence --target-date 2026-07-01 --json` after the formal daily run | evidence accepted with no blockers; observations remain non-blocking: weekly persistence warning for accounts `613` and `614`, macro sync skipped 50 indicators after syncing 4, and market thermometer stayed degraded because `new_investor_accounts` and `etf_net_flow` are stale |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after the `2026-07-01` formal daily run | exit code 0 with `monitor_gate.ok=true`; latest formal evidence is `2026-07-01`, accepted evidence is `2/20`, next action is `wait_for_post_close` for `2026-07-02`, and local scheduler runtime remains ok with 1 beat process and 2 responsive workers |
| Post-close/pre-schedule monitor follow-up during the `2026-07-02` pending window | `show_personal_readiness_status` now normalizes `run_daily` to `wait_for_scheduled_run` while scheduler expectation is `pending`, `due_now`, or `grace_period`, so strict monitor stays green and the continuous trial waits for the configured `23:50 Asia/Shanghai` scheduler instead of prompting premature manual evidence generation |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after post-close/pre-schedule normalization | 79 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after post-close/pre-schedule normalization | passed |
| Governance baseline sync after post-close/pre-schedule normalization | static test function count updated from `6,057` to `6,058`; `show_personal_readiness_status.py` large-file baseline updated to 1,592 non-empty lines pending later helper extraction |
| Formal risk record coverage gate follow-up during `2026-07-01` pre-window | final acceptance now requires risk-center formal evidence to cover every accepted formal daily record, not only aggregate account-level risk counts. `risk_center_formal_evidence` reports `formal_record_count`, `risk_record_count`, `ok_record_count`, and `missing_record_count`, and rejects an accepted window when any formal day lacks a risk evidence record even if account totals look complete |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after formal risk record coverage follow-up | 79 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after formal risk record coverage follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after formal risk record coverage follow-up | exit code 0; monitor gate remains ok at `2/20`, latest formal evidence stays `2026-07-01`, and `risk_center_formal_evidence` now exposes record-level coverage with `formal_record_count=1`, `risk_record_count=1`, and `ok_record_count=1` |
| Governance baseline sync after formal risk record coverage follow-up | `show_personal_readiness_status.py` large-file baseline updated from 1,592 to 1,604 non-empty lines pending later helper extraction |
| Auto-advisor weekly persistence strict gate follow-up during `2026-07-01` pre-window | final acceptance now rejects accepted windows with any weekly report persistence `missing` or `warning` record/account. `auto_advisor_weekly_persistence` reports record/account totals and requires all observed weekly persistence records and accounts to be `ok`, instead of accepting a window with only one successful persisted weekly report |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after weekly persistence strict gate follow-up | 80 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after weekly persistence strict gate follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after weekly persistence strict gate follow-up | exit code 0; monitor gate remains ok at `2/20`, and pending `auto_advisor_weekly_persistence` now exposes `record_count=2`, `missing_record_count=1`, `warning_record_count=1`, `account_count=4`, `missing_account_count=2`, and `warning_account_count=2` for later final acceptance review |
| Governance baseline sync after weekly persistence strict gate follow-up | static test function count updated from `6,058` to `6,059`; `show_personal_readiness_status.py` large-file baseline updated from 1,604 to 1,633 non-empty lines pending later helper extraction |
| Auto-advisor weekly activity freshness gate follow-up during `2026-07-01` pre-window | final acceptance now requires the weekly auto-advisor scheduler run history to be fresh for the accepted evidence window: `last_run_at` must resolve to a date on or after the accepted window start. This prevents a stale scheduler run from before the 20-trading-day trial from satisfying the weekly activity gate |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after weekly activity freshness follow-up | 81 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after weekly activity freshness follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after weekly activity freshness follow-up | exit code 0; monitor gate remains ok at `2/20`, and pending `auto_advisor_weekly_activity` now reports `latest_run_date` and `window_start_date` for final acceptance review |
| Governance baseline sync after weekly activity freshness follow-up | static test function count updated from `6,059` to `6,060`; `show_personal_readiness_status.py` large-file baseline updated from 1,633 to 1,664 non-empty lines pending later helper extraction |
| Auto-advisor weekly persistence coverage follow-up during `2026-07-01` pre-window | final acceptance now computes the expected Friday schedule points inside the accepted evidence window and requires weekly report persistence records to cover those points. A window with only two successful persisted weekly reports will no longer pass if the accepted window spans three expected Friday runs |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after weekly persistence coverage follow-up | 82 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after weekly persistence coverage follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after weekly persistence coverage follow-up | exit code 0; monitor gate remains ok at `2/20`, and `auto_advisor_weekly_persistence` now reports `expected_record_count` for final acceptance review |
| Governance baseline sync after weekly persistence coverage follow-up | static test function count updated from `6,060` to `6,061`; `show_personal_readiness_status.py` large-file baseline updated from 1,664 to 1,686 non-empty lines pending later helper extraction |
| Auto-advisor weekly activity run-count coverage follow-up during `2026-07-01` pre-window | final acceptance now requires `auto_advisor_weekly_activity.total_run_count >= expected_run_count` in addition to freshness. A 20-trading-day accepted window spanning three expected Friday runs no longer passes when the weekly scheduler only ran twice |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after weekly activity run-count coverage follow-up | 47 passed |
| `pytest tests\unit\test_personal_readiness_window_validation.py tests\unit\test_personal_readiness_evidence_inspection.py tests\unit\test_personal_readiness_status_command.py -q` after weekly activity run-count coverage follow-up | 83 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after weekly activity run-count coverage follow-up | passed |
| `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8` after weekly activity run-count coverage follow-up | exit code 0; monitor gate remains ok at `2/20`, next action is `wait_for_post_close` for `2026-07-02`, and `auto_advisor_weekly_activity` now reports both `expected_run_count` and `total_run_count` for final acceptance review |
| Governance baseline sync after weekly activity run-count coverage follow-up | static test function count updated from `6,061` to `6,062`; `show_personal_readiness_status.py` large-file baseline updated from 1,686 to 1,693 non-empty lines pending later helper extraction |
| Auto-advisor weekly activity latest-run coverage follow-up during `2026-07-01` pre-window | final acceptance now requires weekly scheduler `last_run_at` to be on or after the latest expected Friday inside the accepted evidence window. A window spanning three expected weekly runs no longer passes when the total run count is three but the latest run only reached the second Friday |
| `pytest tests\unit\test_personal_readiness_status_command.py -q` after weekly activity latest-run coverage follow-up | 48 passed |
| `ruff check apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py` after weekly activity latest-run coverage follow-up | passed |
| Governance baseline sync after weekly activity latest-run coverage follow-up | static test function count updated from `6,062` to `6,063`; `show_personal_readiness_status.py` large-file baseline updated from 1,693 to 1,715 non-empty lines pending later helper extraction |
| Readiness status weekly schedule helper extraction during `2026-07-01` pre-window | pure weekly schedule counting helpers moved from `show_personal_readiness_status.py` into Application-layer `readiness_status_services.py`, preserving final weekly activity and persistence gate behavior while reducing the command file from 1,715 to 1,688 non-empty lines |
| Auto-advisor scheduled weekly persistence gate follow-up during `2026-07-01` pre-window | window validation now reports `scheduled_weekly_report_*` quality counters for Friday records, and final acceptance uses those scheduled-weekly counters for weekly persistence. Non-Friday daily evidence can still surface weekly persistence warnings for observation without blocking the weekly-on-schedule acceptance gate |
| `pytest tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_window_validation.py -q` after scheduled weekly persistence gate follow-up | 71 passed |
| `ruff check apps\task_monitor\management\commands\validate_personal_readiness_window.py apps\task_monitor\management\commands\show_personal_readiness_status.py tests\unit\test_personal_readiness_status_command.py tests\unit\test_personal_readiness_window_validation.py` after scheduled weekly persistence gate follow-up | passed |
| Governance baseline sync after scheduled weekly persistence gate follow-up | static test function count updated from `6,063` to `6,064`; `show_personal_readiness_status.py` large-file baseline updated from 1,688 to 1,690; `validate_personal_readiness_window.py` added at 1,243 non-empty lines pending later helper extraction |
| Full evidence-quality scheduled weekly visibility follow-up during `2026-07-01` pre-window | full-window `evidence_quality` now reports the same `scheduled_weekly_report_*` counters as `accepted_evidence_quality`, so monitor output can inspect scheduled weekly persistence coverage consistently before and after the accepted-window filter |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after full evidence-quality scheduled weekly visibility follow-up | 22 passed |
| `ruff check apps\task_monitor\management\commands\validate_personal_readiness_window.py tests\unit\test_personal_readiness_window_validation.py` after full evidence-quality scheduled weekly visibility follow-up | passed |
| Governance baseline sync after full evidence-quality scheduled weekly visibility follow-up | `validate_personal_readiness_window.py` large-file baseline updated from 1,243 to 1,286 non-empty lines pending later helper extraction |
| Weekly evidence-quality helper extraction during `2026-07-01` pre-window | duplicated weekly report quality counters for accepted/full evidence windows were consolidated into a shared helper, preserving the scheduled-weekly persistence fields while reducing `validate_personal_readiness_window.py` from 1,286 to 1,210 non-empty lines |
| `pytest tests\unit\test_personal_readiness_window_validation.py -q` after weekly evidence-quality helper extraction | 22 passed |
| `ruff check apps\task_monitor\management\commands\validate_personal_readiness_window.py tests\unit\test_personal_readiness_window_validation.py` after weekly evidence-quality helper extraction | passed |
| Governance baseline sync after weekly evidence-quality helper extraction | `validate_personal_readiness_window.py` large-file baseline reduced from 1,286 to 1,210 non-empty lines pending later extraction below the 1,200 line threshold |
| Weekly evidence-quality helper compaction during `2026-07-01` pre-window | shared weekly quality helper was compacted without changing output keys, reducing `validate_personal_readiness_window.py` from 1,210 to 1,199 non-empty lines and removing its large-file baseline exception |
| Daily readiness risk-report persistence safety follow-up during `2026-07-01` pre-window | scheduled daily readiness runs now default to `persist_risk_report=true`, and scheduler safety reports `scheduled_risk_report_persistence_disabled` when a periodic task disables risk-center daily report persistence |
| Risk-center persisted-report final gate follow-up during `2026-07-01` pre-window | formal risk evidence quality now counts accounts with persisted risk-center daily report IDs, and final acceptance requires persisted report coverage to match the formal risk account count; pure risk readiness helpers were moved to the Application layer, reducing `show_personal_readiness_status.py` to 1,641 and `validate_personal_readiness_window.py` to 1,175 non-empty lines |
| Scheduler-clean final projection follow-up during `2026-07-01` pre-window | validation/status output now separates the basic accepted-evidence projection from a scheduler-clean projection. Current monitor output remains `2/20` accepted but reports `scheduler_clean_suffix_days=0`, `scheduler_clean_remaining_days=20`, and `projected_scheduler_completion_date=2026-07-29`, avoiding the earlier misleading `2026-07-27` final-acceptance expectation caused by legacy/manual evidence |
| Read-only monitor script follow-up during `2026-07-01` pre-window | added `scripts/check-personal-readiness-monitor.ps1` as the Windows local continuous-run monitor wrapper. It runs `show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime`, can print a concise `-SummaryOnly` status, can optionally log output under `var/readiness-monitor/`, and does not generate readiness evidence |
| Daily readiness schedule adjustment during `2026-07-01` pre-window | default daily readiness evidence schedule changed from `23:50` to `19:40 Asia/Shanghai`, because the market is closed in the afternoon and the post-close chains normally finish by the early evening. `setup_personal_readiness_daily --hour 19 --minute 40` updated the local PeriodicTask; the next scheduled evidence is `2026-07-02T19:40:00+08:00` with a 30-minute grace deadline |
| Decision-data reliability audit after schedule adjustment | `repair_decision_data_reliability --target-date 2026-07-01 --strict` completed with macro, quote, Pulse, and Alpha readiness all `ready`; Qlib freshness remained current through `2026-07-01`, quotes refreshed successfully, scoped Alpha inference was queued, and strict monitor stayed green at `2/20` accepted days |
| Market thermometer refresh after decision-data audit | `calculate_market_thermometer --as-of-date 2026-07-01 --json` persisted a decision-usable snapshot with `score=61.06`, `band=hot`, and `valid_component_count=4`; remaining degraded components were `new_investor_accounts` and `etf_net_flow`, while the snapshot remained `must_not_use_for_decision=false` |
| ETF net-flow timeout-budget repair | ETF net-flow provider timeout was raised from `10s` to `35s` so the market thermometer sync can cover AKShare/EastMoney direct fallback and still reach Tushare size-flow proxy. Re-running `calculate_market_thermometer --as-of-date 2026-07-01 --json` wrote a `data_center_consensus` fallback-proxy ETF flow row from Tushare (`stored_count=8`, consensus `stored_count=1`), reducing ETF flow age from `9` to `5` days; `new_investor_accounts` remains the main long-stale component |
| Decision Workspace Alpha alignment refresh | Ran the Decision Workspace refresh service for account `default` with `force=True`; it completed with `recommendations_count=30` and `conflicts_count=0`. Follow-up healthcheck shows Alpha latest trade date `2026-07-01`, workspace `latest_updated_at=2026-07-01T14:31:46.917829+00:00`, top source candidate IDs all anchored to `alpha_rank:*:2026-07-01`, and alpha/workspace consistency remains `ok` |
| Pre-readiness quote refresh schedule hardening | Added `decision-quote-pre-readiness-refresh` at weekdays `18:20 Asia/Shanghai` and ran `setup_decision_quote_refresh` locally. This closes the gap introduced by moving readiness evidence to `18:40`: the existing `15:20` post-close quote refresh could be older than the 4-hour quote freshness gate by the time scheduled readiness evidence runs. PeriodicTask inspection confirms the new task is enabled, and strict monitor remains green |
| Investor-account source-gap and import path follow-up | Re-ran `sync_market_thermometer_inputs --as-of-date 2026-07-01 --json`; AKShare stored 2 investor-account rows, and direct provider inspection confirmed `stock_account_statistics_em()` currently ends at `2023-08`. The remaining long-stale investor-account component is therefore a source-data gap. Fixed `import_investor_accounts` so the documented `--file <csv_path>` form works in addition to the positional path, added command tests, and synced the static test baseline to `6,075` |
| Scheduler default initialization atomicity follow-up | A dry run of `init_scheduler_defaults --disable` exposed a production safety issue: if a later subcommand hits a SQLite lock or other database error, earlier scheduler tasks could already be written as disabled. Restored local critical tasks to enabled, then changed `init_scheduler_defaults` to run all subcommands inside one database transaction and defer printing child output until the full run succeeds. Added failure-path coverage and synced the static test baseline to `6,076`; strict monitor now waits for the scheduled `2026-07-02T18:40:00+08:00` evidence |
| Pre-readiness quote scheduler observability follow-up | `show_personal_readiness_status --json` now reports `quote_pre_readiness_scheduler` for `decision-quote-pre-readiness-refresh`, including enabled state, task path, `18:20` weekday crontab, quote max-age kwargs, run metadata, and safety issues. The top-level monitor rolls up this scheduler as a warning if it becomes unsafe, so the `18:40` readiness evidence window no longer depends on manually inspecting Django Admin to confirm the quote pre-refresh plan. Real strict-monitor output shows this scheduler `status=ok`, `quote_max_age_hours=4.0`, and asset codes `510300.SH,000300.SH`; static test baseline synced to `6,078` |
| Read-only monitor summary quote pre-refresh follow-up | `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` now prints the quote pre-refresh status line so external/local operators can see the `18:20` pre-readiness quote task without opening full JSON. Current output: `Quote pre-refresh: ok enabled=True @ 18:20 max_age_h=4.0 runs=0 due=pending scheduled_for=2026-07-02T18:20:00+08:00`; monitor gate remains ok and still waits for scheduler evidence at `2026-07-02T18:40:00+08:00` |
| Pre-readiness quote missed-run guard follow-up | Quote pre-refresh monitoring now evaluates the `next_required_date` schedule instead of only static PeriodicTask config. For the current window it reports the next quote pre-refresh at `2026-07-02T18:20:00+08:00`; after the 15-minute grace window, a missing or stale `last_run_at` produces `quote_pre_readiness_run_missing_after_grace` and rolls the monitor to warning before the `18:40` daily evidence run. Added pending, completed, and missed-run coverage; static test baseline synced to `6,081` |
| Daily readiness static default follow-up | `core.settings.base.CELERY_BEAT_SCHEDULE` now also defaults `personal-readiness-daily-evidence` to weekdays `18:40 Asia/Shanghai`, matching the database-backed `setup_personal_readiness_daily` default and local PeriodicTask. Operators can still adjust the concrete database schedule from Django Admin through `PeriodicTask` / `CrontabSchedule`, with the safer command path remaining `python manage.py setup_personal_readiness_daily --hour 18 --minute 40` |
| Pre-readiness quote final acceptance gate follow-up | `acceptance_gate.requirements` now includes `quote_pre_readiness_scheduler`, so final strict acceptance cannot pass if `decision-quote-pre-readiness-refresh` is disabled, unsafe, or misses its grace window. Failures map to `fix_quote_pre_readiness_scheduler` with `python manage.py setup_decision_quote_refresh`; target coverage added for the unsafe scheduler case. Validation passed with `68 passed`, ruff, strict monitor, and governance baseline synced to `6,082` static test functions / 1,824 non-empty lines for `show_personal_readiness_status.py` pending later helper extraction |
| Local scheduler runtime final acceptance gate follow-up | When `show_personal_readiness_status --strict-acceptance --require-local-scheduler-runtime` is used, `acceptance_gate.requirements` now includes `scheduler_runtime`. A completed 20-day evidence window cannot pass final acceptance if local Celery beat/workers or required queues are down; failures expose `restore_local_scheduler_runtime` with the runtime remediation command. Added accepted-window coverage for a missing beat runtime. Validation passed with `69 passed`, ruff, strict monitor, and governance baseline synced to `6,083` static test functions / 1,885 non-empty lines for `show_personal_readiness_status.py` pending later helper extraction |
| Pre-readiness quote activity final acceptance gate follow-up | `acceptance_gate.requirements` now includes `quote_pre_readiness_activity`. Once the 20-day evidence window is accepted, final acceptance also requires `decision-quote-pre-readiness-refresh.total_run_count` and `last_run_at` to cover the accepted window, so static scheduler configuration alone is not enough to prove the `18:20` quote refresh actually ran before the `18:40` readiness evidence. Added insufficient-dispatch coverage. Validation passed with `70 passed`, ruff, strict monitor, and governance baseline synced to `6,084` static test functions / 1,957 non-empty lines for `show_personal_readiness_status.py` pending later helper extraction |
| Pre-readiness quote evidence-level proof follow-up | Daily readiness evidence now records `scheduler_evidence.quote_pre_readiness_scheduler`, including the `18:20` schedule, safety status, and `last_run_at`/`total_run_count` snapshot at collection time. Window validation counts formal records with ok/missing/blocked quote pre-readiness proof and final acceptance prefers these evidence-level counts once available, so the 20-day trial can prove each formal evidence day saw a safe pre-readiness quote scheduler that had run for that target date. Validation passed with `97 passed`, ruff, strict monitor, and governance baseline synced to `6,087` static test functions / 1,990 non-empty lines for `show_personal_readiness_status.py` / 1,305 for `validate_personal_readiness_window.py` pending helper extraction |
| Monitor summary evidence-proof visibility follow-up | `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` now prints `Quote pre-proof` with ok/missing/blocked evidence-level counts. Current output shows `status=pending_window ok_records=0 missing=1 blocked=0`, which is expected because the accepted window still contains old manual evidence before the `scheduler_evidence.quote_pre_readiness_scheduler` field existed; the next scheduler-generated formal evidence at `2026-07-02T18:40:00+08:00` should start accumulating ok proof records |
| Monitor summary risk/weekly proof visibility follow-up | The read-only monitor summary now also prints `Risk proof` and `Weekly advisor proof`, exposing formal evidence coverage for persisted risk reports, pre-trade/post-investment checks, and scheduled weekly advisor persistence. Added regression coverage that `persist_risk_report=True` records the persisted `report_id` in daily evidence, proving the scheduler's `persist_risk_report=true` setting will create acceptance-visible risk proof rather than database-only side effects. Validation passed with `98 passed`, ruff, strict monitor, and governance baseline synced to `6,088` static test functions |
| Daily evidence Markdown auditability follow-up | The Markdown evidence file now includes account-level proof fields for `risk_persisted`, `risk_report_id`, `pre_trade_status`, `post_investment_passed`, `weekly_persistence_status`, persisted weekly report ID, and delivered notification count. This keeps the JSON contract as the machine source of truth while making each scheduler-generated daily evidence file directly reviewable by an operator during the 20-trading-day trial. Validation passed with `98 passed`, ruff, governance, and strict monitor |
| Monitor summary evidence provenance follow-up | The read-only monitor summary now prints scheduler/manual/legacy/task-provenance counts from `scheduler_activity`. Current output shows `scheduler=0 manual=1 legacy=1 task_proof=0 unique_task_ids=0`, making it explicit that the accepted 2-day window is not scheduler-clean yet even though the monitor remains green while waiting for the first scheduled `2026-07-02T18:40:00+08:00` evidence |
| Decision-data preflight repair during `2026-07-02` pre-window | Ran `repair_decision_data_reliability --target-date 2026-07-01 --strict` after read-only checks showed the latest manual evidence still had degraded macro sync / market thermometer observations. The repair completed successfully: macro, quote, Pulse, and Alpha readiness all reported `ready`; Qlib freshness was already current through `2026-07-01`; decision quotes were refreshed at the current run time; scoped Alpha inference was queued and cache verification returned fresh scores. Strict monitor remained green and continued waiting for the scheduled `2026-07-02T18:40:00+08:00` evidence |
| Healthcheck decision-data yellow-item audit during `2026-07-02` pre-window | `healthcheck --json` remains `healthy=true` after the preflight repair, with database, Redis, Celery, critical data, decision quotes, and Alpha/workspace consistency all ok. The latest market thermometer is decision-usable but still `data_source=degraded`: stale components are `new_investor_accounts` and `etf_net_flow`, while `must_not_use_for_decision=false`. This is tracked as a yellow source-data quality item, not a blocker for the scheduled readiness evidence |
| Decision-data monitor visibility follow-up | `show_personal_readiness_status --json` now includes the latest formal evidence's `summary.decision_data` payload, and `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` prints a `Decision data` line. Current monitor output shows `status=ok readiness=ok mt=ok source=degraded stale=new_investor_accounts,etf_net_flow must_not_use=False`, so the yellow market-thermometer source gap is visible in the one-screen operator summary during the 20-day scheduler trial |
| Decision-data stale-age visibility follow-up | The monitor summary now prints stale/missing market-thermometer component ages when the underlying evidence has component details, e.g. `stale=new_investor_accounts(1035d),etf_net_flow(9d)`. A pre-window rerun of `sync_market_thermometer_inputs --as-of-date 2026-07-01 --json` confirmed the ETF main-flow sources were still timeout/no-data, while the Tushare size-flow fallback produced a `fallback_proxy` consensus row; database inspection shows canonical `CN_A_ETF_NET_FLOW` remains latest at `2026-06-26`, so the ETF yellow item is a source-lag issue rather than a broken readiness gate |
| Post-evidence persistence monitor visibility follow-up | The read-only monitor summary now prints `Post-evidence DB`, separating old evidence-field proof from the current database persistence check. Current output shows `status=ok risk=ok risk_reports=2 weekly=ok weekly_reports=2 impact=none`: the existing manual `2026-07-01` evidence still lacks scheduler-era persisted-report IDs, but the database has the risk-center daily reports and auto-advisor reports needed for operator confidence while waiting for the first scheduler-generated formal evidence |
| Regime/Pulse evidence auditability follow-up | Future `collect_personal_readiness_evidence` payloads now include explicit `system.checks.regime` and `system.checks.pulse` summaries instead of only the coarse `critical_data` booleans. A read-only direct check for `2026-07-01` returned Regime `Recovery` with confidence `0.3413`, source `akshare`, `records_count=149`, and Pulse composite `0.107`, strength `moderate`, `stale_indicator_count=0`. This improves scheduler evidence auditability for the Regime/Pulse main chain without manually generating a new formal evidence file before the scheduled `2026-07-02T18:40:00+08:00` run |
| Live Regime/Pulse monitor fallback follow-up | `show_personal_readiness_status --json` now exposes `current_macro_context` for the latest closed trading date, and the read-only monitor summary falls back to it when the latest formal evidence predates the new `summary.macro_context` field. Current output shows `Macro context: source=live regime=Recovery confidence=0.3413424364467061 regime_date=2026-07-01 pulse=0.107 pulse_date=2026-07-01 pulse_stale=0`, so operators can see the live Regime/Pulse main-chain state while waiting for the first scheduler-generated evidence with embedded macro-context proof |
| Scheduler timing and macro-context helper follow-up | Moved the operational readiness window earlier now that the market is closed by the afternoon: decision quote pre-refresh runs at `18:20`, daily readiness evidence runs at `18:40`, Qlib post-close scoped inference recovery is limited to `18:00-18:50`, and market-thermometer post-close refresh runs at `17:20 / 18:20`. Local PeriodicTask rows were updated to these times, and `collect_personal_readiness_evidence` now reuses `readiness_status_services.build_current_macro_context()` for Regime/Pulse evidence instead of duplicating collection logic. Validation passed with `108 passed`, ruff, governance consistency, and strict monitor; current next check is `2026-07-02T18:40:00+08:00` |
| Quote pre-refresh monitor expectation fix | After moving the pre-readiness quote task to `18:20`, `quote_pre_readiness_scheduler_status` still expected `19:20` and incorrectly reported `unexpected_quote_pre_readiness_time`. Updated the monitor expectation and test fixtures to `18:20`. Validation passed with `108 passed`, ruff, governance consistency, and strict monitor; current monitor status is back to `in_progress` with quote pre-refresh `ok` and `scheduled_for=2026-07-02T18:20:00+08:00` |
| Market thermometer safe-date read/write follow-up | A read-only database audit found a pre-close `2026-07-02` market-thermometer snapshot while the decision-safe latest closed date was still `2026-07-01`. The application use cases now share the same safe-date resolver as scheduler/manual commands, so omitted `as_of_date` resolves to the previous business day before `16:00 Asia/Shanghai`, and `build_current_payload()` / history output ignore snapshots later than the decision-safe date. Real local DB verification kept the suspicious row in place and confirmed current payload/history expose `2026-07-01`, not `2026-07-02`; targeted market-thermometer tests passed with `33 passed` |
| Healthcheck decision-data safe-date follow-up | `healthcheck --json` still exposed the skipped `2026-07-02` market-thermometer row because `get_decision_data_readiness_payload()` directly used `MarketThermometerSnapshotRepository.get_latest()`. Decision-data readiness now reuses the same safe `build_current_payload(auto_calculate=False)` path as UI/API consumers and emits the skipped raw latest row only under `skipped_latest_market_thermometer` with `skip_reason=latest_snapshot_after_decision_safe_date`. Real healthcheck output is healthy and now reports decision `market_thermometer.observed_at=2026-07-01` while keeping the `2026-07-02` row audit-visible; targeted decision-data/market-thermometer tests passed with `35 passed` |
| Readiness monitor skipped-thermometer visibility follow-up | The evidence summarizer now preserves `skipped_latest_market_thermometer` in `latest_formal_evidence.summary.decision_data`, and `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` prints `MT skipped latest` when a formal scheduler evidence record includes a skipped latest thermometer row. This keeps future pre-close/unsafe snapshot skips visible in the one-screen operator monitor instead of requiring full JSON inspection. Validation passed with `64 passed`, ruff, governance consistency, and strict monitor; the current manual `2026-07-01` evidence predates this field, so the new summary line will appear once a scheduler-generated evidence file carries the skipped snapshot diagnostic |
| Live decision-data monitor follow-up | `show_personal_readiness_status --json` now includes `current_decision_data` when run through the strict monitor path, using the same decision-safe data-center readiness facade as `healthcheck`. The PowerShell summary prefers this live decision data over stale formal evidence while still keeping evidence as a fallback. Current real monitor output now shows `Decision data: source=live ... mt_date=2026-07-01 ...` plus `MT skipped latest: observed_at=2026-07-02 status=skipped reason=latest_snapshot_after_decision_safe_date`, so operators can see the live safe-date decision state during the pre-scheduler window. Validation passed with `110 passed`, ruff, governance consistency, strict monitor, and `healthcheck --json` |
| Scheduler task-provenance preflight audit | Before the first `18:40` scheduler evidence window, verified that `personal-readiness-daily-evidence` points to `apps.task_monitor.application.tasks.run_personal_readiness_daily_task`, is enabled, and carries kwargs `trigger_source=scheduler`, `persist_risk_report=true`, `allow_unclosed_target_date=false`, `run_workspace_refresh=true`, and `include_weekly_advisor=true`. The bound Celery task code passes `self.request.id` as `trigger_task_id` and `self.name` as `trigger_task_name` into `run_personal_readiness_daily`; existing regression coverage confirms this command handoff. Validation passed with `35 passed` across daily task, scheduler initialization, and window validation tests |
| Scheduler task-id regression lock | Added explicit Celery `apply(task_id=...)` coverage for `run_personal_readiness_daily_task`, proving the Celery request id is passed as non-empty `trigger_task_id` together with `trigger_source=scheduler` and the canonical task name. This protects the scheduler-clean acceptance proof from silently degrading into scheduler records without unique task provenance. Validation passed with `98 passed`, governance consistency after static test baseline sync, and strict monitor |
| Decision-data Markdown evidence auditability follow-up | Future `collect_personal_readiness_evidence` payloads now store compact decision-data readiness under `summary.decision_data`, and the generated Markdown file includes `Decision Data` lines for the safe market-thermometer snapshot plus any `skipped_latest_market_thermometer` diagnostics. This keeps the `latest_snapshot_after_decision_safe_date` audit trail visible in the archived daily evidence file itself, not only in the live monitor summary. Validation passed with `110 passed`, ruff, governance consistency, and read-only monitor summary; current monitor remains `in_progress`, waiting for `2026-07-02T18:40:00+08:00` scheduler evidence with live decision data using `mt_date=2026-07-01` and skipped latest `observed_at=2026-07-02` |
| `2026-07-02 11:09 +08:00` pre-window scheduler audit | Did not manually generate formal evidence before the scheduled window. Read-only checks showed `healthcheck --json` healthy, Redis/Celery/database ok, fresh decision quotes, decision data `readiness_status=ok`, Qlib check-only current through `2026-07-01`, and strict monitor green with runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`. PeriodicTask inspection confirmed `decision-quote-pre-readiness-refresh` enabled at weekdays `18:20`, `personal-readiness-daily-evidence` enabled at `mon-fri 18:40` with `trigger_source=scheduler`, `persist_risk_report=true`, `allow_unclosed_target_date=false`, and the weekly auto-advisor task enabled on Fridays `17:30`. The acceptance state remains intentionally `in_progress`: accepted window `2/20`, scheduler-clean window `0/20`, next action `wait_for_post_close` until `2026-07-02T18:40:00+08:00` |
| Scheduler-first operator procedure follow-up | Updated the next acceptance window runbook so daily operation defaults to read-only monitoring and Celery beat generated evidence. Manual `run_personal_readiness_daily` is now documented as diagnostic/backfill only, because a manual formal record may be accepted for the general evidence window but cannot advance the scheduler-clean suffix required for final production-use acceptance. This closes a procedural gap before the first `18:40` scheduler evidence run |
| Missed scheduled-run operator action hardening | `show_personal_readiness_status` now keeps scheduler-clean recovery aligned with the scheduler-first procedure: when a scheduled daily evidence run is past its grace deadline, the normalized `next_action` becomes `inspect_missed_scheduled_run` with a strict-monitor inspection command instead of suggesting manual `run_personal_readiness_daily`. This preserves manual daily runs for explicit diagnosis/backfill while preventing an overdue scheduler day from being papered over by a manual formal record. Validation passed with `110 passed`, ruff, governance consistency, and read-only monitor summary |
| Final acceptance command hardening | Updated the final acceptance runbook so the direct Python command includes `--require-local-scheduler-runtime --local-runtime-ping-timeout 8`, matching the PowerShell `-StrictAcceptance` wrapper. This prevents a future 20-day evidence window from being promoted through a weaker final gate that omits local Celery beat/worker runtime proof |
| Strict-acceptance runtime proof hardening | `show_personal_readiness_status --strict-acceptance` now implicitly enables local scheduler runtime proof even when the caller omits `--require-local-scheduler-runtime`. This makes the final promotion gate code path match the runbook and prevents a bare strict-acceptance invocation from bypassing Celery beat/worker proof. Added command-handle regression coverage; validation passed with `111 passed`, ruff, governance consistency after static test baseline sync to `6,092`, and read-only monitor summary |
| Strict-acceptance help text alignment | Updated the management command help for `--strict-acceptance` so it states that the final gate includes scheduler safety/activity and local runtime proof, matching the now-hardened code path and runbook |
| Regime/Pulse Markdown evidence auditability follow-up | Future `collect_personal_readiness_evidence` payloads now store compact Regime/Pulse proof under `summary.macro_context`, and the generated Markdown file includes a `Macro Context` section with dominant regime, confidence, source, record count, Pulse composite score, strength, transition warning, stale indicator count, and data source. This makes each scheduler-generated daily evidence file directly reviewable for the Regime/Pulse main chain instead of requiring operators to inspect nested `system.checks`. Validation passed with `111 passed`, ruff, governance consistency, and read-only monitor summary |
| Alpha/workspace Markdown evidence auditability follow-up | Future `collect_personal_readiness_evidence` payloads now store compact Alpha/workspace consistency proof under `summary.alpha_workspace_consistency`, and the generated Markdown file includes an `Alpha Workspace` section with Alpha trade date, update time, provider source, status, top codes, workspace account, recommendation count, recommendation codes, source-candidate count, and issue codes. This makes the Alpha/workspace final gate directly reviewable in each daily scheduler evidence file. Validation passed with `111 passed`, ruff, governance consistency, and read-only monitor summary |
| `2026-07-02 12:06 +08:00` scheduler-first pre-window audit | Ran only read-only checks before the scheduled window. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, runtime `beat=1`, `workers=3`, accepted window `2/20`, scheduler-clean window `0/20`, quote pre-refresh pending for `2026-07-02T18:20:00+08:00`, and daily evidence pending for `2026-07-02T18:40:00+08:00`. `healthcheck --json` remained healthy with database, Redis, Celery, critical data, decision quotes, decision-data readiness, and Alpha/workspace consistency all ok; market thermometer stayed decision-usable but degraded by stale `new_investor_accounts` and `etf_net_flow`. Direct `PeriodicTask` inspection confirmed the quote pre-refresh, daily readiness evidence, and weekly auto-advisor tasks are enabled with scheduler kwargs intact and no prior scheduler run count yet. No manual formal evidence was generated. |
| Market thermometer yellow-item repair during `2026-07-02` pre-window | Ran non-evidence data repair only: `sync_market_thermometer_inputs --as-of-date 2026-07-01 --json`, `calculate_market_thermometer --as-of-date 2026-07-01 --json`, Qlib `--check-only`, monitor summary, and `healthcheck --json`. ETF net flow main providers still returned timeout/no-data, but the Tushare ETF size-flow fallback produced a `data_center_consensus` `fallback_proxy` fact for `2026-07-01`, so the decision-safe market thermometer improved from two stale components to one: `new_investor_accounts`. The recalculated `2026-07-01` thermometer is `score=54.28`, `band=warm`, `valid_component_count=5`, `stale_components=[new_investor_accounts]`, `must_not_use_for_decision=false`; monitor remained green and still waits for scheduler evidence at `2026-07-02T18:40:00+08:00`. No manual formal readiness evidence was generated. |
| Investor-account CSV补数 operability follow-up | The remaining market-thermometer yellow item needs external monthly investor-account data. `import_investor_accounts` now has discoverable `--print-template`, `--dry-run`, `--json`, `--fail-on-warning`, and `--value-unit` paths plus help text that documents accepted date columns (`reporting_period` / `date` / `month`), value columns (`value` / `accounts` / `new_accounts`), default canonical unit `户`, and explicit `万户` conversion. Dry-run emits `suspicious_low_account_count` when values look like `万户` while `--value-unit` is still default, and scripts can combine `--dry-run --json --fail-on-warning` to fail safely before any write. When `--value-unit 万户` is supplied, imported values are converted to canonical `户` and raw unit/value are retained in `MacroFact.extra`. The admin API `market-thermometer/import/investor-accounts/` now accepts the same `dry_run`, `value_unit`, and `fail_on_warning` controls, returning HTTP 400 with structured warnings before any write when a dry-run gate fails. `docs/modules/data-center-market-thermometer.md` includes the same template, dry-run, JSON, warning, fail-on-warning, and unit guidance. Validation passed with `43 passed`, ruff, real `import_investor_accounts --dry-run --json --fail-on-warning`, `import_investor_accounts --help`, monitor summary, and governance consistency after syncing the static test baseline to `6,096`. |
| Investor-account补数 quick-reference follow-up | Added the safe investor-account补数 flow to `docs/development/quick-reference.md` so an operator seeing `new_investor_accounts` stale in the one-screen monitor can find the exact commands without opening the module document: `--print-template`, `--dry-run --json --fail-on-warning`, optional `--value-unit 万户`, and the matching admin API endpoint. This is documentation-only and does not generate formal readiness evidence before the scheduled `18:40` run. |
| Investor-account monitor action follow-up | The read-only monitor summary now prints an `MT stale action` line whenever the market thermometer has stale or missing `new_investor_accounts`, pointing operators to `import_investor_accounts --print-template` and the dry-run gate before any write. The hint is ASCII-only to avoid Windows console mojibake and directs 10k-account unit cases to the quick reference. Validation passed with monitor summary and governance consistency; no formal readiness evidence was generated before the scheduled `18:40` run. |
| Readiness schedule console edit follow-up | Added a direct `收市后 Readiness 时间` panel to `/ops/task-monitor/` so staff users can change `decision-quote-pre-readiness-refresh` and `personal-readiness-daily-evidence` without manually editing django-celery-beat rows. The page writes through application use cases and management-command gateways, preserving scheduler defaults and safety repair behavior. `setup_decision_quote_refresh` now accepts `--pre-readiness-hour` / `--pre-readiness-minute`, so operators can use earlier post-close combinations such as `15:35` quote pre-refresh and a 16:00+ daily evidence time instead of waiting for the old late-evening window. Quote pre-refresh monitoring now accepts any concrete post-close HH:MM time instead of hard-coding `18:20`. Validation passed with `82 passed`, ruff, governance consistency, and read-only strict monitor. This is an operability change only and does not manually generate formal readiness evidence. |
| Readiness default schedule moved to post-close afternoon | Changed the default readiness schedule itself, not only the local database rows: `setup_decision_quote_refresh` now defaults the pre-readiness quote refresh to `15:35`, `setup_personal_readiness_daily` defaults the daily evidence task to `16:10`, and `core.settings.base.CELERY_BEAT_SCHEDULE` uses `16:10` for the static fallback. This prevents `init_scheduler_defaults` or a cold-start bootstrap from reverting the system to the old evening window while also avoiding the `15:55` edge case where the latest closed-date resolver still targets the previous trading day before 16:00. Local read-only monitor confirmed quote pre-refresh completed at `15:35`, then daily evidence completed at `16:10` with `Latest formal evidence: 2026-07-02 source=scheduler`, non-empty task provenance, accepted window `3/20`, and scheduler-clean window `2/20`. Evidence inspection accepted both `2026-07-01` and `2026-07-02` scheduler records; `2026-07-02` has `trigger_task_id=50c1c3ef-70df-4af1-a5d2-c7977d8a3b7f` and task `apps.task_monitor.application.tasks.run_personal_readiness_daily_task`. Validation passed with `85 passed`, ruff, governance consistency after static test baseline sync to `6,101`, and strict monitor. |
| `2026-07-02 16:40 +08:00` post-scheduler yellow-item audit | Re-ran only read-only gates after the afternoon scheduler evidence and the non-evidence ETF fallback recalculation. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, `state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence `2026-07-02 source=scheduler`, runtime `beat=1`, `workers=3`, and next check `2026-07-03T16:10:00+08:00`. `healthcheck --json` was healthy and confirmed the decision-safe market thermometer now uses `observed_at=2026-07-02`, `score=47.9`, `band=warm`, `valid_component_count=5`, `must_not_use_for_decision=false`; `etf_net_flow` is no longer stale (`age_days=1`), leaving only `new_investor_accounts(1036d)` as a degraded component. `validate_personal_readiness_window --json` remained intentionally `in_progress` with no blocking issues and projected scheduler completion `2026-07-28`. No manual formal readiness evidence was generated. |
| `2026-07-02 17:00 +08:00` market-thermometer source-gap repair | Closed the remaining market-thermometer stale component without generating readiness evidence. `AkshareUnifiedProviderAdapter` now keeps the existing AKShare investor-account path and falls back to the official SSE monthly investor page query (`COMMON_SSE_TZZ_M_ALL_ACCT_C`) when AKShare's discontinued `stock_account_statistics_em` history does not cover recent months. The fallback stores audited proxy rows with `proxy=sse_monthly_all_account_openings`, `original_unit=万户`, `source_url=https://www.sse.com.cn/aboutus/publication/monthly/investor/`, and canonical `户` values. Real sync wrote `new_investor_accounts stored_count=35`; the latest row is `2026-05-31 value=2984400 户`, source `akshare`, `sse_query_month=202605`. `calculate_market_thermometer --as-of-date 2026-07-02 --skip-sync --json` then persisted `score=50.14`, `band=warm`, `valid_component_count=6`, `stale_components=[]`, `missing_components=[]`, `data_source=calculated`, `must_not_use_for_decision=false`. Read-only monitor and `healthcheck --json` both stayed green, with monitor decision data now showing `mt_source=calculated stale= missing=`. Validation passed with targeted provider/use-case tests and ruff. |
| Market-thermometer proxy observability follow-up | The one-screen readiness monitor now preserves market-thermometer provenance through `summary.decision_data.market_thermometer.proxy_components` and `component_data_provenance`, so audited fallback/proxy feeds are visible instead of being hidden by the compact summary. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` now filters empty proxy rows and prints concrete sources, currently `new_investor_accounts:sse_monthly_all_account_openings`, `turnover:tushare_index_daily_sh000001_plus_sz399001`, `margin_balance:sh_margin_plus_sz_margin`, and `etf_net_flow:tushare_etf_share_size_delta`. Validation passed with `68 passed`, ruff, governance consistency, and read-only monitor summary. |
| Post-evidence weekly persistence noise reduction | `post_evidence_persistence.auto_advisor_weekly_report` now distinguishes `not_due` from `warning`: non-weekly target dates, and the weekly target date before the configured 17:30 run, no longer make the one-screen monitor look degraded. Missing weekly report persistence is still checked once the scheduled weekly report is due. The read-only monitor now reports `Post-evidence DB: status=ok risk=ok ... weekly=not_due ... impact=none` for the current Thursday evidence. Validation passed with `65 passed`, ruff, governance consistency, and read-only monitor summary. |
| Evidence inspection weekly-due alignment | `inspect_personal_readiness_evidence` now uses the same weekly auto-advisor due calculation as the one-screen monitor. Existing daily evidence files from non-weekly dates still show the historical weekly persistence payload, but inspection reports those accounts as `status=not_due` and no longer emits `verify_scheduled_weekly_report_persistence` follow-ups until the weekly schedule is actually due. Validation passed with `80 passed`, ruff, governance consistency, Qlib check-only, and read-only inspection of `2026-07-02` evidence. |
| Evidence inspection post-repair awareness | `inspect_personal_readiness_evidence` now distinguishes historical market-thermometer degradation from current unresolved degradation. When a target-date evidence file still says `source=degraded` but current live decision data for the same date has no stale or missing thermometer components and `must_not_use_for_decision=false`, the observation is reported as `resolved_after_evidence` with current provenance instead of emitting another `refresh_market_thermometer` follow-up. Read-only inspection of `2026-07-02` now preserves the old evidence context while showing current `data_source=calculated`, `valid_component_count=6`, and audited proxy components; follow-up actions no longer include a redundant thermometer refresh. Validation passed with `80 passed`, ruff, governance consistency, and read-only monitor summary. |
| Evidence inspection macro-sync post-repair awareness | Ran non-evidence `repair_decision_data_reliability --target-date 2026-07-02 --strict --skip-alpha`; the repair report showed the six decision-grade macro inputs (`CN_PMI`, `CN_NEW_CREDIT`, `CN_CPI_NATIONAL_YOY`, `CN_SHIBOR`, `CN_LPR`, `CN_M2`) are `decision_safe` and `must_not_use_for_decision=false`. `inspect_personal_readiness_evidence` now keeps historical `workspace.macro_sync` skipped-count observations visible but resolves them as `resolved_after_evidence` when the current same-date decision-grade macro contracts are ready, suppressing redundant `repair_decision_data_reliability` follow-ups. Real `2026-07-02` inspection is accepted with `follow_up_actions=[]`; monitor summary remains green with accepted window `3/20` and scheduler-clean window `2/20`. Validation passed with `81 passed`, ruff, monitor summary, and governance consistency after static test baseline sync to `6,103`. |
| Alpha/workspace source-date consistency repair | A forced default workspace recommendation refresh exposed a stale-source edge case: the outer Alpha cache row was for `2026-07-02`, but the embedded score payload still carried `intended_trade_date=2026-07-01`, so workspace recommendations were refreshed while `source_candidate_ids` still pointed to `alpha_rank:*:2026-07-01`. The Qlib and cache adapters now let the cache-row trade date override embedded score payload `intended_trade_date` while preserving `asof_date` staleness semantics. `AlphaWorkspaceConsistencyChecker` also emits `workspace_alpha_rank_source_stale` if workspace Alpha-rank source ids lag the latest Alpha ranking date. After rerunning the non-evidence workspace refresh, healthcheck shows Alpha latest trade date `2026-07-02`, workspace source ids `alpha_rank:*:2026-07-02`, and `alpha_workspace_consistency.status=ok`. Validation passed with `92 passed`, targeted ruff, governance consistency after static test baseline sync to `6,105`, `healthcheck --json`, `inspect_personal_readiness_evidence --target-date 2026-07-02 --json`, and read-only monitor summary. |
| Auto-advisor weekly due observability follow-up | The weekly auto-advisor scheduler is intentionally configured for Friday `17:30 Asia/Shanghai`. For non-scheduled target dates, `build_auto_advisor_weekly_due_status` no longer reports a misleading same-day `scheduled_for`; it now returns `scheduled_for=null` and `next_scheduled_for` with the next actual weekly run. Current `2026-07-02` evidence inspection remains accepted with `follow_up_actions=[]`, and its weekly persistence observations now say `weekly_report_not_scheduled_for_target_date`, `scheduled_for=null`, `next_scheduled_for=2026-07-03T17:30:00+08:00`. Validation passed with `81 passed`, ruff, governance consistency, and read-only post-evidence persistence inspection. |
| Strict monitor post-evidence persistence gate | Tightened `show_personal_readiness_status --strict-monitor` so it no longer stays green when the latest formal evidence has degraded current DB persistence proof. If `post_evidence_persistence.status` becomes `warning` or `error` after a due weekly auto-advisor run or risk-report persistence check, `monitor_gate.ok=false` with `state=post_evidence_persistence_not_ok`, exposing the issue during the 20-day trial instead of waiting for final acceptance. Current `2026-07-02` remains green because weekly auto-advisor persistence is `not_due` and risk persistence is `ok`; tomorrow's Friday weekly run will be checked once it is due. Validation passed with `82 passed`, ruff, monitor summary, and governance consistency after static test baseline sync to `6,106`. |
| Strict monitor weekly pending-window follow-up | Covered the Friday timing gap between daily readiness evidence at `16:10` and weekly auto-advisor report generation at `17:30`. When the latest formal evidence is for a weekly target date but `post_evidence_persistence.auto_advisor_weekly_report` is still `weekly_report_schedule_not_due_yet`, the strict monitor now remains green while reporting `state=wait_for_post_evidence_persistence` and `next_check_after` equal to the weekly report's `scheduled_for` time. This prevents the monitor from jumping straight to the next trading day's daily evidence before the same-day weekly persistence check is due. Validation passed with `83 passed`, ruff, monitor summary, and governance consistency after static test baseline sync to `6,107`. |
| Monitor summary weekly due visibility | The one-screen PowerShell monitor summary now includes `weekly_due` on the `Post-evidence DB` line, using the same-day weekly `scheduled_for` when it is pending and `next_scheduled_for` on non-weekly target dates. Current read-only output shows `weekly=not_due ... weekly_due=2026-07-03T17:30:00+08:00`, so an operator can see the Friday auto-advisor persistence checkpoint without opening the full JSON. Validation passed with read-only monitor summary and governance consistency. |
| `2026-07-02 23:59 +08:00` overnight read-only acceptance audit | Re-ran only read-only gates after moving the readiness defaults to the afternoon window. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, `state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence `2026-07-02 source=scheduler`, quote pre-refresh completed at `15:35`, daily evidence scheduled next for `2026-07-03T16:10:00+08:00`, and weekly auto-advisor persistence due at `2026-07-03T17:30:00+08:00`. `show_personal_readiness_status --json --require-local-scheduler-runtime --local-runtime-ping-timeout 12` confirmed local runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`, post-evidence DB `status=ok`, risk reports persisted for both active accounts, and weekly report `not_due`. `healthcheck --json` was `healthy=true` with database, Redis, Celery, critical data, decision quotes, market thermometer, and Alpha/workspace consistency all ok. `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained `accepted` with `follow_up_actions=[]`; historical market-thermometer and macro-sync degradation are now classified as `resolved_after_evidence`. No manual formal readiness evidence was generated. |
| `2026-07-03 00:02 +08:00` Friday pre-window read-only audit | Ran the normal monitor before any Friday post-close scheduler events were due. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, `state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence still `2026-07-02 source=scheduler`, quote pre-refresh due at `2026-07-03T15:35:00+08:00`, daily evidence due at `2026-07-03T16:10:00+08:00`, and weekly auto-advisor persistence due at `2026-07-03T17:30:00+08:00`. `show_personal_readiness_status --json --require-local-scheduler-runtime --local-runtime-ping-timeout 12` confirmed `beat=1`, `workers=3`, covered queues `celery,qlib_infer,qlib_train`, and no scheduler/runtime issues. `healthcheck --json` remained `healthy=true`; decision data, current market thermometer, decision quotes, and Alpha/workspace consistency were all ok against the latest closed date `2026-07-02`. No manual formal readiness evidence was generated; the correct operator action is to wait for the three scheduled Friday checkpoints. |
| `2026-07-03` simulated-time checkpoint coverage | Added a pure unit regression test and a read-only `simulate_personal_readiness_checkpoints` management command for the Friday afternoon checkpoint state machine without changing system time or generating evidence. The simulation injects `now=` values for `2026-07-03T15:50:00+08:00`, `16:20`, `17:20`, and `17:45`, proving quote pre-refresh is still in its grace window at `15:50`, daily readiness evidence is pending before `16:10` and in grace at `16:20`, weekly auto-advisor persistence is not due before `17:30`, and becomes due after `17:30`. A real dry-run against local scheduler rows returned `mode=simulation`, `mutates_state=false`, `generates_evidence=false`, quote pre-refresh scheduled for `15:35`, daily readiness scheduled for `16:10`, and weekly due flipping at `17:30`. Validation passed with targeted pytest, ruff, and governance consistency after syncing the static test baseline to `6,109`. This is simulation coverage only; the real scheduler-clean window remains `2/20` until actual Celery beat evidence arrives. |
| `2026-07-03 07:44 +08:00` Friday morning pre-window audit | Re-ran real read-only checks after adding the simulation command. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, latest formal evidence still `2026-07-02 source=scheduler`, accepted window `3/20`, scheduler-clean window `2/20`, quote pre-refresh pending for `2026-07-03T15:35:00+08:00`, daily evidence pending for `2026-07-03T16:10:00+08:00`, weekly auto-advisor due at `2026-07-03T17:30:00+08:00`, and runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`. `healthcheck --json` remained `healthy=true` with database, Redis, Celery, decision data, market thermometer, decision quotes, and Alpha/workspace consistency all ok against the latest closed date `2026-07-02`. No manual formal readiness evidence was generated. |
| Readiness schedule console use-case coverage follow-up | Added a pure unit test for `ConfigureReadinessScheduleUseCase` so the `/ops/task-monitor/` time panel is covered beyond the page POST mock: `15:35` quote pre-refresh and `16:10` daily evidence parse into `quote_pre_refresh_hour=15`, `quote_pre_refresh_minute=35`, `daily_evidence_hour=16`, and `daily_evidence_minute=10` before calling the scheduler configuration gateway. The existing API test still verifies the staff page posts `quote_pre_refresh_time` and `daily_evidence_time`, while the use-case test now locks the application-layer contract used to update the actual django-celery-beat tasks. Validation passed with `19 passed`, ruff, and governance consistency after syncing the static test baseline to `6,110`. |
| `2026-07-03 07:53 +08:00` Friday morning pre-window audit | Re-ran only read-only gates before any Friday scheduler checkpoint was due. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, `state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence `2026-07-02 source=scheduler`, quote pre-refresh pending for `2026-07-03T15:35:00+08:00`, daily evidence pending for `2026-07-03T16:10:00+08:00`, and weekly auto-advisor due at `2026-07-03T17:30:00+08:00`. `healthcheck --json` was `healthy=true` with database, Redis, Celery, decision data, market thermometer, and Alpha/workspace consistency ok against the latest closed date `2026-07-02`. `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained `accepted` with `follow_up_actions=[]`, and `validate_personal_readiness_window --json` remained `in_progress` with no blocking issues. No manual formal readiness evidence was generated; the correct operator action remains waiting for the scheduled Friday checkpoints. |
| `2026-07-03 08:05 +08:00` Friday morning pre-window audit | Re-ran the read-only monitor, healthcheck, window validator, evidence inspection, and simulated checkpoint dry-run before the real Friday scheduler window. Current time was `2026-07-03 08:05:41 +08:00`; monitor stayed green with `state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, quote pre-refresh pending for `15:35`, daily evidence pending for `16:10`, and weekly auto-advisor due at `17:30`. `healthcheck --json` remained healthy; `validate_personal_readiness_window --json` reported no blocking issues; `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained accepted with `follow_up_actions=[]`. The simulated checkpoint command again reported `mode=simulation`, `mutates_state=false`, and `generates_evidence=false`, so it did not affect the formal acceptance window. No manual formal readiness evidence was generated. |
| `2026-07-03 08:09 +08:00` Friday morning Qlib/data-readiness audit | Re-ran read-only health and freshness checks before the real scheduler window. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` stayed green with `accepted window 3/20`, `scheduler-clean window 2/20`, latest formal evidence `2026-07-02 source=scheduler`, quote pre-refresh pending for `15:35`, daily evidence pending for `16:10`, and weekly auto-advisor due at `17:30`. `healthcheck --json` was healthy and confirmed decision data, market thermometer, and Alpha/workspace consistency remain ok against `2026-07-02`. `build_qlib_data --check-only --target-date 2026-07-02` reported `latest_trade_date: 2026-07-02` and no self-build update required. `validate_personal_readiness_window --json` remained `in_progress` with no blocking issues, and `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained accepted with `follow_up_actions=[]`. No manual formal readiness evidence was generated. |
| `2026-07-03 08:12 +08:00` scheduler-row configuration audit | Performed a read-only database audit of the three scheduler rows that gate Friday readiness. `decision-quote-pre-readiness-refresh` is enabled on `1,2,3,4,5` at `15:35 Asia/Shanghai`, runs `apps.data_center.application.tasks.refresh_decision_quote_snapshots_task`, has `quote_max_age_hours=4.0`, and last ran on `2026-07-02T07:35:00+00:00`. `personal-readiness-daily-evidence` is enabled on `mon-fri` at `16:10 Asia/Shanghai`, runs `apps.task_monitor.application.tasks.run_personal_readiness_daily_task`, has safe kwargs (`calendar_source=auto`, `persist_risk_report=true`, `repair_accounts=false`, `allow_unclosed_target_date=false`, `trigger_source=scheduler`), and last ran on `2026-07-02T08:10:00+00:00`. `dashboard-auto-advisor-weekly-report` is enabled on Friday at `17:30 Asia/Shanghai`, has no custom delivery controls, and has not yet run in the current local trial, which is expected before the first Friday due time. `show_personal_readiness_status --json --require-local-scheduler-runtime` also confirmed `beat=1`, `workers=3`, covered queues `celery,qlib_infer,qlib_train`, and `next_check_after=2026-07-03T16:10:00+08:00`. No formal evidence was generated. |
| Strict monitor warning-state hardening | Closed a pre-window monitor blind spot: if a supporting readiness scheduler such as `decision-quote-pre-readiness-refresh` turns the overall readiness status to `warning` while the daily evidence action is still `wait_for_post_close`, strict monitor no longer reports a green wait state. `_build_monitor_gate` now keeps waiting states green only when status is not `warning`; warning wait states fall through to the existing strict-monitor failure path. Added `test_personal_readiness_monitor_gate_blocks_warning_wait_state` so a missed 15:35 quote pre-refresh after its grace window cannot be hidden behind the pending 16:10 daily evidence window. Validation passed with targeted pytest, ruff, and governance consistency after syncing the static test baseline to `6,111`. No formal evidence was generated. |
| `2026-07-03 08:24 +08:00` strict-monitor post-hardening recheck | Re-ran the real strict monitor after the warning-state hardening. Current time was `2026-07-03 08:24:48 +08:00`, before the Friday scheduler window, and the monitor correctly remained green with `state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, quote pre-refresh due at `15:35`, daily evidence due at `16:10`, and weekly auto-advisor due at `17:30`. This confirms the hardening does not break normal pre-window waiting while still allowing warning states to fail strict monitoring. Validation passed with the targeted monitor-gate tests, ruff, and governance consistency. No formal evidence was generated. |
| Strict monitor full-payload regression follow-up | Strengthened the quote pre-refresh warning coverage from a pure monitor-gate unit case to a full `build_personal_readiness_status` payload assertion. `test_personal_readiness_status_blocks_acceptance_when_quote_pre_readiness_scheduler_is_unsafe` now also verifies that a quote pre-readiness scheduler warning produces `monitor_gate.ok=false`, `state=operator_action_required`, and `next_action=none`, so the full status assembly cannot hide quote-pre-refresh failure behind a normal waiting action. Validation passed with targeted pytest, ruff, and governance consistency. No formal evidence was generated. |
| `2026-07-03 08:40 +08:00` simulated-time and worker-registration audit | Re-ran the read-only simulated checkpoint command for `2026-07-03` without changing system time; it still reports `mode=simulation`, `mutates_state=false`, and `generates_evidence=false`, with quote pre-refresh at `15:35`, daily evidence at `16:10`, and weekly auto-advisor due after `17:30`. Hardened the local scheduler runtime gate so strict readiness monitoring now also checks that live Celery workers register the three scheduled task names: daily readiness evidence, quote pre-readiness refresh, and `dashboard.generate_auto_advisor_weekly_reports`. The real monitor stayed green before the Friday window with `registered_tasks_status=ok`, no missing registered tasks, runtime `beat=1`, `workers=3`, and queues `celery,qlib_infer,qlib_train`. Validation passed with targeted pytest and ruff; governance baseline was synced to `6,113` static test functions. No formal evidence was generated. |
| `2026-07-03 08:47 +08:00` task-registry summary visibility follow-up | Added the live Celery task-registration result to the one-screen PowerShell monitor summary so an operator can see worker task coverage without opening full JSON. The read-only summary now prints `Task registry: ok missing=` beside the existing runtime line. The same run stayed green with `monitor_gate.ok=true`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence `2026-07-02 source=scheduler`, quote pre-refresh pending for `15:35`, daily evidence pending for `16:10`, weekly advisor due at `17:30`, runtime `beat=1`, `workers=3`, and queues `celery,qlib_infer,qlib_train`. Validation passed with targeted pytest, ruff, and governance consistency. No formal evidence was generated. |
| `2026-07-03 08:51 +08:00` Friday pre-window health and freshness audit | Ran a read-only pre-window chain before any Friday scheduler checkpoint was due. The monitor summary passed with `Task registry: ok missing=`, accepted window `3/20`, scheduler-clean window `2/20`, quote pre-refresh pending for `15:35`, daily evidence pending for `16:10`, weekly auto-advisor due at `17:30`, and runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`. `healthcheck --json` returned `healthy=true`: database, Redis, Celery, critical data, decision quotes, decision-data readiness, market thermometer, and Alpha/workspace consistency were all ok against latest closed date `2026-07-02`; current market thermometer was calculated with six valid components and no stale/missing components, while proxy provenance remains visible. `build_qlib_data --check-only --target-date 2026-07-02` reported `latest_trade_date=2026-07-02` and no update required. `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained `accepted` with `follow_up_actions=[]`; historical market-thermometer and macro-sync observations were still classified as `resolved_after_evidence`. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 08:55 +08:00` operator next-check summary correction | Corrected the one-screen monitor summary so the displayed operator wait time respects the earlier quote pre-refresh checkpoint. The core daily evidence gate still reports `Next check after: 2026-07-03T16:10:00+08:00`, but the summary now also prints `Next operator check: 2026-07-03T15:50:00+08:00 reason=quote_pre_refresh` and `Operator action: wait until 2026-07-03T15:50:00+08:00`. This prevents an operator from waiting until the daily evidence time while missing the quote pre-refresh grace deadline. Validation passed with read-only monitor summary and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 08:58 +08:00` weekly checkpoint candidate follow-up | Extended the same one-screen `Next operator check` calculation to include the weekly auto-advisor due time from `post_evidence_persistence.auto_advisor_weekly_report.scheduled_for` / `next_scheduled_for`. Current pre-window output still chooses `2026-07-03T15:50:00+08:00 reason=quote_pre_refresh` because it is earlier than daily evidence and the `17:30` weekly report checkpoint, but after the quote and daily gates pass the summary can naturally direct the operator to the weekly auto-advisor persistence checkpoint. Validation passed with read-only monitor summary and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 09:05 +08:00` operator countdown visibility follow-up | Added a countdown to the one-screen monitor summary so the operator can see how far away the next real checkpoint is without manually calculating time differences. The read-only monitor now prints `Operator check in: 403 min` for the current `Next operator check: 2026-07-03T15:50:00+08:00 reason=quote_pre_refresh`. The monitor still passed with accepted window `3/20`, scheduler-clean window `2/20`, runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`, and `Task registry: ok missing=`. Validation passed with read-only monitor summary and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 09:10 +08:00` operator checkpoint rollover guard | Hardened the one-screen operator checkpoint selector so elapsed candidate checkpoints are ignored when choosing the next operator wait time. This keeps the summary from continuing to point at a past quote or weekly checkpoint after the day advances through the scheduler window. Current pre-window output remains `Next operator check: 2026-07-03T15:50:00+08:00 reason=quote_pre_refresh` with a live countdown, because that checkpoint is still in the future. Validation passed with read-only monitor summary and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 09:13 +08:00` initial daily-gate rollover guard | Tightened the same one-screen operator checkpoint selector so the initial daily evidence gate time is also treated as elapsed when it is earlier than the current time. This prevents the afternoon summary from retaining a past `Next check after` value when a later future checkpoint such as weekly auto-advisor persistence is still pending. Current pre-window output is unchanged and still points to `2026-07-03T15:50:00+08:00 reason=quote_pre_refresh`; the fix covers the later same-day rollover path. Validation passed with read-only monitor summary and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 09:17 +08:00` monitor-summary reference-time simulation | Added `-ReferenceTime` to the PowerShell monitor wrapper so the one-screen summary's operator-check rollover can be tested before real time reaches the checkpoint. This parameter only affects summary rendering; Django readiness status still runs against real current time, and no evidence is generated. Real-time monitor output still selected `2026-07-03T15:50:00+08:00 reason=quote_pre_refresh`; simulated summary output with `-ReferenceTime 2026-07-03T16:20:00+08:00` selected `2026-07-03T17:30:00+08:00 reason=weekly_auto_advisor`, proving the summary rolls forward after quote and daily checkpoints are elapsed. Validation passed with both monitor runs and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 09:22 +08:00` no-future-checkpoint summary guard | Tested the summary rollover after all known Friday checkpoints using `-ReferenceTime 2026-07-03T17:45:00+08:00`. The wrapper no longer prints a blank operator checkpoint when quote, daily evidence, and weekly auto-advisor times are all elapsed; it now reports `reason=review_current_status` and `Operator action: rerun monitor now and inspect current status`. The real current-time monitor still points to `2026-07-03T15:50:00+08:00 reason=quote_pre_refresh`. Validation passed with both monitor runs, the read-only checkpoint simulation command, and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 09:25 +08:00` no-future-checkpoint display polish | Polished the no-future-checkpoint summary state so it prints `Next operator check: now reason=review_current_status` instead of leaving the time column blank. Simulated output with `-ReferenceTime 2026-07-03T17:45:00+08:00` now clearly instructs `Operator action: rerun monitor now and inspect current status`, while the real current-time monitor remains unchanged and points to `2026-07-03T15:50:00+08:00 reason=quote_pre_refresh`. Validation passed with both monitor runs and governance consistency. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 12:39 +08:00` Friday midday pre-check | Ran the real read-only monitor before the first Friday scheduler checkpoint. Current time was `2026-07-03T12:39:08+08:00`; strict monitor passed with `Monitor gate: ok=True state=wait_for_post_close`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence `2026-07-02 source=scheduler`, quote pre-refresh pending for `15:35` with operator check at `15:50`, daily evidence pending for `16:10`, weekly auto-advisor due at `17:30`, runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`, and `Task registry: ok missing=`. `healthcheck --json` returned `healthy=true`; database, Redis, Celery, critical data, decision quotes, market thermometer, and Alpha/workspace consistency were all ok, with decision quote snapshots from `2026-07-03T03:09:45/48+00:00` still fresh under the 4h gate. `build_qlib_data --check-only --target-date 2026-07-02` reported Qlib freshness satisfied. `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained accepted with `follow_up_actions=[]`. A separate check for `2026-07-03` evidence correctly found no file yet because the scheduler window had not arrived; no formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 12:46 +08:00` scheduler time-management verification | Queried the live `django_celery_beat` `PeriodicTask` rows for the three readiness-critical jobs and confirmed the active database schedule no longer uses the historical `23:50` daily-evidence timing: `decision-quote-pre-readiness-refresh` is enabled on weekdays at `15:35 Asia/Shanghai`, `personal-readiness-daily-evidence` is enabled on weekdays at `16:10 Asia/Shanghai`, and `dashboard-auto-advisor-weekly-report` is enabled on Fridays at `17:30 Asia/Shanghai`. Verified the `/ops/task-monitor/` scheduler console exposes the “收市后 Readiness 时间” form with `type="time"` inputs for `quote_pre_refresh_time` and `daily_evidence_time`, and that `scheduler_console_view` routes the POST action through the Application-layer `configure_readiness_schedule` service. Targeted tests passed for page rendering, configure action, default scheduler creation, custom pre-readiness quote time, and the guard that rejects daily evidence before 16:00 (`7 passed`). `validate_personal_readiness_window --json` stayed `in_progress` with no blocking issues; `2026-07-03` evidence still correctly does not exist before the scheduler window. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:05 +08:00` unified post-close schedule controls | Extended the `/ops/task-monitor/` “收市后 Readiness 时间” form so the operator can manage all three post-close readiness checkpoints from one page: quote pre-refresh, daily evidence, and the Friday auto-advisor weekly report. The Application use case now reads and writes `dashboard-auto-advisor-weekly-report` alongside the two daily jobs, validates that the weekly auto-advisor run is after the daily evidence time, and delegates concrete updates to the Infrastructure management-command gateway. The gateway now calls `setup_auto_advisor_weekly_report` together with `setup_decision_quote_refresh` and `setup_personal_readiness_daily`; the live database schedule remained unchanged at `15:35`, `16:10`, and Friday `17:30`. Validation passed with the full scheduler-initialization test module plus scheduler-console page/action tests (`16 passed`), targeted `ruff check`, governance consistency (`0 violations`, test baseline synced to `6,116` static test functions), and the real strict monitor. `2026-07-03` evidence still correctly does not exist before the scheduler window; no formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:15 +08:00` weekly schedule scope preservation | Hardened `setup_auto_advisor_weekly_report` so a time-only update from the unified scheduler console preserves any existing `user_id` / `account_ids` weekly-report scope instead of silently rewriting kwargs to `{}`. Clearing the scope now requires the explicit `--clear-scope` flag, and the quick reference documents both behaviors. The existing live weekly task still runs Friday `17:30 Asia/Shanghai` with `kwargs={}`. Validation passed with the full auto-advisor weekly task test module plus scheduler gateway and console action tests (`6 passed`), targeted `ruff check` on Python files, `python manage.py check`, and governance consistency (`0 violations`). The real strict monitor remained green in `wait_for_post_close`; `2026-07-03` evidence still correctly does not exist before the scheduler window. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:24 +08:00` weekly schedule scope contract alignment | Aligned `setup_auto_advisor_weekly_report` with the strict weekly-scheduler status contract: explicit `--account-ids` now requires `--user-id`, and malformed account-id lists raise `CommandError` instead of saving a task that strict monitoring would later mark warning. Time-only updates still preserve existing kwargs, and `--clear-scope` remains the explicit path back to all active accounts. The quick reference now documents the valid scoped form. Validation passed with the full auto-advisor weekly task module plus scheduler gateway and console action tests (`8 passed`), targeted `ruff check`, `python manage.py check`, and governance consistency (`0 violations`, baseline synced to `6,118` static test functions). The real strict monitor remained green in `wait_for_post_close`; the live weekly task still runs Friday `17:30 Asia/Shanghai` with `kwargs={}`. `2026-07-03` evidence still correctly does not exist before the scheduler window; no formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:34 +08:00` configurable weekly time monitor alignment | Aligned the weekly auto-advisor status helper with the new management-page time control. The weekly scheduler safety check no longer hard-codes `17:30`; it accepts any single Friday Asia/Shanghai crontab time after 16:00 and still rejects missing crontab, wrong timezone/day, non-wildcard month/day fields, custom run controls, custom delivery controls, invalid kwargs, and unsafe pre-evidence times. `build_auto_advisor_weekly_due_status` now follows the current `PeriodicTask` crontab when available and only falls back to the default `17:30` schedule when no DB schedule can be read, so monitor due times remain consistent after an operator changes the weekly time. Validation passed with targeted weekly schedule expectation/status tests (`5 passed`), targeted `ruff check`, `python manage.py check`, and governance consistency (`0 violations`, baseline synced to `6,121` static test functions). The real strict monitor remained green and the live due status still resolves to `2026-07-03T17:30:00+08:00` from the current DB schedule. `2026-07-03` evidence still correctly does not exist before the scheduler window; no formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:45 +08:00` weekly-after-daily evidence guard | Closed the remaining bypass path where direct CLI use could set `dashboard-auto-advisor-weekly-report` before the daily readiness evidence time even though the unified management page already rejects that ordering. `setup_auto_advisor_weekly_report` now resolves the current `personal-readiness-daily-evidence` crontab, falls back to the default `16:10` threshold when needed, and rejects weekly times that are not later than daily evidence. The weekly scheduler safety helper applies the same rule, so strict monitoring cannot mark an unsafe direct CLI schedule as ok. Quick reference now states that the weekly report time must be later than daily evidence. Validation passed with targeted weekly setup/safety tests (`4 passed`), targeted `ruff check`, `python manage.py check`, and governance consistency (`0 violations`, baseline synced to `6,123` static test functions). The real strict monitor remained green; live weekly due status still resolves to `2026-07-03T17:30:00+08:00`; `2026-07-03` evidence still correctly does not exist before the scheduler window. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:52 +08:00` reference-time checkpoint dry run | Verified the Friday post-close checkpoint behavior using reference-time simulation only, without changing the system clock and without generating formal readiness evidence. `simulate_personal_readiness_checkpoints --target-date 2026-07-03` confirmed the configured schedule chain: quote pre-refresh due at `15:35` with grace through `15:50`, daily evidence due at `16:10` with grace through `16:40`, and weekly auto-advisor due at `17:30`. A boundary run for `15:34`, `15:50`, `16:10`, `16:41`, and `17:31` showed `pending`, `grace_period`, `due_now`, `overdue`, and weekly `due=true` transitions as expected. The command output explicitly reports `mutates_state=false` and `generates_evidence=false`; because it does not synthesize scheduler completions, overdue states after grace mean "would alert if the task has not actually run by then." Validation passed with checkpoint simulation tests and schedule expectation tests (`3 passed`). No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 13:56 +08:00` Friday pre-window readiness audit | Re-ran the read-only pre-window chain before any Friday scheduler checkpoint was due. Machine time was `2026-07-03T13:54:04+08:00`; strict monitor passed with `monitor_gate.ok=true`, accepted window `3/20`, scheduler-clean window `2/20`, latest formal evidence `2026-07-02 source=scheduler`, quote pre-refresh pending for `15:35` with operator check at `15:50`, daily evidence pending for `16:10`, and weekly auto-advisor due at `17:30`. `healthcheck --json` returned `healthy=true`: database, Redis, Celery, critical data, decision quotes, decision-data readiness, market thermometer, and Alpha/workspace consistency were all ok; decision quotes were fresh with `age_minutes=166`, and Qlib check-only reported `latest_trade_date=2026-07-02` with no update required. `inspect_personal_readiness_evidence --target-date 2026-07-02 --json` remained `accepted` with `follow_up_actions=[]`; the historical market thermometer and macro-sync observations are resolved after evidence, while rotation still reports a non-blocking `skipped=3` yellow item. A direct read-only rotation DB check confirmed `2026-07-02` has 3 signals for 6 active configs. `inspect_personal_readiness_evidence --target-date 2026-07-03 --json` failed with the expected missing-file `CommandError` because the scheduled evidence window had not arrived. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 14:03 +08:00` rotation pre-window no-save preflight | Investigated the remaining rotation yellow item without writing formal evidence or persisted signals. The historical `2026-07-02` DB state still has 3 signals for 6 active configs, but direct Domain-layer generation with repository saving bypassed showed all 3 missing configs are currently generatable for `2026-07-02`: both momentum configs allocate to `510300`/`159915`, and the custom core-satellite config allocates to `510300`. The same no-save preflight for `2026-07-03` generated all 6 active configs successfully: two momentum configs, macro-regime config, custom core-satellite config, balanced stock-bond config, and risk-parity config. The live `personal-readiness-daily-evidence` PeriodicTask kwargs include `run_workspace_refresh=true` and `include_weekly_advisor=true`, so the 16:10 scheduler evidence should exercise this Rotation workspace path instead of preserving the historical skip. No DB repair was applied, no formal evidence was generated, and the scheduler-clean window was not advanced. |
| `2026-07-03 14:06 +08:00` scheduler-runtime waiting audit | Re-ran the strict monitor and full JSON status while still before the Friday scheduler window. Machine time was `2026-07-03T14:06:02+08:00`; `monitor_gate.ok=true` with `state=wait_for_post_close`, daily evidence `due_status=pending`, quote pre-refresh `due_status=pending`, and local runtime proof stayed ok with `beat=1`, `workers=3`, covered queues `celery, qlib_infer, qlib_train`, and all three required task names registered. The daily task remains configured for `16:10 Asia/Shanghai` with `run_workspace_refresh=true`, `include_weekly_advisor=true`, `persist_risk_report=true`, `repair_accounts=false`, and `trigger_source=scheduler`; quote pre-refresh remains configured for `15:35` with a `15:50` grace deadline. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 14:38 +08:00` pre-window regression suite | Used the pre-scheduler wait time for read-only code validation. Machine time was `2026-07-03T14:38:44+08:00`; `python manage.py check` passed with no system-check issues, and the targeted readiness/scheduler/weekly-advisor/API regression suite passed (`169 passed`): personal readiness checkpoint simulation, status command, window validation, daily command/task, evidence collection/inspection, auto-advisor weekly task, scheduler initialization, and task-monitor API coverage. This did not run the formal daily readiness task, did not generate readiness evidence, and did not advance the scheduler-clean window. |
| `2026-07-03 14:48 +08:00` application task architecture cleanup | Used the pre-scheduler wait time to remove an architecture-audit violation in the daily readiness Celery task. `apps/task_monitor/application/tasks.py` no longer calls Django `call_command` from the Application layer; it now invokes the Python daily-readiness runner directly while preserving target-date parsing, closed-date validation, task provenance, workspace refresh flags, weekly-advisor inclusion, risk-report persistence, and strict failure behavior. The task tests were updated to assert the direct runner contract. Validation passed with `python scripts/verify_architecture.py --rules-file governance/architecture_rules.json --format text --include-audit --fail-on-audit-violations` (`Boundary violations: 0`, `Audit violations: 0`), `python scripts/check_governance_consistency.py --baseline governance/governance_baseline.json --format text` (`Governance violations: 0`), and the targeted readiness/scheduler/API suite (`169 passed`). This did not run the formal daily readiness task, did not generate readiness evidence, and did not advance the scheduler-clean window. |
| `2026-07-03 14:55 +08:00` runtime worker code alignment | Confirmed the running readiness Celery workers predated the `14:48` application-task cleanup, then checked Celery active/reserved/scheduled state and found all worker nodes empty. Restarted only the two default-queue readiness workers so the upcoming `15:35` quote pre-refresh and `16:10` daily readiness run can execute against the current worktree code; beat and the qlib worker were left running. The first restart attempt briefly started replacement workers before stopping the old PIDs because `$PID` is a PowerShell read-only variable; this was immediately corrected, leaving only `readiness@%h` and `readiness2@%h` processes started at `2026-07-03 14:53 +08:00`, plus the existing beat and qlib worker. Strict monitor passed after the correction with `monitor_gate.ok=true`, runtime `beat=1`, `workers=3`, queues `celery, qlib_infer, qlib_train`, task registry ok, quote pre-refresh still pending for `15:35`, daily evidence pending for `16:10`, and latest formal evidence still `2026-07-02 source=scheduler`. No formal evidence was generated and the scheduler-clean window was not advanced. |
| `2026-07-03 18:35 +08:00` Friday scheduler-clean acceptance audit | Re-ran the real post-close gates after the `15:35` quote pre-refresh, `16:10` daily readiness evidence, and `17:30` Friday weekly auto-advisor schedule had all passed. `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` passed with `monitor_gate.ok=true`, accepted window `4/20`, scheduler-clean window `3/20`, latest formal evidence `2026-07-03 source=scheduler`, task id `845f5c11-8176-4010-8d0b-a74f42e84562`, quote pre-refresh run count `2`, daily scheduler run count `3`, weekly scheduler run count `1`, runtime `beat=1`, `workers=3`, queues `celery,qlib_infer,qlib_train`, and next required date `2026-07-06`. `inspect_personal_readiness_evidence --target-date 2026-07-03 --json` accepted the scheduler evidence file `var/readiness-evidence/2026-07-03-personal-readiness.json` with sha256 `b524632350aa15985ec78e66e38797fb9380dc40da1ba9fab91dce13e162f747`; decision data, Regime, Pulse, Alpha/workspace consistency, and six active account risk checks were all ok. The evidence file still contains weekly persistence observations captured before the weekly report existed, but the post-evidence DB proof is now ok: risk reports persisted for accounts `613` and `614`, weekly auto-advisor reports `8` and `10` are ready, and matched/delivered notifications are present. `validate_personal_readiness_window --json` confirmed no blocking issues, accepted dates `2026-06-30` through `2026-07-03`, projected scheduler completion `2026-07-28`, and no manual trigger records in the scheduler-clean suffix. No manual formal readiness evidence was generated. |
| Simulation target-date default follow-up | `simulate_personal_readiness_checkpoints --target-date <trade_date>` now derives its default simulated checkpoint times from the requested target date instead of the historical `2026-07-03` fixture. This keeps future dry-runs, such as `--target-date 2026-07-06`, aligned with the date under test while remaining read-only: it does not change system time, generate readiness evidence, or advance the scheduler-clean suffix. |
| Market thermometer proxy visibility follow-up | `inspect_personal_readiness_evidence --json` now promotes market thermometer `proxy_components` into a non-blocking `decision_data.market_thermometer_proxy` observation. Accepted evidence remains accepted and no repair action is generated, but operator review can now see proxy component count, source, proxy name, and verification status without digging through the nested decision-data summary. |
| Zero-equity account visibility follow-up | `repair_personal_account_readiness --dry-run --json` now reports `zero_equity_status` for each target user. The live dry-run shows users `182` and `222` have decision-ready accounts `613` and `614`, while zero-equity real-account placeholders `365` and `519` are marked `non_blocking_placeholder`. If a target has no positive-equity account, the same field reports `blocking_no_positive_equity`, preserving the existing repair path. |
| Rotation scheduler recovery follow-up | The `2026-07-03` scheduler evidence confirms the earlier Rotation skip is no longer present in the current main-chain run: `workspace.result.components.rotation_signals` reports `total_configs=6`, `successful=6`, `skipped=0`, `failed=0`, and `status=success`. The `2026-07-02` skipped-count evidence remains useful historical context, but it is no longer a current yellow item. |
| Main-chain proof monitor visibility follow-up | `scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` now prints the formal acceptance proof counters for Qlib, Workspace core, Alpha/workspace consistency, decision data, and decision quote freshness. The live read-only summary remains green at `4/20` accepted days and `3/20` scheduler-clean days, while showing `records=3`, `ok=3`, and `missing=0` for each of those main-chain proof sections. This improves operator visibility without generating formal evidence or changing acceptance logic. |
| Post-evidence weekly due display follow-up | The one-screen monitor now prints `weekly_due=not_pending` when post-evidence weekly advisor persistence is already complete and no same-day or next scheduled persistence checkpoint remains. This removes the previously blank `weekly_due=` display from the healthy Friday post-evidence state without changing scheduler or acceptance behavior. |
| Final-gate summary visibility follow-up | The one-screen monitor now prints the current failed final-acceptance gate names and both projected dates: evidence-window completion and scheduler-clean completion. The live read-only summary reports `Failed final gates: evidence_window`, projected evidence completion `2026-07-27`, and projected scheduler completion `2026-07-28`, keeping the remaining work explicit without opening full JSON. |
| Qlib freshness status follow-up | A read-only `build_qlib_data --check-only --target-date 2026-07-03` now reports `latest_trade_date=2026-07-03` and "Qlib 数据新鲜度满足要求，无需自建更新." The earlier Tushare rate-limit warnings remain historical build context, while current formal proof and check-only diagnostics both show Qlib freshness is not blocking assisted personal research use. |
| Operator-check duration display follow-up | The one-screen monitor now keeps the raw minute countdown and adds a compact duration, e.g. `Operator check in: 3935 min (2d 17h 35m)`, so weekend or multi-day waits are easier to interpret without manual conversion. This is a display-only change and does not affect scheduler or evidence generation. |
| Market thermometer proxy audit visibility follow-up | The one-screen monitor now prints a dedicated `MT proxy audit` line with proxy component count plus `fallback_proxy` and `unmarked_proxy` counts. The live read-only summary remains green and currently reports `count=4`, `fallback=1`, and `unmarked=3`, making proxy-source usage visible without opening the full JSON evidence. |
| Live macro context monitor follow-up | The one-screen monitor now prefers `current_macro_context` for the macro summary and falls back to evidence only when live context is unavailable. The live read-only summary remains green and now reports `Macro context: source=live`, with Regime and Pulse both observed on `2026-07-03`, avoiding a stale-looking Pulse date from the latest evidence snapshot. |
| Monitor status-date visibility follow-up | The one-screen monitor now prints `Status date`, `latest_closed`, and `expected_latest` near the top of the summary. The live read-only summary remains green and reports all three as `2026-07-03`, making it clear which closed trading day anchors the current wait state. |
| Evidence manifest monitor visibility follow-up | The one-screen monitor now prints the accepted evidence manifest record count, SHA-256, and counted target dates. The live read-only summary remains green and reports `records=4`, manifest SHA-256 `2417bd2d161d682937c4f5602ae7053e812d401f90de1c2159fd12033e5ca161`, and dates `2026-06-30` through `2026-07-03`. |
| Latest evidence file monitor follow-up | The one-screen monitor now prints the latest formal evidence file path and SHA-256 directly below `Latest formal evidence`. The live read-only summary remains green and reports `2026-07-03-personal-readiness.json` with SHA-256 `b524632350aa15985ec78e66e38797fb9380dc40da1ba9fab91dce13e162f747`, so operators can confirm the current audit artifact without running a separate evidence inspection command. |
| Market thermometer proxy classification follow-up | Market-thermometer provenance now normalizes facts that carry an explicit `proxy` but no verification marker to `verification_status=proxy_source`, keeping true fallback proxies separate from ordinary proxy data sources. The one-screen monitor now reports `MT proxy audit: count=4 fallback=1 proxy_source=3 unmarked=0`, so the live decision-data chain has no ambiguous unmarked proxy components while remaining read-only and without generating formal evidence. |
| Failed-gate action monitor follow-up | The one-screen monitor now prints compressed `acceptance_gate.operator_actions` next to failed final gates. The live read-only summary remains green and reports `evidence_window wait_for_post_close target=2026-07-06 next=2026-07-06T16:10:00+08:00`, making the required operator posture explicit without opening JSON. |
| Projected remaining-days monitor follow-up | The one-screen monitor now prints `remaining_days_from_today` beside both projected completion dates. The live read-only summary remains green and reports evidence completion `2026-07-27 remaining_days_from_today=24` and scheduler-clean completion `2026-07-28 remaining_days_from_today=25`. |

## Yellow Items

These do not currently block assisted personal research use, but they should remain visible during the continuous-run trial.

1. Market thermometer no longer has stale or missing components for `2026-07-02`; however, proxy markers remain part of the audit trail. `inspect_personal_readiness_evidence --json` surfaces them as `decision_data.market_thermometer_proxy` observations, and the one-screen monitor now prints `MT proxy audit` with proxy counts and verification classes. Current live summary reports four proxy components, including ETF net flow's Tushare size-flow `data_center_consensus` / `fallback_proxy` path and the SSE monthly account-opening proxy used after AKShare's investor table stops at `2023-08`.
2. Macro sync skipped counts remain visible as historical workspace observations, but the current decision-grade macro chain for `2026-07-02` is recovered: all six readiness macro inputs are `decision_safe` and no longer emit a repair follow-up.
3. Rotation is recovered in the latest formal scheduler evidence: `2026-07-03` reports all 6 active configs generated successfully with `skipped=0`. The `2026-07-02` skipped-count record remains historical context only.
4. Qlib rate-limit warnings are historical build context only. Current check-only diagnostics for `2026-07-03` report `latest_trade_date=2026-07-03` and no self-build update required, while the formal Qlib proof shows `records=3`, `ok=3`, `missing=0`, `blocked=0`.
5. This is a 4-trading-day accepted readiness chain (`2026-06-30` through `2026-07-03`), not final acceptance. The goal still requires 20 accepted trading days and a scheduler-clean suffix before claiming stable production use.
6. The zero-equity real accounts (`365`, `519`) remain as real-account placeholders. Daily readiness now targets the positive-equity simulated accounts (`613`, `614`) unless a specific zero-equity account is requested; `repair_personal_account_readiness --dry-run --json` marks these placeholders as `non_blocking_placeholder` when a positive-equity account already exists.

## Next Acceptance Window

Run for at least 20 trading days before treating the system as the main personal investment workstation.

Daily checks:

1. Run `powershell -ExecutionPolicy Bypass -File scripts/check-personal-readiness-monitor.ps1 -SummaryOnly` before the scheduled window. It must remain read-only and must not generate readiness evidence.
2. Confirm `Monitor gate: ok=True`, `Runtime: ok`, quote pre-refresh and daily evidence are scheduled at the configured post-close times, defaulting to `15:35` / `16:10`.
3. When testing future checkpoint behavior before the real time arrives, use `python manage.py simulate_personal_readiness_checkpoints --target-date <trade_date>`. It is read-only, does not change system time, does not generate evidence, and does not advance the scheduler-clean window.
4. Let Celery beat run `decision-quote-pre-readiness-refresh` and `personal-readiness-daily-evidence` at their configured post-close times; do not create manual formal evidence during a normal scheduler-clean trial day.
5. After the daily evidence run and grace window, rerun the monitor summary and confirm `Latest formal evidence` advanced with `source=scheduler`, non-empty task provenance, and scheduler-clean suffix increased.
6. If monitor reports a missed/overdue scheduler run, inspect with `python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime` before any repair.
7. Use `python manage.py run_personal_readiness_daily --target-date <latest_closed_trade_date> --json` only for explicit diagnostic/backfill work. A manual formal record may help investigate the general evidence window but it does not advance the scheduler-clean suffix needed for final acceptance.
8. If the evidence status is `warning` or `error`, run `python manage.py inspect_personal_readiness_evidence --target-date <blocked_date> --json` before rerunning or repairing data/accounts.
9. Run `python manage.py healthcheck --json` separately when investigating system readiness regressions.
10. Run `python manage.py build_qlib_data --check-only --target-date <latest_trade_date>` separately when investigating Alpha/Qlib freshness.
11. Generate or refresh Alpha ranking and default-account workspace recommendations only when Alpha/workspace consistency is not `ok`.
12. Use the split commands `repair_personal_account_readiness`, `collect_personal_readiness_evidence`, and `validate_personal_readiness_window` only for focused diagnosis.
13. Record any degraded data source, skipped macro indicator, skipped rotation config, zero-equity account, blocked recommendation, missed scheduler run, or manual backfill.

Scheduler setup:

```bash
python manage.py setup_personal_readiness_daily
python manage.py init_scheduler_defaults
```

Read-only status:

```bash
python manage.py show_personal_readiness_status --json
python manage.py simulate_personal_readiness_checkpoints --target-date 2026-07-03
```

Monitor-safe status:

```bash
python manage.py show_personal_readiness_status --json --strict-monitor --require-local-scheduler-runtime --local-runtime-ping-timeout 8
powershell -ExecutionPolicy Bypass -File scripts/check-personal-readiness-monitor.ps1 -SummaryOnly
```

Final acceptance gate:

```bash
python manage.py show_personal_readiness_status --json --strict-acceptance --require-local-scheduler-runtime --local-runtime-ping-timeout 8
powershell -ExecutionPolicy Bypass -File scripts/check-personal-readiness-monitor.ps1 -StrictAcceptance
```

Exit criteria:

1. No red readiness failures for 20 trading days.
2. Decision quotes stay fresh on every trading day.
3. Alpha/workspace consistency stays `ok`.
4. Regime and Pulse refresh successfully every trading day.
5. Risk-center persisted daily reports, pre-trade checks, and post-investment checks are present in every new formal daily readiness evidence record.
6. Auto-advisor reports are generated and persisted on schedule.
